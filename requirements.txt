# Refs: P1-schema S3.1 -- 런타임 의존성. 설치된 버전을 == 로 고정.
SQLAlchemy==2.0.50
alembic==1.19.2
psycopg[binary]==3.3.5
pgvector==0.5.0
# Refs: P2-tools 결정10 -- FastAPI 골격(app/main.py, app/api/*)의 런타임 의존성.
fastapi==0.135.1
uvicorn[standard]==0.41.0
httpx==0.28.1  # TestClient 용 (런타임에는 쓰지 않음, Starlette TestClient 의 필수 의존성)
# Refs: P3-er 결정6 결정3 -- 임베딩(app/embedding.py)·LLM 판정(app/er/judge.py)
# 런타임 호출자가 생겨 핀 추가(P2 결정10 이 "P3 몫"으로 미룬 것). 둘 다 지연
# import(app/embedding.py·app/er/judge.py 본문 참고) -- 키·SDK 없이도 임포트 가능.
openai==2.33.0
anthropic==1.4.0
# Refs: P3-llm-providers D11 U2 -- Gemini 판정기(app/er/judge.py
# GeminiJudge) 런타임 의존성. 지연 import(GeminiJudge.__post_init__/judge
# 본문 참고) -- 키·SDK 없이도 임포트 가능.
google-genai==2.23.0
# Refs: FIX-005 -- app.settings.user_timezone() 이 쓰는 IANA 시간대 DB.
# Windows·슬림 컨테이너에도 tzdata 가 있게 고정 설치한다(표준 라이브러리
# zoneinfo 는 시스템에 시간대 DB 가 없으면 이 패키지로 대체한다).
tzdata==2025.3
