# 07 · 데모 리허설 스크립트 (P11) — P9 배포 후

## 언제 필요한가
`docs/backlog.md` 74행 `[사용자]`: "데모 리허설 스크립트 (발표일 기준 날짜 재계산) / 의존: P9 / 수용기준: 승진 시나리오·수동 트리거 포함". 배포 URL 이 생긴 뒤.

## 왜 사용자 몫인가
발표일·발표 환경(브라우저·알림 권한·화면)은 사용자가 정한다. 시나리오 문장 초안은 에이전트가 쓸 수 있다.

## 준비물 (그때 채운다)
- 발표일 → 일정 발화의 날짜를 발표일 기준으로 재계산(브리핑이 "만남 직전"에 뜨도록).
- `README.md` "데모 시나리오 (3분)" 5단계: 첫 언급 → 사건 누적 → 승진 호칭 변이(ER 병합) → 일정 등록 → 브리핑(웹푸시 + 수동 트리거 `POST /briefings/run`).
- `SERVER-CHECKLIST.md` §5 데모 시나리오 재현 표.
- 데스크톱 Chrome 알림 권한 허용, 오프라인 대비 수동 트리거 경로.

## 끝났다는 증거
리허설 기록(날짜·단계별 통과·걸린 시간)을 아래 표에 남긴다.

| 날짜 | 환경 | 통과 단계 | 문제 | 조치 |
|------|------|----------|------|------|
| | | | | |
