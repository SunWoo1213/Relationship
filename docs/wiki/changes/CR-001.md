# CR-001 · 확신도 결합(4단계) 미측정 신호 재정규화 + 규칙 필터(2단계) 배제→감점

상태: 이행완료(문서, 2026-09-22 — D12·D13·D3 대체·S3.3·CLAUDE.md·resolution-plan·proposal 상단·backlog P4b·review-index·INDEX; 코드 이행은 P4b-er-redesign 패키지가 04-review 로 기록) | 제기: 2026-09-22, 사용자 (P4-pilot-eval 게이트 미달 → `reports/failure_cases.md` §8 후보 ①·② 를 한 CR 로, 결정 2026-09-22 journal DECISION)

## 1. 무엇이 바뀌는가

기획서 본문(`docs/proposal.md`)에는 결합 산식이 없다 — 산식은 `docs/resolution-plan.md` §3.3(D3)·`CLAUDE.md` 원칙3·S3.3 에 있다. 따라서 **기획서 본문은 그대로**이고 상단 "원본 이후 확정된 사항"에 한 줄만 는다. 바뀌는 문장:

| 어디 | 이전 문장 | 새 문장(안) |
|------|----------|------------|
| `CLAUDE.md` 38행 원칙3 · D3 · S3.3 9행 · resolution-plan §3.3 | `confidence = 0.5·s_llm + 0.3·s_emb + 0.2·s_rule` | **① 관측된 신호만 결합한다**: `confidence = Σ w_i·s_i / Σ w_i` (i ∈ 관측 신호). `s_rule` 이 미측정(`rule_checked == 0` — hints 가 비어 규칙을 하나도 검사하지 못한 경우)이면 `s_rule` 을 0 으로 넣지 않고 분모에서 뺀다 → `0.625·s_llm + 0.375·s_emb`. 세 신호가 모두 있으면 **원식과 동일**(0.5/0.3/0.2). `confidence_breakdown` 에 `weights_effective`·`rule_checked` 를 기록한다(원칙9) |
| S3.3 7행 2단계 "규칙 필터 → 통과 후보만" · `app/er/rules.py` | `relation_tag_conflict`·`hierarchy_conflict` 인 후보는 **제거**(3단계 LLM 이 보지 못함) | **② 배제 → 감점**: 충돌 후보를 제거하지 않고 `s_rule` 감점(`rule_passed/rule_checked` 그대로 0 점 반영)으로 남겨 3단계가 후보 전체를 비교한다. `dictionary_conflict`·완화 재검색(승진 인접) 규약은 유지. 단, 후보 0명일 때의 `no_candidates` 강제 경로는 그대로 |

범위(기획서 2장 표)는 움직이지 않는다 — 포함/제외 칸 변화 없음. 원칙1(오병합 > 미검출)·원칙2(임계치 2개)·원칙4(4단계)는 유지된다. **원칙3 의 산식 문장이 바뀌므로 CR 대상이다**(FIX 가 아니다).

근거(`reports/failure_cases.md` §4·§8, 04-review §4b·§7 — 실 실행 ef18143, 40건):
- 게이트 미달 원인은 1·3단계가 아니다: 골드 후보 69/69 후보 목록에 있었고, `s_llm ≥ 0.9` 귀속 정답률 97.0%(98/101), `llm_error` 0.
- **4단계**: `rule_checked = 0` 인 mention 이 141 중 100(`derive_hints` 빈 dict 91 과 교차). `s_rule = 0` 으로 합산되어 확신도 상한이 **0.80** 에 묶이고(실측 최대 0.800), deferred 79 중 52 건이 `[0.7, 0.8)` 에 몰렸다. 이 52 건을 `embedding_only` 는 오병합 없이 merge 로 맞혔다 — 지배의 산술적 원인.
- **2단계**: 규칙 필터가 골드 후보를 떨어뜨린 mention 6건(`relation_tag_conflict` 2·`hierarchy_conflict` 4) 중 1건이 유일한 오병합(`sc-015` t0 "부장님" — 조은우 제외 → 한지원만 남아 0.95 merge), 1건이 유일한 미검출(`sc-007` t2 "문실장님" → `no_candidates`), 4건이 보류.
- 반사실(저장 신호 재합산, 재실행 아님): ① 만 적용 시 `T_merge 0.8` 에서 merge 48 → **97**, 골드 일치 **96**, 오병합 **1건 그대로**(`sc-015` 는 ② 의 대상). ② 는 후보 집합이 바뀌므로 반사실 계산 불가 — 40건으로 검증할 수 없고, 후보가 늘어 오병합 기회도 는다.
- `T_merge` 단독 재설정은 답이 아니다(격자 전체에서 지배가 풀리는 점은 0.5 뿐, 거기선 오병합 6/132). 보정표만 보고 `s_llm` 을 보정하는 변경은 권하지 않는다(과신 증거 없음).

## 2. 영향 분석 (태그로 추적)

- 검증 항목 R: **R4**(자기보고 `s_llm` + 보정표 — 산식 분모 변경으로 R4 "구현완료" 근거 커밋에 재검증 필요), **R3**(D10 방향 — 산식이 바뀌어도 `T_merge`↑ → 오병합↓ 단조성은 유지되어야 하며 재실행 곡선으로 재확인). 기획서 §3 4단계 표(proposal 125~128행)는 문장 변화 없음.
- 결정 D: **D3 → 대체됨(→ D12 "관측 신호 재정규화 결합")**, **새 D13 "규칙 필터는 감점, 배제 아님"**(S3.3 2단계 규약 — 기존 D 카드에 없던 규칙이라 신설). D10 유지(임계치 2개·초기값 0.8/0.3 — 재실행 곡선으로 재확정). D5 유지. D11 무관.
- 명세 S: **S3.3** 7행(2단계)·9행(4단계 산식) 갱신, 18행 회귀 테스트 3종(승진 연결·팀장↔이모 배제·동명이인 분리)은 **② 뒤에도 통과해야 한다**(팀장↔이모는 `dictionary_conflict`·관계 태그 충돌 → 감점으로도 `< T_merge` 가 되는지 회귀로 증명). S3.7 무변경(지표·게이트 식은 그대로).
- 불변 원칙(CLAUDE.md): 원칙3 산식 문장 교체. 원칙1·2·4·9 유지(9: breakdown 에 `weights_effective` 추가).
- 패키지 P — 완료: P3-er(`app/er/confidence.py`·`rules.py`·trace 34건 테스트), P4-pilot-eval(부분완료 adf9f1b — 기준선). 진행 중: 없음(active none). 예정: **새 패키지 `P4b-er-redesign`**(backlog 신설 — 아래 4절) 뒤에 P5. P10 은 재설계 산식 기준으로.
- 영향 커밋(`git log --oneline --grep`): `D3` 26건 · `D10` 29건 · `S3.3` 30건(2026-09-22 기준, 겹침 다수). 산식·규칙을 **구현**한 커밋(되돌릴 대상 아님 — FIX 로 수정): P3-er U3 규칙 필터(`rules.py`), U4 확신도 결합(`confidence.py`), U6 trace(`_breakdown`); P4 U7 선행 e7f0a9c(`run_pilot_eval.py --recheck-traces` 가 trace weights ≠ `ER_WEIGHTS` 면 거부). 정확한 해시는 `bash .claude/scripts/gitlog.sh P3-er D3` 로 이행 시 붙인다.
- 수정할 파일 목록:
  - 문서: `docs/wiki/decisions/D03-confidence-formula.md`(대체됨→D12), 신규 `D12-*.md`·`D13-*.md`, `docs/wiki/specs/S3.3-er-pipeline.md` 7·9행, `CLAUDE.md` 38행, `docs/resolution-plan.md` §3.3(한 줄 + CR 참조), `docs/proposal.md` 상단 1줄, `docs/backlog.md`(P4b 행), `docs/wiki/review-index.md` R3·R4 비고, `docs/wiki/INDEX.md`(D12·D13 태그).
  - 코드(FIX 또는 P4b 안에서): `app/er/confidence.py::combine()`(64행)·`_breakdown()`(133행, `weights_effective`), `app/er/rules.py`(`_CONFLICT_PRIORITY` 54행·`relation_tag_conflict` 96행·`hierarchy_conflict` 108행·완화 재검색 167행 — 제거 대신 감점), `app/settings.py:78 ER_WEIGHTS`(비율은 유지), `app/er/pipeline.py` 2→3단계 후보 전달.
  - 평가 도구: `scripts/run_pilot_eval.py` 66·135·720행(weights 거부 → `weights_effective` 재계산으로), `evaluation/curve.py meta.weights`, `evaluation/calibration.py` placeholder 판정(`matched_person_id is None`, ② 로 후보 배제가 줄면 placeholder 20 → 감소), `evaluation/resolvers/proposed.py` 어댑터(인터페이스 불변).
  - 테스트: `tests/test_er_*`(P3-er, `combine()` 소스 문자열 단언·규칙 배제 단언 기대값 갱신), `tests/test_eval_*`·`tests/test_run_pilot_eval.py`(가중치 거부 테스트).
- 재실행 규약(원칙8): 새 stamp `raw-<ts>.jsonl.gz`·새 `run_id`·새 `meta.commit`. `reports/pilot/raw-20260922-042440.jsonl.gz`·`traces-…` 는 **덮어쓰지 않는다**; `reports/metrics.json`(sha256 25e16dd6…)·calibration·curve·eval.md 는 **재실행 전에 `reports/pilot/<이름>-20260922-042440.*` stamp 사본으로 커밋해 보존**하고 최상위는 재실행 결과로 갱신한다(P4b 01-plan 결정 I(i), 사용자 2026-09-22) — 비교 기준선 = stamp 사본. 비용 40건 ≈ $0.03. 게이트 재판정은 같은 결정 K (a) 식으로 04-review 가 한 번만.

## 3. 선택지

| 안 | 내용 | 비용 | 위험 |
|----|------|------|------|
| **A 수용(①+②)** — 사용자 선택 2026-09-22 | D12·D13 신설, S3.3·CLAUDE.md 갱신, `P4b-er-redesign` 패키지(01-plan → verifier → backend-agent 구현 → eval-agent 재실행 새 stamp → verifier 04-review 게이트) | 카드·문서 1커밋 + 코드·테스트 2~3 단위 + 재실행 $0.03 + 04-review. P5 착수가 P4b 뒤로 밀림 | ② 는 40건으로 검증 불가 — 후보가 늘어 오병합 기회 증가. 완화: ② 를 `s_rule` 감점 + **충돌 후보는 `T_merge` 를 넘어도 `identity` 로 강등**(보수 분기)하는 변형을 P4b 01-plan 결정으로 둔다 |
| B 부분 수용(① 만) | D12 만 신설, ② 는 P10(150건) 뒤로 | A 의 절반, 재실행 1회 | 오류 2건(`sc-015`·`sc-007`)은 그대로 남는다 — 반사실 오병합 1/132 유지. 게이트는 ① 만으로 풀릴 가능성이 높다(merge 48→97, 오병합 1 유지 → `embedding_only` 0·1 과 비교 시 오병합 1 > 0 이면 여전히 지배 — **① 만으로는 지배가 풀리지 않는다**: 오병합 축이 1 vs 0 으로 남는다) |
| C 거절(현행 유지) | 산식·규칙 그대로, P4 부분완료로 P5 진행 | 0 | 결정 K 미달 상태로 P5 착수 = CLAUDE.md "P4 통과 전 P5 이후 시작 금지" 위반. 불가 |

> B 의 위험 열이 핵심이다: ① 만으로는 `sc-015` 오병합 1건이 남아 오병합 축 1 > 0 이므로 `embedding_only` 지배가 **풀리지 않는다**(지배 정의: 오병합 ≤ 이고 미검출 ≤, 하나는 엄격 <). 지배를 풀려면 오병합 0 또는 미검출 < 1 이 필요하고, 그 1건의 원인은 2단계다 → ①+② 가 논리적으로 필요하다.

## 4. 이행 계획 (승인 후, 이 순서로)

1. `CURRENT.md` → `frozen: CR-001` (제품 코드 쓰기 차단)
2. 결정 카드: 새 **D12**(관측 신호 재정규화 결합 — 산식·`weights_effective`·세 신호 모두 있을 때 원식과 동일함을 명시) · 새 **D13**(규칙 필터 감점 규약 — 충돌 후보 유지, `s_rule` 반영, 회귀 3종 유지 조건, 보수 분기 변형은 P4b 결정) · D3 를 `대체됨(→D12)` · S3.3 7·9행 · `CLAUDE.md` 원칙3 · `docs/backlog.md` 에 `P4b-er-redesign` 행(의존: P4 부분완료, CR-001 / 수용 기준: 재실행 게이트 `0.8 [] True True` + 회귀 3종 + P4 기준선 대비 오병합 ≤ 1·미검출 ≤ 1) · `review-index.md` R3·R4 비고 · `INDEX.md` 태그
3. `docs/proposal.md` 상단 "원본 이후 확정된 사항"에 한 줄(본문 유지)
4. 영향 코드: `git revert` 없음(구현 커밋은 유지). 수정은 **`P4b-er-redesign` 패키지 안에서**(`/devlog start P4b-er-redesign` → 01-plan → verifier → 승인 → backend-agent(app/er)·eval-agent(도구·재실행) → verifier 04-review). 재실행은 새 stamp, 기준선 덮어쓰기 금지
5. `frozen: none`(카드·문서 커밋 직후 — 코드는 P4b 가 active 인 상태에서 씀) · journal `CR` 줄 · 이 문서 상태 `이행완료`(P4b 04-review 게이트 통과 시점이 아니라 **카드·문서 이행 완료 시점**; 코드 결과는 P4b 04-review 가 기록)

문서 갱신 한 커밋, 코드 수정은 P4b 단위별 별도 커밋(각 `Refs: CR-001`).

## 5. 되돌리기 (이행 도중 실패하면)

이 CR 이 만든 커밋 목록을 역순으로 `git revert`. 문서는 CR 직전 커밋(adf9f1b)으로. P4b 가 재실행한 산출물은 새 stamp 라 기준선(`raw-20260922-042440.jsonl.gz`)은 영향 없음.

**갱신 이력**
- 2026-09-22 §2 파일 목록 보강(P4b 02-plan-verify 권고 R-3·R-7): 코드에 `app/er/types.py`(`ScoredCandidate.penalized_by`·`ERConfig.penalized_merge_policy`)·`scripts/er_smoke.py:109`(`combine()` 호출부) 추가; 문서에 `.claude/skills/entity-resolution/SKILL.md`(S3.3 절차 미러 — P4b U0 에서 D12·D13 정합, 권위 문구 추가) 추가. 36행 기준선 문구는 P4b 결정 I(i) 로 정정됨.

승인: 사용자 (2026-09-22) — A 수용(①+②). 코드 이행은 P4b-er-redesign 패키지.
