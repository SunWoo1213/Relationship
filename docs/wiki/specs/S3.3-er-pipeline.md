# S3.3 · ER 4단계 + 확신도

원문: `docs/resolution-plan.md` §3.3 · 관련: D3 D5 D10 원칙1 원칙2 원칙3 원칙4 · 적용: P3-er · 절차 상세: `.claude/skills/entity-resolution`

```
1. 후보 검색   별칭 임베딩 top-K(K=10) → 인물별 max 유사도 → s_emb
2. 규칙 필터   호칭 사전 호환 / 위계 일치 / 관계 태그 일치 → 통과 후보만, s_rule
3. LLM 판정   대화 맥락 + 후보 → {matched_person_id | null, s_llm, reason} (구조화 출력)
4. 분기        confidence = 0.5·s_llm + 0.3·s_emb + 0.2·s_rule
              ≥ T_merge        → 연결 (update_person 별칭 누적)
              [T_new, T_merge) → ask_user(kind="identity")
              < T_new          → ask_user(kind="new_person")
```

- 승진 케이스: 2단계 위계 제약을 "인접 위계까지 허용"으로 완화한 재검색 1회. 재검색 여부 trace에 기록.
- 초기값 T_merge=0.8, T_new=0.3 (설정값).
- **LLM 단일 호출로 대체 금지**(원칙4). **T_merge 미만 자동 병합 금지**(원칙1·2).
- 회귀 테스트 필수: 승진(팀장→부장님) 연결, 팀장↔이모 배제, 동명이인 분리.
