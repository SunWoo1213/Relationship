# S3.5 · 메모리 승격 + 패턴 감지

원문: `docs/resolution-plan.md` §3.5 · 관련: D9 R8 R11 원칙6 · 적용: P6-memory

- 3계층: 작업(인메모리 최근 N턴) / 에피소드(`events`, 원문 보존) / 시맨틱(`person_facts`).
- 승격 트리거: 같은 인물의 미승격 events ≥ 5건.
- 승격 동작: LLM이 사실 후보 추출 → `person_facts` upsert → 각 사실을 `fact_sources`로 근거 이벤트에 연결.
- 패턴: 같은 type 90일 내 3회 이상 → `pattern:{type}` 사실 (규칙, D9). LLM은 문장화만.
- **원문은 어떤 경우에도 삭제하지 않는다.**
