# S3.7 · 평가 명세

원문: `docs/resolution-plan.md` §3.7 · 관련: D1 D3 D10 원칙8 · 적용: P1-pilot-dataset, P3-baselines, P4-pilot-eval, P10-final-eval · 절차 상세: `.claude/skills/eval-harness`

- 데이터셋 스키마: eval-harness 스킬 1절. 카테고리에 `new_person` 추가.
- 지표: P/R/F1, 오병합률(핵심), 미검출률, 추출 F1(`events.type` 고정 집합), 툴 호출 정확도, `ask_user_rate_by_kind`, `calibration`(s_llm 구간별 정답률).
- 곡선: x = T_merge ∈ {0.5,…,0.95}, T_new=0.3 고정. y = 오병합률·ask_user(identity)율·미검출률.
- 베이스라인 3종 + 제안 방식, 동일 데이터·동일 지표.
- 산출물: `reports/metrics.json`(원수치), `reports/calibration.json`, `reports/eval.md`(metrics.json만으로 재생성 가능).
- **P4 미달 시 재시도가 아니라 실패 케이스 분석 산출물 + S3.3 재설계.**
