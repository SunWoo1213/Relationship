# D8 · 제목 "관계 그래프" → "관계 메모리"

상태: 유효 | 해결하는 검증: R17 | 원문: `docs/resolution-plan.md` §1 D8

**결정** 확정 제목: "대화형 관계 메모리 에이전트 — 자연어 대화에서 자동 구축되는 개인 관계 메모리 기반 브리핑 시스템". 저장되는 것은 사용자–인물 관계뿐(별 구조). 인물 간(A–B) 관계는 제외(원칙7).

**코드에서 지켜야 할 것** 인물–인물 엣지 테이블·필드를 만들지 않는다. 이름에 graph 를 쓰지 않는다.
