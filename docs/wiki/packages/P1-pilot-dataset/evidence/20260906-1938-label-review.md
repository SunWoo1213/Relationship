# P1-pilot-dataset 라벨 검수 기록 (20260906-1938)

검수자: verifier (fable, 새 컨텍스트) | 표본: 40/40 | 사용자 검수: 함정 12건·new_person 지나가는 언급 3건

- 검수 대상: `docs/wiki/packages/P1-pilot-dataset/evidence/20260906-1923-review-packet.md`(전문) + 원본 `data/scenarios/{promotion,alias,pronoun,normal,new_person}.json`·`schema.json`·`manifest.json`(읽기만, 수정 0)
- 판단 근거: 01-plan "결정 확정" A~G·H-1~H-3, 리스크 "개인정보"·"한국어 구어체 부족"·"함정 난이도의 자의성", U2~U4 줄(43~45행), 생성 프롬프트 `evidence/20260906-1850-gen-prompt-u2.md`·`-1900-gen-prompt-u3.md`·`-1930-gen-prompt-u4.md`, D01 결정 절, D10 결정 절, CLAUDE.md 원칙 1·7·8
- 읽지 않음(오염 방지): `.claude/skills/entity-resolution/SKILL.md`, `app/er/`
- 이 파일은 지적만 남긴다. 데이터·라벨·코드·계획을 고치지 않았다(원칙8). 반영·기각은 U6(eval-agent)와 사용자가 정하고, 상태 열은 검수자가 닫는다(R-11).
- 데이터 커밋 기준: `40c36f8`(U5) — 검수한 JSON 은 이 커밋 상태다.

## 1. 지적 표

| id | 시나리오 id | 지적 | 상태 |
|---|---|---|---|
| 1 | sc-001, sc-002, sc-003, sc-004, sc-006, sc-007, sc-008 | **(1)(2) 승진 후 호칭이 seed 인물의 `aliases` 에 이미 들어 있다** — sc-001 `김부장`·`부장님`, sc-002 `이과장`, sc-003 `최대리`, sc-004 `강실장`, sc-006 `임주임`, sc-007 `문실장`, sc-008 `김부장`. `schema.json` 은 `aliases` 를 "호칭 변이 … 승진해도 옛 호칭을 지우지 않는다"(누적 골드)로, `seed_persons` 를 "대화 전 이미 등록돼 있는 인물"로만 정의하고 **어느 별칭이 대화 전에 등록돼 있는지는 정의하지 않는다**. U2 생성 프롬프트는 "승진 전 호칭은 seed 인물의 aliases 에 있어야 자연스럽다" 라고 써서 aliases = 대화 전 등록 별칭으로 읽힌다. 그러면 (a) P4 러너가 aliases 전부를 사전 적재할 경우 승진 후 호칭(`부장님`·`최대리`·`실장님`…)이 별칭 완전일치로 풀려 promotion 8건이 "호칭 변경 추적"을 전혀 측정하지 않고, (b) 일부만 적재하려면 P4 가 시나리오마다 규칙을 지어내야 해 결정 E 가 막으려던 "러너가 임의로 DB 를 채우는" 상황(원칙8)이 별칭 단위로 되살아난다. 요구: 대화 전 등록 별칭을 기계가 알 수 있게 표시(예: `seed_aliases` 또는 persons[].aliases[] 원소별 `known_before` — schema_version 3 후보)하거나, seed 인물의 aliases 에서 대화 중 처음 생기는 호칭을 빼고 그 규칙을 schema 설명에 명문화. 사용자 결정 필요. | 반영 76add8a |
| 2 | sc-004 | **(2) 함정 정답이 별칭 목록 비대칭으로 새어 나간다.** trap.reason 은 "2턴의 '팀장님' 은 … 문맥으로만 강 실장으로 정해진다" 인데 `팀장님` 이 p1(강도윤) aliases 에는 있고 p2(윤서준: `윤팀장`/`윤서준`) 에는 없다. 별칭 완전일치만으로 gold(p1)가 나오므로 함정이 아니라 정답 힌트다(지적 1 의 적재 방식에 따라 결정됨). 두 사람 모두 `팀장님` 을 갖거나(sc-015 의 `부장님` 처럼) 둘 다 갖지 않아야 문맥 판정이 측정된다. | 반영 76add8a |
| 3 | sc-006 | **(2) 지적 2 와 같은 구조.** trap.reason "2턴의 '주임님' 은 … 문맥으로만 조은우로 정해진다" 이나 `주임님` 은 p2(조은우) aliases 에만 있고 p1(임채원: `채원씨`/`임주임`)에는 없다. 별칭 일치가 gold 를 그대로 준다. | 반영 76add8a |
| 4 | sc-008 | **(2) 지적 2 와 같은 구조.** trap.reason "1턴의 '우리 회사 부장님' 은 수식어로만 직장 쪽으로 갈린다" 이나 `부장님` 은 p2(한지원) aliases 에만 있고 p1(김태윤: `아빠`/`김차장`/`김부장`)에는 없다. 수식어 없이도 별칭 일치로 p2 가 나온다. | 반영 76add8a |
| 5 | sc-002 | **(2) 함정이 발화에서 실제로 발생하지 않는다.** trap.reason "성씨를 떼고 부르는 순간 두 사람이 겹친다" 인데 mentions 4개 전부 성씨·본명이 붙어 있다: t0 `이대리`, t1 `박과장님`, t2 `박과장님`, t3 `이서연 과장님`. 성씨 없는 `과장님` 이 이서연을 가리키는 턴이 없어 함정 kind `promotion_collides_with_existing_title` 이 이름값을 못 한다(01-plan 리스크 "함정 난이도의 자의성" — 너무 쉬우면 오병합률 0). 최소 한 턴은 맨 `과장님` 으로 이서연을 가리키고 문맥으로 갈리게 해야 한다. 같은 이유로 `allowed: identity,none` 은 모호한 mention 이 하나도 없는 건에 identity 를 허용하는 셈이다. | 반영 76add8a |
| 6 | sc-012 | **(1)(2) 사람도 `권쌤`(t3) = `영어쌤`(t1) 임을 정할 수 없다.** 둘 다 seed 밖 새 인물 p2(권나연)로 라벨됐지만 발화 어디에도 영어쌤의 성씨가 권 이라는 단서가 없다(t1 "영어쌤은 그냥 넘어가주셨는데", t3 "권쌤은 그럴 때 웃어주시더라"). 제3의 선생님일 수 있어 두 surface 를 같은 새 인물로 묶는 것은 저자만 아는 정보다(01-plan "사람이 문맥으로 정할 수 있어야 한다"). 단서를 넣거나(예: "영어 권쌤"), 두 mention 을 분리/ambiguous 로 두거나, p2 를 seed 로 넣고 함정 성격을 다시 정해야 한다. | 반영 76add8a |
| 7 | sc-012 | **(1) `allowed` = `identity, new_person, none` 세 값 전부** — enum 의 모든 값이라 어떤 행동도 통과한다(D10 은 임계치 스윕에 따른 구간 변동을 허용하는 것이지 판정 자체를 면제하는 것이 아니다). 특히 seed 밖 등록 대상(권나연)이 있는 건에 `none` 이 들어 있어, 확인 질문 없이 조용히 create 하거나 아예 등록하지 않아도 통과다 — D1 "승인 시에만 create_person" 의 반대 방향이 허용된다. 같은 유형인 sc-035·sc-037(`new_person`)·sc-036(`identity, new_person`)은 `none` 을 빼고 있어 이 건만 규칙이 다르다. | 반영 76add8a |
| 8 | sc-003, sc-007 | **(1) `allowed: identity, none` 의 근거 없음.** 두 건 모두 함정 없음·seed 인물 1명·승진 호칭 변경뿐인데 같은 구조의 sc-001·sc-005 는 `none` 만 허용한다. U2 프롬프트 규칙은 "함정·모호 건은 넓게, 명백한 건은 `none`". 후보 인물이 하나뿐인 건에서 identity 질문을 허용하면 질문률 지표가 이 건들에서 무조건 통과가 된다(D10 느슨함). 어느 쪽이 맞는지 규칙을 정하고 4건을 맞춰야 한다. | 반영 76add8a |
| 9 | sc-037 | **(1) t2 `형`("형이랑 술 마시면서 얘기 마니 했다") 이 mentions 에 없다.** 선행사 시우형(p1)을 가리키는 지칭이며, 같은 구조의 sc-018 t3 `누나`→p1, sc-032 t1 `형`→p1 은 라벨돼 있다. 빠지면 P4 가 이 지칭을 정답으로 연결해도 셀 수 없고, new_person 등록 후 후속 지칭이 이어지는지(등록 대상의 핵심)가 측정에서 빠진다. | 반영 76add8a |
| 10 | sc-037 | **(6) t0 "주말에 큰집 갔다가 사촌형 시우 만났어" 에 meeting 이벤트가 없다.** 같은 구조 sc-024 t0(카페에서 떠들었어)·sc-031 t0(한강에서 자전거 탔어)·sc-040 t0(한강 걷는데)은 meeting/relative·none 으로 라벨했다. "주말에" 는 relative. | 반영 76add8a |
| 11 | sc-016, sc-024, sc-014, sc-031, sc-003, sc-006, sc-030 | **(6) 턴 안에 시간 표현이 없을 때 `occurred_at_kind` 규칙이 없어 상반된 라벨이 있다.** sc-016 t0 "예린이랑 카페에서 세 시간 수다떰" = `relative` vs sc-024 t0 "동혁이랑 보라랑 카페에서 두 시간 떠들었어" = `none`; sc-014 t1 "서다인 사진 진짜 잘 찍음"(t0 에 '오늘') = `relative` vs sc-031 t2 "사진 잘 찍어줘서 고마웟어"(t0 에 '어제') = `none`; sc-003 t3·sc-006 t3 "커피 사줬어"(시간어 없음) = `relative` vs sc-030 t1 "치킨 사줬어" = `none`. 앞 턴의 시간어를 상속하는지 여부를 정하고 51 relative / 14 none 을 그 규칙으로 재점검해야 한다(결정 B 의 F1 분모가 흔들린다). | 반영 76add8a |
| 12 | sc-003, sc-009, sc-016, sc-017, sc-026, sc-033 | **(6) '요즘' 의 `occurred_at_kind` 가 갈린다.** sc-003 t0·sc-009 t1·sc-016 t1 은 `relative`, sc-017 t1·sc-026 t1·sc-033 t4 는 `none`. 발화 구조가 같다("요즘 … 한대/하더라"). 한쪽으로 통일 필요. | 반영 76add8a |
| 13 | sc-001, sc-005 | **(6) 미래 약속을 이벤트로 라벨했다 — 결정 C(일정 라벨은 P10) 와 normal 카테고리 관행에 어긋난다.** sc-001 t3 "부장님이 다음주에 밥 사준다고 함" = meal/relative, sc-005 t4 "9월 20일에 축하 겸 밥 먹기로 했어" = meal/absolute. 반면 같은 성격의 sc-007 t3·sc-011 t3·sc-025 t2·sc-027 t2·sc-028 t2·sc-029 t3·sc-033 t3·sc-034 t2 는 이벤트 없음(manifest `schedule_utterance_scenarios` 가 일정 발화로 분류). 데이터셋 유일의 `absolute` 1건이 바로 이 미래 약속이라, 지우면 absolute 0건이 되는 점도 함께 결정해야 한다. | 반영 76add8a |
| 14 | sc-001, sc-002, sc-004, sc-006, sc-008, sc-037 (vs sc-005, sc-023) | **(6) 승진·이직·취업 소식의 `personal_share` 라벨이 일관되지 않는다.** sc-005 t1 "이번에 회장 됐다고 연락 왔어" 와 sc-023 t0 "이번에 본사로 옮기신대" 는 personal_share/relative 인데 sc-001 t1 "김팀장 이번에 부장 달았대", sc-002 t0 "과장으로 승진했더라", sc-004 t0 "실장 되셨대", sc-006 t0 "주임 달았어", sc-008 t0 "부장으로 승진하셨대", sc-037 t1 "취업했다고" 는 이벤트가 없다. promotion 카테고리에서 승진 소식 자체가 이벤트인지 규칙을 정해야 F1 이 재현된다. | 반영 76add8a |
| 15 | sc-005, sc-022, sc-034 | **(6) 사용자가 없는 자리에서 인물끼리/혼자 한 일을 전언으로 들은 경우의 type 규칙이 없다.** sc-005 t3 "첫 회의 때 엄청 긴장했다더라" = meeting, sc-022 t0 "규진이랑 세호랑 어제 같이 술 마셨대" = meal, sc-034 t0 "아빠가 지난주에 낚시 다녀오셨대" = other — 그러나 같은 전언인 sc-017 t1 "이직 준비한대" = personal_share. 특히 sc-022 t0 는 규진–세호 사이의 일(원칙7: 인물 간 관계는 배경으로만)이라 사용자–인물 이벤트 `meal` 로 두면 A–B 사건이 사용자 사건으로 저장되는 방향을 정답으로 만든다. "사용자 참여 사건만 활동 type, 전언은 personal_share" 같은 규칙을 정하고 재라벨 필요. | 반영 76add8a |
| 16 | sc-008, sc-015 | **(6) '잔소리' 의 type 이 갈린다.** sc-008 t1 "부장님은 오늘도 잔소리" = conflict, sc-015 t2 "은우가 또 잔소리함" = other. 같은 행위·같은 강도. | 반영 76add8a |
| 17 | manifest.json `virtual_names` | **(4) 실존 공인과 동명인 성명 4건**: `박지훈`(아이돌·배우, sc-002·sc-035), `오지호`(배우, sc-032), `김민준`(배우, sc-001·sc-010), `배준호`(축구선수, sc-009). 모두 흔한 이름이고 발화 문맥(김팀장·배대리·동네 형)이 공인을 가리키지 않으므로 개인정보 리스크는 낮으며, 01-plan 리스크 절 자체가 "흔한 가상 성명(예: 김민준, 이서연 …)" 을 허용 예시로 든다. 다만 목록이 검사 (7) 의 유일한 방어선이라 사용자가 "동명 허용" 을 명시적으로 결정해 기각하거나 교체해야 한다. 그 외 104개는 실존 인물로 읽히지 않는다. | 기각 결정 L(사용자 2026-09-07): 동명 허용 — 흔한 이름, 문맥이 공인 아님, 01-plan 리스크 절 허용 예시. 데이터 무변경(manifest 에 4명 그대로, 76add8a) |
| 18 | sc-004, sc-005, sc-009, sc-020 (규칙: schema.json `occurred_at_kind` 설명) | **(6) 재검수 신규 — 결정 J(a) 의 시간 표현 판정 기준이 한 줄이 아니다(재검수 항목 (c)).** U6 는 `schema.json` `occurred_at_kind` 설명에 "'이번 인사'·'첫 회의' 처럼 시간명사가 아닌 명사를 꾸미는 '이번/첫' 은 시간 표현이 아니다" 라는 하위 규칙을 덧붙였고 전건에 그대로 적용했다(`이번에` 6/6 relative: sc-001 t1·sc-002 t0·sc-005 t1·sc-006 t0·sc-008 t0·sc-023 t0 / `이번 인사에` sc-004 t0 none / `첫 회의 때` sc-005 t3 none — evidence `20260907-recheck-consistency.txt` §C·§D). 문제 두 가지: (i) 이 하위 규칙은 **eval-agent(데이터 작성자)가 정한 것**이지 결정 J(a) 원문("오늘/어제/주말에/요즘 = relative … 없으면 none")에 없다. (ii) 자기 기준 안에서도 어긋난다 — `첫 회의 때` 의 `때` 는 시간명사인데 none 이고, `점심 같이 먹었어`(sc-009 t0)·`점심 사주셔서`(sc-020 t1)의 `점심` 은 부사어가 아니라 먹은 대상(목적어)인데 relative 다(`점심에`(sc-033 t0)만 부사어). 즉 '부사어 이번에 = relative / 관형어 이번 = none' 이라는 통사 구분은 `점심` 에는 적용되지 않았다. 추출기는 `이번에` 와 `이번 인사에` 를 다르게 내지 않으므로 이 구분은 F1 의 잡음이 된다. **사용자가 한 줄로 정해야 한다.** 안 A(권고): "시점을 뜻하는 낱말(오늘·어제·요즘·이번·첫·때·점심·저녁·주말 …)이 그 턴에 있으면 문장 성분과 무관하게 relative, 기간(세 시간·3년)만 제외" → sc-004 t0·sc-005 t3 none→relative 2건 변경. 안 B: U6 하위 규칙 유지 + 목적어 `점심` 도 none → sc-009 t0·sc-020 t1 relative→none 2건 변경. 어느 안이든 `schema.json` 설명을 그 한 줄로 바꾼다. | 반영 6906af4 — 안 A(결정 M) 채택: sc-004 t0·sc-005 t3 none→relative, `schema.json` `occurred_at_kind` 설명이 결정 M 한 줄로 교체(통사 하위 규칙 폐기 명시), sc-009 t0·sc-020 t1 relative 유지. 3차 독립 스캔 76건 불일치 0(`20260907-recheck3-kind-scan.txt`) |
| 19 | sc-036 | **(1) 재검수 신규 — t1 `그 친구`("그 친구도 대리라서 임대리라고 부르래")가 mentions 에 없다.** 임세아(p2)를 직접 가리키는 지시 표현이며, U6-2("같은 낱말이라도 인물을 직접 가리키면 센다")와 pronoun 카테고리의 `그 사람`(sc-018 t2)·`그 분`(sc-023 t1)·`그분`(sc-020 t1) 라벨과 어긋난다. new_person 등록 대상의 **첫 지칭**이 t1 앞머리의 `그 친구` 이므로 빠지면 등록 판정 시점이 t1 `임대리` 로 밀린다. 요구: `{turn:1, surface:"그 친구", gold_person_id:"p2"}` 추가(검사 (12) 통과 — 발화에 그대로 있음). | 반영 6906af4 — sc-036 mentions 6개, `{turn:1, surface:"그 친구", gold_person_id:"p2"}` 가 t1 `임대리` 앞에 있음(`20260907-recheck3-decision-N.txt`). 발화에 그대로 있어 검사 (12) 통과(`20260907-recheck3-validate-strict.txt` rc=0) |
| 20 | sc-013, sc-035 (판단 필요: sc-035 t1, sc-012 t0·t1) | **(6) 재검수 신규 — 결정 J(c)(d) 로 라벨된 턴과 같은 구조인데 이벤트가 없는 턴.** (1) sc-013 t1 "저녁엔 채영이랑 약속 있어서 나감" — 사용자–채영 사이의 실제 만남(과거형 `나감`)인데 이벤트 없음. 같은 구조 sc-010 t1 "저녁엔 동창 민준이 만나서 술 마심" 은 meal/relative. → meeting/relative(`저녁엔`) 추가 필요. (2) sc-035 t2 "하람씨 첫날부터 야근하던데 좀 안쓰럽더라" — 인물 혼자의 일을 사용자가 보고 전하는 근황인데 이벤트 없음. 같은 구조 sc-026 t1 "요즘 회사 일 많다고 좀 지쳐 보였어" 는 personal_share/relative, sc-024 t3 "둘 다 요즘 힘들다고 하더라" 도 personal_share. → J(d) 대로 personal_share 추가 필요(kind 는 지적 18 결과에 따라 `첫날` 처리). (3) 판단 필요 — sc-035 t1 "박과장님이 나보고 하람씨 사수 하라고 함"(박과장→사용자 업무 지시. 사용자–인물 사건이면 other. sc-029 t0 자세 교정 = other 와 비교), sc-012 t0 "수학쌤이 오늘 숙제 진짜 많이 냄"·t1 "영어쌤은 그냥 넘어가주셨는데"(교사→학생(사용자) 행위. 같은 시나리오 t2 "질문하러 갔다가 혼났어" 는 conflict 로 라벨돼 있어 t0·t1 만 비우는 근거가 없다). 이 3턴은 "사용자에게 한 행위를 이벤트로 보는가" 의 경계라 eval-agent 가 규칙을 정해 03-log 에 남기고 적용/비적용을 명시해야 한다. 이전 검수(§2)는 이 건들을 (6) 통과로 두었으나 J(c)(d)·U6-3 이 명문화된 뒤 전수 대조에서 드러난 것이다. | 반영 6906af4 — (1) sc-013 t1 meeting/relative (2) sc-035 t2 personal_share/relative (3) sc-035 t1 other/none·sc-012 t0 other/relative 추가, sc-012 t1 은 U6-5 예외 (i) 부작위로 이벤트 없음 — 규칙은 03-log U6-2 항목·`schema.json` events (h) 에 명문화. 3차 판정: 부작위 제외 동의(`20260907-recheck3-u6-5-sweep.txt`) |
| 21 | sc-034 (비교: sc-029 t2, sc-025 t1) | **(6) 3차 재검수 신규 — U6-5 전건 적용에서 빠진 턴.** sc-034 t1 `월척 잡았다고 사진 계속 보내심ㅋㅋ` 이 `personal_share/none` 인데, t0 `아빠가 지난주에 낚시 다녀오셨대` 가 이미 같은 낚시 근황의 `personal_share/relative` 다. U6-5 예외 (ii)("한 사건을 여러 턴에 나눠 서술하면 사건이 성립한 턴 하나에만")로 보면 t1 은 이벤트 없음이어야 하고(change-summary §2 가 같은 이유로 sc-025 t1 `택배 오늘 도착했는데` 를 제외했다), `사진 계속 보내심` 을 **인물→사용자 행위**로 보면 U6-5 본문에 따라 활동 type 이어야 한다(sc-029 t2 `스트레칭 영상도 보내줬어` = favor, sc-025 t0 `김치 담가서 보내주셨어` = favor 와 같은 꼴). 어느 규칙으로도 두 번째 `personal_share` 는 나오지 않는다 — 76add8a 부터 있던 라벨이며 2차 재검수는 J(d) 만 보고 통과시켰고, U6-5 가 생긴 뒤 전건 적용(change-summary §2 "40건 160턴 전수")에서 이 턴만 빠졌다. 요구: eval-agent 가 (ii) 세부로 제거 또는 favor/other 로 교체 중 하나를 03-log 에 근거와 함께 남기고 적용. kind 는 어느 쪽이든 `none`(시점 낱말 없음). | 반영 aeed0bd — sc-034 events[1] = `{turn:1, type:"favor", occurred_at_kind:"none"}`(결정 O). diff 6906af4..aeed0bd 의 데이터 변경은 이 1행 + manifest distribution(favor 9→10·personal_share 26→25)뿐. 4차 판정: t0(아빠 낚시 = 사용자 부재 근황)·t1(아빠→사용자 사진 전송)은 주체·수신자가 다른 별개 사건 — 예외 (ii) 비적용 동의. 인물→사용자 `보내` 턴 3/3(sc-025 t0·029 t2·034 t1) favor 로 일관 |
| 22 | 규칙 문구(`schema.json` events (h)·03-log U6-2·`dump_scenarios.py` 검수 항목 6) — 데이터 대상 sc-011 t2 | **(6) 3차 재검수 신규 — U6-5 type 대응표의 `favor` 정의가 규칙 본문과 방향이 어긋난다.** 본문은 "사용자→인물, 인물→사용자 **어느 방향이든**" 인데 대응표는 "**사용자에게 이득이 되는** 호의 = favor" 로 한 방향만 적었다. 현재 favor 9건 중 8건은 인물→사용자이고 sc-011 t2 `유진언니한테 용돈 조금 드렸음` 만 사용자→인물 favor 라 대응표 글자대로면 이 라벨은 favor 가 아니다(`other`? 규정 없음). 라벨은 favor 가 자연스러우므로 데이터가 아니라 **문구를 고쳐야** 한다: "호의(방향 무관) = favor" 처럼. P10 150건에서 사용자→인물 호의(선물·도움)가 늘면 라벨러마다 갈리는 지점이다(#18 과 같은 종류의 잡음). 데이터 변경 0건 예상. | 반영 aeed0bd — `schema.json` events (h) 와 `dump_scenarios.py` 검수 항목 6 이 모두 "어느 방향이든 호의 = favor"(결정 P 원문과 동일), 구 문구 "사용자에게 이득이 되는 호의" 는 data/·scripts/ 에 0회(03-log 72행 U6-2 원문·81행 정정문 인용에만 남음 — 시간순 기록 규칙). 데이터 변경 0, sc-011 t2 favor 무변경 |

## 2. 시나리오별 판정 요약

셀 값: `통과` / 지적 번호 / `—`(해당 없음: 함정 아님, new_person 아님).

| id | (1) 라벨 | (2) 함정 | (3) 구어체 | (4) 개인정보 | (5) 상담 아님 | (6) events | (7) new_person |
|---|---|---|---|---|---|---|---|
| sc-001 | 1 | — | 통과 | 17 | 통과 | 13, 14 | — |
| sc-002 | 1, 5 | 5 | 통과 | 17 | 통과 | 14 | — |
| sc-003 | 1, 8 | — | 통과 | 통과 | 통과 | 11, 12 | — |
| sc-004 | 1 | 2 | 통과 | 통과 | 통과 | 14 | — |
| sc-005 | 통과 | — | 통과 | 통과 | 통과 | 13, 15 | — |
| sc-006 | 1 | 3 | 통과 | 통과 | 통과 | 11, 14 | — |
| sc-007 | 1, 8 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-008 | 1 | 4 | 통과 | 통과 | 통과 | 14, 16 | — |
| sc-009 | 통과 | — | 통과 | 17 | 통과 | 12 | — |
| sc-010 | 통과 | 통과 | 통과 | 17 | 통과 | 통과 | — |
| sc-011 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-012 | 6, 7 | 6 | 통과 | 통과 | 통과 | 통과 | 7 (등록 대상 있음에도 none 허용) |
| sc-013 | 통과 | 통과 | 통과 | 통과 | 통과 | 통과 | — |
| sc-014 | 통과 | — | 통과 | 통과 | 통과 | 11 | — |
| sc-015 | 통과 | 통과 | 통과 | 통과 | 통과 | 16 | — |
| sc-016 | 통과 | — | 통과 | 통과 | 통과 | 11, 12 | — |
| sc-017 | 통과 | — | 통과 | 통과 | 통과 | 12 | — |
| sc-018 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-019 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-020 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-021 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-022 | 통과 (ambiguous 타당) | — | 통과 | 통과 | 통과 | 15 | — |
| sc-023 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-024 | 통과 (ambiguous 타당) | — | 통과 | 통과 | 통과 | 11 | — |
| sc-025 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-026 | 통과 | — | 통과 | 통과 | 통과 | 12 | — |
| sc-027 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-028 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-029 | 통과 | — | 통과 | 통과 | 통과 | 통과 | — |
| sc-030 | 통과 | — | 통과 | 통과 | 통과 | 11 | — |
| sc-031 | 통과 | — | 통과 | 통과 | 통과 | 11 | — |
| sc-032 | 통과 | — | 통과 | 17 | 통과 | 통과 | — |
| sc-033 | 통과 | — | 통과 | 통과 | 통과 | 12 | — |
| sc-034 | 통과 | — | 통과 | 통과 | 통과 | 15 | — |
| sc-035 | 통과 | — | 통과 | 17 | 통과 | 통과 | 통과 (등록 대상) |
| sc-036 | 통과 | 통과 | 통과 | 통과 | 통과 | 통과 | 통과 (등록 대상·유사 호칭) |
| sc-037 | 9 | — | 통과 | 통과 | 통과 | 10, 14 | 통과 (등록 대상) |
| sc-038 | 통과 | 통과 | 통과 | 통과 | 통과 | 통과 | 통과 (지나가는 언급 2) |
| sc-039 | 통과 | 통과 | 통과 | 통과 | 통과 | 통과 | 통과 (지나가는 언급 2, 재언급 포함) |
| sc-040 | 통과 | 통과 | 통과 | 통과 | 통과 | 통과 | 통과 (지나가는 언급 2) |

항목별 지적 분포: (1) 라벨 정확성 — 지적 1·5·6·7·8·9 (시나리오 12건 관련) / (2) 함정 난이도 — 지적 2·3·4·5·6 (함정 12건 중 5건) / (3) 구어체 — 0 / (4) 개인정보 — 지적 17 (동명 4건, 발화 본문 위반 0) / (5) 고민 상담 — 0 / (6) events — 지적 10~16 (7건, 시나리오 22건 관련) / (7) new_person 하위 유형 — 지적 7 (sc-012 만; sc-035~040 통과).

## 3. 전체 소견

- **카테고리 균형**: 5범주 8/8/8/10/6 은 계획대로. 관계 태그는 직장 23/60(38%), 위계는 `하` 6/60(10%) 로 치우쳤다. `하` 인물이 등장하는 함정이 sc-006 하나뿐이라 "위계 신호가 갈라 주는 경우" 가 거의 측정되지 않는다(D3 의 `s_rule` 이 위계를 쓴다면 이 데이터로는 그 기여를 보기 어렵다). 연인 4건.
- **함정 난이도 분포(12건)**: 실제로 문맥 판정을 요구하는 건은 sc-010(동명 `민준`, `동창` 단서), sc-013(채원/채영 편집거리 1 + ambiguous), sc-015(`부장님` 이 두 사람 aliases 에 모두 있음, `퇴사` 단서), sc-036(임대리/이대리) 의 4건과 지나가는 언급 3건(sc-038~040) 이다. sc-004·sc-006·sc-008 은 별칭 목록 비대칭으로 정답이 새고(지적 2~4, 지적 1 의 적재 규칙에 종속), sc-002 는 함정이 발화에 존재하지 않으며(지적 5), sc-012 는 반대로 사람도 못 푸는 연결(권쌤=영어쌤)을 정답으로 둔다(지적 6). 즉 오병합 유도 함정 8건(promotion 4 + alias 4) 중 **현재 상태로 유효한 것은 3건**(sc-010·sc-013·sc-015)이다. U2 요구 "두 카테고리 각각 최소 3건" 을 실질로 채우려면 promotion 쪽 3건이 손질돼야 한다. 반대로 "사람도 못 푸는" 쪽으로 기운 건은 sc-012 하나뿐이고 ambiguous 3건(sc-013 t3, sc-022 t1, sc-024 t4)은 모두 타당하다.
- **구어체 수준**: 40건 전부 조사 생략·축약형·`ㅋㅋ/ㅠ` 가 있고 오타는 시나리오당 최대 1개(sc-031 고마웟어, sc-037 마니). 문어체 완결 문장으로 기운 건 없음 — 항목 (3) 위반 0. 다만 발화 길이가 10~26자(허용 8~60)에 몰려 있고 한 턴에 지칭이 대부분 하나라, 실사용의 긴 발화·한 턴 다중 지칭이 빠져 있다. 규칙 위반은 아니나 이 방향 역시 P4 를 실사용보다 좋게 보이게 하는 편향이다(01-plan 리스크 "한국어 구어체 부족" 과 같은 방향). 150건(P10) 에서 보완 대상.
- **D10 허용 집합의 느슨함**: `none` 만 20건, `identity+none` 17건, 세 값 전부 1건(sc-012), `new_person` 만 2건, `identity+new_person` 1건. `identity+none` 17건에서는 "질문한다/안 한다" 가 채점되지 않고 오병합(잘못된 인물 연결)과 `new_person` 오탐만 채점된다 — 함정·2인 이상 pronoun 에서는 D10 취지에 맞다. 문제는 근거 없이 넓힌 sc-003·sc-007(지적 8) 과 전부 허용한 sc-012(지적 7). 이 3건을 바로잡으면 허용 집합은 대체로 규칙(U2·U3 프롬프트 문장)으로 설명된다.
- **events 라벨**: type 7종 선택 자체는 대체로 무리 없으나, `occurred_at_kind` 는 "턴 안에 시간어가 없을 때" 와 "요즘" 에 대한 규칙이 없어 같은 구조가 반대로 라벨된 쌍이 여럿이다(지적 11·12). 이 상태로 P4 가 F1 을 내면 수치가 라벨러의 그때그때 판단을 재는 것이 된다(원칙8). U6 에서 규칙 3줄(시간어 상속 여부 / 요즘 / 미래 약속·전언 처리)을 01-plan 또는 schema 설명에 적고 65개 이벤트를 그 규칙으로 한 번 훑는 것이 필요하다.
- **개인정보·범위**: 발화 본문에 실제 회사명·연락처·숫자 식별자 0건(정규식 보조 스캔 포함). 고민 상담·감정 조언 요청 발화 0건(sc-022 "힘든가 봐", sc-040 "무서워서" 는 상황 서술). 인물 간 관계는 sc-022 t0·sc-036 t0 에서 배경으로만 등장 — 원칙7 위반 없음(단 sc-022 t0 의 이벤트 라벨은 지적 15).

## 4. 사용자 검수 권장 목록 (결정 G)

함정 12건 + 지나가는 언급 3건(= 함정 중 sc-038~040) 중 특히 봐야 할 것:

1. **sc-012** (지적 6·7) — 권쌤=영어쌤 연결을 사용자도 문맥으로 못 정하는지 확인. 정하지 못하면 이 건은 함정이 아니라 "저자만 아는 정답" 이다. 허용 집합 세 값 전부도 함께 결정.
2. **sc-002** (지적 5) — 성씨 없는 `과장님` 턴이 없어 함정이 비어 있음. 발화 추가(재작성) 여부 결정.
3. **sc-004 / sc-006 / sc-008** (지적 1~4) — 별칭 적재 규칙에 따라 함정이 사라지는 건. 지적 1 의 스키마 결정(`seed_aliases` 도입 vs aliases 정리)이 먼저 필요하며, 이 결정은 promotion 8건 전체에 영향을 준다.
4. **sc-036** — 유효한 함정. 사용자 확인 포인트: t1 "그 친구도 대리라서 임대리라고 부르래" 만으로 임대리가 이대리와 다른 사람임을 사용자가 자연스럽게 읽는지(읽힌다고 판단함).
5. **sc-015** — 유효한 함정. t3 "우리 부장님은 언제 퇴사하시려나" 가 친구 은우(별명 부장님)가 아니라 한부장으로 읽히는지 사용자 확인.
6. **sc-039** — 지나가는 언급 중 가장 경계에 가까운 건. `기사님` 이 두 턴에 걸쳐 등장하고 한부장이 음료수까지 챙겨 준 상대라, "두 번 나왔다고 등록 대상이 되지 않는다" 는 why 를 사용자가 받아들이는지 확인. 검수자 판단은 타당(이름·지속 관계 없음).
7. **sc-038 / sc-040** — 연예인·정치인·행인. 검수자 판단은 타당. 사용자 확인은 "그 정치인 닮았다고" 처럼 비유로 쓰인 인물이 등록 대상이 아니라는 데 동의하는지 정도.
8. sc-010, sc-013 — 유효한 함정. 특이 사항 없음(사용자 확인은 형식적).

## 5. 의견 (확신 없음 — 상태 없음, U6 판단 참고용)

- **인용형(메타언어) 지칭을 mention 으로 세는 규칙**: "X 라고 부르면/불러야/적혀 있음" 꼴의 X 를 mention 으로 라벨한 건이 sc-001 t2 `김부장님`, sc-003 t2 `최대리`, sc-005 t2 `회장님`, sc-009 t3 `준호형`, sc-014 t3 `자기`, sc-016 t2 `린이`, sc-036 t1 `임대리` 로 일관돼 있으나 sc-015 t0 "은우 별명이 부장님이야" 의 `부장님` 은 라벨하지 않았다(같은 턴 `은우` 와 이중 계산 회피로 보임). 규칙을 한 줄 적어 두면 P4 의 mention 검출 F1 해석이 쉬워진다. 특히 sc-014 t3 `자기` 는 표면형이 너무 일반적이라 검출 실패가 ER 실패로 섞일 수 있다.
- **sc-032 t1 `형`**("동네 형이라 그런지 편하더라") 은 서술어 명사(분류)이지 지칭이 아닐 수 있다. mention 으로 둘지 뺄지는 위 규칙과 함께 결정. (반면 sc-037 t2 `형이랑` 은 명백한 지칭 — 지적 9.)
- **sc-009 t3 `other / none`**("준호형이라 부르니까 좋아하더라") 은 사건이라기보다 호칭 선호 사실이다. "events 는 실제 사건이 있을 때만" 규칙에 비추면 빼는 쪽이 자연스럽다. 같은 이유로 sc-035 t0 `other`(신입 입사 소식) 는 지적 14 의 규칙에 따라 personal_share 또는 없음이 될 수 있다.
- **sc-018 t2 `그 사람`→p2**: 두 인물 모두 직전에 호의(반찬·이사 도움)를 베풀어 "오지랖" 이 둘 다에 걸리며, 결정 근거는 존대 수준(누나에게 '그 사람' 은 부자연)과 인접성뿐이다. 사람이 정할 수 있다고 봤으나 pronoun 8건 중 가장 약한 근거.
- **sc-009 위계 `동`**: 사용자가 `준호형` 이라 부르는 동료를 `동` 으로 두었다. 직급 동일이면 `동` 이 맞을 수 있으나 호칭 사전이 `형` 을 상으로 볼 경우 `s_rule` 이 엇갈린다 — 의도라면 그대로, 아니면 `상`.
- **sc-010 alias `민준`(김팀장 쪽)**: 부하 직원이 상사 김민준을 맨 이름 `민준` 으로 부르는 별칭은 현실성이 낮다. 함정 성립을 위한 장치로 이해했고 규칙 위반은 아니다.
- **동일 가상 성명의 관계 재사용**: 정하윤(sc-005 친구/동, sc-030 가족/하), 최유진(sc-003 직장/하, sc-011 가족/상), 조은우(sc-006 직장/하, sc-015 친구/동) 등이 시나리오마다 다른 관계로 등장한다. 시나리오가 독립이라 문제는 아니나, P4 러너가 시나리오 간 DB 를 비우지 않으면 오염된다 — P4 01-plan 인계 사항으로 적어 두는 것을 권한다.
- **`absolute` 1건뿐**: 지적 13 이 반영되면 `occurred_at_kind: absolute` 가 0건이 된다. 결정 B 가 절대 시각 값은 라벨하지 않되 종류는 라벨한다고 했으므로, 과거 절대 날짜("지난 3일에 …") 발화를 1~2건 두는 것이 F1 분모 관점에서 낫다.

## 재검수 (2026-09-07, verifier 새 컨텍스트, 대상 커밋 76add8a)

재검수자: verifier (fable, 새 컨텍스트 — 20260906-1938 검수와 다른 컨텍스트) | 대상: `git diff 40c36f8 76add8a -- data/scenarios/`(718행) + `data/scenarios/*.json` HEAD 실물(76add8a 이후 `data/scenarios/` 변경 0 — `git diff --stat 76add8a HEAD -- data/scenarios/` 빈 출력) | 판정 기준: 01-plan "검수 후 결정 — 사용자(2026-09-07)" 결정 I·J·K·L + 지적 원문 | 참고(주장, 증거 아님): `20260906-2010-u6-change-summary.md`, `20260906-2010-review-packet-2.md`(854행, sc-036 블록이 JSON 과 일치함을 대조) | 읽지 않음: `.claude/skills/entity-resolution/SKILL.md`, `app/er/`

증거 파일(이 재검수에서 생성): `evidence/20260907-recheck-validate-strict.txt`(`validate_scenarios.py --strict` rc=0, 검사 (0)~(15) PASS, 40건/오류 0) · `evidence/20260907-recheck-consistency.txt`(읽기 전용 스캔 — §A seed 인물 aliases 와 인용/승진 턴 대조, §B 2인 이상 seed 의 alias 교집합·직급형 호칭, §C 72개 이벤트 턴의 시간 낱말과 kind, §D `이번` 전건 8턴, §E ambiguous 턴 이벤트). 재계산 수치(스크립트): 시나리오 40 · mentions 134 · events 72 · aliases 120 · virtual_names 100(박지훈·오지호·김민준·배준호 잔존, 결정 I 로 빠진 8개 부재) · type 분포 conflict 6/favor 9/meal 18/meeting 11/other 2/personal_share 25/praise 1 · kind 분포 relative 43/none 28/absolute 1 — change-summary §4 및 `manifest.json` `distribution` 과 전부 일치.

### 집계

닫힘 16(상태 `반영` + 해시 76add8a) · 기각 1(#17, 결정 L) · 열림 3(#18·#19·#20, 전부 재검수 신규). 지적 1~17 중 반영 불충분으로 열림 유지한 것: 0.

### 지적 1~17 판정 근거 (JSON 실물 기준)

| id | 확인한 것 |
|---|---|
| 1 | promotion 8건 seed 인물 aliases 에서 승진 후 호칭 13개 제거를 diff 로 확인(sc-001 김부장·부장님 / sc-002 이과장·과장님 / sc-003 최대리 / sc-004 팀장님·강실장 / sc-005 회장님 / sc-006 임주임·주임님 / sc-007 문실장 / sc-008 김부장·부장님). consistency §A: 남은 seed alias 중 인용·승진 턴에 나오는 것은 승진 **전** 호칭(김팀장·이대리·강팀장·채원씨·아빠)과 "별명이 부장님이야"(sc-015, 기존 사실 진술)·"린이라고 부르는 건 나뿐이야"(sc-016, 습관 진술)·"자기라고 부르면"(sc-014, 습관 진술)뿐 — 대화 중 처음 생기는 호칭 0. `schema.json` aliases 설명에 결정 I 명문화 확인 |
| 2 | sc-004 p1 `['강팀장']`, p2 `['윤팀장','윤서준']` — `팀장님` 양쪽 부재. trap.reason 에 결정 I 문장 추가 |
| 3 | sc-006 p1 `['채원씨']`, p2 `['조주임']` — `주임님` 양쪽 부재 |
| 4 | sc-008 p1 `['아빠','김차장']`, p2 `['한부장']` — `부장님` 양쪽 부재 |
| 5 | sc-002 t3 `이제 막 승진한 과장님은 호칭 어색하대`(mention `과장님`→p1), t4 `이서연 과장님이라고 부르니 좀 웃김ㅋㅋ` 추가(5턴), `과장님` 이 두 사람 aliases 어디에도 없음. 맨 `과장님` 이 이서연을 가리키는 턴이 생겼고 단서("이제 막 승진한")로 사람이 정할 수 있다 — 결정 K 대로 |
| 6 | sc-012 t3 `알고 보니 영어쌤이 권쌤이더라ㅋㅋ` 삽입(mention 영어쌤→p2·권쌤→p2), 옛 t3 가 t4 로. 결정 K 대로 |
| 7 | sc-012 `allowed: ["identity","new_person"]` |
| 8 | sc-003·sc-007 `allowed: ["none"]` |
| 9 | sc-037 mentions 에 `{turn:2, surface:"형", gold:"p1"}` |
| 10 | sc-037 events 에 `{turn:0, meeting, relative}` |
| 11 | consistency §C 72건 전수: 그 턴 안에 시간 낱말이 없는데 relative 인 것 0, 앞 턴 상속 0(sc-014 t1 은 이벤트 자체 제거, sc-031 t2·sc-030 t1·sc-003 t3·sc-006 t3 none). 지적한 쌍 전부 해소. 남는 것은 시간 낱말 **정의**의 문제라 별도 지적 18 |
| 12 | `요즘` 이 있는 이벤트 턴 6건(sc-009 t1·016 t1·017 t1·024 t3·026 t1·033 t4) 전부 relative(sc-003 t0 은 U6-3 으로 이벤트 제거) |
| 13 | sc-001 t3 meal 제거. sc-005 t4 발화 교체(아래 재검수 항목 (a)) |
| 14 | 승진·이직·취업·입사·이사 소식 턴 personal_share: sc-001 t1·002 t0·003 t1·004 t0·005 t1·006 t0·007 t1·008 t0·016 t1·017 t1·023 t0·027 t0·035 t0·037 t1 — 예외 0. sc-032 t0 의 `이직` 은 사용자 자신의 일이라 meal 만(맞음) |
| 15 | sc-005 t3·022 t0·034 t0 → personal_share. 같은 규칙으로 sc-030 t2·024 t3·039 t2 도 personal_share(consistency §C). 사용자 부재 사건에 활동 type 이 남은 것 0 |
| 16 | sc-015 t2 conflict/none(`또 잔소리함` — 시간 낱말 없음). sc-008 t1 conflict/relative(`오늘도`) |
| 17 | manifest `virtual_names` 에 박지훈·오지호·김민준·배준호 잔존, 발화 무변경 — 결정 L 대로 기각 |

### 재검수 항목 3건 판정

- **(a) sc-005 t4 미래 약속 → 과거 절대 날짜 발화 교체(`9월 20일에 축하 겸 밥 먹기로 했어` → `9월 3일에 축하 겸 밥 먹었어ㅋㅋ`, meal/absolute 유지)** — **확인(허용)**. 근거: 결정 J(b)는 "미래 약속은 events 가 아니다" 이며 교체 후 발화는 과거 사건이라 J(b) 를 지킨다. 원칙 8 의 금지는 "평가 대상 파이프라인에 유리하도록 데이터를 고르거나 고치는 것"(01-plan 결정 F 원문)이고, 이 교체는 `absolute` 표본 0건을 막아 F1 분모를 **유지**하는 방향이지 판정을 쉽게 만드는 방향이 아니다(날짜 파싱 케이스가 하나 남는 쪽이 오히려 어렵다). 결정 B(값이 아니라 종류만 라벨)도 유지된다. 발화 수정은 결정 K 가 sc-002·sc-012 에 허용한 것과 같은 종류이며 지적 13 이 "absolute 0건이 되는 점도 함께 결정" 을 요구했으므로 범위 밖 임의 수정이 아니다. 변경은 diff·change-summary §3·03-log 에 드러나 있어 숨김 없음. 남는 절차: 사용자가 결정 목록에 "sc-005 t4 과거 절대 날짜 교체 승인" 한 줄을 추가하면 완결(결정 K 의 명시 범위 밖이었기 때문). 데이터 변경 요구 없음.
- **(b) sc-039 t2 `한부장님이 기사님한테 음료수까지 챙겨주심` = personal_share** — **확인(personal_share 유지)**. 근거: 결정 J(d) 원문 "사용자–인물 사이 사건만 meal/meeting/favor/conflict/praise" — 이 행위는 한부장→기사님이라 활동 type 은 불가. "이벤트 없음" 안과 비교하면, 같은 구조인 sc-030 t2 `엄마도 용돈 챙겨줬대`(엄마→하윤)·sc-022 t0(규진–세호)·sc-034 t0(아빠 혼자 낚시)가 모두 personal_share 이므로 이 건만 비우면 J(d) 가 다시 갈린다. 원칙 7 우려는 라벨 수준에서 성립하지 않는다 — `events[]` 골드는 `{turn,type,kind}` 뿐이라 인물 간 관계를 저장하지 않으며, `기사님` 은 `passing_mentions` 로 등록 금지가 별도로 채점된다. 데이터 변경 요구 없음.
- **(c) `이번(에)` 의 relative/none** — **열림(지적 18)**. 전건 grep 결과(consistency §D): `이번에` 가 있는 이벤트 턴 6건 전부 relative, `이번 인사에` 1건(sc-004 t0) none, `이번 주말에`(sc-034 t2) 이벤트 없음. 즉 U6 는 "부사어 `이번에` = relative / 관형어 `이번`+비시간명사 = none" 이라는 하위 규칙을 세워 **일관 적용**했다(불일치가 아니라 규칙 선택의 문제). 그러나 그 규칙은 사용자 결정 J(a) 에 없고 eval-agent 가 `schema.json` 설명에 추가한 것이며, 같은 통사 기준을 `점심`(목적어인데 relative)·`때`(시간명사인데 none)에는 적용하지 않아 기준이 두 개다. 한 줄 규칙으로 사용자 결정이 필요 — 지적 18 에 안 A(권고)/안 B 와 영향 이벤트를 적었다. 어느 안이든 변경은 2건이다.

### 전건 일관성 결과

- **결정 I**: consistency §A·§B. seed 인물 aliases 에 대화 중 첫 등장 호칭 0. 2인 이상 seed 시나리오 16건 중 alias 교집합이 있는 것은 sc-010 `민준`(양쪽, 의도된 동명 함정)·sc-015 `부장님`(양쪽, "별명이 부장님이야" 기존 사실 + 한부장) 2건뿐이고 둘 다 대칭. 직급형 호칭이 한쪽에만 있는 sc-010·020·033 의 `팀장님` 은 상대 인물이 팀장이 아니라 공유 가능성이 없다. 비대칭 0. seed 밖 인물(sc-012 p2·035 p1·036 p2·037 p1)의 aliases 는 `schema.json` 설명대로 규칙 적용 대상이 아니다.
- **결정 J(a)~(e) 72개 전수**(consistency §C): (a) 시간 낱말 없는 턴이 relative 인 것 0 · 기간 표현(세 시간·두 시간) none 2/2 · 앞 턴 상속 0. 남는 것은 지적 18(낱말 정의). (b) 미래 약속·계획 턴(sc-001 t3·007 t3·010 t3·011 t3·014 t2·018 t1·020 t3·021 t0·t2·023 t2·024 t4·025 t2·026 t2·027 t1·t2·028 t2·029 t3·032 t2·033 t3·034 t2·t3·036 t3) 이벤트 0. (c) 예외 0(지적 14 행). (d) 사용자 부재 사건에 활동 type 0(지적 15 행). 반대로 사용자–인물 사건인데 personal_share 인 것 0. 사주다·쏘다(음식·음료) = meal, 갖다주다·보내주다·봐주다·들어주다·드리다 = favor — 구분 일관. (e) 잔소리 2/2 conflict. **누락 방향**은 지적 20.
- **U6-1 인용형 지칭**: sc-001 t2·002 t4·003 t2·004 t2·005 t2·009 t3·014 t3·015 t0·016 t2·035 t0·036 t1 전부 mention — 예외 0.
- **U6-2 서술어 명사**: sc-032 t1 `형` 제거·sc-037 t0 `사촌형` 미계수·sc-035 t0 `신입` 미계수·sc-037 t2 `형` 계수 — 규칙대로. 예외 1 = sc-036 t1 `그 친구`(지적 19).
- **U6-3 상태 서술**: sc-003 t0·009 t3·014 t1 제거 확인. 남은 상태 서술 턴(sc-009 t2·011 t1·017 t2·t3·018 t2·019 t2·024 t2·030 t3·032 t1·036 t1) 이벤트 0 — 일관.
- **U6-4**: consistency §E — sc-013 t3·022 t1·024 t4 이벤트 0.
- **허용 집합**: `none` 22 · `identity,none` 14 · `new_person` 2 · `identity,new_person` 2(sc-012·036) · 세 값 전부 0. 함정 없음+후보 1명 건에 identity 가 남은 것 0.

### 사용자 검수 권장 목록 (결정 G — 반영 후 기준으로 갱신, 함정 12 + 지나가는 언급 3)

1. **sc-002**(재작성) — t3 `이제 막 승진한 과장님` 을 사용자가 박과장이 아니라 이서연으로 읽는지, t4 `이서연 과장님이라고 부르니` 가 자연스러운지. 이제 유효한 함정.
2. **sc-012**(재작성) — t3 `알고 보니 영어쌤이 권쌤이더라` 로 두 호칭이 한 사람임을 사용자도 읽는지. `allowed: identity/new_person` 동의 여부(등록 대상이 있으므로 `none` 배제).
3. **sc-004 / sc-006 / sc-008** — aliases 에서 공유 호칭이 빠져 t2·t2·t1 이 문맥으로만 갈린다. 사용자는 각 trap.reason 의 단서("습관대로 불렀다가 혼남" / "인수인계 받으라고" / "우리 회사")가 사람에게 충분한지 확인. 검수자 판단: 충분.
4. **sc-015** — U6-1 로 t0 `부장님`→은우(별명), t3 `우리 부장님`→한부장 — 같은 표면형·다른 정답이 한 시나리오 안에 있다. 사용자가 t3 을 `퇴사` 문맥으로 상사로 읽는지.
5. **sc-036** — 지적 19(`그 친구` mention 누락) 반영 후 재확인. t1 만으로 임대리 ≠ 이대리 를 읽는지.
6. **sc-039** — 재검수 (b): t2 personal_share 유지에 동의하는지, `기사님` 2회 등장이 등록 대상이 아니라는 why 를 받아들이는지.
7. **sc-038 / sc-040** — U6 에서 라벨 무변경(sc-040 t0 kind 만 none). 비유·행인이 등록 대상이 아니라는 데 동의하는지.
8. **sc-010 / sc-013** — 무변경(kind 만 조정, sc-013 은 지적 20 (1) 대상). 형식적 확인.

### 이번 재검수의 의견 (상태 없음)

- sc-009 t3 `준호형`·sc-014 t3 `자기` 는 인용형 턴에 나오는 alias 인데 발화가 "처음 부르기 시작했다" 고 하지 않아 결정 I 위반으로 보지 않았다. 다음 데이터(P10)에서는 이런 턴의 alias 를 넣을지 말지를 생성 프롬프트에 한 줄로 두는 편이 낫다.
- 지적 20 (3) 의 경계("사용자에게 한 행위")는 J(d) 가 "사용자–인물 사이 사건" 이라고만 해서 생긴 빈틈이다. P10 프롬프트/스키마 설명에 "행위의 한쪽이 사용자이면 활동 type" 처럼 적으면 없어진다.
- 재검수에서 데이터·코드·계획을 고치지 않았다. 생성한 파일은 evidence 2개와 이 파일의 상태 열·지적 18~20 행·이 절뿐이다.

## 3차 재검수 (2026-09-07, verifier 새 컨텍스트, 대상 커밋 6906af4)

재검수자: verifier (fable, 새 컨텍스트 — 1·2차와 다른 컨텍스트) | 대상: `git diff 76add8a 6906af4 -- data/scenarios/`(promotion 16행·alias 10행·new_person 5행·manifest 11행·schema 4행) + `data/scenarios/*.json` HEAD 실물(6906af4 = HEAD, `git diff --stat 6906af4 HEAD -- data/` 빈 출력) | 판정 기준: 01-plan "검수 후 결정" 결정 M(88행)·결정 N(89행)·재검수 (b)·#19·#20 지시(90행) + 03-log U6-2 항목의 U6-5 규칙 문장 + `schema.json` events (h) | 참고(주장, 증거 아님): `20260907-1100-u6-2-change-summary.md` §1~§5, `20260907-1100-review-packet-3.md`. eval-agent 의 `20260907-1100-u6-2-kind-scan.txt` 는 **읽지 않고** 독립 스캔으로 대체 | 읽지 않음: `.claude/skills/entity-resolution/SKILL.md`, `app/er/`

증거 파일(이 재검수에서 생성, 접두 `20260907-recheck3-`): `-validate-strict.txt`(`validate_scenarios.py --strict` rc=0, 검사 (0)~(15) PASS, 40건/오류 0) · `-kind-scan.txt`(verifier 독립 시점 낱말 목록 33개 + `때`(`때문` 제외)로 76개 이벤트 재판정 — 불일치 0, relative 48 / none 28 / absolute 0) · `-decision-N.txt`(sc-005 t4 UTF-8 바이트열 대조·이벤트 제거·absolute 0건·sc-036 mentions 실물·manifest distribution) · `-u6-5-sweep.txt`(160턴 전부에 verifier 판정 어휘를 붙인 스윕). 재계산: 시나리오 40 · mentions 135 · events 76(conflict 6·favor 9·meal 17·meeting 12·other 5·personal_share 26·praise 1) · kind relative 48·none 28·absolute 0 — `manifest.json` `distribution` 및 change-summary §4 와 일치.

### 집계

닫힘 19(#1~#16 반영 76add8a · #18~#20 반영 6906af4) · 기각 1(#17, 결정 L) · **열림 2(#21·#22, 전부 3차 신규)**. #18~#20 중 반영 불충분으로 열림 유지한 것: 0.

### #18·#19·#20 판정 근거 (JSON 실물)

- **#18 / 결정 M** — sc-004 t0 `personal_share/relative`, sc-005 t3 `personal_share/relative`(diff 268·341행). `schema.json` `occurred_at_kind` 설명에서 "'이번 인사'·'첫 회의' 처럼 … 시간 표현이 아니다" 문장이 사라지고 "시점을 가리키는 낱말이 하나라도 있으면 문장 성분과 무관하게 relative … 기간 표현 … 제외" 로 교체(문자열 `부사어 이번에` 는 폐기 설명문 안에만 1회 남음). 지적이 안 A 에서 요구한 변경 2건과 정확히 같고 안 B 방향 변경(sc-009 t0·sc-020 t1) 없음. 결정 M 전건: 독립 스캔 76/76 일치 — 특히 `이번`(이벤트 턴 7: sc-001 t1·002 t0·004 t0·005 t1·006 t0·008 t0·023 t0 전부 relative), `첫`(sc-005 t3·sc-035 t2 relative), `점심`(sc-009 t0·020 t1·033 t0 relative), `때`(sc-005 t3 relative; sc-033 t4 는 `때문` 이지만 `요즘` 으로 relative), 기간만 있는 턴(sc-016 t0·024 t0) none. 시점 낱말 없이 relative 인 것 0, 시점 낱말 있는데 none 인 것 0.
- **#19** — sc-036 `mentions[1] = {turn:1, surface:"그 친구", gold_person_id:"p2"}`, `임대리`(t1) 앞. 발화 `그 친구도 대리라서 임대리라고 부르래ㅋㅋ` 에 표면형 그대로 있음. 검사 (12)·(15) PASS.
- **#20** — (1) sc-013 `{turn:1, meeting, relative}` (2) sc-035 `{turn:2, personal_share, relative}` (3) sc-035 `{turn:1, other, none}`·sc-012 `{turn:0, other, relative}` 추가, sc-012 t1 이벤트 없음. 지적이 요구한 "규칙을 정해 03-log 에 남기고 적용/비적용을 명시" 는 03-log U6-2 항목 "U6-5 신설" 문단 + change-summary §2 제외 턴 표로 충족.
- **결정 N** — `git show 40c36f8:data/scenarios/promotion.json` 의 sc-005 t4 `9월 20일에 축하 겸 밥 먹기로 했어` 와 HEAD 가 **UTF-8 바이트열 동일**(`39ec9b94…ec96b4`, `-decision-N.txt`), sc-005 utterances 5개 전부 40c36f8 과 동일. events 에 turn 4 없음(40c36f8 의 meal/absolute 가 제거). 전 파일 `absolute` 0건 — `schema.json` 설명이 이 사실과 P10 보충을 적고 있음.
- **재검수 (b)** — sc-039 t2 `personal_share/none` 유지 확인(변경 없음).

### 결정 M 스캔 결과 (독립)

이벤트 76건 · 불일치 0. eval-agent 의 kind-scan 과 독립적으로 낱말 목록을 더 넓게(그제·다음달·오랜만·조만간·최근·방금·며칠 등 추가) 잡았으나 결과는 같다 — 넓힌 낱말이 이벤트 턴에 나타나지 않기 때문이며, 그 낱말들이 있는 턴(sc-034 t3 `오랜만에`, sc-024 t4 `조만간`, sc-018 t3 `나중에`)은 전부 미래·계획 턴이라 이벤트가 없다. 경계 판단 1건: sc-039 t0 `야근하다가` 는 활동이지 시점 낱말이 아니므로 none 이 맞다.

### U6-5 판정

규칙 자체: 01-plan 90행이 eval-agent 에게 위임한 범위 안이고, 이미 있던 sc-012 t2·sc-004 t2(conflict)·sc-007 t0·sc-029 t0(other) 와 같은 기준이라 **타당**. 전건 일관성(`-u6-5-sweep.txt` 160턴): 사용자가 한쪽인 행위 턴에 type 이 있는지 — 위반 **1건 sc-034 t1(#21)**. 부작위 제외 — sc-008 t3(사용자 측)·sc-012 t1·sc-033 t2(인물 측) 3/3 이벤트 없음, 부작위인데 이벤트 있는 것 0. 세부(같은 사건 두 번째 턴) 제외 — sc-019 t1·t3, 020 t2, 025 t1, 031 t1, 033 t1, 037 t2, 038 t1·t2, 039 t1, 040 t1·t2 이벤트 없음. 반대로 같은 자리라도 **type 이 다른** 사건은 라벨돼 있다(sc-020 t1 meal, sc-024 t1 meal, sc-031 t2 favor, sc-010 t2 favor) — "성격이 다른 사건 둘이면 두 줄" 과 같은 방향으로 일관. 기존 conflict 6건 전부 인물→사용자(혼남·잔소리·부딪힘·깨짐), other 5건 전부 수업·과제·업무 지시 — 충돌 0. favor 9건 중 사용자→인물 1건(sc-011 t2)이 대응표 문구 밖(#22, 문구 문제).

change-summary §3 반전 가능 판단 4건:
1. **sc-012 t1·sc-033 t2 부작위 제외 — 동의.** `넘어가주다` 의 `-주다` 는 이득의 뉘앙스일 뿐 일어난 행위가 없다(U6-3(f)). favor 로 세면 "안 혼냈다" 마다 favor 가 붙어 P4 favor 정밀도가 라벨러 해석을 재게 된다. 사용자 측 부작위 sc-008 t3 과 대칭.
2. **sc-006 t2 `other/none` 추가 — 동의.** 주어가 사용자(채원에게 지시)든 상사(사용자에게 지시)든 사용자가 한쪽이고 마찰·이득이 아니므로 other. sc-035 t1 과 같은 구조. `events[]` 에 person_id 가 없어 주어 해석이 라벨에 영향을 주지 않는다. 시점 낱말 없음 → none.
3. **sc-039 t1 제외 — 결과 동의, 근거 보강.** "t0 배달 상황의 세부" 보다 **`배달 기사님` 이 `persons` 에 없는 passing_mention 이라 이벤트를 붙일 인물이 없다** 가 더 강한 근거다(t2 는 한부장이 행위 주체라 personal_share 가 가능). P10 규칙에 "persons 밖 인물만의 일에는 이벤트 없음" 을 한 줄 두는 것을 권한다(의견, 상태 없음).
4. **sc-037 t2 제외 — 동의.** 술(=meal, t1 에 있음)·얘기(=meeting, t0 에 있음)로 type 이 중복된다. sc-010 t2 `들어줬어`(favor, 별도 type) 와 다른 점이 그것이다.

### 새 지적

#21(sc-034 t1, 데이터 1건)·#22(favor 문구, 데이터 0건 예상). 둘 다 U6-5 가 생기기 전에는 드러나지 않던 것이다. 반영 담당은 eval-agent(U6-3), 닫는 것은 검수자(R-11).

### 사용자 검수 권장 목록 최종본 (결정 G — 함정 12 + 지나가는 언급 3, 6906af4 기준)

함정 12 = promotion 4(sc-002·004·006·008) + alias 4(sc-010·012·013·015) + new_person 4(sc-036·038·039·040). 지나가는 언급 3 = sc-038·039·040(함정 12 안에 포함). 이번 라운드(6906af4)가 함정 건에 준 변경은 sc-006(other 이벤트)·sc-012(t0 이벤트)·sc-013(t1 이벤트)·sc-036(`그 친구` mention)뿐이고 trap.kind·allowed·ambiguous 는 불변.

| # | 시나리오 | 무엇을 확인하면 되는가(한 줄) |
|---|---|---|
| 1 | sc-002 (promotion_collides_with_existing_title) | t3 `이제 막 승진한 과장님` 을 박과장이 아니라 이서연으로 읽히는지 — 읽히면 함정 유효 |
| 2 | sc-004 (same_title_two_persons) | t2 `습관대로 팀장님이라 불렀다가 혼남` 의 `팀장님` 이 aliases 없이도 강 실장으로 읽히는지 |
| 3 | sc-006 (promotion_collides_with_existing_title) | t2 `주임님한테 인수인계 받으라고 했음` 의 `주임님` 이 조은우(원래 주임)로 읽히는지, 그리고 이 턴의 `other` 이벤트(지시)에 동의하는지 |
| 4 | sc-008 (same_title_two_persons) | t1 `우리 회사 부장님` 이 아빠가 아니라 한부장으로 읽히는지 |
| 5 | sc-010 (nickname_shared) | t1 `동창 민준이` 가 김팀장이 아니라 친구로 읽히는지(형식적) |
| 6 | sc-012 (same_title_two_persons + 등록 대상) | t3 `알고 보니 영어쌤이 권쌤이더라` 로 두 호칭이 한 사람임을 읽는지, `allowed: identity/new_person` 에 동의하는지, t1 `그냥 넘어가주셨는데` 가 이벤트 아님(부작위)에 동의하는지 |
| 7 | sc-013 (similar_name_two_persons) | t3 `아까 걔` 를 사용자도 정할 수 없는지(ambiguous 유지), t1 `약속 있어서 나감` 이 meeting 인 데 동의하는지 |
| 8 | sc-015 (nickname_shared_with_title) | t0 `부장님`→은우(별명), t3 `우리 부장님`→한부장 — 같은 표면형이 시나리오 안에서 갈리는 것을 사용자도 `퇴사` 문맥으로 읽는지 |
| 9 | sc-036 (new_person_resembles_existing) | t1 `그 친구도 대리라서 임대리라고 부르래` 만으로 임대리 ≠ 이대리 를 읽는지, `그 친구` 가 임세아의 첫 지칭이라는 데 동의하는지 |
| 10 | sc-038 (passing_mention) | `남주 배우`·`그 아이돌` 이 등록 대상이 아니라는 데 동의하는지 |
| 11 | sc-039 (passing_mention) | `기사님` 이 두 턴 나와도 등록 대상이 아니라는 why 를 받아들이는지, t2 personal_share(한부장→기사님) 유지에 동의하는지 |
| 12 | sc-040 (passing_mention) | `웬 아저씨`(행인)·`그 정치인`(비유)이 등록 대상이 아니라는 데 동의하는지 |
| — | 지나가는 언급 3 = 위 10~12 | 세 건 공통: 인물 카드가 생기거나 new_person 질문이 나오면 오탐이라는 채점 방향(D1)에 동의하는지 |

### 이번 재검수의 의견 (상태 없음)

- `events[]` 골드에 person_id 가 없어 sc-006 t2 처럼 주어가 생략된 지시 턴은 "누구의 이벤트인가" 를 채점하지 않는다. P4 가 이벤트 F1 을 `{turn,type,kind}` 로만 맞추면 되므로 문제 없으나, P10 에서 이벤트에 person 을 붙일 계획이면 이 턴 같은 것이 먼저 흔들린다.
- 재검수에서 데이터·코드·계획을 고치지 않았다. 생성한 파일은 evidence 4개(`20260907-recheck3-*`)와 이 파일의 상태 열 3행·지적 21~22 행·이 절뿐이다.

## 4차 재검수 (2026-09-07, verifier 새 컨텍스트, 대상 커밋 aeed0bd)

재검수자: verifier (fable, 새 컨텍스트 — 1~3차와 다른 컨텍스트) | 대상: `git diff 6906af4 aeed0bd -- data/scenarios/ scripts/dump_scenarios.py`(normal.json 1행·schema.json 1행·manifest.json 4행·dump_scenarios.py 1행) + `data/scenarios/normal.json` HEAD 실물(aeed0bd = HEAD, `git diff --stat aeed0bd HEAD -- data/ scripts/` 빈 출력) | 판정 기준: 01-plan "검수 후 결정" 결정 O(91행)·결정 P(92행) + 03-log U6-2 항목의 U6-5 예외 (ii) 문장 | 참고(주장, 증거 아님): `20260907-1200-u6-3-change-summary.md` §1·§2 | 읽지 않음: `.claude/skills/entity-resolution/SKILL.md`, `app/er/`. 전건 재스캔은 하지 않았다(3차에서 완료, 이번 diff 는 1턴).

증거 파일(이 재검수에서 생성): `20260907-recheck4-validate-strict.txt`(`python scripts/validate_scenarios.py --strict` rc=0, 검사 (0)~(15) PASS, 40건/오류 0).

### 판정

- **#21 / 결정 O — 반영 aeed0bd.** `normal.json` sc-034 `events = [{turn:0, personal_share, relative}, {turn:1, favor, none}]`, `expected_ask_user.allowed = ["none"]` 불변. diff 에서 데이터 변경은 이 1행뿐이고 manifest `distribution.event_types` 가 favor 9→10·personal_share 26→25 로 따라갔다(총 76 불변). kind `none` 은 결정 M 기준으로 맞다(t1 에 시점 낱말 없음, `계속` 은 빈도). **eval-agent 의 "별개 사건" 판단에 동의한다** — U6-5 (ii) 는 "한 사건을 여러 턴에 나눠 서술" 한 경우인데, t0 `아빠가 지난주에 낚시 다녀오셨대` 는 사용자 부재 근황(J(d) personal_share)이고 t1 `월척 잡았다고 사진 계속 보내심` 은 아빠→사용자 행위로 주체·수신자·행위가 모두 다르다. sc-025 t1 `택배 오늘 도착했는데`(t0 김치 발송의 도착 = 같은 사건의 시점 묘사, 제외)와 구조가 다르고, 오히려 sc-029 t0 other + t2 favor(`영상도 보내줬어`) 처럼 "type 이 다른 사건은 각각 라벨" 한 기존 방향과 같다. 인물→사용자 `보내` 턴은 데이터 전체에서 sc-025 t0·sc-029 t2·sc-034 t1 세 개이고 이제 3/3 favor 로 일관. 원칙 8: personal_share(최다 클래스)에서 favor 로 옮긴 것이라 이벤트 F1 을 쉽게 만드는 방향이 아니다.
- **#22 / 결정 P — 반영 aeed0bd.** `schema.json` `events` description (h): `어느 방향이든 호의 = \`favor\`` · `scripts/dump_scenarios.py` 검수 항목 6 (h): `어느 방향이든 호의 = favor` · 03-log U6-3 항목 정정문: `"어느 방향이든 호의 = favor" 로 정정한다` — 셋 다 결정 P 원문("어느 방향이든 호의 = favor")과 글자가 같다. 구 문구 `사용자에게 이득이 되는 호의` 는 `data/`·`scripts/` 에 0회, 03-log 에는 72행(U6-2 원문)·81행(정정문의 인용)에만 남아 있고 이는 로그 소급 수정 금지 규칙에 따른 것이며 권위 문구는 schema (h)·dump 항목 6 으로 명시돼 있다. 데이터 변경 0(sc-011 t2 favor 무변경, favor 10건 중 사용자→인물 1건 편중은 change-summary §5 가 P10 인계로 적음).

### 최종 집계

**반영 21**(#1~#16 76add8a · #18~#20 6906af4 · #21~#22 aeed0bd) · **기각 1**(#17, 결정 L) · **열림 0**. 총 22.

### 검수 완료 선언

**라벨 검수 완료 — 수용 기준 "라벨 검수 완료" 의 기계 판정 조건 충족**: `grep -cE '\| *열림 *\|' docs/wiki/packages/P1-pilot-dataset/evidence/20260906-1938-label-review.md` = 0, `validate_scenarios.py --strict` rc=0(`20260907-recheck4-validate-strict.txt`). 사용자 검수 최종 목록(결정 G — 함정 12 + 지나가는 언급 3)은 **3차 절의 표를 그대로 유지**한다 — aeed0bd 가 함정 12건에 준 변경은 없다(sc-034 는 normal, 함정 아님).

### 이번 재검수의 의견 (상태 없음)

- 재검수에서 데이터·코드·계획을 고치지 않았다. 생성한 파일은 evidence 1개(`20260907-recheck4-validate-strict.txt`)와 이 파일의 상태 열 2행·이 절뿐이다.

## 사용자 검수 (2026-09-07, 메인 세션 AskUserQuestion — 결정 G)

대상: 3차 재검수 절 최종 목록 12건(함정 sc-002·004·006·008·010·012·013·015·036 + 지나가는 언급 sc-038·039·040). 메인 세션이 각 건의 발화·지칭→골드·이벤트·확인 포인트를 대화에 제시했고, 사용자가 **12/12 동의**(이의 0). 데이터 변경 없음. 검수 기록의 열림 0 유지.
