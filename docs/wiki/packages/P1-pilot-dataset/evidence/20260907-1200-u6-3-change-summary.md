# U6-3 전→후 대조 — 3차 재검수 #21·#22 반영 (결정 O·P)

작성: eval-agent (opus) · 2026-09-07 12:00 · 대상 지적: `evidence/20260906-1938-label-review.md` #21·#22 (`## 3차 재검수` 절)
반영 근거: `01-plan.md` "검수 후 결정 — 사용자(2026-09-07)" **결정 O**(#21) · **결정 P**(#22)
평가한(=수정 전) 데이터 커밋: dev `6906af4`(U6-2) + 이 단위의 미커밋 변경 — verifier 3차 재검수 대상 커밋과 같다.
지적 표의 `상태` 열은 건드리지 않았다(R-11 — 닫는 것은 검수자).

## 1. 데이터 변경 (전 → 후) — 1턴, 이번 단위의 데이터 변경은 이것이 전부다

| 시나리오·턴 | 발화 | 전 | 후 | 근거 |
|---|---|---|---|---|
| sc-034 t1 (`data/scenarios/normal.json`) | `월척 잡았다고 사진 계속 보내심ㅋㅋ` | `{type: personal_share, occurred_at_kind: none}` | `{type: favor, occurred_at_kind: none}` | 결정 O — 인물→사용자 행위이므로 U6-5 본문에 따라 활동 type. sc-029 t2(`스트레칭 영상도 보내줬어` = favor)·sc-025 t0(`김치 담가서 보내주셨어` = favor)와 같은 꼴 |

- `occurred_at_kind` 는 **결정 M 기준으로 재확인**했다: t1 발화에 시점 낱말(오늘·어제·이번·주말·점심 등)이 없고 날짜·시각도 없으므로 `none` 유지. `계속` 은 빈도 표현이라 시점이 아니다.
- 검수자가 제시한 두 선택지(① U6-5 예외 (ii) 세부 묘사로 보아 **제거** / ② `favor`·`other` 로 **교체**) 중 사용자 결정 O 가 ②(favor)를 택했다. ①을 택하지 않은 이유도 결정 O 에 있다 — t0 는 `아빠가 낚시를 다녀왔다`(인물 근황, personal_share), t1 은 `아빠가 나에게 사진을 계속 보냈다`(인물→사용자 행위)로 **행위 주체·수신자가 다른 별개 사건**이라 "한 사건의 세부 묘사" 가 아니다. sc-025 t1(`택배 오늘 도착했는데`)은 t0 의 김치 발송과 같은 사건의 도착 시점 묘사여서 제외한 것과 구분된다.
- 전건 재점검: 이 규칙 해석으로 다른 턴이 추가로 바뀌는지 확인 — `personal_share` 25건 중 인물→사용자·사용자→인물 행위로 재분류될 턴은 없다(3차 재검수 `-u6-5-sweep.txt` 가 위반 1건 = sc-034 t1 만 보고했고, 그 1건을 이 단위에서 닫았다).

## 2. 문구 정정 (결정 P) — 데이터 변경 0건, 3곳

U6-5 type 대응표의 `favor` 정의가 규칙 **본문**("사용자→인물, 인물→사용자 어느 방향이든")과 어긋나 한 방향만 적혀 있었다. 라벨(sc-011 t2 `유진언니한테 용돈 조금 드렸음` = 사용자→인물 favor)이 자연스러우므로 데이터가 아니라 문구를 고친다.

| # | 파일 | 전 | 후 |
|---|---|---|---|
| 1 | `data/scenarios/schema.json` (`$defs.event` description (h)) | `사용자에게 이득이 되는 호의 = \`favor\`` | `어느 방향이든 호의 = \`favor\`` |
| 2 | `scripts/dump_scenarios.py` (검수 항목 6, (h)) | `사용자에게 이득이 되는 호의 = favor` | `어느 방향이든 호의 = favor` |
| 3 | `03-log.md` | (U6-2 항목 원문은 **고치지 않는다** — 로그는 시간순 기록이라 소급 수정하지 않는 형식 규칙) | 새 **U6-3 항목**에 "U6-5 대응표 정정(결정 P)" 문장으로 남긴다 |

정정 후 대응표 전문(권위 문구): **행위의 한쪽이 사용자이면(양방향) 활동 type 을 쓴다 — 꾸짖음·질책·불이익 = `conflict`, 어느 방향이든 호의 = `favor`, 음식·음료 제공 = `meal`, 함께한 활동 = `meeting`, 칭찬 = `praise`, 마찰도 이득도 아닌 업무 지시·수업·과제 부과 = `other`.**
문구가 바뀌어도 재분류되는 기존 라벨은 없다(favor 10건 중 사용자→인물 1건 sc-011 t2 가 이제 대응표 글자 안에 들어온다 — 라벨 무변경).

## 3. 전·후 수치

| 항목 | 전(6906af4) | 후 |
|---|---|---|
| 시나리오 | 40 | 40 (변경 1건 = sc-034) |
| mentions | 135 | 135 |
| events | 76 | 76 |
| events.type | conflict 6 · **favor 9** · meal 17 · meeting 12 · other 5 · **personal_share 26** · praise 1 | conflict 6 · **favor 10** · meal 17 · meeting 12 · other 5 · **personal_share 25** · praise 1 |
| occurred_at_kind | relative 48 · none 28 · absolute 0 | 불변 (relative 48 · none 28 · absolute 0) |
| aliases · trap · ambiguous · virtual_names | 120 · 12 · 3 · 100 | 불변 |
| `manifest.json` md5 | `b23db1f4a649e3c009d468a56cb66298` | `bf3e17d1587ecf5084a167d89a5c3357` (`--write-distribution` 재생성, 2·3회차 멱등) |

## 4. 검증 (재현 명령 · 출력 파일)

| 명령 | 출력 | 결과 |
|---|---|---|
| `python scripts/validate_scenarios.py --write-distribution` ×3 | `20260907-1200-u6-3-distribution-idempotent.txt` | 1회차 `distribution 갱신`, 2·3회차 `변경 없음(멱등)`, md5 동일 `bf3e17d1…`, rc=0 |
| `python scripts/validate_scenarios.py --strict` | `20260907-1200-u6-3-validate-strict.txt` | rc=0, 검사 (0)~(15) 전부 PASS, `시나리오 40건 / 오류 0` |
| `python scripts/validate_scenarios.py --strict --json` | `20260907-1200-u6-3-validate-strict.json` | `ok: true`, `issue_count: 0`, `scenario_count: 40`, `schema_version: 2` |
| `python -m pytest tests/test_validate_scenarios.py -q` | `20260907-1200-u6-3-pytest.txt` | `69 passed`, rc=0 (검증기·테스트 코드 무변경) |
| `python scripts/dump_scenarios.py --out …-review-packet-4.md` | `20260907-1200-review-packet-4.md` (854행) | 4차 재검수용 패킷. 검수 항목 6 (h) 에 정정 문구 반영 확인 |

`git diff --stat` (이 단위 데이터·스크립트 범위): `data/scenarios/normal.json` 1행 · `data/scenarios/schema.json` 1행 · `data/scenarios/manifest.json` 4행 · `scripts/dump_scenarios.py` 1행.

## 5. 한계·인계 (숨기지 않는다 — 원칙8)

- 이번 변경으로 `personal_share` 25/76 = 33% 편중은 그대로다. `praise` 1건·`other` 5건·`absolute` **0건**의 표본 부족도 그대로 P10-final-eval 로 인계된다(U6·U6-2 인계 유지).
- `favor` 10건 중 사용자→인물은 여전히 1건(sc-011 t2)뿐이다. 결정 P 로 문구는 양방향이 되었지만 **데이터는 방향이 한쪽으로 쏠려 있어**, 사용자→인물 호의의 추출 성능은 P4 에서 사실상 측정되지 않는다. P10 에서 사용자→인물 호의(선물·도움) 표본을 늘려야 한다.
- U6-5 는 이 단위로 세 번째 정정(U6-2 신설 → 3차 재검수 #21 누락 턴 → #22 문구)을 거쳤다. 규칙 신설 시 전건 스윕을 **규칙 본문 기준**으로 돌렸는지(대응표 글자 기준이 아니라) 검수자가 4차에서 확인할 지점이다.
- eval-agent 자체 판단 1건(검수자 판정 대상): §1 의 "t0 와 t1 은 별개 사건" 해석. 세부 묘사(예외 (ii))로 보아 **제거**하는 반대 판단도 논리적으로 가능하나, 결정 O 가 교체를 지시했고 주체·수신자가 다르다는 근거를 위에 적었다.
