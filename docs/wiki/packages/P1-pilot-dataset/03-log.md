# P1-pilot-dataset · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-06 21:05 · docs(P1-pilot-dataset): 계획·계획검증 승인, 패키지 착수 — 파일럿 데이터셋 40건·검증기·verifier 라벨 검수 · 4c80c31
- 변경: 01-plan(architect 초안 + 결정 A~G·H-1~H-3 확정 블록), 02-plan-verify(verifier 1차 보류 → 2차 통과, 기계 검증 1~6차), 05-remediation(F-033bb1 해소), evidence 6파일, backlog P1 하위 불릿 U1~U7, CURRENT active, 03-log 생성
- 이유(기획서·카드 연결): S3.7 평가 명세의 입력 데이터셋(backlog P1 "파일럿 데이터셋 30~50건"). P4 게이트 선행. eval-harness §1 스키마를 persons·events·seed_persons·expected_ask_user.allowed·ambiguous 로 확장(D1 D3 D10 근거)
- 정합성 확인: 원칙 1 4 7 8 9 / D1 D3 D10 / S3.7 S3.1 S3.2 / security(가상 이름·키 미기록·네트워크 없음) — 위반 없음(verifier 점검표 8행 통과)
- 남은 것 · 다음 단위: U1 스키마·검증기·테스트(eval-agent, L-004 승인 후). R-8: U2 위임 시 메인 세션이 위임 프롬프트를 evidence/<ts>-gen-prompt.md 로 저장
- Refs: P1-pilot-dataset S3.7 S3.1 S3.2 D1 D3 D10 원칙8 F-033bb1

## 2026-09-06 18:01 · feat(P1-pilot-dataset): U1 시나리오 스키마·검증기·검증기 테스트 · 1e1320c
- 변경: `data/scenarios/schema.json`(시나리오 객체, Draft 2020-12, schema_version 1), `data/scenarios/manifest.schema.json`(매니페스트 스키마 — schema.json $defs 가 아닌 별도 파일), `data/scenarios/manifest.json`(counts 전부 0·total 0·virtual_names []·generator 키 5종 null), `scripts/validate_scenarios.py`(검사 (0)~(10) 함수 분리, `--dir`/`--json`/`--strict`, 네트워크·DB·app import 없음), `tests/test_validate_scenarios.py`(35건), `requirements-dev.txt`(jsonschema==4.26.0), evidence 2파일
- 이유(기획서·카드 연결): S3.7 이 요구하는 "오병합률·미검출률 분리 측정" 의 입력을 기계가 검사하게 한다. 결정 A(persons)·B(events type·occurred_at_kind)·C(schedule 라벨 없음)·D+H-1(expected_ask_user.allowed·mentions[].ambiguous)·E(seed_persons)·F+H-2(generator 키)를 enum·필수 키로 고정. 위반 표본 8종(01-plan 42행)을 전부 잡는 것이 U1 완료 판정
- 정합성 확인: 원칙1(함정·trap 라벨로 오병합을 측정 가능하게) / 원칙4·D10(허용 집합 라벨이라 T_merge 스윕이 라벨을 위반하지 않음) / 원칙7(인물–인물 필드 없음) / 원칙8(같은 입력 → 같은 요약, Issue 정렬 결정적, generator.seed=null·seed_reason 으로 재현 불가를 숨기지 않음) / S3.1(events.type 7종·relation_tag 5종·hierarchy 3종 enum 이 `app.db.models` 튜플과 순서까지 일치함을 테스트가 대조, 코드 import 없음) / R-1(allowed enum ≠ ask_user.kind, schedule 제외) · R-2(virtual_names 화이트리스트 검사) · R-7(id 전역 연번 `sc-001`·카테고리별 5파일) · R-8(prompt_ref string|string[]) · R-9(ambiguous_mention_count 일치 검사) / security(가상 이름만·키 없음·네트워크 없음) — 위반 없음
- 남은 것 · 다음 단위: U2 promotion 8건 + alias 8건(함정 3건 이상). R-8 대로 메인 세션이 U2 위임 프롬프트를 `evidence/<ts>-gen-prompt.md` 로 저장하고 `manifest.generator` 를 채운다. registry 행·README 는 U7. 남긴 결정 2건 — (a) 없는 시나리오 파일은 기본 0건, `--strict` 면 FAIL(U5·U7 수용 기준 검증은 `--strict` 로), (b) 이름 판정 규칙 = display_name 전체 + 한글 2~4자 alias(`utterances` 본문은 검사하지 않는다 — 사람 검수 항목) **U5 에서 추가(사용자 결정)**: 파일명 ↔ `category` 대응 검사(검사 11).
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D3 D6 D10 원칙1 원칙4 원칙7 원칙8 R-1 R-2 R-7 R-8 R-9

## 2026-09-06 18:18 · feat(P1-pilot-dataset): U2 promotion 8건 + alias 8건 — 함정 8건·ambiguous 1건, virtual_names 68 · 09875aa
- 변경: `data/scenarios/promotion.json`(sc-001~sc-008), `data/scenarios/alias.json`(sc-009~sc-016), `data/scenarios/manifest.json`(counts.promotion=8·counts.alias=8·total=16·trap_count=8·ambiguous_mention_count=1·virtual_names 68개·generator 5키 채움), `docs/wiki/packages/P1-pilot-dataset/05-remediation.md`(pytest 소견 2건 F-7bea05·F-1ba055 추가·본문 작성), evidence 2파일
- 이유(기획서·카드 연결): 01-plan U2(43행). 승진 호칭 변경과 별칭 혼용을 **의도적으로** 넣어 오병합률·미검출률을 분리 측정할 수 있게 한다(S3.7). 함정 건은 두 카테고리 각각 4건(promotion sc-002·004·006·008, alias sc-010·012·013·015)으로 최소 3건 요건 초과 — `trap.kind` 5종(`promotion_collides_with_existing_title` 2 / `same_title_two_persons` 3 / `nickname_shared` 1 / `similar_name_two_persons` 1 / `nickname_shared_with_title` 1)
- 정합성 확인: 원칙1(함정 mention 의 gold 를 다른 인물로 라벨 — 오병합이 지표에 잡힌다) / 원칙4·D10(`expected_ask_user.allowed` 허용 집합, 명백한 6건은 `["none"]`·함정/약단서 10건은 `["identity","none"]`·sc-012 는 seed 밖 인물이라 `new_person` 포함) / 원칙7(persons 는 사용자–인물 관계만, A–B 관계는 발화 배경으로만) / 원칙8(대화형 생성이라 seed 없음을 `seed_reason` 에 그대로 기록, `same_family_as_judge: true` 명시, 실패한 pytest 2건을 숨기지 않고 소견화) / D6(승진 후에도 옛 호칭을 aliases 에 남김) / security(실존 인물·실제 회사명·연락처 없음, 가상 성명만) — 위반 없음
- 남은 것 · 다음 단위: **U2 완료 판정 중 pytest 미충족** — `tests/test_validate_scenarios.py` 의 U1 시점 고정 단정 2건(`total == 0`, `virtual_names == []`)이 데이터가 들어오면 반드시 깨진다. eval-agent 는 테스트를 고치지 않는다(위임 프롬프트 "하지 말 것") → F-7bea05·F-1ba055 로 backend-agent/메인 세션에 인계. 검증기 자체는 rc=0·issue_count 0. 다음 단위 U3(pronoun 8 + normal 10)
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D6 D10 원칙1 원칙4 원칙7 원칙8 F-7bea05 F-1ba055

## 2026-09-06 18:28 · fix(P1-pilot-dataset): F-7bea05·F-1ba055 해소 + 검사 (12)(13) 추가 — 시점 의존 단정 제거, surface·구어체 규칙 기계 검사 · baee71e
- 변경: `scripts/validate_scenarios.py`(검사 `check_surface_in_utterance`(12)·`check_utterance_shape`(13) 추가, 경계 상수 `UTTERANCE_MIN_CHARS=8`/`UTTERANCE_MAX_CHARS=60`/`MIN_TURNS=2`/`MAX_TURNS=6`, `CHECK_NAMES` 에 12·13 추가하고 **11 은 U5 예정으로 비워 둠**, docstring "구어체 규칙" 절·검사 목록 12항목으로 갱신), `tests/test_validate_scenarios.py`(35 → 51건: 시점 의존 2건 교체 + (12)(13) 15건), `docs/wiki/packages/P1-pilot-dataset/05-remediation.md`(F-7bea05·F-1ba055 해소), evidence 2파일
- 이유(기획서·카드 연결): (a) F-7bea05·F-1ba055 — U1 테스트가 저장소 데이터셋에 `total == 0`·`virtual_names == []` 를 단정해 U2 가 데이터를 넣는 순간 깨졌다. 검증기의 성질이 아니라 달력을 시험하는 단정이라 빈 fixture(의도 보존)와 시점 무관 불변식(저장소)으로 갈랐다. (b) 검사 (12) — `surface` 가 그 turn 발화에 없으면 **어떤 방식도 맞힐 수 없는 라벨**이라 P4 에서 베이스라인 4종의 Recall 을 똑같이 깎아 비교를 흐린다. (c) 검사 (13) — 01-plan 리스크 "한국어 구어체 부족"(85행)의 완화책 (d) 발화 길이 8~60자와 U2 생성 프롬프트(`evidence/20260906-1850-gen-prompt-u2.md` 28행)의 턴 수 2~6 을 기계가 세게 했다. 이 리스크는 **과대평가 방향의 편향**이므로 사람 검수(U6)에만 맡기지 않는다
- 정합성 확인: 원칙8(같은 입력 → 같은 수치. 시점 의존 단정 제거로 재현성이 오히려 올라감. `Issue(order=True)` 정렬은 그대로라 출력 순서 결정적) / S3.7(라벨 무결성 검사만 추가, 지표 계산은 P4) / 데이터 불변(`data/scenarios/*.json` 수정 0 — U2 데이터 소유자와 다른 커밋) / security(네트워크·DB·LLM·app import 없음) — 위반 없음. 검사 (13) 은 문체(ㅋㅋ·오타·조사 생략)를 판정하지 않는다. 기계가 셀 수 있는 것만 세고 문체는 U6 사람 검수의 항목으로 남긴다
- 검증: `evidence/20260906-1828-fix-pytest.txt` → `51 passed`, rc=0 / `evidence/20260906-1828-fix-validate.txt` → rc=0, 검사 (0)~(10)·(12)·(13) 전부 PASS, `시나리오 16건 / 오류 0`, `--json` 의 `issue_count: 0`·`total: 16`. **저장소 실물 16건은 (12)(13) 위반 0** — 데이터 수정 불필요
- 남은 것 · 다음 단위: U3(pronoun 8 + normal 10). U3~U4 작성 시 검사 (13) 이 길이·턴 수를 즉시 잡으므로 초안 단계에서 확인할 것. 검사 (11)(파일명 ↔ `category`)은 U5 예정 — `CHECK_NAMES` 에 번호를 넣지 않았다(하지 않는 검사에 PASS 를 찍지 않기 위해)
- Refs: P1-pilot-dataset S3.7 S3.1 원칙8 F-7bea05 F-1ba055

## 2026-09-06 19:15 · feat(P1-pilot-dataset): U3 pronoun 8건 + normal 10건 — 지시대명사 선행사 라벨·ambiguous 2건 추가, virtual_names 99 · aa6ecfc
- 변경: `data/scenarios/pronoun.json`(sc-017~sc-024), `data/scenarios/normal.json`(sc-025~sc-034), `data/scenarios/manifest.json`(counts.pronoun=8·counts.normal=10·total=34·ambiguous_mention_count 1→3·virtual_names 68→99·generator.prompt_ref 에 U3 프롬프트 경로 추가, trap_count 8 불변), evidence 2파일
- 이유(기획서·카드 연결): 01-plan U3(44행). `pronoun` 은 "걔·얘·쟤·그분·그 분·그 사람·아까 걔" 의 **선행사가 앞 턴에 있는** 건만 쓰고, 선행사 후보가 둘 이상이라 문맥으로 정해지는 건을 7/8 에 두어 지시대명사 해석이 후보 집합 위에서 측정되게 했다(S3.7). 사람도 정할 수 없는 mention 2개(sc-022 `걔`, sc-024 `걔`)만 H-1 규칙대로 `ambiguous: true`·`gold_person_id: null`. `normal` 10건은 함정 없는 기준선이며 결정 C 대로 일정 발화만 4건(sc-025 담주 일요일 / sc-027 담주 토요일 / sc-029 목요일 저녁 / sc-033 다음 주 수요일)에 섞어 두어 P10 이 라벨만 덧붙일 수 있게 했다
- 정합성 확인: 원칙1(사람도 못 정하는 mention 을 억지 정답으로 라벨하지 않고 분모에서 빼도록 `ambiguous` 로 표시 — 오병합 유도를 데이터가 아니라 라벨 정직성으로 다룬다) / 원칙4·D10(`expected_ask_user.allowed` — 선행사 후보 2 이상 7건은 `["identity","none"]`, 단일 선행사 sc-023 과 `normal` 10건은 `["none"]`) / 원칙7(고민 상담·감정 조언 발화 없음, 인물 간 관계는 발화 배경으로만) / 원칙8(생성 경로를 `generator.prompt_ref` 에 U3 프롬프트로 누적 기록, 함정 0건임을 `trap_count` 불변으로 드러냄) / 결정 A·E(전 시나리오 `persons` + `seed_persons` = 등장 인물 전원, `normal` 은 새 인물 없음) / S3.1(관계 태그·위계를 U2 의 직장 편중에서 친구 10·직장 6·가족 5·지인 5·연인 2(위계 동 18·상 9·하 1, 인물 항목 28개 기준) 으로 재배분) / security(실존 인물·실제 회사명 없음, 가상 성명만·`virtual_names` 화이트리스트 검사 통과) — 위반 없음
- 검증: `evidence/20260906-1915-u3-validate.txt` → rc=0, `issue_count: 0`, `scenario_count: 34`, `counts.pronoun 8`·`counts.normal 10`, `ambiguous_mention_count: 3`, `trap_count: 8`(검사 (0)~(10)·(12)(13) 전부 PASS) / `evidence/20260906-1915-u3-pytest.txt` → `51 passed`, rc=0 (U1·fix 테스트 불변) / 평가한 코드 커밋: dev `09875aa`
- 남은 것 · 다음 단위: U4(`new_person` 6건 — 등록 대상과 지나가는 언급 혼합). `--strict` 는 `new_person.json` 부재로 여전히 FAIL 이며 U4 이후 U5 수용 기준 검증에서 쓴다. `normal` 의 일정 발화 4건은 골드 라벨이 없다(결정 C) — P10 이 라벨을 덧붙일 때 이 4건을 찾도록 이 줄을 근거로 남긴다
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D10 원칙1 원칙4 원칙7 원칙8

## 2026-09-06 19:45 · feat(P1-pilot-dataset): U4 new_person 6건 — 등록 대상 3·지나가는 언급 3, --strict rc=0 (total 40) · 6775463
- 변경: `data/scenarios/new_person.json`(sc-035~sc-040), `data/scenarios/manifest.json`(counts.new_person=6·total 34→40·trap_count 8→12·virtual_names 99→108·generator.prompt_ref 에 U4 프롬프트 경로 추가, ambiguous_mention_count 3 불변), evidence 2파일
- 이유(기획서·카드 연결): 01-plan U4(45행). D1(신규 인물 등록은 **확인형** — 자동 `create_person` 금지, `ask_user(kind="new_person")` 승인 후에만 등록)이 옳게 동작하는지를 양방향으로 측정 가능하게 했다. (a) **등록 대상 3건**(sc-035 새로 온 팀원 / sc-036 동호회에서 소개받은 사람 / sc-037 처음 언급되는 사촌형)은 새 인물을 `persons[]` 에 넣되 `seed_persons` 에서 빼 `new_person` 질문이 **일어나야** 하는 쪽을, (b) **지나가는 언급 3건**(sc-038 연예인 / sc-039 배달 기사·점원 / sc-040 길에서 본 모르는 사람 + 뉴스 속 인물)은 그 언급을 `persons[]`·`mentions[]` 어디에도 넣지 않고 `trap.kind="passing_mention"` + `expected_ask_user.allowed=["none"]` 으로 라벨해 질문이나 인물 생성이 **일어나면 오탐**인 쪽을 만든다(D1 이유 절 "자동 등록은 지나가는 언급(연예인 등)을 카드로 만든다"의 반례 집합). 함정 4건 추가 = sc-036 `new_person_resembles_existing`(seed '이대리' vs 새 인물 '임대리' — 성씨 한 글자 차이·같은 직급) + 지나가는 언급 3건
- 정합성 확인: 원칙1(등록 대상 3건은 `new_person` 질문을 허용해 미검출을 감수하고, 지나가는 언급 3건은 질문·생성 자체를 오탐으로 세어 오병합 방향 비용을 더 무겁게 둔다) / 원칙4·D10(`expected_ask_user.allowed` 허용 집합 — sc-035·037 `["new_person"]`, 호칭이 겹치는 sc-036 `["identity","new_person"]`, 지나가는 언급 3건 `["none"]`) / 원칙7(고민 상담·감정 조언 요청 발화 없음. sc-040 의 "무서워서 그냥 빨리 지나갔음ㅠ" 은 상황 서술이며 조언을 구하지 않는다. 인물 간 관계는 배경으로만) / 원칙8(지나가는 언급을 "안 보이게 지우는" 것이 아니라 `trap.reason` 에 surface 목록과 배제 사유를 적어 P4 가 오탐을 셀 근거를 남겼다. `trap_count` 12 로 함정 건수를 숨기지 않는다) / 결정 A·E(전 시나리오 `persons` + `seed_persons`, 새 인물은 seed 밖 / sc-037 은 `seed_persons: []`) / 결정 B(events 는 실제 사건이 있는 6건만, 고정 7종·`occurred_at_kind` 전부 `relative`) / S3.1(관계 태그 직장 4·지인 1·가족 1·친구 1·연인 1, 위계 상 3·동 4·하 1 — 인물 항목 8개 기준. U2·U3 에서 얇았던 연인·하 를 보탰다) / security(실존 인물·실제 회사명 없음. 연예인은 실명 대신 "남주 배우"·"그 아이돌", 뉴스 인물은 "그 정치인" 일반 명사. 가상 성명 9개 추가는 `virtual_names` 화이트리스트에 등록) — 위반 없음
- 검증: `evidence/20260906-1945-u4-validate-strict.txt` → **`--strict` rc=0**(5파일 전부 존재), `issue_count: 0`, `scenario_count: 40`, `total: 40`, `counts.new_person: 6`, `trap_count: 12`, `ambiguous_mention_count: 3` / `evidence/20260906-1945-u4-pytest.txt` → `51 passed`, rc=0 (U1·fix 테스트 불변) / 평가한 코드 커밋: dev `aa6ecfc`
- 남은 것 · 다음 단위: U5(전건 무결성 점검 + 검수 패킷 + `distribution` 배분표 + 검사 (11) 파일명↔category 추가). **U5·U6 검수자에게 넘기는 판단 2건** — (1) 스키마 v1 은 "지나가는 언급"을 표현하는 전용 필드가 없어 `trap.reason` 의 자연어에 surface 를 적었다. P4 가 오탐을 기계로 세려면 이 문자열을 파싱해야 하므로, 필요하면 `schema_version` 2 에서 `passing_mentions: [{turn, surface}]` 를 추가하는 편이 낫다(FIX 아님 — 01-plan "소비자와의 계약 미확정"). (2) sc-036 의 함정 난이도(성씨 한 글자 차이)가 적정한지는 검수자가 판정한다
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D10 원칙1 원칙4 원칙7 원칙8 R-5

## 2026-09-06 19:26 · feat(P1-pilot-dataset): U5 schema_version 2(passing_mentions)·검사 (11)(14)(15)·배분표 기계 생성·검수 패킷 · 40c36f8
- 변경: `data/scenarios/schema.json`(schema_version 1→2, 선택 필드 `passing_mentions` + `$defs.passing_mention{turn,surface,why}` 추가 — 기존 필드 무변경), `data/scenarios/manifest.schema.json`(schema_version 2·`const: 2`, `distribution` 설명에 U5 결정 명시), `data/scenarios/manifest.json`(schema_version 2, `distribution` 11키를 `--write-distribution` 이 기계 생성), `data/scenarios/new_person.json`(sc-038·039·040 에 `passing_mentions` 6개 — `trap.reason` 의 surface·턴·사유를 구조화. 다른 시나리오·다른 필드 무변경), `scripts/validate_scenarios.py`(검사 `check_file_category`(11)·`check_passing_mentions_in_utterance`(14)·`check_passing_mentions_disjoint`(15) 추가, `check_turn_range`(3) 를 `passing_mentions` 까지 확장, `FILE_CATEGORY`·`SCHEDULE_UTTERANCE_SCENARIOS`·`NEWLINE`·`DISTRIBUTION_GENERATED_BY` 상수, `build_distribution`·`write_distribution`, `--write-distribution` 옵션, docstring 검사 목록 (0)~(15)·"지나가는 언급" 절·남긴 결정 2건), `scripts/dump_scenarios.py`(**신규** — 검수 패킷 생성기), `tests/test_validate_scenarios.py`(51→69건), evidence 5파일
- 이유(기획서·카드 연결): 01-plan U5(46행) "전건 무결성 점검 + 검수 패킷 생성 … 이 단위는 **검수를 하지 않는다** — 검수 요청까지다"(L-002). (a) **판 2** — U4 03-log 가 남긴 인계 (1): 판 1 은 "지나가는 언급"을 `trap.reason` 의 자연어에만 적어 P4 가 오탐을 세려면 문자열을 파싱해야 했다. 01-plan "소비자와의 계약 미확정" 이 정한 대로 FIX 가 아니라 `schema_version` 을 올리고 선택 필드를 **추가만** 했다(판 1 데이터는 그대로 유효). (b) **검사 (11)** — U1 03-log 의 "U5 에서 추가(사용자 결정): 파일명 ↔ `category` 대응 검사". 파일 배치가 어긋나도 `counts` 는 category 값으로 세므로 검사 (6) 이 통과한다 — 배분표가 맞아 보이는 조용한 실패다. (c) **검사 (14)(15)** — 발화에 없는 표면형을 지나가는 언급으로 라벨하면 그 시나리오의 오탐률이 **항상 0** 이 되어 D1(확인형 등록)이 지켜지는지 못 본다. 같은 턴에서 mention 과 겹치면 한 지칭이 정답 분자와 오탐 분자에 동시에 들어가 P4 의 정밀도가 모델이 아니라 라벨 때문에 흔들린다. (d) **배분표** — 02-plan-verify 권고 R-5(하위 유형·trap 열)를 손으로 세지 않고 실제 데이터에서 계산해 쓴다(원칙8). (e) **검수 패킷** — 검수자가 40건을 표로 읽고 R-4(고민 상담 발화 아님)를 포함한 7항목을 판정할 수 있게 한다
- 정합성 확인: 원칙1(지나가는 언급을 구조화해 **오탐(=인물 생성·new_person 질문)** 을 기계가 세게 했다 — 오병합 방향 비용을 측정 가능하게 유지) / 원칙4·D1·D10(라벨을 추가만 했고 임계치·허용 집합 의미 불변) / 원칙7(새 필드도 사용자–인물 관계 밖의 정보를 담지 않는다. 검수 항목 5번이 고민 상담 발화를 명시적으로 거른다) / 원칙8(배분표를 손으로 세지 않고 `--write-distribution` 이 계산·멱등 확인, 검수 패킷은 시각 의존이 파일명에서 온 머리 줄 하나뿐, 시나리오 내용은 `passing_mentions` 추가 외 **무변경** — 데이터를 성능에 유리하게 고치지 않았다) / S3.7(라벨 무결성 검사만 추가, 지표 계산은 여전히 P4) / L-002(패킷은 판정하지 않고 판정할 것을 나열한다. 검수자는 verifier 새 컨텍스트 또는 사용자) / R-10(패킷의 지적 표 헤더를 `상태` 한 열로만 제시하고 값 어휘는 표 밖 산문에 둔다) / R-11(상태 열을 닫는 것도 검수자) / security(네트워크·DB·LLM·`app/` import 없음. 가상 성명만) — 위반 없음
- 검증: `evidence/20260906-1926-u5-validate-strict.txt`(`--strict --json`) → `ok: true`, `issue_count: 0`, `scenario_count: 40`, `total: 40`, `schema_version: 2` / `evidence/20260906-1926-u5-validate-strict-text.txt` → **rc=0**, 사람이 읽는 출력에 `[PASS] (0)`~`[PASS] (15)` 16행(적재 1 + 교차 검사 15), `시나리오 40건 / 오류 0` / `evidence/20260906-1926-u5-pytest.txt` → `69 passed`(51 → 69, +18: (11) 3건·(14)(15)·`passing_mentions` 스키마 8건·배분표 6건·저장소 불변식 1건) / `evidence/20260906-1926-u5-distribution-idempotent.txt` → `--write-distribution` 2·3회차 md5 가 1회차와 동일(`4eb5732…`), 메시지 `distribution 변경 없음(멱등)`, rc=0 / 검수 패킷 `evidence/20260906-1923-review-packet.md`(860행, 40건 전건 + `virtual_names` 108 전체) / 평가한 코드 커밋: dev `6775463`(U4) + 이 단위의 미커밋 변경
- 배분표 요약(`manifest.json.distribution`, 기계 생성): 카테고리 promotion 8·pronoun 8·alias 8·normal 10·new_person 6 / 관계 태그(persons 항목 60개 기준) 직장 23·친구 16·지인 9·가족 8·연인 4 / 위계 동 31·상 23·하 6 / trap.kind 7종 12건(passing_mention 3·same_title_two_persons 3·promotion_collides_with_existing_title 2·nickname_shared 1·nickname_shared_with_title 1·similar_name_two_persons 1·new_person_resembles_existing 1) / new_person 하위 유형 register_target 3(sc-035~037)·passing_mention 3(sc-038~040) / 일정 발화 4건(sc-025·027·029·033, **수동 지정** — 결정 C 로 골드 라벨이 없어 기계가 셀 근거가 데이터에 없다) / ambiguous mention 3(sc-022 t1 걔·sc-024 t4 걔·sc-013 t3 걔) / events 66건(meal 19·favor 12·meeting 11·personal_share 10·other 6·conflict 5·praise 3) / occurred_at_kind relative 51·none 14·absolute 1 / 발화 턴 3~5, 길이 10~26자
- 남은 것 · 다음 단위: **U6 은 검수 결과가 있어야 시작한다** — 메인 세션이 verifier(새 컨텍스트)에게 검수 패킷 `evidence/20260906-1923-review-packet.md` 를 주고 `evidence/<ts>-label-review.md` 를 쓰게 한다(결정 G·H-3, 전건 40/40 + 사용자 검수는 함정 12건·지나가는 언급 3건). registry 행·README·수용 기준 evidence 는 U7. **남긴 결정 4건** — (1) 배분표를 별도 스크립트가 아니라 `validate_scenarios.py --write-distribution` 에 둔다(적재·집계 코드가 두 벌이 되지 않게, 그리고 **검증을 통과한 데이터셋에서만** 쓰게 강제하려고). (2) 검증기 기본 실행은 `distribution` 과 실제 집계의 일치를 **검사하지 않는다**(집계 결과이지 계약이 아니다 — U1 결정과 같은 이유). 대신 재실행 멱등 evidence + 저장소 테스트 `test_repository_distribution_matches_recomputed` 로 낡음을 잡는다. `counts`·`ambiguous_mention_count`·`trap_count` 는 P4 가 분모로 읽는 계약이라 검사 (6)(8)(10) 이 그대로 본다. (3) 검사 (3)(turn 범위)을 `passing_mentions` 까지 넓혔다 — (14) 가 범위 밖 turn 을 건너뛰므로 (3) 이 안 보면 어느 검사에도 안 걸리는 구멍이 된다. (4) `write_distribution`·`dump_scenarios` 는 항상 LF 로 쓴다(플랫폼 CRLF 로 바뀌면 같은 데이터가 다른 바이트를 내어 멱등 확인이 깨진다). 저장소 `core.autocrlf=true` 라 체크아웃 직후 CRLF 인 `manifest.json` 에 처음 `--write-distribution` 을 돌리면 **작업 트리 바이트만** 바뀐다(git 은 정규화해 diff 0). 검수자·P4 가 놀라지 않게 남긴다
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D10 원칙1 원칙4 원칙7 원칙8 R-4 R-5 R-10 R-11 L-002

## 2026-09-06 20:10 · feat(P1-pilot-dataset): U6 라벨 검수 지적 17건 반영 — 결정 I·J·K·L, 시나리오 29/40 수정 · 76add8a
- 변경: `data/scenarios/promotion.json`(sc-001~008 전건), `data/scenarios/alias.json`(sc-009~016 전건), `data/scenarios/pronoun.json`(sc-017·018·022·024), `data/scenarios/normal.json`(sc-026·030·032·033·034), `data/scenarios/new_person.json`(sc-035·037·039·040), `data/scenarios/manifest.json`(`virtual_names` 108→100 — 결정 I 로 쓰이지 않게 된 8개 제거, `distribution` 재생성), `data/scenarios/schema.json`(설명만: `aliases` 에 결정 I, `events` 에 결정 J (a)~(g), `occurred_at_kind` 에 J(a) 판정 규칙과 marker 목록, `mention.surface` 에 U6-1·U6-2 mention 규칙 — **구조·enum 무변경이라 schema_version 2 유지**), `scripts/dump_scenarios.py`(검수 항목 1·6 텍스트에 결정 I·J 인용), evidence 5파일
- 이유(기획서·카드 연결): 01-plan U6(47행) "verifier 또는 사용자가 `evidence/<ts>-label-review.md` 에 남긴 지적을 수정하고 U5 의 검증·배분표를 재생성한다". 반영 근거는 01-plan "검수 후 결정 — 사용자(2026-09-07)" 의 **결정 I**(aliases = 대화 시작 전 알려진 별칭만, 공유 호칭 비대칭 금지) · **결정 J**(events 라벨 규칙 (a)~(e)) · **결정 K**(sc-002·sc-012 재작성) · **결정 L**(#17 동명 허용 기각 — 데이터 무변경). 지적 1~4 를 반영하지 않으면 promotion 8건이 "호칭 변경 추적" 이 아니라 별칭 완전일치를 재게 되고(오병합 유도 함정 8건 중 실질 유효가 3건뿐이었다), 지적 11~16 을 반영하지 않으면 이벤트 F1 이 모델이 아니라 라벨러의 그때그때 판단을 재게 된다(원칙8)
- 검수자 의견(§5)에서 **규칙으로 올린 것 4가지**(01-plan "필요 시 규칙으로 올려 03-log 에 남긴다"). 전건에 일관 적용했다: **U6-1 인용형 지칭** — 호칭을 인용하는 표현(`X 라고 부른다`·`별명이 X`·`명함에 X 라고 적혀 있음`)의 X 는 그 인물을 가리키므로 mention 으로 센다(빠져 있던 sc-015 t0 `부장님`→p1 추가. 같은 시나리오 t3 `부장님`→p2 와 표면형이 같고 정답이 달라 함정이 강해졌다). **U6-2 서술어 명사** — 인물을 범주로 서술하는 명사(`동네 형이라 그런지`, `사촌형 시우` 의 `사촌형`)는 지칭이 아니므로 세지 않는다(sc-032 t1 `형` 제거, sc-037 t2 `형` 추가 — 같은 낱말이라도 직접 가리키면 센다). **U6-3 이벤트는 사건에만** — 성향·평판·감상·호칭 사실 같은 상태 서술은 이벤트가 아니다(sc-009 t3·sc-014 t1·sc-003 t0 제거). **U6-4 ambiguous 턴** — `ambiguous: true` mention 이 있는 턴에는 이벤트를 붙이지 않는다(어느 인물의 이벤트인지 정해지지 않는다. 기존 라벨과 이미 일치, 명문화만). 네 규칙은 `schema.json` 설명과 `dump_scenarios.py` 검수 항목에 인용해 다음 검수자가 같은 기준으로 보게 했다
- 정합성 확인: 원칙1(결정 I 로 공유 호칭이 어느 쪽 aliases 에도 없게 되어 오병합 유도 함정이 **문맥 판정을 실제로 요구**한다 — 검수 §3 이 "유효 3건" 이라 판정한 것을 8건으로 되돌렸다. sc-015 t0 mention 추가는 같은 표면형·다른 정답을 한 시나리오 안에 만든다) / 원칙4·D1·D10(허용 집합만 좁혔다 — sc-003·007 `["identity","none"]`→`["none"]`(#8), sc-012 세 값 전부 → `["identity","new_person"]`(#7). 등록 대상이 있는 건에서 "조용히 지나가기" 가 통과하던 구멍을 막았다) / 원칙7(J(d) 로 sc-022 t0 규진–세호 사이의 일이 사용자 `meal` 로 저장되던 방향을 끊고 `personal_share` 로 바꿨다 — A–B 사건을 사용자 사건으로 만드는 정답을 지웠다) / 원칙8(**성능에 유리한 쪽으로 고치지 않았다** — 이번 변경은 대부분 데이터를 **더 어렵게** 만든다. 별칭 지름길 제거·허용 집합 축소·mention 추가. 반대로 얇아진 `praise` 1건·`other` 2건·`absolute` 1건과 `personal_share` 35% 편중을 change-summary §5 에 한계로 적었다) / D6(런타임 DB 의 별칭 누적 정책과 어긋나지 않는다 — `persons[].aliases` 는 대화 **이전** 상태이고, 대화에서 배운 호칭이 누적되는 것은 P4 러너·ER 쪽 이야기다. `schema.json` 설명에 이 구분을 적었다) / 결정 C(미래 약속은 이벤트가 아니라는 J(b) 가 "일정 라벨은 P10" 과 같은 방향. 미래 약속 발화 자체는 sc-001 t3·sc-011 t3·sc-018 t3·sc-025 t2 등에 그대로 남아 P10 이 라벨을 덧붙일 수 있다) / L-002·R-11(지적 표의 `상태` 열을 **건드리지 않았다** — 닫는 것은 검수자) / security(네트워크·DB·LLM 없음, 실명·연락처 없음, `app/` 미접근) — 위반 없음
- 검증: `evidence/20260906-2010-u6-validate-strict.txt` → rc=0, 검사 (0)~(15) 전부 PASS, `시나리오 40건 / 오류 0` / `evidence/20260906-2010-u6-validate-strict.json` → `ok: true`, `issue_count: 0`, `scenario_count: 40`, `total: 40`, `trap_count: 12`, `ambiguous_mention_count: 3` / `evidence/20260906-2010-u6-pytest.txt` → `69 passed`, rc=0(검증기·테스트 코드 무변경 — 결정 I 는 기계 검사를 두지 않는 사람 검수 항목이다) / `evidence/20260906-2010-u6-distribution-idempotent.txt` → 1·2·3회차 md5 동일(`fa83fcc…`), `distribution 변경 없음(멱등)` / 새 검수 패킷 `evidence/20260906-2010-review-packet-2.md`(854행) / 지적별 전→후 대조표 `evidence/20260906-2010-u6-change-summary.md` / 평가한 코드 커밋: dev `40c36f8`(U5) + 이 단위의 미커밋 변경 — 검수 기준 커밋과 같다
- 전·후 수치: 시나리오 40 불변(변경 29건, 무변경 11건 = sc-019·020·021·023·025·027·028·029·031·036·038) / mentions 130 → 134 / events 66 → 72 / aliases 133 → 120(13개 제거: sc-001 김부장·부장님, sc-002 이과장·과장님, sc-003 최대리, sc-004 팀장님·강실장, sc-005 회장님, sc-006 임주임·주임님, sc-007 문실장, sc-008 김부장·부장님) / `virtual_names` 108 → 100 / ambiguous 3·trap 12 불변 / **events type** conflict 5→6, favor 12→9, meal 19→18, meeting 11→11, other 6→2, personal_share 10→25, praise 3→1 / **occurred_at_kind** relative 51→43, none 14→28, absolute 1→1 / 발화 턴 3~5(sc-002·sc-012 가 4→5), 길이 10~26자
- 남은 것 · 다음 단위: **재검수는 verifier(새 컨텍스트)** — 새 패킷 `evidence/20260906-2010-review-packet-2.md` 와 `evidence/20260906-2010-u6-change-summary.md` + `git diff` 로 17건을 다시 보고 `evidence/20260906-1938-label-review.md` 의 `상태` 열을 닫는다(R-11, U6 을 한 eval-agent 가 자기 지적을 닫지 않는다). **검수자가 판정해야 할 eval-agent 자체 판단 2건**(change-summary §3): (1) sc-005 t4 를 미래 약속 → 과거 절대 날짜 발화로 바꿔 `absolute` 0건을 피한 것(§5 마지막 의견 반영, 결정 J(b) 는 지켰다). (2) sc-039 t2(한부장→배달 기사 음료수)를 J(d) 글자대로 `personal_share` 로 둔 것 — 원칙7 을 근거로 이벤트 없음이라는 반대 판단도 가능하다. **P10 인계 3건**: `praise` 1건·`other` 2건·`absolute` 1건의 표본 부족, `personal_share` 35% 편중, 위계 `하` 6/60·발화 길이 10~26자·한 턴 지칭 1개 편중(검수 §3 소견, U6 범위 밖). registry 행·README·수용 기준 evidence 는 U7
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D6 D10 원칙1 원칙4 원칙7 원칙8 원칙9 R-11 L-002
## 2026-09-07 11:00 · feat(P1-pilot-dataset): U6-2 재검수 지적 #18~#20 + 결정 M·N 반영 — 시점 낱말 규칙 일원화·sc-005 t4 원문 복원·U6-5 신설 · 6906af4
- 변경: `data/scenarios/promotion.json`(sc-004 t0 kind, sc-005 t4 발화 복원·이벤트 제거·t3 kind, sc-006 t2 이벤트), `data/scenarios/alias.json`(sc-012 t0 이벤트, sc-013 t1 이벤트), `data/scenarios/new_person.json`(sc-035 t1·t2 이벤트, sc-036 t1 mention), `data/scenarios/manifest.json`(`distribution` 재생성 — `--write-distribution`), `data/scenarios/schema.json`(설명만: `occurred_at_kind` 를 결정 M 문장으로 교체하고 U6 통사 하위 규칙 삭제, `events` 에 (a) 문구 수정 + (h) U6-5 추가 — **구조·enum 무변경이라 schema_version 2 유지**), `scripts/dump_scenarios.py`(검수 항목 6 텍스트를 결정 M·U6-5 로 교체), evidence 6파일(`20260907-1100-u6-2-*` 4 + `-review-packet-3.md` + `-u6-2-change-summary.md`)
- 이유(기획서·카드 연결): 01-plan "검수 후 결정 — 사용자(2026-09-07)" 의 **결정 M**(J(a) 보강 — 그 턴에 시점 낱말이 있으면 **문장 성분과 무관하게** relative, 기간 표현만 제외. eval-agent 가 U6 에서 임의로 세운 통사 하위 규칙 '부사어 이번에 = relative / 관형어 이번 = none' 은 폐기)·**결정 N**(U6 이 사용자 결정 없이 교체한 sc-005 t4 발화를 원문 `9월 20일에 축하 겸 밥 먹기로 했어` 로 되돌리고 J(b) 대로 이벤트만 제거 — 라벨 규칙을 맞추려고 발화를 바꾸지 않는다, 원칙8)·재검수 신규 지적 **#19**(sc-036 t1 `그 친구` mention 누락 — 등록 대상의 첫 지칭 시점이 밀렸다)·**#20**(J(c)(d) 와 같은 구조인데 이벤트가 없던 턴). #18 은 두 기준이 공존해 추출 F1 이 라벨러의 통사 판단을 재는 상태였고(원칙8), 이 단위로 기준이 하나가 됐다
- **U6-5 신설**(01-plan 90행 "판단 필요 3턴 규칙은 U6-2 에서 eval-agent 가 정해 03-log 에 남긴다"): **행위의 한쪽이 사용자이면(사용자→인물·인물→사용자 양방향) 사용자–인물 사건이므로 활동 type 을 붙인다. 꾸짖음·질책·불이익 = conflict, 사용자에게 이득이 되는 호의 = favor, 음식·음료 제공 = meal, 함께한 활동 = meeting, 칭찬 = praise, 마찰도 이득도 아닌 업무 지시·수업·과제 부과 = other. 예외 (i) 부작위('굳이 말 안 했어'·'별말 안 하고 넘어갔어'·'그냥 넘어가주셨는데')는 일어난 일이 아니므로 이벤트가 아니다, (ii) 한 사건을 여러 턴에 나눠 서술하면 사건이 성립한 턴 하나에만 붙인다.** J(b)·U6-1·U6-3·U6-4 가 우선한다. 근거: 이미 라벨된 sc-012 t2(혼났어 = conflict)·sc-007 t0(피티 = other)·sc-029 t0(자세 교정 = other)이 이미 '인물→사용자' 행위이며, J(d) 가 방향을 말하지 않아 생긴 빈틈이다. 전건(40건 160턴) 적용 결과 이벤트를 새로 붙인 턴은 3개(sc-012 t0 과제 부과·sc-035 t1 업무 지시·**sc-006 t2 사용자→인물 지시** — 검수자가 짚지 않았지만 같은 규칙에 걸려 추가했다), 예외로 제외한 턴 목록은 change-summary §2 에 전부 적었다
- 정합성 확인: 원칙8(**성능에 유리한 쪽으로 고치지 않았다** — 결정 N 으로 `absolute` 표본이 1 → **0** 이 되어 P4 가 그 kind 를 측정할 수 없게 됐고, 이 손실을 change-summary §5 와 `schema.json` 설명에 그대로 적었다. 결정 M 재판정은 스크립트로 76건 전수 대조해 불일치 0 을 evidence 로 남겼다 — 손으로 고른 것이 아니다) / 원칙1·D10(mention·이벤트를 **더한** 변경이라 판정 대상이 늘었고 허용 집합·함정·ambiguous 는 불변. #19 로 sc-036 의 등록 판정 시점이 t1 앞머리로 돌아와 오병합 유도 함정이 원래 난이도를 회복했다) / 원칙7(새 이벤트 5건 중 A–B 사건을 사용자 사건으로 만든 것은 없다 — sc-035 t2 는 J(d) 대로 personal_share) / 원칙9·L-002·R-11(지적 표 `상태` 열 무접촉, 검수자가 닫는다) / 결정 B·C(일정 골드 라벨은 여전히 없다. sc-005 t4 는 발화만 남고 이벤트가 없어 P10 이 일정 라벨을 덧붙일 자리가 된다) / security(네트워크·DB·LLM·`app/` 접근 없음, 실명·연락처 없음) — 위반 없음
- 검증: `evidence/20260907-1100-u6-2-validate-strict.txt` → rc=0, 검사 (0)~(15) 전부 PASS, `시나리오 40건 / 오류 0` / `evidence/20260907-1100-u6-2-validate-strict.json` → `ok: true`, `issue_count: 0`, `scenario_count: 40`, `total: 40`, `trap_count: 12`, `ambiguous_mention_count: 3` / `evidence/20260907-1100-u6-2-pytest.txt` → `69 passed`, rc=0(검증기·테스트 코드 무변경) / `evidence/20260907-1100-u6-2-distribution-idempotent.txt` → 1·2·3회차 md5 동일(`b23db1f4…`), 2·3회차 `distribution 변경 없음(멱등)` / `evidence/20260907-1100-u6-2-kind-scan.txt` → 이벤트 76건을 결정 M 으로 재판정, **불일치 0**(기간 표현 턴 2건은 none) / 새 검수 패킷 `evidence/20260907-1100-review-packet-3.md`(854행) / 전→후 대조 `evidence/20260907-1100-u6-2-change-summary.md` / 평가한 데이터 커밋: dev `76add8a`(U6) + 이 단위의 미커밋 변경 — verifier 재검수 대상 커밋과 같다
- 전·후 수치: 시나리오 40 불변(변경 7건 = sc-004·005·006·012·013·035·036) / mentions 134 → **135** / events 72 → **76**(−1 결정 N, +5 U6-5·#20) / aliases 120·trap 12·ambiguous 3·`virtual_names` 100 불변 / **events type** meal 18→17, meeting 11→12, other 2→5, personal_share 25→26, conflict 6·favor 9·praise 1 불변 / **occurred_at_kind** relative 43→**48**, none 28→28, absolute 1→**0**
- 남은 것 · 다음 단위: **재검수 3라운드는 verifier(새 컨텍스트)** — `evidence/20260907-1100-review-packet-3.md` + `-u6-2-change-summary.md` + `git diff` 로 #18·#19·#20 을 보고 `evidence/20260906-1938-label-review.md` 의 `상태` 열 3행을 닫는다(R-11). **검수자가 판정해야 할 eval-agent 판단 4건**(change-summary §3): (1) sc-012 t1·sc-033 t2 의 '넘어가주다' 를 부작위로 보아 제외한 것 (2) sc-006 t2 를 U6-5 로 새로 센 것 (3) sc-039 t1 을 세부 묘사로 제외한 것 (4) sc-037 t2 를 같은 자리로 보아 제외한 것. **P10 인계 추가 1건**: `absolute` 0건(결정 N 의 결과) — 과거 절대 날짜 발화를 P10 에서 넣어야 `occurred_at_kind` F1 의 absolute 분모가 생긴다. registry 행·README·수용 기준 evidence 는 U7
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D10 원칙1 원칙4 원칙7 원칙8 원칙9 R-11 L-002
## 2026-09-07 12:00 · fix(P1-pilot-dataset): U6-3 3차 재검수 #21·#22 반영 — sc-034 t1 favor(결정 O)·U6-5 대응표 정정(결정 P) · aeed0bd
- 변경: `data/scenarios/normal.json`(sc-034 t1 events[1].type `personal_share`→`favor`, kind `none` 유지 — **이번 단위의 데이터 변경은 이 1턴이 전부**), `data/scenarios/manifest.json`(`distribution` 재생성 — `--write-distribution`), `data/scenarios/schema.json`(설명만: `events` (h) 대응표의 `favor` 문구 — **구조·enum 무변경이라 schema_version 2 유지**), `scripts/dump_scenarios.py`(검수 항목 6 (h) 같은 문구), evidence 7파일(`20260907-1200-u6-3-*` 6 = validate-strict.txt/.json·pytest·distribution-idempotent·dump·change-summary + `-review-packet-4.md`)
- 이유(기획서·카드 연결): 01-plan "검수 후 결정 — 사용자(2026-09-07)" 의 **결정 O**(#21 — sc-034 t1 `월척 잡았다고 사진 계속 보내심ㅋㅋ` 은 인물→사용자 행위이므로 U6-5 본문대로 `favor`. sc-029 t2·sc-025 t0 와 같은 꼴이고, 두 번째 `personal_share` 는 U6-5 어느 갈래로도 나오지 않는다)·**결정 P**(#22 — 대응표 문구가 규칙 본문과 방향이 어긋난 것을 문구 쪽에서 고친다. 데이터 변경 0). #21 을 두면 U6-5 가 전건에 적용되지 않은 상태로 P4 가 이벤트 F1 을 재게 되고(원칙8), #22 를 두면 P10 150건에서 사용자→인물 호의가 늘 때 라벨러마다 갈린다
- **U6-5 대응표 정정(결정 P)**: U6-2 항목에 적은 대응표 문구 "**사용자에게 이득이 되는 호의 = favor**" 를 **"어느 방향이든 호의 = favor"** 로 정정한다. U6-5 본문("행위의 한쪽이 사용자이면 — 사용자→인물·인물→사용자 **양방향**")이 처음부터 양방향이었고 대응표만 한 방향으로 좁게 적혀 있었다. **U6-2 항목 원문은 고쳐 쓰지 않는다**(03-log 는 시간순 기록이라 소급 수정하지 않는다) — 이 항목이 정정문이며, 권위 문구는 `schema.json` `events` (h) 와 `scripts/dump_scenarios.py` 검수 항목 6 이다. 정정 후 대응표 전문: **꾸짖음·질책·불이익 = conflict, 어느 방향이든 호의 = favor, 음식·음료 제공 = meal, 함께한 활동 = meeting, 칭찬 = praise, 마찰도 이득도 아닌 업무 지시·수업·과제 부과 = other**(예외 (i) 부작위 (ii) 한 사건은 성립한 턴 하나에만 — 불변). 문구 정정으로 재분류되는 기존 라벨은 없다(sc-011 t2 사용자→인물 favor 가 이제 글자 안에 들어온다, 라벨 무변경)
- 정합성 확인: 원칙8(**성능에 유리한 쪽으로 고치지 않았다** — 라벨을 지우지 않고 더 좁은 type 으로 바꿨다. `personal_share` 는 최다 클래스라 여기서 빼내는 것이 오히려 어려운 쪽이다. `favor` 10건 중 사용자→인물이 여전히 1건뿐이라는 편중을 change-summary §5 에 그대로 적고 P10 으로 인계했다. 결정 O 가 제시한 두 선택지 중 제거를 택하지 않은 근거도 §1 에 적었다) / 원칙4·D1·D10(`expected_ask_user.allowed`·mention·함정·ambiguous 무변경 — ER 판정 난이도는 그대로다) / 원칙7(사용자–인물 사건 type 을 사용자–인물 사건에만 쓴다는 J(d)·U6-5 의 방향을 오히려 정확히 지켰다 — t0 는 인물 근황(personal_share), t1 은 인물→사용자 행위(favor)로 분리) / 결정 M(kind 재확인 — t1 에 시점 낱말 없음, `계속` 은 빈도라 시점이 아니므로 `none` 유지) / 결정 B·C(일정 골드 라벨 없음 불변) / R-11·L-002(`evidence/20260906-1938-label-review.md` 의 `상태` 열 **무접촉** — 닫는 것은 검수자) / security(네트워크·DB·LLM·`app/` 접근 없음, 실명·연락처 없음) — 위반 없음
- 검증: `evidence/20260907-1200-u6-3-validate-strict.txt` → rc=0, 검사 (0)~(15) 전부 PASS, `시나리오 40건 / 오류 0` / `evidence/20260907-1200-u6-3-validate-strict.json` → `ok: true`, `issue_count: 0`, `scenario_count: 40`, `schema_version: 2` / `evidence/20260907-1200-u6-3-pytest.txt` → `69 passed`, rc=0(검증기·테스트 코드 무변경) / `evidence/20260907-1200-u6-3-distribution-idempotent.txt` → 실행 전 md5 `b23db1f4…`, 1회차 `distribution 갱신` 후 `bf3e17d1…`, 2·3회차 `변경 없음(멱등)`·md5 동일 / 새 검수 패킷 `evidence/20260907-1200-review-packet-4.md`(854행) / 전→후 대조 `evidence/20260907-1200-u6-3-change-summary.md` / 평가한 데이터 커밋: dev `6906af4`(U6-2) + 이 단위의 미커밋 변경 — verifier 3차 재검수 대상 커밋과 같다
- 전·후 수치: 시나리오 40 불변(변경 1건 = sc-034) / mentions 135·events 76 **불변**(type 만 이동) / aliases 120·trap 12·ambiguous 3·`virtual_names` 100 불변 / **events type** favor 9→**10**, personal_share 26→**25**, conflict 6·meal 17·meeting 12·other 5·praise 1 불변 / **occurred_at_kind** relative 48·none 28·absolute 0 불변 / 발화 턴 3~5, 길이 10~26자 불변
- 남은 것 · 다음 단위: **4차 재검수는 verifier(새 컨텍스트)** — `evidence/20260907-1200-review-packet-4.md` + `-u6-3-change-summary.md` + `git diff` 로 #21·#22 를 보고 `evidence/20260906-1938-label-review.md` 의 `상태` 열 2행을 닫는다(`grep -cE '\| *열림 *\|'` 출력 0 이 검수 완료 조건). **검수자가 판정해야 할 eval-agent 판단 1건**(change-summary §1): sc-034 t0 와 t1 을 별개 사건으로 보아 예외 (ii)(세부 묘사 제외)를 적용하지 않은 것. **P10 인계 유지**: `absolute` 0건, `praise` 1건·`other` 5건 표본 부족, `personal_share` 33% 편중, `favor` 의 사용자→인물 1건 편중(신규). 그다음 U7(registry 행·README 데이터셋 절·수용 기준 기계 검증 evidence)
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D10 원칙1 원칙4 원칙7 원칙8 원칙9 R-11 L-002
## 2026-09-07 13:00 · docs(P1-pilot-dataset): U7 registry 12행·README 평가 데이터셋 절·수용 기준 기계 검증 evidence · f78e9dc
- 변경: `docs/wiki/registry.md`(P1-pilot-dataset **12행 추가** — 데이터셋 8행(`schema.json`·`manifest.schema.json`·`manifest.json` + 카테고리 5파일, 각 행에 카테고리·건수·id 범위)·스크립트 2행(`validate_scenarios.py` 검사 (0)~(15)·`--strict`/`--json`/`--write-distribution`, `dump_scenarios.py`)·테스트 1행(`tests/test_validate_scenarios.py` 69건)·문서 1행(`evidence/20260906-1938-label-review.md`). **기존 행 무접촉**, 커밋 열은 `git log --oneline -- <경로>` 로 그 파일을 실제로 만들거나 바꾼 커밋만 적었다), `README.md`(253~292행 **"### 평가 데이터셋(파일럿 40건)"** 절 신설 — "엔티티 해석(ER) 실행법" 뒤·"## 문서 안내" 앞. 무엇인가(40건·5 카테고리·`schema_version` 2·골드 라벨 종류) / 실행법 5명령 / 라벨 규칙 요약(결정 I 의 aliases·결정 J·M·O·P·U6-5 의 events (a)~(e)·`ambiguous`·`passing_mentions`) / 검수 절차 4단계 / 알려진 한계 5항. 값·키 없음. "문서 안내" 표에 `평가 데이터셋은 어디에` 1행), `docs/wiki/packages/P1-pilot-dataset/01-plan.md`(작업 단위 U1~U7 체크박스 `[ ]`→`[x]` + 줄 끝 커밋 해시 괄호 — `git diff` 7행 교체, 다른 문장 무변경), `evidence/20260907-1300-u7-acceptance.txt`(신규), 03-log 이 항목. **`data/`·`scripts/`·`tests/` 는 손대지 않았다**(`git status --porcelain -- data/ scripts/ tests/` 빈 출력을 evidence 머리에 기록)
- 이유(기획서·카드 연결): 01-plan U7(48행)과 산출물 절 37~38행. (a) **수용 기준 4문장 ↔ 기계 출력 대응** — 01-plan 50~58행이 "30~50건 / 5범주 / `data/scenarios/` JSON / 라벨 검수 완료" 를 각각 어떤 명령의 어떤 출력으로 판정하는지 정해 두었다. 그 출력을 evidence 파일 하나에 명령 그대로(`$ ` 접두) 남겨야 04-review 가 문장 해석 논쟁이 아니라 재실행으로 판정한다(`docs/wiki/verification.md` — "확인했습니다"는 검증이 아니다). (b) **registry** 는 "무엇이 이미 있는가" 이므로, 소비자인 P3-baselines·P4-pilot-eval 이 데이터 로더·검증기를 다시 만들지 않도록 경로와 계약(`counts`·`ambiguous_mention_count`·`trap_count` 가 P4 의 분모)을 남긴다. (c) **README 절** 은 저장소를 처음 보는 사람이 코드를 읽지 않고 라벨 규칙과 **한계**를 알게 한다 — 한계를 README 에 적는 것이 원칙8("성능 미달도 결과다")의 최소 실천이다
- 정합성 확인: 원칙8(evidence 의 모든 수치가 명령 출력 그대로이고 평가한 커밋 해시 `aeed0bd` 를 머리에 적었다 — 같은 커밋을 체크아웃하면 같은 값이 나온다. `grep -c "반영 "` 이 29 인 것과 검수 기록의 최종 집계 21 이 다른 이유(행 수 vs 상태 셀)를 보충 명령으로 함께 남겨 수치를 좋게 보이도록 고르지 않았다) / 원칙1(README 한계 절에 함정·`passing_mentions` 가 오병합·오탐 방향을 재는 장치임을 남겨 두었다) / 원칙7(README 가 사용자–인물 관계만 담는 라벨 구조를 그대로 설명, A–B 관계 필드 없음) / L-002(검수 절차 절에 "만든 쪽이 검수하지 않는다"·"반영한 사람이 자기 지적을 닫지 않는다"를 명문화) / 결정 C(일정 골드 라벨 없음을 README 에 명시) / security(README·registry·evidence 에 키·값·실명·연락처 없음. 이 단위는 네트워크·DB·LLM 을 쓰지 않았다) — 위반 없음
- 검증: `evidence/20260907-1300-u7-acceptance.txt` — (기준 1·2) `--strict --json` 요약 `total 40 ok True counts {'alias': 8, 'new_person': 6, 'normal': 10, 'promotion': 8, 'pronoun': 8}`(30~50 안, 5범주 각 1건 이상) / (기준 3) `python scripts/validate_scenarios.py --strict` → 검사 `[PASS] (0)`~`[PASS] (15)` 16행, `시나리오 40건 / 오류 0`, **rc=0** / 검증기 테스트 `python -m pytest tests/test_validate_scenarios.py -q` → **69 passed** / (기준 4) `grep -c "반영 "` = 29(행 수), 지적 표 상태 셀만 세면 `반영 21`·`기각 1`, `grep -cE '\| *열림 *\|'` = **0**, 머리 줄 검수자 = `verifier (fable, 새 컨텍스트) | 표본: 40/40`(eval-agent 아님), 사용자 검수 `12/12 동의` 줄 / `ls data/scenarios` 8파일 / 평가한 커밋: dev `aeed0bd`(U6-3), 작업 트리의 `data/`·`scripts/`·`tests/` 미커밋 변경 없음
- 남은 것 · 다음 단위: **패키지 완료 검토는 verifier(새 컨텍스트)** — `04-review.md` 에서 수용 기준 4문장을 `evidence/20260907-1300-u7-acceptance.txt` 재실행으로 판정하고 `verify-impl.sh`(registry 등록 확인)를 돌린다. 라벨 자체는 재해석하지 않는다(R-3 — 검수는 `20260906-1938-label-review.md` 로 이미 닫혔다). 03-log U6-3 항목의 `pending` 해시와 이 항목의 `pending` 해시는 메인 세션이 `/commit` 에서 채운다. **P4-pilot-eval 01-plan 이 받아야 할 인계**: (1) 시나리오 사이 DB 초기화(같은 가상 성명이 다른 관계로 재등장) (2) `absolute` 0건·`praise` 1·`other` 5·`personal_share` 25/76 편중 → 이벤트 F1 은 클래스별로 보고할 것 (3) `ambiguous` 3 mention 분모 제외 (4) `passing_mentions` 6개는 오탐 분자 (5) 필드가 더 필요하면 FIX 가 아니라 `schema_version` 을 올린다
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D10 원칙1 원칙7 원칙8 원칙9 R-3 L-002
## 2026-09-10 09:40 · docs(P1-pilot-dataset): 완료 검토(04-review) — verifier 판정 완료, verify-impl FAIL 0 WARN 0, F-14f3ef 해소 · pending
- 변경: `docs/wiki/packages/P1-pilot-dataset/04-review.md`(신규 — verifier fable, 새 컨텍스트. §1 사전 21:52 출력 + 최종 09:38 출력, §2 수용 기준 4행, §3 부정 케이스 8종 + 대조군, §4 닫는 R 없음, §5 registry 12행 대조, §6 열린 문제·R-4~R-12 처리 표·관찰 5건, §7 P4·P10 인계, `결과: 완료`), `05-remediation.md`(F-14f3ef 해결 단계 1행·재검증 결과 파일·영향 확인 채움, `findings.py` 로 `상태: 해소` — 머리 줄 `열림: 0 (필수 0) | 해소: 4`), evidence 신규 6파일(`20260910-0934-review-scope.txt` 범위 대조, `20260910-0938-review-verify-impl-final.txt`·`-pytest.txt`·`-lint.txt`·`-commits.txt`·`-summary.txt`). 이전 verifier 세션의 `20260906-2152-*` 16파일(모두 HEAD f78e9dc 기준)은 재실행 없이 그대로 증거로 썼다. **코드·데이터·01-plan·카드·backlog·review-index·registry 무접촉**
- 이유(기획서·카드 연결): 03-log U7 항목 "패키지 완료 검토는 verifier(새 컨텍스트)" · L-002(구현자 eval-agent 와 다른 모델이 판정) · `docs/wiki/verification.md` "구현 검증 기법"(수용 기준은 evidence 파일·해시·경로로만, 부정 케이스는 직접 실행). 수용 기준은 backlog 23행 원문 그대로 두고 01-plan 50~58행의 기계 판정 4문장으로 판정했다 — 문장 해석 논쟁 없이 `--strict --json` 출력·`grep` 셀 계수·`test -f` 로 닫힌다(원칙8)
- 정합성 확인: 원칙8(수치는 전부 명령 출력 인용. `grep -c "반영 "` 29 vs 상태 셀 21 의 차이를 그대로 적음. 라벨은 재해석하지 않음 — R-3) / 원칙1(부정 케이스 b·c 가 미아 gold·비ambiguous null 을 잡음 — 오병합 방향 라벨 오류를 검증기가 막는다) / 원칙7(R-4 검수 항목 "고민 상담 아님" 이 dump·label-review 에 실재함을 확인) / 원칙9(판정 근거를 §2·§3 표의 evidence 열에 파일 단위로 남김) / L-002(검토자 줄을 `verify-impl.sh` 가 검사 — `PASS 검토자 = verifier`) / security(네트워크·DB·LLM·`.env` 미접근, 변이 검사는 scratchpad 복사본만 — 원본 `data/` md5 불변) — 위반 없음
- 검증: `evidence/20260910-0938-review-verify-impl-final.txt` → `FAIL=0 WARN=0`, `PASS 검토자 = verifier (L-002)`, `PASS 증거 확인:` 4행 / `findings.py … --source verify-impl` → `새 소견 0, 해소 1, 열림 0 (필수 0)`, `✓ F-14f3ef 해소` / `evidence/20260910-0934-review-scope.txt` → `git diff --stat 4c80c31..f78e9dc -- app/ alembic/ reports/` 빈 출력, 검증기·덤프에 네트워크·DB·`app` import 없음, 테스트 FAIL 코드 단정 19종 / §3 변이 8/8 `오류 ≥ 1`·대조군 `오류 0` / §5 registry 12/12 존재, 커밋 열 11/12 일치(106행 label-review 에 `f78e9dc` 누락)
- 남은 것 · 다음 단위(메인 세션): (1) 사용자에게 04-review 판정 제시 → `승인:` 줄 (2) 이 항목과 U7 항목(87행)의 `pending` 을 커밋 해시로 — U7 은 `f78e9dc` (3) registry 106행 커밋 열에 `f78e9dc` 추가, 105행 "import 없이 대조" 문구 정정(04-review §6 관찰 1·2) (4) backlog 23행 P1 행·U1~U7 체크박스 완료 표기 (5) review-index 변경 없음(닫는 R 없음) (6) `CURRENT.md` 패키지 닫음, HANDOFF·journal (7) 미커밋 `.gitignore`·`졸업작품신청서.md` 는 이 패키지와 분리. **P4-pilot-eval 01-plan 인계는 04-review §7**(P4 13항·P10 5항 — DB 초기화, aliases 사전 상태, ambiguous 3 분모 제외, passing_mentions 6 오탐 분자, allowed 허용 집합, R-6 제3 범주, 이벤트 F1 클래스별·absolute 0·praise 1, generator 재기록, 라벨 재해석 금지, schema_version 승격, 표본 크기, F-251dc2/F-bdd6c5, 함정 12건 / P10: absolute·praise·other·personal_share·favor 방향·위계 하·길이·일정 라벨)
- Refs: P1-pilot-dataset S3.7 S3.1 D1 D10 원칙1 원칙7 원칙8 원칙9 R-3 L-002
