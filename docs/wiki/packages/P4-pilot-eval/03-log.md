# P4-pilot-eval · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-18 · docs(P4-pilot-eval): 계획검증 통과·계획 승인 — 결정 K 지배 기준, 패키지 착수 · b164f36
- 변경: `01-plan.md` 개정(의존 59c67cc·P3-llm-providers 추가 의존, 수용 기준 backlog 61행 문구, 결정 K (i)→(a) 지배 기준 226행·U4 `gate` 67행·판정 표 107행, R-1 U6 비용 문장 인용, R-2 행 번호 정정, R-3·R-4), `02-plan-verify.md`(verifier fable — 1차 09:30 보류 H-1 → 2차 09:50 통과, §1 verify-plan 0922·0930·0943·0954 FAIL 0/WARN 3 의도, 점검표 8/8, `승인: 사용자 (2026-09-18)`), `05-remediation.md`(F-0e133a 필수 해소, F-95c6a7·F-0ffff5 권고 열림 = registry 기존 행 확장 의도), `evidence/` 10파일(verify-plan 6·plan-refs 2·plan-signatures·validate-scenarios), `P3-llm-providers/04-review.md` 결과 줄 형식 1줄(verify-plan 의존 검사 regex), `CURRENT.md active: P4-pilot-eval`, journal, HANDOFF, 이 로그
- 이유(기획서·카드 연결): backlog 61행 "파일럿 평가(오병합률·미검출률·보정표·곡선 초안)" — S3.7 §5 P4 행, R3(D10 두 임계치 방향)·R4(자기보고 s_llm 보정) 를 닫는다. 결정 K 개정은 `exact_match.py:264~276` 의 오병합 0 가능성 때문에 단일 부등식이 방식 품질과 무관하게 미달을 낼 수 있어서(원칙8)
- 정합성 확인: 원칙 1·2·3·8·9 / D3 D4 D5 D10 D11 / S3.7 S3.3 / 보안 — 위반 없음(02-plan-verify 점검표 8/8). 코드 변경 0
- 남은 것 · 다음 단위: U1 러너 골격·격리·적재(eval-agent, L-004 승인 후). 결정 I: U6 전에 사용자 스모크 03·08(OpenAI) 1회. R-5~R-7 구현 시 반영
- Refs: P4-pilot-eval D3 D4 D5 D10 D11 S3.7 S3.3 R3 R4 F-0e133a L-002 L-004

## 2026-09-18 · feat(P4-pilot-eval): U1 러너 골격·격리·적재 — 세이브포인트 롤백·적재기 재사용·LLM mention 당 1회·hints None · fb81234
- 변경: `evaluation/runner.py` 신규(`run_pilot`·`RunSummary`·`RunnerError`·`build_runner_env`·`T_MERGE_GRID`·`T_NEW`·`ROW_EXTRA_KEYS`), `tests/test_eval_runner.py` 신규 29건, `docs/wiki/registry.md` 2행, `evidence/20260918-1239-u1-pytest.txt`·`20260918-1239-u1-pytest-all.txt`, 이 로그. `app/`·`data/` 변경 0
- 이유(기획서·카드 연결): 01-plan U1(64행) (i)~(v). 결정 D(i) 시나리오별 세이브포인트 롤백, 결정 C(i) LLM 1회 → 임계치 10벌, 결정 A(ii)·P3-llm-providers 인계 1(러너 매핑에 `LLM_PROVIDER=openai` 명시·`LLM_PROVIDERS_ENABLED` 제거), 인계 8(`embedder` 필수·임베딩 수 단언), 인계 9(두 벌 방지), 원칙9(`trace_id` 보존). 계획과 다르게 구현한 점: (1) 결정 C(i) 를 "confidence 에 `band_for` 재적용"이 아니라 "임계치마다 `resolve_mention()` 재호출 + LLM·임베딩 기억 래퍼"로 구현 — 러너가 밴드 규칙을 옮겨 적지 않아 `embedding_only` 동점 강등(R-5)·강제 강등이 방식 코드 그대로 유지되고, 제안 방식 행마다 자기 `er_resolve` trace 가 생긴다. LLM 호출 수는 계획과 같다(mention × 방식 1회, 테스트 단언). (2) 시그니처에 키워드 인자 4개 추가(`resolver_kwargs`·`env`·`before_rollback`·`overwrite`, 모두 기본값) — 스텁 주입·U7 실행 중 trace 덤프·원시 결과 덮어쓰기 방지. (3) JSONL 에 필수 4키 외 10키 추가 — U2 가 JSONL 만으로 분모 규칙(`passing_mentions`·`ambiguous`·`expected_ask_user.allowed`)을 적용하도록
- 정합성 확인: 원칙1·2(방식 판정 무개입, `ERConfig(t_merge, t_new=0.3)` 만 주입) / 원칙8(격자 고정, 기존 JSONL 덮어쓰기 거부) / 원칙9(`trace_id`·`llm_fresh_call`) / D5(별칭·mention 임베딩이 같은 기억 객체) / D10 / 보안(키·프롬프트 원문 미기록, summary 에 공급자 변수 두 값만)
- 검증: `POSTGRES_PORT=5433 PYTHONIOENCODING=utf-8 python -m pytest tests/test_eval_runner.py -q -rs` → 29 passed rc=0 (evidence/20260918-1239-u1-pytest.txt); 전체 `tests/` → 947 passed rc=0(기준 918 + 29, skip 0) (evidence/20260918-1239-u1-pytest-all.txt); `git diff --name-only -- app/ data/` 0줄
- 남은 것 · 다음 단위: U2 지표 계산기(JSONL 입력, 토큰 합산은 `llm_fresh_call` 행만). U7 은 `before_rollback` 으로 trace 표본 덤프. 실 공급자 경로(`judge_from_env`/`caller_from_env` 실 생성)는 U6 dry-run·U7 에서 처음 돈다
- Refs: P4-pilot-eval S3.7 D5 D10 원칙8 원칙9

## 2026-09-18 · feat(P4-pilot-eval): U2 지표 계산기 — 분모 규칙 3개·채점표 6칸·허용 집합·격자 사본 중복 금지 · a97521b
- 변경: `evaluation/metrics.py` 신규(`compute_metrics`·`load_rows`·`validate_rows`·`classify_gold_row`·`mention_level_rows`·`group_rates`·`ratio`·`format_t_merge`·`MetricsError`·`DENOMINATOR_RULE`·CLI `--rows/--out`), `tests/test_eval_metrics.py` 신규 94건, `docs/wiki/registry.md` 2행, `evidence/20260918-1300-u2-pytest.txt`·`20260918-1300-u2-pytest-all.txt`, 이 로그(U1 항목 해시 `pending`→`fb81234`). `app/`·`evaluation/runner.py`·`data/` 변경 0
- 이유(기획서·카드 연결): 01-plan U2(65행). 입력은 U1 JSONL 만(DB·네트워크·LLM·`app/` 호출 0 — 같은 파일 → 같은 수치, 원칙8). 분모 규칙 3개(`ambiguous` 3건 제외 / `passing_mentions` 6건 오탐 분자 / ask_user 제3 범주), 허용 집합 채점(`expected_ask_user.allowed`, 단일 정답 아님), 방식별·`t_merge` 별 산출물 전부(`false_merge_rate`·`miss_rate` 분리 = 원칙1 비대칭 비용, `ask_user_rate_by_kind` 3키, `forced_reason`·`llm_error` 분포, P3-er §7 부분집합 `rule_checked==0`·`relaxed_retry`·`derive_hints` 빈 dict). 계획과 다르게 판단한 점: **(1) 제3 범주를 `decision=="identity"` 로 좁혔다** — `new_person` 도 제품에서는 `ask_user(kind=new_person)` 이지만 "아는 누구도 아니다"라는 단정이라 골드와 대조해 `miss`/정답으로 채점한다. 둘 다 빼면 `miss` 분자가 **구조적으로 0** 이 되어 미검출률과 결정 K 의 지배 기준(미검출률 축)이 무의미해진다. 마찰 축(`ask_user_rate_by_kind`)에서는 `identity`·`new_person` 을 둘 다 ask_user 로 세고, 두 축이 배타적이지 않음을 `meta.denominator_rule` 에 적었다. **(2) `llm_error` 행을 기본 분모에 남겼다** — 01-plan 233행은 "분모에서 뺀 수를 meta 에" 였으나 한 방식만 분모가 줄면 방식 간 비교가 깨진다(`evaluation/resolvers/base.py` 불변 규약 2 "그것은 그 방식의 성능이 아니라 평가의 결함이다"). 대신 `point.llm_error` 분포 + `point.excluding_llm_error`(오류 행을 뺀 같은 지표·제거 행 수)를 함께 내 두 읽기 모두 수치가 남는다. **(3) `--validate` 는 넣지 않았다** — 판정 표 98행의 대상은 `metrics.json`(최상위 `meta`·`gate`)이고 그 구조는 U4 가 확정한다. (4) 계획에 없던 `by_category`·`group_rates()`·`excluding_llm_error`·`mention_level_rows()` 추가(U8 실패 유형 분석·사본 중복 방지의 단일 출처)
- 정합성 확인: 원칙1(오병합률·미검출률 분리, 제3 범주 분리) / 원칙2(두 임계치는 러너가 주입, 계산기는 밴드를 다시 매기지 않는다 — `band_for` 재구현 0) / 원칙8(모든 비율 `{"n","d","rate"}`, 분모 0 → `rate: null`, 같은 행 → 같은 바이트, 잘못된 행은 예외로 멈춤) / 원칙9(`forced_reason`·`llm_error`·`confidence` 부분집합을 그대로 집계) / 인계 14(`score` 는 출력 어디에도 없다) / 보안(키·프롬프트 원문 미접근, 파일 읽기만)
- 검증: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_eval_metrics.py -q -rs` → 94 passed rc=0 (evidence/20260918-1300-u2-pytest.txt); 전체 `POSTGRES_PORT=5433 … tests/ -q -rs` → 1041 passed rc=0(기준 947 + 94, skip 0) (evidence/20260918-1300-u2-pytest-all.txt); `git diff --name-only -- app/ data/` 0줄. 실물 40건 라벨 불변 확인: 비-ambiguous 골드 mention 중 `gold_person_id` 없는 건 0, `expected_ask_user.allowed` 없는 시나리오 0(검증기 전제)
- 남은 것 · 다음 단위: U3 보정표(`s_llm` 구간 × 공급자·모델·방식, `score_clamped`·`llm_skipped`·`llm_error` 제외 — 이 모듈의 `mention_level_rows()`·`llm_fresh_call` 규칙을 그대로 쓴다). U4 는 `methods[m].by_t_merge[t]` 를 `metrics.json` 최상위 다섯 키로 옮기고 `meta`·`gate` 를 얹은 뒤 `--validate` 를 이 모듈에 붙인다(판정 표 98행). JSONL→metrics 사슬의 실물 증명은 U6 dry-run(`--stub`)
- Refs: P4-pilot-eval S3.7 D10 원칙1 원칙2 원칙8 원칙9

## 2026-09-18 · feat(P4-pilot-eval): U3 보정표 — s_llm 구간 10칸 × 공급자·모델·방식, 제외 6종 분리 카운트 · ee124b7
- 변경: `evaluation/calibration.py` 신규(`compute_calibration`·`bin_index`·`bin_label`·`empty_bins`·`s_llm_status`·`BINS`·`BIN_LABELS`·`BIN_RULE`·`CORRECT_DEFINITION`·`EXCLUSION_REASONS`·`S_LLM_METHODS`·`S_LLM_SOURCE`·`CalibrationError`·CLI `--rows/--out/--t-merge/--model-configured/--indent`), `tests/test_eval_calibration.py` 신규 74건, `docs/wiki/registry.md` 2행, `evidence/20260918-1323-u3-pytest.txt`·`20260918-1323-u3-pytest-all.txt`, 이 로그(U2 항목 해시 `pending`→`a97521b`). `app/`·`data/`·`evaluation/runner.py`·`evaluation/metrics.py`·`reports/` 변경 0
- 이유(기획서·카드 연결): 01-plan U3(67행). D3 "보정 `s_llm` 0.1 구간별 실제 정답률 → `reports/calibration.json` (P4). 이 표가 자기보고 점수의 신뢰 근거" = R4 를 닫는 입력. 입력은 U1 JSONL 만(DB·네트워크·LLM·`app/`·`os.environ` 접근 0 — 같은 파일 → 같은 바이트, 원칙8). 구간 10칸 `[lo,hi)` + 마지막만 `[0.9,1.0]`, 경계값은 위쪽 칸(`0.1`→`0.1-0.2`, `0.8`→`0.8-0.9`, `1.0`→`0.9-1.0`)을 코드(`BINS` 상수·`bin_index` 의 역순 비교, `int(v*10)` 미사용)와 `meta.bin_rule` 양쪽에 명시. 그룹 키 `model` 은 판정이 돌려준 값, 설정 문자열은 `meta.model_configured`(R-6·P3-llm-providers §7 인계 4 — Gemini `model_version`). 정답 정의는 결정 F(i) 한 줄이고 코드로는 U2 `classify_gold_row` **같은 함수**(두 리포트가 다른 정답을 쓰면 안 된다). 격자 사본 중복은 U2 규칙 그대로(단위 = `mention_level_rows()` 한 행, 호출 건수는 `llm_fresh_call`). 계획과 다르게 판단한 점: **(1) 제외 이유를 3종이 아니라 6종으로 두었다** — 01-plan 이 적은 `score_clamped`·`llm.skipped`·`llm.error` 에 `passing_mention`·`ambiguous_mention`(U2 분모 규칙 1·2 — 골드가 없거나 null 이라 정오를 정의할 수 없다)과 **`placeholder_s_llm`** 을 더했다. placeholder 는 `app/er/confidence.py::_forced_decision` 의 귀속 규약(`no_matched`·`out_of_range_id` 등에서 `s_llm=s_emb=s_rule=0`)이 만든 **자기보고가 아닌 0.0** 으로, 그대로 세면 `0.0-0.1` 칸이 오염돼 보정표가 거짓말을 한다(판정: `confidence_breakdown["matched_person_id"] is None` / `llm_single` 은 `raw_decision is None`). 빼되 **수를 그룹·전체 양쪽에 남겨** 숨기지 않는다(원칙8). 진짜 자기보고 0.0(인물을 고르고 0.0 을 보고)은 그대로 집계한다(테스트 1건). **(2) `llm_single` 의 "skipped" 판정은 `detail["llm_calls"] == 0`** — 그 방식에는 `llm_skipped` 키가 없고(통과 후보 개념이 없다) 호출 0 이 같은 뜻이다. **(3) 채점 임계치를 `meta.scored_at_t_merge` 로 명시하고 `--t-merge` 를 두었다** — 한 mention 의 10행은 `s_llm` 이 같지만 `decision` 은 임계치마다 다르므로 "어느 임계치의 결정으로 채점했는가"가 수치의 일부다. 기본은 `sweep_index` 최소 행(= `llm_fresh_call` 행, 격자 최저 `T_merge`). **(4) `meta.model_configured` 는 CLI 인자로만 받는다** — JSONL 에 없는 값이고 실행 시점 환경변수를 읽으면 같은 입력이 같은 출력을 내지 않는다(U6·U7 이 `--model-configured openai=<OPENAI_MODEL>` 로 넘긴다). **(5) 공급자·모델을 알 수 없는 제외 행**(예: 호출 실패로 `provider`/`model` 이 `None`)은 그룹에 넣을 수 없어 최상위 `unassigned[method]` 로 따로 센다 — 집계 행에 `provider`/`model` 이 없으면 멈춘다(어느 모델의 보정표인지 말할 수 없다)
- 정합성 확인: 원칙3(`s_llm` = 자기보고 0~1, 로그확률 아님 — 출처를 `meta.s_llm_source` 에 방식별로 문자열로 남김) / 원칙8(모든 칸에 `n`·`correct` 동반, 분모 0 → `accuracy: null`, 제외는 이유별 카운트로 남김, 같은 행 → 같은 바이트) / 원칙9(판정 근거인 `confidence_breakdown`·`forced_reason`·`llm_error` 를 그대로 읽고 새로 만들지 않음) / 원칙4(방식 코드·`app/er` 무수정, 재판정 0) / D3(가중치·분해 해석 변경 0) / 보안(키·프롬프트 원문 미접근, 파일 읽기만, `.env` 미독)
- 검증: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_eval_calibration.py -q -rs` → 74 passed rc=0 (evidence/20260918-1323-u3-pytest.txt); 전체 `POSTGRES_PORT=5433 … tests/ -q -rs` → 1115 passed rc=0(기준 1041 + 74, skip 0·fail 0) (evidence/20260918-1323-u3-pytest-all.txt); `git diff --name-only -- app/ data/ evaluation/runner.py evaluation/metrics.py reports/` 0줄
- 남은 것 · 다음 단위: U4 곡선·`metrics.json` 조립. U4 는 `meta.model`(응답 모델명)을 이 모듈과 **같은 출처**(`detail["model"]`)에서 가져온다. U6 dry-run 은 JSONL→metrics→calibration→curve 사슬에 `--model-configured` 를 넣어 돌린다. U7 이 `reports/calibration.json` 실물을 만들고(이 단위는 `reports/` 를 만들지 않았다) evidence 에 설정 모델명과 응답 모델명을 둘 다 적는다(R-6). U8 은 `excluded.placeholder_s_llm`·`llm_error_kinds` 를 실패 유형 분석의 입력으로 쓴다 — 강제 경로에서 LLM 이 실제 보고한 `s_llm` 은 `app/er` 이 `detail` 로 내보내지 않아 이 표가 그 구간을 볼 수 없다(`app/` 무수정 제약, P10 후보)
- Refs: P4-pilot-eval R4 D3 원칙3 원칙8 원칙9

## 2026-09-18 · feat(P4-pilot-eval): U4 곡선·metrics.json 조립 — 게이트 지배 기준(결정 K)·D10 연속 쌍 단조·--validate · a59ecb4
- 변경: `evaluation/curve.py` 신규(`build_metrics_document`·`curve_rows`·`write_curve_csv`·`forced_reason_summary`·`build_gate`·`validate_metrics_document`·`CurveError`·`SERIES`·`EXPECTED_GRID`·`GATE_T_MERGE`·`CURVE_COLUMNS`·`DOMINANCE_RULE`·`D10_RULE`·CLI `--rows/--out/--curve/--embedding-model/--dataset-hash/--run-id/--model-configured/--indent`), `tests/test_eval_curve.py` 신규 55건, `evaluation/metrics.py` **CLI 한 곳만**(`--validate <metrics.json>` 추가, `--rows` 를 선택 인자로 — 기존 94건 무변경 통과), `docs/wiki/registry.md` 2행 + metrics 행 비고, `evidence/20260918-1346-u4-pytest.txt`·`-u4-pytest-all.txt`·`-u4-cli-smoke.txt`, 이 로그(U3 항목 해시 `pending`→`ee124b7`). `app/`·`data/`·`evaluation/runner.py`·`evaluation/calibration.py`·`reports/` 변경 0
- 이유(기획서·카드 연결): 01-plan U4(68행)·결정 K 지배 기준(227행)·결정 H(`top_k_swept:false`, 224행)·결정 J(곡선 형식 `curve.csv` + 표, 226행)·결정 C·L. S3.7 §4 "x = `T_merge` ∈ {0.5,…,0.95}, `T_new`=0.3 고정, y = 오병합률·ask_user(identity)율·미검출률" 이 곡선의 정의이고, 이 게이트가 P5 착수 여부를 가른다(원칙1·2). 02-plan-verify R-7 세 항목을 코드와 테스트로 고정: (1) `d10_direction` 은 양 끝점이 아니라 **연속 격자점 쌍 전부**(중간 역전을 잡는 테스트 1건), (2) 격자 키는 `format_t_merge()` 문자열이라 `0.8` 이 `0.8000000000000002` 로 새지 않는다(`0.1+0.7` 접힘 테스트), (3) `gate.baselines` = `RESOLVERS` − `proposed` 4키 단언. O-5 로 `gate.false_merge_ranking`(5방식 오병합률 순위, n/d 포함)을 게이트 옆에 실었다 — 지배 기준은 두 축 대칭이지만 원칙1 은 비대칭이므로 독자가 직접 판단할 수 있어야 한다. 계획과 다르게 판단한 점: **(1) `rate: null` 이면 게이트를 통과시키지 않는다** — 분모 0 은 "측정했더니 0"이 아니라 "측정하지 못함"이라 `pass:false` + `gate.reason` + `gate.undetermined`(어느 베이스라인의 비교가 불가였는지)로 남긴다. 따라서 `pass = (dominated_by == []) and d10_direction and reason is None` 이고, `dominated_by == []` 만으로 통과하지 않는다(원칙8). **(2) `curve.csv` 에 `t_new` 열을 더했다**(계획 68행의 열 나열에는 없다) — 01-plan 판정 표 104행이 `{x['t_new'] for x in r} == {'0.3'}` 를 요구하고, "스윕하지 않았다"가 파일 안에서 보여야 한다. **(3) `--validate` 의 규칙은 `metrics.json` 을 만드는 `curve.py` 가 갖고 `metrics.py` 는 함수 안에서 import 해 호출만 한다** — 스키마의 단일 출처를 만드는 쪽에 두고 모듈 순환을 피했다. `metrics.py` 변경은 CLI 한 곳(+`--rows` 선택화, 둘 다 주면 rc=2)뿐이다. **(4) 최상위 비-방식 키를 `meta`·`gate` 둘로 유지**(O-4) — 곡선 계열 정의·`forced_reason` 별도 집계는 `meta.curve`·`meta.forced_reason` 안에 넣었다. **(5) `embedding_model`·`dataset_hash`·`run_id`·`model_configured` 는 호출자 인자**(U3 와 같은 자세) — JSONL 에 없는 값이라 여기서 환경변수를 읽으면 같은 입력이 같은 출력을 내지 않는다. `provider`/`model` 은 `detail["model"]`(U3 과 같은 출처)에서 읽고, 값이 둘 이상이면 단일 필드는 `null` 이고 `providers_observed`/`models_observed` 에 전부 남는다. **(6) `weights` 는 `ERConfig()` 모듈 기본값**(0.5/0.3/0.2, D3)을 지연 import 로 읽고 `weights.source` 에 "환경변수를 읽지 않는다"를 적었다
- 정합성 확인: 원칙1(오병합률·미검출률을 분리한 채로 두 축을 비교, 순위표 동반) / 원칙2(`T_new` 0.3 고정·`T_merge` 만 x 축, 어긋나면 `CurveError`) / 원칙8(지표를 다시 계산하지 않고 U2 블록을 복사, 같은 입력 → 같은 바이트, `rate` null 을 통과로 바꾸지 않음, 실물 `reports/` 미생성) / 원칙9(`forced_reason` 을 곡선과 별도로 보존) / 결정 H(`top_k_swept:false`) / 결정 K(세 곳 정의 동일 — 코드·테스트·`gate.rule` 문자열) / 보안(키·프롬프트 원문·`.env` 미접근, 네트워크 0)
- 검증: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_eval_curve.py tests/test_eval_metrics.py -q -rs` → 149 passed rc=0(신규 55 + 기존 94 무변경) (evidence/20260918-1346-u4-pytest.txt); 전체 `POSTGRES_PORT=5433 … tests/ -q -rs` → 1170 passed rc=0(기준 1115 + 55, skip 0·fail 0) (evidence/20260918-1346-u4-pytest-all.txt); 판정 표 99·104·108행 명령을 합성 JSONL 로 미리 실행 — `--validate` rc=0, 게이트 출력 `0.8 [] True True`, `curve.csv` `[0.5…0.95] {'0.3'} 5 150`, 깨뜨린 사본은 rc=1 + 이유 (evidence/20260918-1346-u4-cli-smoke.txt); `git diff --name-only -- app/ data/ evaluation/runner.py evaluation/calibration.py reports/` 0줄
- 남은 것 · 다음 단위: U5 리포트 생성기(`evaluation/report.py`) — 입력은 `metrics.json` 하나뿐이며 표·곡선 표는 `meta.curve.series`·`by_t_merge`, 게이트 절은 `gate`(순위표 포함), 한계 절에 "`llm_single`·완전일치 곡선은 임계치를 읽지 않아 평탄한 것이 정상"(R-5). U6 dry-run 은 `python -m evaluation.curve --rows … --out … --curve …` 를 사슬에 넣고 `--dataset-hash`·`--run-id`·`--embedding-model`·`--model-configured` 를 채운다(이 단위는 호출자 인자로 비워 둔다). U7 이 `reports/metrics.json`·`reports/curve.csv` 실물을 만들고, 04-review 는 `gate` 를 같은 파일의 수치로 독립 재계산한다(O-4)
- Refs: P4-pilot-eval R3 D10 S3.7 원칙1 원칙2 원칙8 원칙9
## 2026-09-18 · feat(P4-pilot-eval): U5 리포트 생성기 — metrics.json 한 파일 → eval.md, 숫자 토큰 감시·멱등 · dd6a996
- 변경: `evaluation/report.py` 신규(`render_report`·`optimum_candidate`·`fmt_ratio`·`fmt_rate_value`·`fmt_score`·`fmt_number`·`ReportError`·`NULL_RATE`·`MISSING`·`CURVE_LINK`·`SERIES_LABEL`·CLI `--metrics/--out`), `tests/test_eval_report.py` 신규 42건, `docs/wiki/registry.md` 2행(커밋 열 `pending`), `evidence/20260918-1407-u5-pytest.txt`·`-u5-pytest-all.txt`·`-u5-cli-smoke.txt`, 이 로그(U4 항목 해시 `pending`→`a59ecb4`). `app/`·`data/`·`evaluation/{runner,metrics,calibration,curve}.py`·`reports/` 변경 0
- 이유(기획서·카드 연결): 01-plan U5(69행) — "입력은 `metrics.json` 하나뿐", 표(방식 × 지표)·곡선 표/링크·운영 최적점 **후보** 한 문단·한계 절. 결정 J(i)(곡선은 `curve.csv` + Markdown 표, PNG 는 P10)·결정 K(게이트 절에 지배 기준 문장·`dominated_by`·`d10_direction`·`pass`·`reason`)·결정 L(제3 범주 정의를 한계 절에 인용)·02-plan-verify O-5(`T_merge=0.8` 오병합률 순위표 5방식을 분자/분모와 함께 **게이트 결과 옆에**)·R-5(`exact_*`·`llm_single` 곡선 평탄이 정상이라는 한 줄). 원칙8(같은 입력 → 같은 수치·재현 불가능한 수치 금지)이 이 단위의 판정 기준이고, 판정 표 102행(`eval.md` 재생성 diff 0줄)이 그 기계 증명이다
- 정합성 확인: 원칙1(오병합률·미검출률을 분리한 두 열 + 순위표로 비대칭 비용을 독자가 판단) / 원칙2(`T_new` 고정·x 축은 `T_merge` 하나) / 원칙8(지표 재계산 0 — `{n,d,rate}` 를 옮겨 적을 뿐, `rate: null` 을 `0%` 로 적지 않음, 본문 숫자는 전부 입력에서, 날짜·시각·환경변수·DB·네트워크 미독, `reports/` 실물 미생성) / 원칙9(강제 경로·`llm_error`·부분집합을 곡선과 별도 절로 보존) / 결정 J·K·L / 보안(키·`.env`·프롬프트 원문 미접근, 네트워크 0)
- 계획과 다르게 판단한 점: **(1) 게이트를 다시 판정하지 않는다** — 본문의 통과/미달은 `gate.pass` 를 그대로 옮긴 것이고 이 모듈에는 판정식이 없다(두 곳에서 판정하면 두 판정이 생긴다, O-4 의 독립 재계산은 04-review 의 몫). **(2) 검증 실패 입력은 rc=1 이고 출력 파일을 만들지 않는다** — `validate_metrics_document` 를 먼저 호출하고, 깨진 입력으로 만든 리포트는 "재현 불가능한 수치"이므로 부분 출력도 남기지 않는다. **(3) 운영 최적점 후보의 선택 규칙을 코드에 고정**(오병합률이 최소인 격자점들 중 `T_merge` 가 가장 낮은 점 — 원칙1 의 비대칭 비용을 그대로 옮긴 순수 선택). 고르지 못할 때(전부 `rate: null`)는 후보를 만들지 않고 그 사실을 적는다. **(4) 표본 수 문장도 입력에서 읽는다** — "40건" 을 문자열로 박지 않고 `meta.scenario_count` 를 넣어 "…건이므로 방향(direction)이지 확정값이 아니다" 를 만든다(인계 15). 본문 어디에도 `150`·`40` 같은 상수를 쓰지 않는다. **(5) `meta.commit` 행을 메타 표에 둔다**(L-001 — 평가한 커밋을 리포트에 적는다). U4 `build_metrics_document()` 에 그 인자가 없으므로 지금은 `기록 없음`으로 나오고, U6·U7 이 `--run-id`/`meta` 로 채우면 자동으로 표시된다(이 단위에서 `curve.py` 를 고치지 않았다 — 금지 범위)
- 검증: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_eval_report.py -q -rs` → 42 passed rc=0 (evidence/20260918-1407-u5-pytest.txt); 전체 `POSTGRES_PORT=5433 … tests/ -q -rs` → **1212 passed** rc=0(기준 1170 + 42, skip 0·fail 0) (evidence/20260918-1407-u5-pytest-all.txt); 합성 `metrics.json`(5방식 × 격자 10 × 시나리오 2 × mention 8 = 800행)으로 판정 표 102행 — 2회 생성 후 `diff` **0줄**·`cmp` 동일·18411 bytes·CRLF 없음·절 8개, 깨뜨린 사본(`meta.t_new=0.4`)은 rc=1 + 이유 줄이고 출력 파일 미생성, `git status --porcelain reports/` 0줄 (evidence/20260918-1407-u5-cli-smoke.txt). 숫자 토큰 감시: 본문에서 인용 태그를 걷어낸 모든 숫자가 입력 문서의 수 집합 안에 있고(역단언 5건이 `F1 0.87`·`40건`·`0.62`·`1200` 을 잡는다), 값 민감도 5건이 "키를 참조하지 않는 템플릿"을 막는다
- 남은 것 · 다음 단위: U6 실행 CLI·dry-run·비용 가드 — 사슬 끝에 `python -m evaluation.report --metrics <out>/metrics.json --out <out>/eval.md` 를 넣고 rc 를 확인한다(입력은 그 한 파일). `meta.commit`·`meta.embedding_model`·`dataset_hash`·`run_id` 는 U6·U7 이 채운다(비면 리포트에 `기록 없음`으로 남는다). U7 이 `reports/eval.md` 실물을 만들고 04-review 가 같은 명령으로 재생성 diff 0줄을 다시 본다. U8 은 이 리포트의 한계 절·카테고리 표를 이어받아 실패 케이스 전건을 쓴다(오병합 전건·`forced_reason`·`llm_error`·함정 7종). U9 는 registry 행의 `pending` 해시를 확정한다
- Refs: P4-pilot-eval S3.7 D10 원칙1 원칙2 원칙8 원칙9 L-001

## 2026-09-21 · feat(P4-pilot-eval): U6 실행 CLI·dry-run·비용 가드 — 네트워크 0 사슬 증명, 실행 전 비용 추정·$5 상한, 키 없으면 rc=2 · 0283ac0
- 변경: `scripts/run_pilot_eval.py` 신규(`dataset_fingerprint`·`compute_run_id`·`estimate_run`·`format_estimate`·`CostEstimate`·`missing_real_env`·`StubJudge`/`StubSingleCaller`/`StubEmbedder`·`_run_chain`·CLI `--dry-run/--stub/--out/--scenarios/--limit/--max-cost-usd/--price-in/--price-out/--price-embed/--commit/--run-id/--estimate-only`), `tests/test_run_pilot_eval.py` 신규 35건, `evaluation/curve.py` **CLI·`build_metrics_document` 인자 두 개만**(`--commit`→`meta.commit`, `--run-mode`→`meta.run_mode`; 기존 55건 무변경 통과), `reports/cost_estimate.md` 신규, `docs/user-setup/10-pilot-eval-run.md` 신규 + `docs/user-setup/README.md` 색인 10행, `docs/wiki/registry.md` 4행(커밋 열 `pending`) + curve 행 비고 + U5 두 행 `pending`→`dd6a996`, `evidence/20260921-1318-u6-pytest.txt`·`-u6-pytest-all.txt`·`-u6-dry-run.txt`, 이 로그(U5 항목 해시 `pending`→`dd6a996`). `app/`·`data/`·`evaluation/{runner,metrics,calibration,report}.py` 변경 0, `reports/` 는 `cost_estimate.md` 하나만 새로 생김(실물 `metrics.json`·`eval.md` 는 U7 몫)
- 이유(기획서·카드 연결): 01-plan U6(70행) — dry-run 으로 사슬(JSONL→metrics→calibration→curve→eval.md)을 키 없이 증명하고, 실행 전 호출 수·토큰·비용을 찍고, 결정 B 의 `--max-cost-usd`(기본 $5, AWS Budgets $10 알림의 절반)로 막고, 키가 없으면 rc=2 로 이름만 안내한다(security §1). 판정 표 96~98행(dry-run rc=0·`network_calls=0`·`run_mode=stub`·시나리오마다 `embedded_alias_count == alias_count`)·107행(`cost_estimate.md`)이 이 단위의 기계 판정이고, 결정 C(i)(mention 당 1회 호출 → 밴드 10벌)가 비용 추정식의 근거다
- 정합성 확인: 원칙8(같은 입력 → 같은 `metrics.json` 바이트 — `run_id`·`dataset_hash` 에 시각이 없고 스텁 점수도 DB 시퀀스 `person_id` 에 의존하지 않게 만들었다 / 실측이 아닌 값을 실측이라 적지 않는다 / 스텁 산출물을 `reports/` 아래에 쓰는 것을 거부) / 원칙1·2(격자·`T_new` 는 러너 상수 그대로, 이 스크립트가 임계치를 다시 정하지 않는다) / 결정 A(ii)(러너 env `build_runner_env()` 재사용 — 두 번째 env 조립기 없음) / 결정 B·C(i)·D(i)(시나리오별 롤백, 세션 전체도 커밋하지 않는다) / 보안(`.env` 미독, 환경변수 통째 출력 없음, 키 값 미출력 — 테스트가 canary 값으로 확인) / 중복 구현 금지(판정 검증은 `FakeJudge`(=`validate_judgement`), 적재는 `load_scenario_state`, 지표·보정표·곡선·리포트는 U2~U5 `main(argv)` 호출만)
- 계획과 다르게 판단한 점: **(1) `evaluation/curve.py` 에 `--commit` 과 함께 `--run-mode` 도 넣었다** — 위임은 `--commit` 만 지정했으나 수용 기준 해석(01-plan 84행)의 판정 명령이 `meta['run_mode']` 를 읽으므로 그 키가 없으면 U7 이 `KeyError` 로 판정 불가다. `metrics.json` 의 유일한 작성자가 `curve.py` 라 여기 두지 않으면 두 번째 작성자가 생긴다. 둘 다 호출자 인자이고 생략 시 `null`(기존 동작 유지, 55건 무변경). **(2) 사슬에 `--validate` 단계를 넣었다**(runner→metrics→calibration→curve→**validate**→report) — 판정 표 99행을 실행 중에 닫고, 깨진 `metrics.json` 으로 리포트를 만들지 않는다. **(3) U2 단계 산출물을 `metrics-stage.json` 으로 따로 쓴다** — 사슬 네 단계가 실제로 각각 도는지(rc 확인)를 보이기 위해서이고, 리포트 입력은 여전히 `curve.py` 가 만든 `metrics.json` 하나다. **(4) 스텁 판정 점수를 후보 표시 이름 해시로 만든다** — `person_id` 로 만들면 DB 시퀀스가 실행마다 달라져 `metrics.json` 바이트 재현성이 깨진다(처음 구현은 깨졌고 2회 실행 비교에서 잡혔다). 스텁 단일호출도 프롬프트에서 숫자를 지운 뒤 해시한다. **(5) 비용 추정의 프롬프트 문자 수를 실제 빌더로 센다** — 상수를 지어내지 않기 위해 `llm_single.build_prompt`·`app.er.judge.build_prompt` 를 라벨 사전 상태로 호출했다. 문자→토큰(`1.5`)과 출력 토큰(`80`)만 가정값이고 출력·문서에 "가정"이라고 적었다. 단가도 오프라인 상수이며 출처 문장을 함께 찍고 CLI 로 덮어쓸 수 있다. **(6) 스모크 03·08 결과 이관은 하지 않았다**(사용자 결정 2026-09-21 — 두 스모크가 아직 미실행이라 U6 를 먼저 진행). evidence 에 없는 결과를 지어내지 않는다. **(7) `--limit`·`--estimate-only` 를 더했다** — 테스트·사용자 확인이 전량 실행 없이 돌 수 있게 하는 실행 범위 인자이고 기본값은 전량이다
- 검증: `POSTGRES_PORT=5433 PYTHONIOENCODING=utf-8 python -m pytest tests/test_run_pilot_eval.py tests/test_eval_curve.py -q -rs` → **90 passed** rc=0(신규 35 + 기존 55 무변경) (evidence/20260921-1318-u6-pytest.txt); 전체 `POSTGRES_PORT=5433 … tests/ -q -rs` → **1247 passed** rc=0(기준 1212 + 35, skip 0·fail 0) (evidence/20260921-1318-u6-pytest-all.txt); 판정 표 96~99·100·101행 — 전량 dry-run(40 시나리오·mention 141·**7050 행** = 141 × 5방식 × 10임계치) rc=0, `run_mode=stub network_calls=0`, 시나리오 40줄 전부 `aliases=N embedded=N … ok`, `--validate` rc=0(`OK`), 보정표 구간 `10 0`, `curve.csv` `[0.5…0.95] {'0.3'} 5`, `meta` = `stub None stub-embedding stub dd6a996 run-7e7eb91a18d3`(모델이 `null` 인 것은 스텁 판정기와 스텁 단일호출의 모델명이 둘이라 `models_observed` 로 갔기 때문) / 비용 가드 `--max-cost-usd 0.0001` → rc=3 이고 출력 디렉터리조차 만들지 않음 / 실 실행 모드에서 키 변수를 비우면 rc=2, `OPENAI_API_KEY` **이름만**, 디렉터리 미생성 (evidence/20260921-1318-u6-dry-run.txt); 2회 실행 `metrics.json`·`calibration.json`·`curve.csv`·`eval.md` **바이트 동일**(테스트 `test_dry_run_metrics_json_is_byte_identical`); `git status --porcelain reports/` → `?? reports/cost_estimate.md` 한 줄, `git diff --name-only -- app/ data/` **0줄**
- 남은 것 · 다음 단위: **스모크 03·08 결과 이관은 U7 전(사용자 실행 후)** — 결정 I 의 순서만 바뀌었고 이관 자체는 남아 있다(인계 13, [권고] 6). U7 실 실행은 `docs/user-setup/10-pilot-eval-run.md` 대로 사용자가 돌리고(`--out reports/pilot --commit $(git rev-parse HEAD)`), 그 출력의 `[run] tokens_in/tokens_out` 으로 `reports/cost_estimate.md` §3(1건당 실측)·§4(150건 외삽)의 "U7 직후 채움" 칸을 채운다 — 그 전에는 비워 둔다(원칙8). U7 은 `before_rollback` 자리에 trace 표본 덤프를 붙여 `agent_traces step='er_resolve'` 재계산(abs diff 0)을 실행 중에 남긴다(결정 D(i)). U9 는 registry 4행·이 항목의 `pending` 해시를 확정하고 README 에 "파일럿 평가 실행법" 절을 잇는다
- Refs: P4-pilot-eval D4 D5 원칙8 L-001 L-004

## 2026-09-21 · feat(P4-pilot-eval): U7 선행 보강 — 실행 중 trace 표본 덤프 + 확신도 재계산(abs diff 0), --recheck-traces · e7f0a9c
- 변경: `scripts/run_pilot_eval.py`(신규 공개물 `TraceDumpError`·`TraceRecheckError`·`TraceRecheck`·`TRACE_DUMP_SCHEMA_VERSION`·`TRACE_DUMP_METHOD`·`dump_er_traces`·`recompute_confidence`·`check_trace_entry`·`recheck_trace_dump`·`format_trace_recheck`·`_trace_entry`·`_matched_candidate`·`_Weights`, `_run_chain` 에 `traces-<ts>.jsonl` 덤프·재계산 단계, CLI `--traces-per-scenario`·`--recheck-traces`, 모듈 docstring 절 1개), `tests/test_run_pilot_eval.py` **19건 추가**(기존 35건 무변경, 파일 하나 유지), `docs/user-setup/10-pilot-eval-run.md`(재계산 증거 절·기대 출력 2줄·rc=1 설명), `docs/wiki/registry.md` 2행 비고 확장, `evidence/20260921-1843-u7pre-*.txt` 3개, 이 로그(U6 항목 해시 `pending`→`0283ac0`). `app/`·`data/`·`evaluation/**`·`reports/` 변경 0
- 이유(기획서·카드 연결): 01-plan 106행 판정 표 "확신도 재계산(표본)" 은 U7 evidence 에 `agent_traces step='er_resolve'` 재계산(실행 중 덤프, 실행 로그와 같은 ts)과 `0.5·s_llm+0.3·s_emb+0.2·s_rule` 대 기록 `confidence` 의 abs diff 0 을 요구한다. 결정 D(i)(시나리오별 롤백, 220·236행) 때문에 trace 는 **실행 중에만** 조회되므로, 이 장치 없이 유료 실행을 하면 증거를 잃고 재실행(= 원칙8 위반 유혹)이 필요해진다. 그래서 U7 **전에** `before_rollback` 자리에 덤프를 붙이고 스텁 경로에서 먼저 증명한다
- 판단 1 — **표본 정책은 "전량"**(`--traces-per-scenario` 로만 줄인다, 기본 `None`): (i) 01-plan 174행 계약이 "1행 = 판정 1개" 이고 제안 방식 행 수 = 덤프 줄 수여야 `dumped != proposed 행 수` 를 사슬이 스스로 잡는다(실제로 그 단언을 넣었다), (ii) 표본을 고르는 규칙을 두는 순간 "어느 것을 골랐나"가 판정에 개입한다(원칙8 — 게이트 패키지의 자기검증 유혹, 01-plan 239행), (iii) 크기: 한 줄 실측 ≈ 1.2KB(evidence (5)), 전량 1410행(= mention 141 × 격자 10) ≈ **1.7MB** 로 원시 JSONL 7.4MB 의 23% 수준이다. 크기를 줄이려고 `candidates[]` **전체는 옮기지 않고 귀속 후보 1개(`matched_candidate`)만** 남겼다 — 후보 전체는 원시 JSONL 의 `MentionDecision.candidates` 에 이미 있고(이중 출처 금지), 재계산에 필요한 것은 `confidence_breakdown` 과 "그 값이 귀속 후보의 값과 같은가" 뿐이다(원칙9). 프롬프트 원문·`llm.reason` 자유 서술·키는 넣지 않는다(security §1, 테스트가 단언)
- 판단 2 — **diff 판정은 허용오차 없는 `abs diff == 0.0`**: 근거는 코드다. (i) `app/er/confidence.py` 모듈 docstring 6~16행(F-7fe239)이 "`round()` 를 어디에도 쓰지 않는다"를 규약으로 못박았고 `_breakdown()`(144~153행)은 `combine()` 이 낸 double 을 **그대로** 싣는다 — 기록값에 반올림이 없다. (ii) 그래서 재계산은 같은 함수(`app.er.confidence.combine`)를 **같은 인자·같은 덧셈 순서**로 부르면 비트까지 같다(`s_emb` 클램프도 그 함수 안에 있어 따로 옮겨 적지 않았다). (iii) JSONB·`json.dumps` 왕복은 double 을 정확히 복원한다(psycopg 가 numeric → float, `repr` 왕복). 허용오차를 두면 판정을 느슨하게 만드는 것이므로 두지 않았고, 대신 실패 시 `max_abs_diff` 와 어느 trace 인지(`line·trace_id·scenario/mention·t_merge`)를 찍는다. 부정 테스트가 `confidence += 1e-12` 를 잡는다(evidence (2))
- 판단 3 — **가중치는 하드코딩하지 않는다**: trace 에 기록된 `confidence_breakdown["weights"]`(판정 당시 `ERConfig` 가 실제로 쓴 값)를 쓰되, 그 값이 제품 상수 `app.settings.ER_WEIGHTS`(0.5/0.3/0.2, 원칙3)와 다르면 재계산을 거부한다(테스트 1건). 산식·클램프의 단일 출처는 `app.er.confidence.combine` 이고 이 스크립트에 가중합 식을 다시 적지 않았다(테스트가 소스 문자열로도 단언)
- 판단 4 — **덤프 0행은 실패다**(01-plan 140행 "proposed +2" 전제): 제안 방식 행에 `trace_id` 가 없거나, 그 id 로 `step='er_resolve' AND tool_name='er'` 행이 조회되지 않거나, trace 의 `mention` 이 JSONL 행과 다르면 `TraceDumpError` → rc=1. 사슬은 덧붙여 `덤프 줄 수 == proposed 행 수`(전량 모드)와 `recheck.dumped == 쓴 줄 수`도 단언한다. 무결성 검사 2개를 더 넣었다: 강제 경로가 아닌데 `s_llm != llm.s_llm` 이면 실패, 귀속 후보의 `s_emb`/`s_rule` 이 `confidence_breakdown` 과 다르면 실패(원칙9 — 근거가 서로 어긋나면 수치를 믿을 수 없다)
- 계획과 다르게 한 것: 새 모듈을 만들지 않고 U6 스크립트 안에 두었다(registry grep 결과 기존 trace 덤프·재계산 구현 없음, `evaluation/` 에 새 파일을 늘리지 않는다). 재계산은 `--recheck-traces <path>` 로 **덤프 파일만 입력**으로 독립 실행된다(다른 인자를 무시하고 그 자리에서 끝낸다 — DB·네트워크·키 0)
- 검증(이 세션의 한계 포함): `PYTHONIOENCODING=utf-8 POSTGRES_PORT=5433 python -m pytest tests/test_run_pilot_eval.py -q -rs` → **47 passed / 7 skipped** rc=0 (evidence/20260921-1843-u7pre-pytest.txt); 전체 `… tests/ -q` → **1013 passed / 253 skipped** (evidence/20260921-1843-u7pre-pytest-all.txt, 1013+253 = 1266 = 기준 1247 + 신규 19, 실패 0); `--recheck-traces` CLI 4경로 — 정상 `dumped=5 recomputed=5 max_abs_diff=0.0` rc=0 / `confidence += 1e-12` rc=1(어느 trace 인지 출력) / `s_llm` 변조 rc=1(`llm.s_llm` 과 어긋남) / 없는 경로 rc=1 (evidence/20260921-1843-u7pre-recheck-cli.txt). **Docker Desktop 이 꺼져 있어(`docker ps` 실패, 5433 접속 타임아웃 — evidence 에 그대로) 사슬 층(실 PostgreSQL) 테스트 7건과 스텁 dry-run 실행은 이 세션에서 돌리지 못했다**(우회하지 않았다, security §6). 그 두 건(`test_dry_run_dumps_traces_before_rollback_and_rechecks`·`test_dry_run_traces_per_scenario_caps_the_dump`)과 `--dry-run --stub` 출력은 **Docker 기동 후 재실행이 필요하다** — 그 전에는 이 단위를 완료로 보지 않는다(원칙8: 돌리지 않은 것을 돌렸다고 적지 않는다)
- 남은 것 · 다음 단위: (1) Docker 기동 후 `POSTGRES_PORT=5433 python -m pytest tests/test_run_pilot_eval.py -q -rs`(skip 0)과 전량 `--dry-run --stub --out <tmp>` 를 돌려 `[traces] dumped=1410 recomputed=1410 max_abs_diff=0.0` 과 파일 크기를 evidence 로 남긴다. (2) **원시 JSONL 5MB 초과 위험** — `.githooks/pre-commit` 이 5MB 초과 파일을 막는데 스텁 전량 실행의 `raw-<ts>.jsonl` 이 7.4MB 였다(U6 evidence). 결정 E(원시 JSONL 커밋)와 충돌하므로 U7 전에 사용자 결정이 필요하다(압축·분할·예외 중 택1 — 이 단위에서 정하지 않았다). (3) U7 실행 후 `traces-<ts>.jsonl` 을 `reports/pilot/` 에 원시 JSONL 과 같은 stamp 로 남기고 04-review 가 `--recheck-traces` 로 재확인한다
- 후속 검증(메인 세션, 2026-09-21 19:03 — 사용자가 Docker 기동): (1) 의 두 명령 실행. `pytest tests/test_run_pilot_eval.py -q -rs` → **54 passed / skip 0**(evidence/20260921-1903-u7pre-pytest-db.txt). `run_pilot_eval.py --dry-run --stub --out <scratchpad> --commit 0283ac0` → rc=0, `[traces] dumped=1410 recomputed=1410 max_abs_diff=0.0`, 사슬 6단계 rc=0, `network_calls=0`(evidence/20260921-1903-u7pre-stub-chain.txt). 파일 크기 실측: raw 7,404,682 bytes · traces 1,739,409 bytes. (2) 는 **사용자 결정(2026-09-21): gzip 으로 커밋**(`raw-<ts>.jsonl.gz`, 결정 E 유지·훅 변경 없음) — 도구의 `.gz` 입력 지원은 U7 전 별도 단위.
- Refs: P4-pilot-eval S3.7 S3.3 D3 D5 원칙3 원칙8 원칙9

## 2026-09-21 · feat(P4-pilot-eval): U7 선행 단위 2 — 원시 판정 JSONL gzip 지원(결정적 `.gz` 출력·`--rows` 입력·5MB 가드) · 1a5643b
- 변경: `evaluation/metrics.py`(신규 공개물 `GZIP_SUFFIX`·`open_jsonl()`·`iter_jsonl()`, `load_rows()` 가 그 위로 옮겨감 — 수치 계산 코드는 한 줄도 건드리지 않았다, 모듈 docstring 1절), `evaluation/calibration.py`·`evaluation/curve.py`(`--rows` help 문구만 — 둘 다 `load_rows` 를 쓰므로 코드 변경 0), `scripts/run_pilot_eval.py`(신규 공개물 `COMMIT_SIZE_LIMIT_BYTES`·`GZIP_COMPRESSLEVEL`·`GzipError`·`gzip_jsonl()`·`sha256_bytes_of()`·`format_commit_sizes()`, `_run_chain` 에 압축·왕복 검증·크기 줄, CLI `--drop-raw-plain`, `recheck_trace_dump` 가 공용 `iter_jsonl` 사용, 모듈 docstring 1절), `tests/test_run_pilot_eval.py` **+11**·`tests/test_eval_metrics.py` **+5**·`tests/test_eval_calibration.py` **+2**·`tests/test_eval_curve.py` **+1**(합 19, 기존 테스트 무변경), `docs/user-setup/10-pilot-eval-run.md`(§"커밋되는 원시 판정은 `raw-<ts>.jsonl.gz` 다" + 기대 출력 2줄 + 5번 절차 파일명), `docs/wiki/registry.md` 4행 비고, `evidence/20260921-1937-u7pre2-*.txt` 4개, 이 로그(직전 U7 선행 보강 항목 `pending` → `e7f0a9c`). `app/`·`data/`·`reports/`·`.githooks/`·`.claude/` 변경 **0**(`git status --porcelain` 로 확인)
- 이유(카드 연결): 결정 E(01-plan 205행)는 원시 `pilot/raw-<ts>.jsonl` 을 커밋하라고 하는데 스텁 전량 실측이 7,404,682 bytes 라 `.githooks/pre-commit` 27행(5,242,880 bytes 초과 차단)에 걸린다. **사용자 결정(2026-09-21): gzip 으로 커밋한다** — 결정 E 는 유지하고 훅은 바꾸지 않는다. 그러면 U7(유료 실 실행) 전에 도구가 `.gz` 를 읽고 써야 한다. 커밋된 파일 하나만으로 지표를 다시 계산할 수 없으면 재계산 방법이 LLM 재호출뿐이고, 그것은 원칙8 이 막는 것이다
- 판단 1 — **여는 로직은 `evaluation.metrics` 한 곳**: registry grep(`jsonl`·`load_rows`·`--rows`) 결과 JSONL 로더는 `load_rows` 하나였고 calibration·curve 가 이미 그것을 import 한다. 그래서 새 모듈을 만들지 않고 그 옆에 `open_jsonl`(확장자로 gzip/평문 선택)·`iter_jsonl`(줄 번호 + gzip 손상 → `MetricsError` 번역)을 두고 `load_rows` 와 `recheck_trace_dump`(스크립트)가 **둘 다 그것만** 쓴다. 진입점 3개(`metrics`/`calibration`/`curve`)는 `--rows` help 문구만 바뀌었고 `--recheck-traces` 의 `.gz` 지원은 공짜로 따라왔다
- 판단 2 — **쓰기는 "러너 평문 → 사슬이 압축 → 이후 단계는 압축본"**: 러너(`run_pilot`)의 줄 단위 쓰기·중단 시 부분 결과 보존 성질을 건드리지 않으려고 러너는 그대로 두고, 사슬이 러너 종료 후 압축한다. 그리고 metrics·calibration·curve 에 **`.gz` 경로를 넘긴다** — 커밋될 파일로 지표가 나온다는 것을 실행이 매번 증명한다(evidence (2)(3): 평문 입력과 `cmp` 동일)
- 판단 3 — **평문은 기본 보존, `--drop-raw-plain` 으로만 삭제**: (i) 기존 테스트·01-plan 판정 명령·U7 evidence 가 평문 `raw-<ts>.jsonl` 을 전제하고 위임도 "기존 테스트 무변경 통과"를 요구했다, (ii) 압축 단계가 실패해도 원본이 남아야 유료 실행 결과를 잃지 않는다, (iii) 커밋 대상은 `.gz` 하나로 명확하고 `git add` 는 명시 경로만 쓰므로 평문이 실수로 들어가지 않는다(들어가도 훅이 막는다). 삭제는 **왕복 sha256 검증을 통과한 뒤에만** 한다(`gzip_jsonl(drop_plain=True)`)
- 판단 4 — **결정적 gzip**: 헤더 기본값에 수정 시각과 원본 파일명이 들어가 실행마다 바이트가 달라지므로 `mtime=0`·`filename=""`·`compresslevel=9`(상수)로 고정했다. 같은 평문 → 같은 바이트(evidence (4): `bytes_equal=True`, 헤더 mtime `00000000`, FNAME 플래그 0). 기존 "같은 입력 2회 `metrics.json` 바이트 동일" 성질과 같은 취지다(원칙8)
- 판단 5 — **크기 초과는 경고이고 rc 를 바꾸지 않는다**: 한도 상수는 `COMMIT_SIZE_LIMIT_BYTES = 5_242_880` 하나(테스트가 `.githooks/pre-commit` 본문에 같은 수가 있는지 대조한다). 사슬 끝에서 커밋 대상(`raw.gz`·`traces`)의 `[size] … limit=… ok|OVER` 를 찍고 초과면 `[warn] 커밋 한도 초과: …` 를 낸다. **rc 0/1/2/3 규약은 유지**했다 — 한도는 평가의 옳고 그름이 아니라 커밋 훅의 규칙이고, 유료 실 실행이 끝난 뒤 파일 크기로 rc≠0 을 돌려주면 "지표가 틀렸다"와 "파일이 크다"가 구분되지 않는다(그리고 rc≠0 은 재실행 유혹 = 원칙8 위반으로 이어진다). `traces-<ts>.jsonl`(1,739,409 bytes)은 한도 안이라 평문 유지 — 형식을 바꾸지 않았다
- 실측(스텁 전량 40 시나리오·7050행, evidence `20260921-1937-u7pre2-stub-chain.txt`): `raw-<ts>.jsonl` 7,404,682 bytes → **`raw-<ts>.jsonl.gz` 161,456 bytes**(2.2%, 한도의 3%). 사슬 6단계 rc 전부 0, `[traces] dumped=1410 recomputed=1410 max_abs_diff=0.0`, `network_calls=0`, `[size] … ok` 2줄, `[ok] 사슬 완료`. **스텁 판정 문자열이 반복적이라 압축률이 실 실행보다 유리하다** — 실 실행 크기는 그때 `[size]` 줄로 확인한다(지금 수치로 실 실행을 단정하지 않는다, 원칙8)
- 검증: `POSTGRES_PORT=5433 PYTHONIOENCODING=utf-8 python -m pytest tests/test_run_pilot_eval.py tests/test_eval_metrics.py tests/test_eval_calibration.py tests/test_eval_curve.py -q -rs` → **296 passed** rc=0(기존 277 무변경 + 신규 19) (evidence/20260921-1937-u7pre2-pytest.txt); 전체 `… tests/ -q -rs` → **1285 passed** rc=0(기준 1266 + 19, skip 0·fail 0) (evidence/20260921-1937-u7pre2-pytest-all.txt); 커밋본 단독 재계산 — `python -m evaluation.metrics --rows <raw>.jsonl.gz` 결과가 평문 결과와 `cmp` 동일·sha256 동일, calibration·curve(`metrics.json`+`curve.csv`)도 동일, gzip 2회 압축 바이트 동일, 손상된 `.gz` → `MetricsError: … gzip 읽기 실패 (EOFError: …)` rc=1 / `--recheck-traces` 도 rc=1 (evidence/20260921-1937-u7pre2-gzip-recompute.txt); `git status --porcelain app/ data/ reports/ .githooks/ .claude/` **0줄**
- 남은 것 · 다음 단위: (1) **01-plan 본문은 고치지 않았다**(계획 개정은 메인 세션·사용자 몫) — 평문 경로를 전제한 곳: **46행**(산출물 목록 `reports/pilot/raw-<ts>.jsonl`), **71행**(U7 산출 목록), **80행**(수용 기준 판정 명령 `open('reports/pilot/raw-<ts>.jsonl',encoding='utf-8')` — 그대로 두면 U7 후 판정이 실패한다. `python -c "import json,gzip;rows=[json.loads(l) for l in gzip.open('reports/pilot/raw-<ts>.jsonl.gz','rt',encoding='utf-8')];print(len({r['scenario_id'] for r in rows}), sorted({r['method'] for r in rows}))"` 로 바꾸거나 `python -m evaluation.metrics --rows ….gz` 로 대체해야 한다), **126행**(registry 행 예정 목록), **205행**(결정 E 본문). 결정 E 이행 방식 = gzip(사용자 결정 2026-09-21)은 이 로그에 기록한다. (2) U7 실행 후 커밋 대상은 `raw-<ts>.jsonl.gz` + `traces-<ts>.jsonl` 이고 평문 raw 는 `git add` 하지 않는다. (3) U9 가 registry 의 `pending` 해시와 이 항목 해시를 확정한다
- Refs: P4-pilot-eval S3.7 D4 D5 원칙8 원칙9 L-001 L-004

## 2026-09-21 · docs(P4-pilot-eval): 01-plan 원시 판정 경로 표기 .gz 개정 — 결정 E 이행 방식(gzip) · 1c84d35
- 변경: `01-plan.md` 46·71·80·126·205행 — `raw-<ts>.jsonl` → `raw-<ts>.jsonl.gz`, 80행 판정 명령 `open(…)` → `gzip.open(…,'rt',encoding='utf-8')`, 205행 결정 E 에 개정 주석. 수용 기준 문구·기대 출력(`40 [...]`)·결정 E 의 결론(원시 전부 커밋)은 그대로. 이 로그(직전 항목 `pending`→`1a5643b`), `evidence/20260921-2015-verify-plan-gz.txt`, HANDOFF·journal
- 이유(기획서·카드 연결): 사용자 결정(2026-09-21) — raw 평문 7,404,682 bytes 가 `.githooks/pre-commit` 5MB 한도 초과 → gzip 으로 커밋(1a5643b). 80행 판정 명령이 평문을 가정한 채면 U7 뒤 커밋본으로 판정이 실패한다(원칙8 재현성). 사용자 선택 '경로 표기만 .gz 로 개정'(verifier 재검증은 생략 — 의미 변경 없음)
- 정합성 확인: 수용 기준 = backlog 61행 글자 그대로 유지(변경 0) / 원칙8 / 결정 E 결론 불변. 코드 변경 0
- 검증: `bash .claude/scripts/verify-plan.sh P4-pilot-eval` → FAIL 0 / WARN 3(기존 의도된 3건 그대로, evidence/20260921-2015-verify-plan-gz.txt)
- 남은 것 · 다음 단위: 스모크 03·08(OpenAI) + U7 실 실행 — 키 필요, 실행 방식 사용자 결정. 커밋 대상 = `raw-<ts>.jsonl.gz` + `traces-<ts>.jsonl`
- Refs: P4-pilot-eval S3.7 원칙8

## 2026-09-22 · docs(P4-pilot-eval): 스모크 03 실호출 증거 등재 — OpenAI 경로 첫 실호출(F-87c597 1/3) · pending
- 변경: `packages/P3-er/evidence/20260921-2011-er-smoke-real.txt`(신규, 319 bytes — 사용자가 2026-09-21 20:11 본인 셸에서 `!` 접두로 스모크 카드 03 `--provider openai` 실행, 출력 그대로), 이 로그(직전 항목 `pending`→`1c84d35`), HANDOFF·journal(VERIFY 줄). 코드·`reports/`·`.claude/` 변경 **0**
- 이유(카드 연결): F-87c597(P3-er, R4 "실호출로 검증된 공급자 0/3") — 첫 실호출. 스모크 카드 03 이 지정한 명령을 사용자가 직접 돌렸고(사용자 결정 2026-09-21: 에이전트는 실 실행을 돌리지 않는다, `.env` 는 사용자 셸이 로드), 결과 파일을 에이전트가 이관만 했다. 실측: `provider=openai model=gpt-4o-mini-2024-07-18 tokens_in=410 tokens_out=48 s_llm=0.9 matched_person_id=1 confidence=0.8384 band=merge` rc=0 — 0.5·0.9 + (0.3·s_emb + 0.2·s_rule) = 0.8384 로 3신호 결합식(원칙3)이 실 응답에서도 재현됐고 `≥ T_merge 0.8` → merge(원칙2). 키 문자열 미혼입(파일 전문 319 bytes)
- 한계(원칙8 — 있는 그대로): `reason` 필드는 사용자 셸(cp949 콘솔) 이중 인코딩으로 원문 복구 불가(utf-8·cp949 양쪽 decode 모두 치환문자). 수치 필드는 온전하다. 증거 파일은 고치지 않는다(증거 원문 보존). **스모크 08·U7 은 `PYTHONIOENCODING=utf-8` 을 앞에 붙여 실행**해야 같은 손실이 없다 — 안내 명령에 반영
- 남은 것 · 다음 단위: (1) 스모크 08(OpenAI 경로, 사용자 셸) → evidence 이관 (2) U7 실 실행(`docs/user-setup/10-pilot-eval-run.md`) → `reports/pilot/raw-<ts>.jsonl.gz`·`traces-<ts>.jsonl`·`metrics.json` 커밋(평문 raw 제외), F-87c597 evidence 이관, `cost_estimate.md` §3·§4 실측, 판정 표 (3) 미달이면 재실행 금지 → U8
- Refs: P4-pilot-eval P3-er F-87c597 R4 D11 원칙2 원칙3 원칙8 원칙9
