== verifier(fable) 계획 재검증 P3-er 01-plan 개정 1 — 산술 직접 계산 (2026-09-05 22:06) ==
명령: python - <<EOF (아래 식 그대로) EOF

회귀(a) 승진 픽스처(결정 9): s_llm=0.95, s_emb=0.85, s_rule=2/3 (완화 통과 위계를 미통과로 계상)
  0.5*0.95 + 0.3*0.85 + 0.2*(2/3) = 0.8633333333333333  >= 0.8 -> True   (계획 "≈ 0.863" 일치, 여유 0.063)
  완화 통과를 통과로 셌다면: 0.5*0.95 + 0.3*0.85 + 0.2*1.0 = 0.9299999999999999 (계획은 이 값을 쓰지 않는다 — 보수 계상 확인)
LLM 실패 강등(s_llm=0) 상한: 0.3*1 + 0.2*1 = 0.5  < 0.8
힌트 없는 발화 상한(s_rule=0): 0.5*1 + 0.3*1 + 0.2*0 = 0.8  >= 0.8 -> True  (리스크 절 "0.8 = T_merge" 일치)

경계 부동소수:
  0.5*0.2 + 0.2*1            = 0.30000000000000004  raw>=0.3 True   round6 True
  0.5*0.8 + 0.3*1.0 + 0.2*0.5 = 0.7999999999999999   raw>=0.8 False  round6 True   <- "정확히 T_merge" 인데 raw 비교는 identity 로 떨어진다 (허용오차 필요성의 실제 사례)
  0.5*0.6 + 0.3*1.0 + 0.2*1.0 = 0.8                   raw>=0.8 True
허용오차 폭 비교(값 0.7999996, 진짜로 T_merge 보다 4e-7 작음):
  round(x,6) >= round(0.8,6)          -> True   (T_merge 미만을 merge 로 판정 — 원칙1 방향과 반대)
  round(x,9) >= round(0.8,9)          -> False
  math.isclose(x, 0.8, abs_tol=1e-9)  -> False
결론: 표현 오차(1e-16 급)만 흡수하려면 1e-9 급 허용오차면 충분하고, round(·,6) 은 필요 이상으로 넓다.
