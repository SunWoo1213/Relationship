# P3-er · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-06 01:00 · docs(P3-er): 계획·계획검증 승인, 패키지 착수 · pending
- 변경: packages/P3-er/01-plan.md(architect(opus) 초안 → 개정 1: verifier 보류 3건·권고 7건 반영, 사용자 결정 23:45·00:10, U1~U9), 02-plan-verify.md(verifier(fable) 점검표 8행 — 1차 보류 → 재검증 통과, 승인 줄), 05-remediation.md(필수 4 해소·권고 7 해소·기존 registry 행 8 + 재검증 권고 6: F-7fe239 F-93f063 F-8809f2 F-f3b245 F-5a97ef F-1d65ac), evidence/20260905-{2018,2029-2,2038-3,2142-4,2206-5,2206-6}-verify-plan*.txt·2035-plan-review-findings·2206-plan-review-arith, CURRENT active: P3-er, 03-log 생성
- 이유(기획서·카드 연결): resolution-plan §4 P3 "ER 4단계 + 확신도 + trace". S3.3(후보 검색→규칙 필터→LLM 판정→확신도 분기, 완화 재검색 1회, 회귀 3종)·D3(3신호 가중합, s_llm 자기보고 — R4)·D10(두 임계치)·D5(별칭 임베딩 — R9)·D1 D2(ask_user 경로)·D6(merge 는 별칭만, 이름은 확인 후 — 사용자 결정). 선행 P2-tools 완료 b676799. 이 패키지가 CLAUDE.md 원칙 1~4 의 뿌리를 구현.
- 정합성 확인: 원칙1(null·llm_failed 병합 금지, 완화 후 T_merge 불변·s_rule 미통과 계상)·2(두 임계치 설정값+인자)·3(가중치 0.5/0.3/0.2, 자기보고 명시)·4(4단계 모듈 분리, LLM 입력은 통과 후보만)·8(FakeJudge·grouped_embedder 결정적, 실호출은 smoke)·9(trace 1행 step=er_resolve tool_name="er", breakdown·decision·llm) / D1 D2 D3 D4 D5 D6 D10 / S3.3 S3.7 S3.2 S3.4 / S3.1 무변경(벡터 인덱스 연기) / 보안 §1(키는 환경변수로만, .env 미독) — 위반 없음. WARN 8 은 기존 registry 행(비고만)
- 남은 것 · 다음 단위: U1 F-ca12ad(@traced except 경로 begin_nested·기록 실패 시 원래 예외 보존) + traced step/tokens 확장 + 1535차원 flush 실패 재현 테스트. 권고 6건은 U4(F-7fe239 허용오차 1e-9·F-f3b245 llm_failed 귀속·F-5a97ef 범위 집합)·U6(F-1d65ac null 경로 테스트)·U7(F-93f063 원시 SQL 재조회·F-8809f2 apply 중복 거부)에서. 사용자 승인 → `--stage backend-agent`.
- Refs: P3-er R4 R9 D3 D5 D10 S3.3

## 2026-09-06 10:40 · feat(P3-er): U1 F-ca12ad 수정 + `@traced` 확장(step·tokens) · 2b82882
- 변경: `app/tools/context.py`(except 경로를 `ctx.session.begin_nested()` 세이브포인트 안에서만 tool_error 기록 시도 → 그 세이브포인트를 열거나 그 안의 flush 가 다시 실패하면(세션이 이미 DEACTIVE) 조용히 포기하고 bare `raise` 로 원래 예외를 그대로 올림, `session.rollback()` 미호출; `traced(tool_name, step="tool_call")` 로 시그니처 확장 — 성공 행의 `step` 은 인자값, 반환값이 `trace_tokens() -> (int, int)` 를 제공하면 그 값을 `tokens_in`/`tokens_out` 에 씀, 없으면 기존대로 0; 모듈 docstring 에 "F-ca12ad" 절 신설 + 기존 F-4d8d96 절에 "이번 단위(P3-er U1)에서도 이 한계는 그대로 유지, 별도 커넥션은 P5-loop 결정" 한 줄 재확인), `tests/test_tools_context.py`(`test_traced_flush_failure_preserves_original_error` — U1 재현 수단은 1535차원 가짜 embedder → `create_person`(`_add_alias`) flush 실패, 호출자 예외가 `PendingRollbackError` 가 아니라 `DataError`(psycopg `expected 1536 dimensions, not 1535`)이고 `exc.__context__` 도 `PendingRollbackError` 가 아님을 단언 — 세션 자체는 DEACTIVE 상태로 남으므로 테스트가 `db_session.rollback()` 으로 회복한 뒤(실제 호출자인 `get_session()` 이 하는 일과 동일, `traced()` 구현은 여전히 rollback 을 부르지 않음) tool_error 행이 있다면 `step="tool_error"`·`error` 클래스명 일치만 확인; 테스트 위 주석에 U2 이후 `check_dimension()` 이 flush 전에 잡으면 재현 수단을 강제 flush 실패(NOT NULL 위반)로 바꿀 것(F-3ca6b5)이라고 명시 / `test_traced_step_argument_and_trace_tokens_are_recorded` — `@traced("fake", step="er_resolve")` 로 감싼 가짜 함수와 `trace_tokens()` 를 제공하는 `_FakeTokenResult` 로 `step`·`tokens_in`·`tokens_out` 기록을 단언 / 기존 `_make_pending_question` 헬퍼를 이 파일에 자체 추가(테스트 파일간 비공유 관례, `test_tools_records.py` 와 동일 패턴)). registry·01-plan U1 체크박스는 건드리지 않음(메인 세션 몫).
- 이유(기획서·카드 연결): P2 04-review §6 F-ca12ad(evidence `P2-tools/evidence/20260905-1942-review-tool-error-probe.txt` — `PendingRollbackError` 가 원래 `DataError` 를 가리고 tool_error 행도 없음) 재현·수정. 01-plan 37행·38행·87행 U1 체크리스트·171행 결정7(begin_nested + 기록 실패 무시, `session.rollback()` 금지) 그대로 구현. `traced()` 확장은 U6(`app/er/pipeline.py`)가 `step="er_resolve"`·LLM `usage` 토큰을 같은 관측 경로로 재사용하기 위한 선행 작업(01-plan "결정5").
- 정합성 확인: 원칙9(모든 판정에 근거 — tool_error 기록이 실패해도 원래 예외는 절대 가려지지 않음)·원칙1(오병합 방지와 같은 비대칭 비용 방향 — "원래 예외 보존" > "오류 trace 항상 남김")·결정7(begin_nested·기록 실패 무시·rollback 미호출, 세 조건 모두 코드에 반영)·F-4d8d96(한계 유지, docstring 재확인 한 줄 추가, 별도 커넥션으로 우회하지 않음)·F-3ca6b5(U1→U2 재현 수단 전환을 테스트 주석에 명시). `@traced("search_person")` 처럼 인자 하나만 쓰는 기존 6개 호출부는 무변경으로 동작(회귀 208건 전부 통과가 증거). 보안 §1 위반 없음(키·비밀 미포함).
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/test_tools_context.py -q -rs` → 9 passed(evidence `20260906-1030-u1-pytest-context.txt`) / `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → 208 passed, skip 0(evidence `20260906-1030-u1-pytest-all.txt`, 기존 206건 + 신규 2건) / `POSTGRES_PORT=5433 python scripts/tools_check.py` → 7/7 ok(evidence `20260906-1030-u1-tools_check.txt`). Docker Desktop 이 세션 시작 시 꺼져 있어 사용자 대신 기동 후 로컬 `capstone2-postgres-1`(5433) 접속 확인.
- 계획과 다르게 한 것: 테스트 (i) 에서 예외 자체 단언 뒤 `db_session.rollback()` 을 호출해야 했다 — flush 실패로 세션이 SQLAlchemy `DEACTIVE` 상태가 되어(결정7 이 막는 것은 "구현이 rollback 을 부르는 것"이지 "호출자가 나중에 복구하는 것"이 아님) 같은 세션으로 후속 `SELECT`(트레이스 행 조회)를 하려면 먼저 회복해야 했다. 01-plan 은 이 세부까지는 명시하지 않았으나 결정7·F-4d8d96 문서의 취지(트랜잭션 경계는 호출자 몫)와 일치한다. 그 밖에는 계획대로.
- 남은 것 · 다음 단위: U2(임베딩 런타임 공급자, `check_dimension()`, F-3ca6b5 재현 수단 전환).
- Refs: P3-er S3.3 원칙9

## 2026-09-06 11:30 · feat(P3-er): U2 임베딩 런타임 공급자 · pending
- 변경: `app/embedding.py`(`OpenAIEmbeddingProvider` 런타임 단일 출처로 신설 — `text-embedding-3-small` 고정, `dimension` 은 결정6-b 대로 호출 없이 상수 `EMBEDDING_DIM` 반환, 키는 `os.environ["OPENAI_API_KEY"]` 로만 읽고 없으면 클라이언트 생성 전에 사람이 읽는 `RuntimeError`(키 문자열 미노출), `openai` 는 `_get_client()` 안에서만 지연 import, `client` 인자로 테스트 스텁 주입 지점 제공; `EmbeddingProvider` Protocol 에 `dimension` 추가, `runtime_checkable`/`isinstance` 제거; `as_provider()` 를 `isinstance(embedder, EmbeddingProvider)` 대신 `getattr(embedder, "embed", None) is not None` 확인으로 교체(callable/None 분기는 그대로); `check_dimension(vector, expected=EMBEDDING_DIM)` 신설 — 불일치 시 `app.tools.types.InvalidValue` 를 지연 import 로 던져 순환 임포트를 피함; 모듈 docstring 에 결정6-a 사유·`.env` 미독 원칙 명시), `app/tools/persons.py`(`_add_alias` 의 `provider.embed([alias])[0]` 직후·`session.add`/`flush` 이전에 `check_dimension()` 호출 한 줄 추가, import 문에 `check_dimension` 추가 — 그 밖의 로직·시그니처 무변경), `requirements.txt`(`openai==2.33.0`(이미 설치돼 있던 버전 그대로 핀)·`anthropic==1.4.0`(신규 설치 후 그 버전으로 핀) 추가, 둘 다 지연 import 라는 주석), `tests/test_tools_context.py`(F-ca12ad 재현 수단을 강제 flush 실패로 전환 — 새 `@traced("dummy_flush_failure")` 헬퍼 `_dummy_flush_failure`(정상 flush 로 person 행을 쓴 뒤 `PersonAlias(alias=None)` 로 두 번째 flush 를 NOT NULL 위반으로 실패시킴)를 추가하고 `test_traced_flush_failure_preserves_original_error` 가 이것을 호출하도록 교체(제목·단언 유지, docstring/주석을 "U2 에서 전환 완료" 로 갱신) / `test_add_alias_rejects_dimension_mismatch_before_flush` 신설 — 1535차원 가짜 embedder 로 `create_person` 호출 시 `InvalidValue` 가 flush 전에 발생하고(`DataError`/`PendingRollbackError` 아님) `tool_error` 행 1개가 정상 기록됨을 단언 / import 에 `Person`·`PersonAlias` 추가), `tests/test_embedding_provider.py` 신설(14건 — `dimension` 상수 반환(모델명 무관 포함)·`as_provider()` None/callable/embed-속성-객체/비callable-거부·`_DangerousDimensionProvider` 로 "dimension 접근 시 예외를 던지는 객체를 오판하지 않음" 검증·`check_dimension()` 정상/불일치/커스텀 expected·`OpenAIEmbeddingProvider` 키 없음 시 사람이 읽는 오류(가짜 키 마커로 키 미노출 확인, 진짜 키 형식 문자열은 테스트에도 넣지 않음)·스텁 client 로 배치 호출 1회·`index` 정렬·빈 리스트 스킵 검증), `scripts/embed_pilot.py` 무변경(아래 "계획과 다르게 한 것" 참고).
- 이유(기획서·카드 연결): 01-plan 35행·88행(U2 체크리스트)·115~116행(판정 방법 표 F-ca12ad·차원 사전 검출)·166~169행(결정6 a·b·c)·37행(F-3ca6b5 재현 수단 전환)을 그대로 구현. D4(임베딩은 `EmbeddingProvider` 인터페이스 뒤에 둔다, 공급자 교체는 클래스 하나)·D5(별칭 단위 임베딩, `person_embeddings` 없음)·R9(인물당 벡터 1개가 아니라 별칭 top-K→인물별 max, 이번 단위는 그 뒤의 실제 공급자 도입분)·원칙9(F-ca12ad 회귀가 남아 있어야 P3 이후 단계에서도 원래 예외가 가려지지 않는다는 근거를 유지). security.md §1 — 키는 `os.environ` 만, `.env` 미독, 로그·trace·테스트에 키 문자열 없음.
- 정합성 확인: 결정6-a(임베딩 이동 시도 → 실제 계약 충돌 확인 → 파일럿은 그대로 두고 이 모듈을 런타임 단일 출처로 선언, 아래 "계획과 다르게 한 것"에 근거 상술)·결정6-b(`dimension` 무호출 상수 반환, `check_dimension()` 을 flush 직전에 적용)·F-3ca6b5(재현 수단이 U1→U2 경계에서 정확히 계획대로 전환됨 — 차원 불일치는 이제 flush 전에 `InvalidValue` 로 막히고, flush 실패 회귀는 강제 NOT NULL 위반으로 대체)·F-ca12ad(새 재현 수단에서도 `begin_nested()` 세이브포인트가 세션 무효 상태를 감지해 조용히 포기하고 원래 `DataError`/`IntegrityError` 계열을 그대로 올림, `PendingRollbackError` 로 가려지지 않음 — evidence 로 재확인)·원칙4(4단계 구현은 아직 없음, 이 단위는 D4/D5 의 임베딩 인터페이스 기반 작업)·security.md §1(키 미노출 — 테스트가 가짜 키 마커로 직접 검증, 실제 키 형식 문자열은 코드·테스트 어디에도 없음, secret-guard 훅이 최초 초안의 `sk-` 접두 테스트 문자열을 막아 마커로 교체). 툴 7종 시그니처·스키마 무변경.
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/test_tools_context.py tests/test_embedding_provider.py -q -rs -k "flush_failure or dimension"` → 8 passed(evidence `20260906-1130-u2-pytest-flush-dimension.txt`) / `python -m pytest tests/test_embed_pilot.py -q` → 10 passed, 무변경(evidence `20260906-1130-u2-pytest-embed-pilot.txt`) / `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → 223 passed, skip 0(evidence `20260906-1130-u2-pytest-all.txt`, 기존 208 + 신규 15) / `python scripts/tools_check.py` → 7/7 ok(evidence `20260906-1130-u2-tools_check.txt`) / `POSTGRES_PORT=5433 python -m alembic check` → `No new upgrade operations detected.`(evidence `20260906-1130-u2-alembic-check.txt`) / `grep -n "dotenv" app/ scripts/ -r` → app/ 0건, scripts/embed_pilot.py 4건만(evidence `20260906-1130-u2-grep-dotenv.txt`) / `pip freeze | grep -iE "^(openai|anthropic)=="` → `anthropic==1.4.0`·`openai==2.33.0`(evidence `20260906-1130-u2-pip-freeze.txt`, `anthropic` 은 이번 단위에서 신규 설치, `openai` 는 기존 설치본 그대로 핀).
- 계획과 다르게 한 것: **`scripts/embed_pilot.py` 를 변경하지 않았다(결정6-a 조항 발동, 우회 아님)** — 01-plan 은 "옮기고, 계약이 충돌하면 우회하지 말고 파일럿 쪽을 그대로 두고 03-log 에 적으라"고 명시했다. 실제로 옮기려 하면 다음 세 가지가 충돌한다: (i) `dimension` — 파일럿은 `text-embedding-3-large`(3072차원)까지 비교해야 하므로 "실제 응답 벡터 길이"가 필요한데, 이 모듈의 `dimension` 은 결정6-b 에 따라 모델과 무관하게 상수 `EMBEDDING_DIM`(1536)을 호출 없이 돌려준다 — `text-embedding-3-large` 를 돌리면 파일럿이 잘못된 차원(1536)을 리포트하게 된다; (ii) `name` — 파일럿의 `summarize()`/`run_pilot()` 출력이 `provider.name` 을 쓰는데 런타임 공급자에는 그 속성이 없다; (iii) `usage`(토큰 사용량 누적) — 파일럿은 비용 추정을 위해 호출마다 누적하는데 런타임 공급자는 비용 추정이 이 단위의 범위가 아니다(P0-cost 미착수). 따라서 파일럿의 로컬 `EmbeddingProvider` Protocol·`OpenAIEmbeddingProvider` 클래스를 **그대로 두었고**, `app/embedding.py` 를 "**런타임(app/) 단일 출처**"로 한정해 선언했다 — 파일럿은 "결정용 코드"라는 자기 계약을 유지한다. 이로써 `tests/test_embed_pilot.py` 10건은 손대지 않아도 무변경으로 통과한다(증거 자체가 "손대지 않았다"는 사실). 그 밖에 `EmbeddingProvider` Protocol 에서 `runtime_checkable`/`Callable` 관련 미사용 import 를 정리했다(더 이상 `isinstance()` 로 판별하지 않으므로) — 01-plan 이 명시하지 않은 사소한 정리이나 동작에는 영향 없다.
- 남은 것 · 다음 단위: U3(호칭 사전 + 규칙 필터, `app/er/types.py`·`app/er/dictionary.py`·`app/er/rules.py`).
- Refs: P3-er R9 D4 D5 D6 원칙9 F-ca12ad F-3ca6b5
