# P3-er · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-06 01:00 · docs(P3-er): 계획·계획검증 승인, 패키지 착수 · pending
- 변경: packages/P3-er/01-plan.md(architect(opus) 초안 → 개정 1: verifier 보류 3건·권고 7건 반영, 사용자 결정 23:45·00:10, U1~U9), 02-plan-verify.md(verifier(fable) 점검표 8행 — 1차 보류 → 재검증 통과, 승인 줄), 05-remediation.md(필수 4 해소·권고 7 해소·기존 registry 행 8 + 재검증 권고 6: F-7fe239 F-93f063 F-8809f2 F-f3b245 F-5a97ef F-1d65ac), evidence/20260905-{2018,2029-2,2038-3,2142-4,2206-5,2206-6}-verify-plan*.txt·2035-plan-review-findings·2206-plan-review-arith, CURRENT active: P3-er, 03-log 생성
- 이유(기획서·카드 연결): resolution-plan §4 P3 "ER 4단계 + 확신도 + trace". S3.3(후보 검색→규칙 필터→LLM 판정→확신도 분기, 완화 재검색 1회, 회귀 3종)·D3(3신호 가중합, s_llm 자기보고 — R4)·D10(두 임계치)·D5(별칭 임베딩 — R9)·D1 D2(ask_user 경로)·D6(merge 는 별칭만, 이름은 확인 후 — 사용자 결정). 선행 P2-tools 완료 b676799. 이 패키지가 CLAUDE.md 원칙 1~4 의 뿌리를 구현.
- 정합성 확인: 원칙1(null·llm_failed 병합 금지, 완화 후 T_merge 불변·s_rule 미통과 계상)·2(두 임계치 설정값+인자)·3(가중치 0.5/0.3/0.2, 자기보고 명시)·4(4단계 모듈 분리, LLM 입력은 통과 후보만)·8(FakeJudge·grouped_embedder 결정적, 실호출은 smoke)·9(trace 1행 step=er_resolve tool_name="er", breakdown·decision·llm) / D1 D2 D3 D4 D5 D6 D10 / S3.3 S3.7 S3.2 S3.4 / S3.1 무변경(벡터 인덱스 연기) / 보안 §1(키는 환경변수로만, .env 미독) — 위반 없음. WARN 8 은 기존 registry 행(비고만)
- 남은 것 · 다음 단위: U1 F-ca12ad(@traced except 경로 begin_nested·기록 실패 시 원래 예외 보존) + traced step/tokens 확장 + 1535차원 flush 실패 재현 테스트. 권고 6건은 U4(F-7fe239 허용오차 1e-9·F-f3b245 llm_failed 귀속·F-5a97ef 범위 집합)·U6(F-1d65ac null 경로 테스트)·U7(F-93f063 원시 SQL 재조회·F-8809f2 apply 중복 거부)에서. 사용자 승인 → `--stage backend-agent`.
- Refs: P3-er R4 R9 D3 D5 D10 S3.3

## 2026-09-06 10:40 · feat(P3-er): U1 F-ca12ad 수정 + `@traced` 확장(step·tokens) · 2b82882
- 변경: `app/tools/context.py`(except 경로를 `ctx.session.begin_nested()` 세이브포인트 안에서만 tool_error 기록 시도 → 그 세이브포인트를 열거나 그 안의 flush 가 다시 실패하면(세션이 이미 DEACTIVE) 조용히 포기하고 bare `raise` 로 원래 예외를 그대로 올림, `session.rollback()` 미호출; `traced(tool_name, step="tool_call")` 로 시그니처 확장 — 성공 행의 `step` 은 인자값, 반환값이 `trace_tokens() -> (int, int)` 를 제공하면 그 값을 `tokens_in`/`tokens_out` 에 씀, 없으면 기존대로 0; 모듈 docstring 에 "F-ca12ad" 절 신설 + 기존 F-4d8d96 절에 "이번 단위(P3-er U1)에서도 이 한계는 그대로 유지, 별도 커넥션은 P5-loop 결정" 한 줄 재확인), `tests/test_tools_context.py`(`test_traced_flush_failure_preserves_original_error` — U1 재현 수단은 1535차원 가짜 embedder → `create_person`(`_add_alias`) flush 실패, 호출자 예외가 `PendingRollbackError` 가 아니라 `DataError`(psycopg `expected 1536 dimensions, not 1535`)이고 `exc.__context__` 도 `PendingRollbackError` 가 아님을 단언 — 세션 자체는 DEACTIVE 상태로 남으므로 테스트가 `db_session.rollback()` 으로 회복한 뒤(실제 호출자인 `get_session()` 이 하는 일과 동일, `traced()` 구현은 여전히 rollback 을 부르지 않음) tool_error 행이 있다면 `step="tool_error"`·`error` 클래스명 일치만 확인; 테스트 위 주석에 U2 이후 `check_dimension()` 이 flush 전에 잡으면 재현 수단을 강제 flush 실패(NOT NULL 위반)로 바꿀 것(F-3ca6b5)이라고 명시 / `test_traced_step_argument_and_trace_tokens_are_recorded` — `@traced("fake", step="er_resolve")` 로 감싼 가짜 함수와 `trace_tokens()` 를 제공하는 `_FakeTokenResult` 로 `step`·`tokens_in`·`tokens_out` 기록을 단언 / 기존 `_make_pending_question` 헬퍼를 이 파일에 자체 추가(테스트 파일간 비공유 관례, `test_tools_records.py` 와 동일 패턴)). registry·01-plan U1 체크박스는 건드리지 않음(메인 세션 몫).
- 이유(기획서·카드 연결): P2 04-review §6 F-ca12ad(evidence `P2-tools/evidence/20260905-1942-review-tool-error-probe.txt` — `PendingRollbackError` 가 원래 `DataError` 를 가리고 tool_error 행도 없음) 재현·수정. 01-plan 37행·38행·87행 U1 체크리스트·171행 결정7(begin_nested + 기록 실패 무시, `session.rollback()` 금지) 그대로 구현. `traced()` 확장은 U6(`app/er/pipeline.py`)가 `step="er_resolve"`·LLM `usage` 토큰을 같은 관측 경로로 재사용하기 위한 선행 작업(01-plan "결정5").
- 정합성 확인: 원칙9(모든 판정에 근거 — tool_error 기록이 실패해도 원래 예외는 절대 가려지지 않음)·원칙1(오병합 방지와 같은 비대칭 비용 방향 — "원래 예외 보존" > "오류 trace 항상 남김")·결정7(begin_nested·기록 실패 무시·rollback 미호출, 세 조건 모두 코드에 반영)·F-4d8d96(한계 유지, docstring 재확인 한 줄 추가, 별도 커넥션으로 우회하지 않음)·F-3ca6b5(U1→U2 재현 수단 전환을 테스트 주석에 명시). `@traced("search_person")` 처럼 인자 하나만 쓰는 기존 6개 호출부는 무변경으로 동작(회귀 208건 전부 통과가 증거). 보안 §1 위반 없음(키·비밀 미포함).
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/test_tools_context.py -q -rs` → 9 passed(evidence `20260906-1030-u1-pytest-context.txt`) / `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → 208 passed, skip 0(evidence `20260906-1030-u1-pytest-all.txt`, 기존 206건 + 신규 2건) / `POSTGRES_PORT=5433 python scripts/tools_check.py` → 7/7 ok(evidence `20260906-1030-u1-tools_check.txt`). Docker Desktop 이 세션 시작 시 꺼져 있어 사용자 대신 기동 후 로컬 `capstone2-postgres-1`(5433) 접속 확인.
- 계획과 다르게 한 것: 테스트 (i) 에서 예외 자체 단언 뒤 `db_session.rollback()` 을 호출해야 했다 — flush 실패로 세션이 SQLAlchemy `DEACTIVE` 상태가 되어(결정7 이 막는 것은 "구현이 rollback 을 부르는 것"이지 "호출자가 나중에 복구하는 것"이 아님) 같은 세션으로 후속 `SELECT`(트레이스 행 조회)를 하려면 먼저 회복해야 했다. 01-plan 은 이 세부까지는 명시하지 않았으나 결정7·F-4d8d96 문서의 취지(트랜잭션 경계는 호출자 몫)와 일치한다. 그 밖에는 계획대로.
- 남은 것 · 다음 단위: U2(임베딩 런타임 공급자, `check_dimension()`, F-3ca6b5 재현 수단 전환).
- Refs: P3-er S3.3 원칙9

## 2026-09-06 11:30 · feat(P3-er): U2 임베딩 런타임 공급자 · 2c63c60
- 변경: `app/embedding.py`(`OpenAIEmbeddingProvider` 런타임 단일 출처로 신설 — `text-embedding-3-small` 고정, `dimension` 은 결정6-b 대로 호출 없이 상수 `EMBEDDING_DIM` 반환, 키는 `os.environ["OPENAI_API_KEY"]` 로만 읽고 없으면 클라이언트 생성 전에 사람이 읽는 `RuntimeError`(키 문자열 미노출), `openai` 는 `_get_client()` 안에서만 지연 import, `client` 인자로 테스트 스텁 주입 지점 제공; `EmbeddingProvider` Protocol 에 `dimension` 추가, `runtime_checkable`/`isinstance` 제거; `as_provider()` 를 `isinstance(embedder, EmbeddingProvider)` 대신 `getattr(embedder, "embed", None) is not None` 확인으로 교체(callable/None 분기는 그대로); `check_dimension(vector, expected=EMBEDDING_DIM)` 신설 — 불일치 시 `app.tools.types.InvalidValue` 를 지연 import 로 던져 순환 임포트를 피함; 모듈 docstring 에 결정6-a 사유·`.env` 미독 원칙 명시), `app/tools/persons.py`(`_add_alias` 의 `provider.embed([alias])[0]` 직후·`session.add`/`flush` 이전에 `check_dimension()` 호출 한 줄 추가, import 문에 `check_dimension` 추가 — 그 밖의 로직·시그니처 무변경), `requirements.txt`(`openai==2.33.0`(이미 설치돼 있던 버전 그대로 핀)·`anthropic==1.4.0`(신규 설치 후 그 버전으로 핀) 추가, 둘 다 지연 import 라는 주석), `tests/test_tools_context.py`(F-ca12ad 재현 수단을 강제 flush 실패로 전환 — 새 `@traced("dummy_flush_failure")` 헬퍼 `_dummy_flush_failure`(정상 flush 로 person 행을 쓴 뒤 `PersonAlias(alias=None)` 로 두 번째 flush 를 NOT NULL 위반으로 실패시킴)를 추가하고 `test_traced_flush_failure_preserves_original_error` 가 이것을 호출하도록 교체(제목·단언 유지, docstring/주석을 "U2 에서 전환 완료" 로 갱신) / `test_add_alias_rejects_dimension_mismatch_before_flush` 신설 — 1535차원 가짜 embedder 로 `create_person` 호출 시 `InvalidValue` 가 flush 전에 발생하고(`DataError`/`PendingRollbackError` 아님) `tool_error` 행 1개가 정상 기록됨을 단언 / import 에 `Person`·`PersonAlias` 추가), `tests/test_embedding_provider.py` 신설(14건 — `dimension` 상수 반환(모델명 무관 포함)·`as_provider()` None/callable/embed-속성-객체/비callable-거부·`_DangerousDimensionProvider` 로 "dimension 접근 시 예외를 던지는 객체를 오판하지 않음" 검증·`check_dimension()` 정상/불일치/커스텀 expected·`OpenAIEmbeddingProvider` 키 없음 시 사람이 읽는 오류(가짜 키 마커로 키 미노출 확인, 진짜 키 형식 문자열은 테스트에도 넣지 않음)·스텁 client 로 배치 호출 1회·`index` 정렬·빈 리스트 스킵 검증), `scripts/embed_pilot.py` 무변경(아래 "계획과 다르게 한 것" 참고).
- 이유(기획서·카드 연결): 01-plan 35행·88행(U2 체크리스트)·115~116행(판정 방법 표 F-ca12ad·차원 사전 검출)·166~169행(결정6 a·b·c)·37행(F-3ca6b5 재현 수단 전환)을 그대로 구현. D4(임베딩은 `EmbeddingProvider` 인터페이스 뒤에 둔다, 공급자 교체는 클래스 하나)·D5(별칭 단위 임베딩, `person_embeddings` 없음)·R9(인물당 벡터 1개가 아니라 별칭 top-K→인물별 max, 이번 단위는 그 뒤의 실제 공급자 도입분)·원칙9(F-ca12ad 회귀가 남아 있어야 P3 이후 단계에서도 원래 예외가 가려지지 않는다는 근거를 유지). security.md §1 — 키는 `os.environ` 만, `.env` 미독, 로그·trace·테스트에 키 문자열 없음.
- 정합성 확인: 결정6-a(임베딩 이동 시도 → 실제 계약 충돌 확인 → 파일럿은 그대로 두고 이 모듈을 런타임 단일 출처로 선언, 아래 "계획과 다르게 한 것"에 근거 상술)·결정6-b(`dimension` 무호출 상수 반환, `check_dimension()` 을 flush 직전에 적용)·F-3ca6b5(재현 수단이 U1→U2 경계에서 정확히 계획대로 전환됨 — 차원 불일치는 이제 flush 전에 `InvalidValue` 로 막히고, flush 실패 회귀는 강제 NOT NULL 위반으로 대체)·F-ca12ad(새 재현 수단에서도 `begin_nested()` 세이브포인트가 세션 무효 상태를 감지해 조용히 포기하고 원래 `DataError`/`IntegrityError` 계열을 그대로 올림, `PendingRollbackError` 로 가려지지 않음 — evidence 로 재확인)·원칙4(4단계 구현은 아직 없음, 이 단위는 D4/D5 의 임베딩 인터페이스 기반 작업)·security.md §1(키 미노출 — 테스트가 가짜 키 마커로 직접 검증, 실제 키 형식 문자열은 코드·테스트 어디에도 없음, secret-guard 훅이 최초 초안의 `sk-` 접두 테스트 문자열을 막아 마커로 교체). 툴 7종 시그니처·스키마 무변경.
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/test_tools_context.py tests/test_embedding_provider.py -q -rs -k "flush_failure or dimension"` → 8 passed(evidence `20260906-1130-u2-pytest-flush-dimension.txt`) / `python -m pytest tests/test_embed_pilot.py -q` → 10 passed, 무변경(evidence `20260906-1130-u2-pytest-embed-pilot.txt`) / `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → 223 passed, skip 0(evidence `20260906-1130-u2-pytest-all.txt`, 기존 208 + 신규 15) / `python scripts/tools_check.py` → 7/7 ok(evidence `20260906-1130-u2-tools_check.txt`) / `POSTGRES_PORT=5433 python -m alembic check` → `No new upgrade operations detected.`(evidence `20260906-1130-u2-alembic-check.txt`) / `grep -n "dotenv" app/ scripts/ -r` → app/ 0건, scripts/embed_pilot.py 4건만(evidence `20260906-1130-u2-grep-dotenv.txt`) / `pip freeze | grep -iE "^(openai|anthropic)=="` → `anthropic==1.4.0`·`openai==2.33.0`(evidence `20260906-1130-u2-pip-freeze.txt`, `anthropic` 은 이번 단위에서 신규 설치, `openai` 는 기존 설치본 그대로 핀).
- 계획과 다르게 한 것: **`scripts/embed_pilot.py` 를 변경하지 않았다(결정6-a 조항 발동, 우회 아님)** — 01-plan 은 "옮기고, 계약이 충돌하면 우회하지 말고 파일럿 쪽을 그대로 두고 03-log 에 적으라"고 명시했다. 실제로 옮기려 하면 다음 세 가지가 충돌한다: (i) `dimension` — 파일럿은 `text-embedding-3-large`(3072차원)까지 비교해야 하므로 "실제 응답 벡터 길이"가 필요한데, 이 모듈의 `dimension` 은 결정6-b 에 따라 모델과 무관하게 상수 `EMBEDDING_DIM`(1536)을 호출 없이 돌려준다 — `text-embedding-3-large` 를 돌리면 파일럿이 잘못된 차원(1536)을 리포트하게 된다; (ii) `name` — 파일럿의 `summarize()`/`run_pilot()` 출력이 `provider.name` 을 쓰는데 런타임 공급자에는 그 속성이 없다; (iii) `usage`(토큰 사용량 누적) — 파일럿은 비용 추정을 위해 호출마다 누적하는데 런타임 공급자는 비용 추정이 이 단위의 범위가 아니다(P0-cost 미착수). 따라서 파일럿의 로컬 `EmbeddingProvider` Protocol·`OpenAIEmbeddingProvider` 클래스를 **그대로 두었고**, `app/embedding.py` 를 "**런타임(app/) 단일 출처**"로 한정해 선언했다 — 파일럿은 "결정용 코드"라는 자기 계약을 유지한다. 이로써 `tests/test_embed_pilot.py` 10건은 손대지 않아도 무변경으로 통과한다(증거 자체가 "손대지 않았다"는 사실). 그 밖에 `EmbeddingProvider` Protocol 에서 `runtime_checkable`/`Callable` 관련 미사용 import 를 정리했다(더 이상 `isinstance()` 로 판별하지 않으므로) — 01-plan 이 명시하지 않은 사소한 정리이나 동작에는 영향 없다.
- 남은 것 · 다음 단위: U3(호칭 사전 + 규칙 필터, `app/er/types.py`·`app/er/dictionary.py`·`app/er/rules.py`).
- Refs: P3-er R9 D4 D5 D6 원칙9 F-ca12ad F-3ca6b5

## 2026-09-06 12:00 · feat(P3-er): U3 호칭 사전 + 규칙 필터(2단계) · 02e6f14
- 변경: `app/er/__init__.py`(신설, 짧은 docstring — `resolve`/`apply_resolution`/`Judge`/`FakeJudge` 재export 는 U5~U7 몫이라고 명시, 아직 재export 없음), `app/er/types.py`(신설 — `ScoredCandidate`(P2 `Candidate` 를 감싸 `person_id`·`display_name`·`aliases`·`relation_tag`·`hierarchy`·`s_emb`(=`similarity` 이름 이동)·`rule_flags`(원본 6개, 재계산 안 함)·`rule_checked`·`rule_passed`·`s_rule`·`passed_rules`·`excluded_by`·`relaxed_pass`, 불변 dataclass), `Judgement`(`matched_person_id`·`s_llm`(주석에 "자기보고, 로그 확률 아님(R4)")·`reason`·`tokens_in`·`tokens_out`·`model`), `Resolution`(`trace_id`·`mention`·`candidates`·`relaxed_retry`·`matched_person_id`·`confidence`·`breakdown`·`band`·`band_by_threshold`·`forced_reason`·`ask_payload`·`suggested_display_name`(결정3-b)), `AppliedResolution`(자리만 — U7 이 채움), `ERConfig`(`t_merge=0.8`·`t_new=0.3`·`w_llm=0.5`·`w_emb=0.3`·`w_rule=0.2`·`top_k=SEARCH_TOP_K`·`judge_timeout=20.0`·`judge_max_retries=1`, `__post_init__` 이 가중치 합 1.0(오차 1e-9)과 `0≤t_new≤t_merge≤1` 을 검증해 위반 시 `InvalidValue`), `JudgeUnavailable` 예외, `ER_TRACE_STEP="er_resolve"`·`ER_TRACE_TOOL_NAME="er"`·`ER_VERSION="1"`), `app/er/dictionary.py`(신설 — `Entry(group, hierarchy, rank)`, 결정10 직급→위계 표(사원·인턴 0/주임·대리 1/과장·팀장 2/차장·실장 3/부장·본부장 4/이사·상무·전무 5/대표 6, `USER_RANK_ANCHOR=2`, `rank>anchor→상`·`==→동`·`<→하`), "사장"만 모호 항목(`Entry(None,None)`)으로 분리(대표는 직장 확정), 가족(엄마·아빠·어머니·아버지·할머니·할아버지·이모·고모·삼촌·형·누나·오빠·언니·동생)·연인(여자친구·남자친구·자기)·친구(친구·동기·베프)·지인(선배·후배·이웃) 표, `normalize()`(공백 제거 → `님`/`씨` 접미 최대 1개 제거 → 성씨 1글자 접두는 나머지가 표제어일 때만 제거), `lookup()`·`derive_hints()`·`compatible()`), `app/er/rules.py`(신설 — `apply_rules(candidates, hints, *, relaxed=False)`(검사 3종: 관계 태그 일치(hints.relation_tag vs 후보 `relation_tag` 필드)·위계 일치(hints.hierarchy vs 후보 `hierarchy`, 엄격은 인접 무관 탈락·완화는 인접만 허용하되 `rule_passed` 미계상)·호칭 사전 호환(hints.relation_tag vs 후보 `aliases` 를 사전으로 되짚은 그룹) → `rule_checked`/`rule_passed`/`s_rule`(분모 0 이면 0.0)/`passed_rules`/`excluded_by`(우선순위 `relation_tag_conflict`>`hierarchy_conflict`>`dictionary_conflict`)/`relaxed_pass`), `run_rule_stage()`(엄격 적용 → 통과 0 이고 "위계 불일치만으로, 그리고 인접" 인 후보가 있을 때만 완화 1회 재평가 → `(passed, all_scored, relaxed_retry)`), 내부 공유 `_evaluate()` 로 `apply_rules`/완화 적격 판정이 같은 로직을 쓰게 함, DB·LLM 호출 없음(순수 함수), docstring 에 "완화는 재조회가 아니라 재평가"(01-plan 리스크 200행) 명시), `app/tools/persons.py`·`app/tools/types.py`·`app/db/models.py` 무변경(요구된 상수는 import 만 — `HIERARCHIES`·`_is_hierarchy_adjacent`·`RELATION_TAGS` 재정의 없음). 테스트 3개 신설: `tests/test_er_dictionary.py`(31건 — 직급→위계 표 전체·정규화(김팀장님/박대리씨/이모 반례)·대명사·미등재/가족·연인·친구·지인 대표 항목/`compatible` 세 값), `tests/test_er_rules.py`(8건 — (a)관계 태그 그룹 불일치 배제+완화 안 함, (b)승진 픽스처(checked=3·passed=2·s_rule≈0.6667·relaxed_pass=True·relaxed_retry=True), (c)2칸 차이는 완화해도 탈락(적격성 자체가 인접 여부로 판별되어 relaxed_retry=False), (d)정보 없음(checked=0·s_rule=0.0·배제 없음), (e)동명이인 2명 모두 통과, (f)완화는 1회만(`apply_rules` 스파이로 호출 횟수 단언)), `tests/test_er_types.py`(13건 — ERConfig 가중치 합·임계치 순서·경계 동일값 허용, trace 상수, `ScoredCandidate`/`Judgement`/`Resolution` 기본값·`to_dict()`, `JudgeUnavailable`).
- 이유(기획서·카드 연결): 01-plan 89행(U3 체크리스트)·17~20행(모듈 목록)·59행(types 목록)·128행(결정1 -- 4단계 모듈 분리)·129행(결정2 -- s_rule 분모·모순일 때만 배제·완화 통과 미계상)·172행(결정8 -- ERConfig 검증)·181~196행(결정10 -- 직급→위계 표·정규화·모호 항목)·200행(리스크 -- 완화는 재조회가 아니라 재평가)·40행(회귀(a) 승진 픽스처 수치, 결정9 의 구체값과 별개로 규칙 단계 수치만 이 단위 몫). `.claude/skills/entity-resolution` "2. 규칙 필터" 절(호칭 사전 + 소속·위계 제약, s_rule = 통과/검사(위계 일치·관계 태그 일치·호칭 사전 호환)). 원칙1(오병합 방지 -- 엄격은 인접 무관 탈락, 완화는 통과로 세지 않음)·원칙4(2단계를 별도 모듈로 분리, LLM 미호출).
- 정합성 확인: `ScoredCandidate.rule_flags` 는 `search_person` 이 채운 6개 원본 신호를 그대로 옮겨 담을 뿐 `rules.py` 가 재계산하지 않음(원칙9 -- 원본 신호와 규칙 판정을 구분해 남긴다). `_is_hierarchy_adjacent`/`HIERARCHIES` 를 재정의하지 않고 `app.tools.persons` 에서 import(중복 구현 금지). `s_rule` 분모 0 → 정확히 `0.0`(0.5 중립값 없음, 결정2). "호칭 사전 호환" 검사는 후보의 저장된 `relation_tag` 필드가 아니라 **후보 `aliases` 를 사전으로 되짚은 그룹**을 쓰므로 "관계 태그 일치" 검사와 값의 출처가 다르다(같은 조건에서 함께 참/거짓이 되는 경우가 많지만, 후보의 별칭이 사전 밖의 고유명사뿐이면 이 검사만 평가 불가로 분모에서 빠진다) -- 스킬 문서의 "위계 일치·관계 태그 일치·호칭 사전 호환" 3종 문구를 문자 그대로 구분되는 세 신호로 구현한 결과이며, 계획 문서에 이 구분을 명시한 문장은 없어 이 단위의 설계 해석임을 밝힌다(아래 "계획과 다른 점" 참고). `excluded_by` 우선순위는 위임 프롬프트 항목3 이 준 어휘 순서(`relation_tag_conflict`/`hierarchy_conflict`/`dictionary_conflict`) 그대로 적용. 완화 적격 판정은 "위계 불일치**만**으로, 그리고 그 불일치가 인접"일 때만 참이 되도록 `_evaluate()` 를 공유해 두 함수(`apply_rules`/적격 판정)가 같은 결과를 내게 함. `ERConfig` 검증은 결정8 문구(가중치 합 1e-9, `0≤t_new≤t_merge≤1`) 그대로. 스키마·툴 시그니처·`app/settings.py`(`ER_*`)·DB·LLM 호출 코드는 이 단위에서 만들지 않음(U4~U6 경계 준수).
- 검증: `python -m pytest tests/test_er_dictionary.py tests/test_er_rules.py tests/test_er_types.py -q` → 52 passed(evidence `20260906-1200-u3-pytest-er.txt`) / `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → 275 passed, skip 0(evidence `20260906-1200-u3-pytest-all.txt`, 기존 223 + 신규 52) / `python scripts/tools_check.py` → 7/7 ok(evidence `20260906-1200-u3-tools_check.txt`).
- 계획과 다르게 한 것: (1) 위 "정합성 확인"에 적은 대로 "호칭 사전 호환" 검사를 "후보 `aliases` 를 사전으로 되짚은 그룹 vs hints.relation_tag" 로 해석해 구현했다 -- 01-plan·스킬 문서 모두 이 검사의 정확한 입력을 명시하지 않아(관계 태그 일치와 무엇이 다른지 문장으로 특정되지 않음) 스킬의 3종 문구를 문자 그대로 세 개의 구분되는 신호로 만드는 쪽을 택했다. 승진 픽스처(결정9 수치, checked=3·passed=2)와 정확히 들어맞는 것으로 이 해석을 교차검증했다(테스트 (b)). U5(`candidates.py`)가 `search_person` 실제 `Candidate` 를 `ScoredCandidate` 로 변환할 때 이 해석이 자연스럽게 이어지는지 재확인이 필요하면 U5 인계 사항으로 남긴다. (2) `run_rule_stage` 의 완화 적격 판정에 "그 불일치가 인접"이라는 조건을 `_evaluate()` 를 통해 별도로 재확인하는 헬퍼(`_hierarchy_only_adjacent_conflict`)를 두었다 -- 01-plan 은 "위계 불일치만으로 탈락한 후보 중 `hierarchy_adjacent` 인 것" 이라고 이미 이 조건을 명시했으므로 계획대로이나, 구현 함수 이름은 계획 문서에 없어 이 단위가 붙였다. 그 외에는 계획대로.
- 남은 것 · 다음 단위: U4(확신도·두 임계치 — `app/er/confidence.py`, `app/settings.py` 의 `ER_*` 상수와 `er_config(env)` 로더, `tests/test_er_confidence.py`). U5 인계: `candidates.py` 가 `search_person` 의 `Candidate`(`person.aliases`)를 `ScoredCandidate` 로 변환할 때 `aliases` 필드에 전체 별칭 목록을 그대로 넣어야 "호칭 사전 호환" 검사가 작동한다(별칭 일부만 넘기면 이 검사의 recall 이 떨어진다).
- Refs: P3-er R4 D3 S3.3 원칙4 원칙9

## 2026-09-06 13:30 · feat(P3-er): U4 확신도·두 임계치(4단계) · 593c254
- 변경: `app/settings.py`(`ER_T_MERGE=0.8`·`ER_T_NEW=0.3`·`ER_WEIGHTS={"llm":0.5,"emb":0.3,"rule":0.2}`·`ER_TOP_K=SEARCH_TOP_K`·`ER_JUDGE_TIMEOUT=20.0`·`ER_JUDGE_MAX_RETRIES=1`·`ER_TOLERANCE=1e-9`(F-7fe239 단일 출처) 추가, `er_config(env=None) -> ERConfig` 로더 신설 — `.env.example` 이 이미 정한 `T_MERGE`/`T_NEW`/`W_LLM`/`W_EMB`/`W_RULE` 을 읽고 없거나 빈 문자열이면 모듈 상수로 폴백, 파싱 실패는 `app.tools.types.InvalidValue` 로 사람이 읽는 메시지, 검증 자체는 `ERConfig.__post_init__` 이 함(이 함수는 검증을 중복 구현하지 않음), `app.er.types.ERConfig` 는 함수 안에서 지연 import(순환 import 회피 — `app.er.types` 가 최상위에서 `app.settings.SEARCH_TOP_K` 를 import 하므로 `app.settings` 가 최상위에서 `app.er.types` 를 다시 import 하면 순환이 된다; 타입 힌트는 `TYPE_CHECKING` 가드로 유지), `.env` 는 읽지 않음(security.md §1)), `app/er/confidence.py` 신설(4단계, 순수 함수 — DB·LLM 호출 0):
  - `ge_with_tolerance(a, b, tolerance=ER_TOLERANCE)`(별칭 `_ge`, 같은 객체) — 임계치 비교의 **단일** 함수, `round()` 미사용(F-7fe239: `round(·,6)` 은 `T_merge-5e-7` 까지 merge 로 오판해 원칙1 과 반대 방향이므로 폐기하고 1e-9 급 `math.isclose` 하나로 통일).
  - `combine(s_llm, s_emb, s_rule, config)` — 가중합, `s_emb` 클램프(`[0,1]` 밖이면 min/max), `s_llm`/`s_rule` 은 방어적으로 `[0,1]` 재검증(벗어나면 `InvalidValue`).
  - `band_for(confidence, config)` — `ge_with_tolerance` 로만 `merge`/`identity`/`new_person` 3구간 판정.
  - `Decision` frozen dataclass(`confidence`·`confidence_breakdown`·`band`·`band_by_threshold`·`forced_reason`·`action`·`decision`·`matched_person_id`·`llm_error_kind`, `to_dict()`), `decide(*, judgement, passed, llm_failed, config) -> Decision` — 5경로: (1) `passed=[]` → `no_candidates`(최우선, `judgement`/`llm_failed` 무관, `band=band_by_threshold="new_person"`) (2) `llm_failed=True` → null 과 같은 귀속 규약(`matched_person_id=None`·세 신호 0·`confidence=0`·`band_by_threshold=band_for(0.0)`) + `forced_reason="llm_failed"`, `band` 는 통과 후보 유무로 `identity`/`new_person` (3) `judgement.matched_person_id` 가 `passed` 의 `person_id` 집합 밖(배제 후보·존재하지 않는 id) → (2)와 같은 귀속에 `llm_error_kind="out_of_range_id"` 추가(F-5a97ef) (4) `judgement.matched_person_id is None` → (2)와 같은 귀속, `forced_reason="no_matched"`(결정3-c(b)) (5) 정상 — `matched_person_id` 로 통과 후보 하나를 찾아 그 후보의 `s_emb`/`s_rule`/`rule_checked`/`rule_passed` 로 귀속(결정3-c(a)), `combine()`+`band_for()` 로 산식 그대로. `judgement is None`인데 `llm_failed=False`·`passed` 비어있지 않은 호출은 계약 위반으로 `InvalidValue`. `_forced_decision()` 내부 helper 가 (2)(3)(4) 세 경로의 공통 귀속을 한 곳에서 만든다(이중 출처 금지).
- `tests/test_er_confidence.py` 신설(37건, DB 없음): `-k boundary` 7건(정확히 `T_merge`/`T_new`, `0.5·0.2+0.2·1.0=0.30000000000000004`, `T_merge-5e-7` 은 merge 아님(F-7fe239 회귀)·`T_merge-5e-10` 은 merge, `combine()` 이 `config.w_*` 속성만 읽어 가중치 합 >1 인 (검증 안 된) 설정에서도 최종 confidence 를 재클램프하지 않음(개별 `s_emb` 클램프와는 별개)), `-k threshold` 4건(고정 시드 무작위 스윕 각 2000 조합 — `confidence < T_merge` 이면 항상 `band!=merge`, `matched_person_id=None` 이면 항상 `band!=merge`, `llm_failed` 면 항상 `band!=merge`, 스윕·경계가 같은 `ge_with_tolerance`/`_ge` 를 쓰는지 값으로 교차검증), 나머지 26건 — `ERConfig` 경유 가중치 합·임계치 순서 거부, `combine()` s_rule 분모 0 반영·s_emb 클램프 양방향·s_llm/s_rule 범위 밖 거부, `er_config(env)` 기본값/오버라이드/빈 문자열 폴백/파싱 실패/`os.environ` 기본 인자(monkeypatch), `decide()` 5경로(정상 귀속값·null·llm_failed·out_of_range(배제 후보·존재하지 않는 id 둘 다)·no_candidates·`judgement=None`+`llm_failed=False`+통과 후보 있음 → `InvalidValue`), `action` 매핑 3종, `confidence_breakdown` 값만으로 재계산한 합이 `confidence`/`result.confidence` 와 절대오차 `<1e-9`(S3.7), `Decision.to_dict()` 직렬화.
- 이유(기획서·카드 연결): 01-plan 90행(U4 체크리스트) 그대로 — `combine`/`band_for`/`decide`·`ERConfig`·`er_config(env)`. 결정2(129행, `s_rule` 은 값만 소비 — 이 모듈이 재계산하지 않음)·결정3-c(138~141행, 귀속·null 분기)·결정5(144~164행, trace `confidence_breakdown`/`decision` 필드 이름)·결정8(172행, 세 층 설정 — 모듈 상수/환경변수/`resolve(config=...)` 인자, 이 단위는 1·2층). 위임 프롬프트가 지정한 세 권고(F-7fe239 허용오차 단일화, F-f3b245 llm_failed 귀속 명시, F-5a97ef 범위 밖 id 처리)를 이 단위에서 코드로 닫는다.
- 정합성 확인: 원칙1·2(`T_merge` 미만·`matched_person_id=None`·`llm_failed` 모든 조합에서 `band!=merge` — 무작위 스윕 2000×3 + 경계 회귀로 확인, 불변식 "`matched_person_id is None` 이면 `band!=merge`"을 코드 구조 자체가 강제: 세 forced 경로 모두 `matched_person_id=None` 을 반환값에 명시)·3(`combine()` 이 3신호 가중합 그대로, `s_llm` 로그 확률 미사용 — 모듈 docstring 에 R4 명시)·4(4단계 중 4단계만 이 단위, 나머지는 U3 이하가 이미 함 — 이 모듈은 판정만 하고 검색·LLM 을 호출하지 않음, 순수 함수)·8(임계치·가중치 튜닝 없음 — `ERConfig` 기본값 그대로 사용, `T_merge∈{0.5..0.95}` 스윕은 테스트가 값 주입으로만 확인하고 운영 기본값은 불변)·9(모든 판정 경로가 `confidence_breakdown`·`decision` 두 dict 를 만들어 U6 이 그대로 trace `output` 에 얹을 수 있게 함). `app/er/types.py` 의 `ERConfig`/`ScoredCandidate`/`Judgement`/`Resolution` 은 import 만 하고 재정의하지 않음(필드 추가 없음). 툴 시그니처·스키마·DB·LLM 무변경.
- 계획과 다르게 한 것: (1) F-7fe239 해결 단계가 예시로 든 `_ge(a, b) = a > b or math.isclose(a, b, abs_tol=1e-9)` 대신 `a >= b or math.isclose(...)` 로 `>=` 를 직접 썼다 — `a > b` 만으로는 부동소수 표현 오차로 `a` 가 `b` 보다 근소하게 **작은데 실제로는 같아야 하는** 경우(`isclose` 가 뒤에서 잡아 주므로 결과는 동일)뿐 아니라 `a == b` 를 하드코딩 없이 첫 항에서 바로 참으로 처리하는 편이 더 읽기 쉬워 표현을 바꿨다(의미·결과는 위임 프롬프트 의도와 동일 — `≥` 판정, 1e-9 급 허용오차 하나). (2) llm_failed·out_of_range 경로의 `band_by_threshold` 를 위임 프롬프트 문구("new_person" 리터럴)대신 `band_for(0.0, config)` 계산값으로 통일했다 — null 경로가 이미 `band_for(0)` 명시였고, "비교는 단일 함수 하나로만" 원칙(F-7fe239)을 세 forced 경로 모두에 일관 적용하는 것이 이중 출처를 줄인다고 판단했다(통상 설정 `t_new>0` 에서 값은 동일하게 `"new_person"`, `t_new=0` 인 극단 설정에서만 다를 수 있음을 `_forced_decision()` docstring 에 명시). (3) `out_of_range_id` 판정(F-5a97ef 해결 단계는 "U5 `ClaudeJudge`/`FakeJudge` 공통 후처리"로 적었다)을 U5 뿐 아니라 **`decide()` 자체에도** 방어적으로 넣었다 — 위임 프롬프트 본문이 "결정 3-c" 절에서 이 판정을 `decide()` 스펙 항목으로 명시했고, U4 는 U5 를 신뢰하지 않고 스스로도 범위를 검증하는 편이 원칙1(오병합 방지)에 더 안전하다고 보았다. U5 가 `JudgeUnavailable(error="out_of_range_id")` 로 더 일찍 걸러도 이 방어 코드는 죽은 코드가 아니라 이중 방어로 남는다.
- 권고 3건 처리: **F-7fe239** — `app/er/confidence.py::ge_with_tolerance`(별칭 `_ge`) 단일 함수, `ER_TOLERANCE=1e-9`(`app/settings.py`) 단일 출처, `round()` 0건(grep 으로 재검증 가능), 경계·스윕 테스트가 같은 함수를 import 해 교차검증. **F-f3b245** — `llm_failed` 경로가 `_forced_decision()` 을 통해 null 경로와 완전히 같은 귀속(`matched_person_id=None`·`s_llm=s_emb=s_rule=0`·`confidence=0`·`band_by_threshold=new_person`)을 쓰도록 코드 구조로 강제(별도 분기로 다른 값을 낼 수 없음), `test_decide_llm_failed_with_passed_candidates_is_identity` 로 값 단언. **F-5a97ef** — `decide()` 가 `passed` 의 `person_id` 집합을 "통과 후보 id 집합"으로 명시적으로 검증하고(01-plan 34행 표기로 통일, 133행의 "후보 id 집합" 표기와의 불일치는 이 구현이 34행 쪽을 따름), 범위 밖이면 `forced_reason="llm_failed"` + `llm_error_kind="out_of_range_id"` 로 API 장애(`timeout`/`api_error`/`schema`, U5 몫)와 구분 가능한 값을 반환. 세 소견 모두 05-remediation.md 에 해결 단계·완료 판정 명령·재검증 절을 채웠다(상태 줄은 메인 세션이 닫는다).
- 검증: `python -m pytest tests/test_er_confidence.py -q` → 37 passed(evidence `20260906-1330-u4-pytest-confidence.txt`) / `-k boundary` → 7 passed(evidence `20260906-1330-u4-pytest-boundary.txt`) / `-k threshold` → 4 passed(evidence `20260906-1330-u4-pytest-threshold.txt`) / `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → 312 passed, skip 0(evidence `20260906-1330-u4-pytest-all.txt`, 기존 275 + 신규 37) / `python scripts/tools_check.py` → 7/7 ok(evidence `20260906-1330-u4-tools_check.txt`).
- 남은 것 · 다음 단위: U5(후보 검색 어댑터(1단계) + LLM 판정(3단계) — `app/er/candidates.py`·`app/er/judge.py`). U5 인계: (a) `decide()` 는 `judgement.matched_person_id` 범위 검증을 이미 방어적으로 하지만, `Judge.judge()` 구현(`ClaudeJudge`/`FakeJudge`) 도 F-5a97ef 해결 단계가 요구한 대로 **자체적으로** `JudgeUnavailable(error="out_of_range_id")` 를 던지는 편이 낫다(API 장애와 모델 환각을 더 이른 지점에서 구분). (b) U6 파이프라인은 `decide()` 호출 시 `passed`(2단계 통과 후보만, `run_rule_stage()` 의 첫 번째 반환값)를 넘겨야 하고, trace `candidates[]` 에는 `run_rule_stage()` 의 두 번째 반환값(배제 후보 포함 전체)을 넣어야 한다 — `decide()` 는 이 둘을 구분하지 않으므로 U6 이 올바른 리스트를 골라 넘길 책임이 있다. (c) `Decision.decision` dict 에는 `T_merge`/`T_new`/`band`/`band_by_threshold`/`forced_reason`/`action`/`matched_person_id` 만 있다 — 결정5 스키마의 `relaxed_retry`/`hierarchy_relaxed_retry`/`applied`/`pending_question_id`/`applied_at` 은 `decide()` 가 그 정보를 갖고 있지 않으므로(시그니처에 없음) U6/U7 이 이 dict 에 병합해 채워야 한다.
- Refs: P3-er R4 D3 D10 S3.3 원칙1 원칙2 F-7fe239 F-f3b245 F-5a97ef

## 2026-09-06 12:04 · feat(P3-er): U5 후보 검색 어댑터(1단계) + LLM 판정(3단계, 공급자 중립) · b1f2782
- SDK 규약 확인 결과(위임 프롬프트 결정3-b 요구, `claude-api` 스킬로 확인): 설치본 `anthropic==1.4.0`(httpx2 기반), `openai==2.33.0`(httpx 기반) — 계획과 일치. `anthropic.Anthropic(timeout=, max_retries=)`/`openai.OpenAI(timeout=, max_retries=)` 모두 `api_key` 인자를 넘기지 않으면 SDK 가 각각 `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` 를 읽는다 — 단, **`OpenAI()` 는 생성 시점에 키 부재를 즉시 예외로 낸다**(`anthropic.Anthropic()` 은 생성 시점에 키를 요구하지 않음 — 실제 호출 시점에 확인), 테스트가 `judge_from_env` 로 `OpenAIJudge` 를 만들 때는 더미 키를 `monkeypatch.setenv` 해야 한다(03-log 이 이 차이를 기록). 예외 클래스는 두 SDK 모두 `APITimeoutError`/`RateLimitError`(→ `APIStatusError` 서브클래스)/`APIStatusError`/`APIConnectionError` 이름을 그대로 노출해 **하나의 매핑 함수**(`_call_with_error_mapping(errors_module, fn)`)로 공유 가능함을 실제 생성자 시그니처로 확인(`httpx2.Response`/`httpx.Response` 로 인스턴스 생성 검증). `messages.create(tools=[...], tool_choice={"type":"tool","name":...})` 응답의 `content[i].type=="tool_use"`→`.input`(dict), `usage.input_tokens`/`output_tokens`, `.model` 확인. `chat.completions.create(tools=[{"type":"function","function":{...}}], tool_choice={"type":"function","function":{"name":...}})` 응답의 `choices[0].message.tool_calls[0].function.arguments`(JSON 문자열, `json.loads` 필요)·`usage.prompt_tokens`/`completion_tokens`·`.model` 확인.
- **사용자 결정: 공급자 중립(2026-09-06, 중간 지시)** — 원 위임 프롬프트(결정3)는 "Anthropic SDK 단일"을 전제했으나, 작업 중 조정자가 "LLM 판정기를 Claude 에 한정하지 말고 OpenAI·Gemini 등으로도 바꿀 수 있게 공급자 중립으로 짜라"고 지시했다. 이에 따라 `app/er/judge.py` 를 **공급자 무관 핵심**(`Judge` Protocol·`build_prompt()`·`JUDGEMENT_SCHEMA`(판정 JSON 스키마 딕셔너리 단일 출처)·`validate_judgement(raw, allowed_ids)`·`JudgeUnavailable.error` 어휘·`FakeJudge`)과 **공급자별 구현**(`ClaudeJudge`·`OpenAIJudge`, 둘 다 `_call_with_error_mapping()`+`validate_judgement()` 공유)으로 재구성하고, 선택 팩토리 `judge_from_env(env=None)`(`LLM_PROVIDER` 환경변수, 기본 `anthropic`)를 추가했다. `Gemini` 는 `google-genai` 의존성이 없어 이번에 구현하지 않고 `judge_from_env` 가 사람이 읽는 `InvalidValue`("gemini 판정기는 아직 구현되지 않았다 — google-genai 의존성 추가 필요")로 예약만 한다. 01-plan(결정3 "Anthropic SDK 채택")은 이번 단위에서 고치지 않는다 — **메인 세션이 개정 표기**를 하기로 했다(위임 프롬프트 지시 그대로).
  - 결정3 대비 달라진 점: (1) `ClaudeJudge` 단일 클래스 → `Judge` Protocol + `ClaudeJudge`/`OpenAIJudge` 2 구현 + `judge_from_env` 팩토리. (2) `Judgement` 에 `provider: str = "fake"` 필드 추가(`app/er/types.py`, 최소 수정 — `to_dict()` 에도 반영, 기존 테스트는 필드 추가만으로 깨지지 않음: `frozen dataclass` 필드 순서 뒤쪽에 기본값 있는 필드로 추가). (3) `app/settings.py` 에 `LLM_PROVIDER = "anthropic"` 상수 추가(`judge_from_env` 의 2층 오버라이드 기준값), `.env.example` 에 이름만 `LLM_PROVIDER=anthropic`·`OPENAI_MODEL=`·`GEMINI_API_KEY=`·`GEMINI_MODEL=` 추가(값 비움, 기존 `OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`ANTHROPIC_MODEL` 은 재사용 — OpenAI 판정기와 임베딩이 같은 `OPENAI_API_KEY` 를 공유하는 것은 의도된 것: 둘 다 같은 OpenAI 계정 키다).
- 변경: `app/er/candidates.py`(신규, 1단계 어댑터) — `search_candidates(ctx, mention, hints=None, *, top_k=SEARCH_TOP_K) -> SearchCandidates`. `search_person(ctx, mention, effective_hints)` 를 **그대로 재사용**(재구현 없음), `hints is None` 이면 `derive_hints(mention)` 으로 유도해 **유도 결과 자체도** `SearchCandidates.hints` 로 함께 돌려준다(U6 이 2단계·trace 에 같은 hints 를 다시 써야 하므로 — 위임 프롬프트 "U6 이 rules 와 trace 에 같은 hints 를 써야 한다" 요구). `Candidate`(P2) → `ScoredCandidate` 변환: `s_emb` = `rule_flags["embedding_skipped"]` 면 `0.0`, 아니면 `similarity` 를 `[0,1]` 클램프. `aliases` 는 `PersonOut.aliases` 가 이미 채워져 있으면 그대로 쓰고(현재 `search_person` 구현은 항상 채운다 — U3 인계 "없으면 조회" 분기는 방어적으로만 남김), 비어 있으면 `person_aliases` 를 직접 조회해 채운다. DB 쓰기 없음(`search_person` 자체의 `@traced` 트레이스 행 1개는 예외 — persons/aliases 는 불변).
- 변경: `app/er/judge.py`(신규, 3단계, 공급자 중립) — 위 "사용자 결정" 절 그대로. `JUDGEMENT_SCHEMA`(`matched_person_id: int|null`·`s_llm: 0~1`·`reason: str`, `additionalProperties:false`) 단일 딕셔너리를 Claude 는 `tools=[{"name":...,"strict":True,"input_schema":JUDGEMENT_SCHEMA}]`+`tool_choice={"type":"tool","name":...}` 로, OpenAI 는 `tools=[{"type":"function","function":{"name":...,"parameters":JUDGEMENT_SCHEMA,"strict":True}}]`+`tool_choice={"type":"function","function":{"name":...}}` 로 감싼다. `validate_judgement(raw, allowed_ids)` 가 스키마·범위(통과 후보 id 집합 ∪ {None}, F-5a97ef "통과 후보 집합" 표기로 통일)를 검증해 `schema`/`out_of_range_id` 로 나눠 `JudgeUnavailable` 을 던진다 — **두 공급자 구현이 이 함수 하나를 공유**(`test_both_providers_call_shared_validate_judgement` 로 단언). `_call_with_error_mapping(errors_module, fn)` 이 `APITimeoutError→timeout`/`RateLimitError→rate_limit`/`APIStatusError→api_error`/`APIConnectionError→connection` 매핑을 공유(모듈만 다르게 넘겨 재사용). SDK 자체 `max_retries=ER_JUDGE_MAX_RETRIES(1)` 로 재시도를 위임(재시도 루프 직접 구현 없음, 결정3-c). `build_prompt()` 는 후보의 `person_id`·`display_name`·`aliases`·`relation_tag`·`hierarchy`·`rule_flags` 와 mention·utterance 만 담고 키·환경변수·전체 대화 이력을 넣지 않는다.
- 변경: `app/er/types.py`(`Judgement.provider: str = "fake"` 필드 + `to_dict()` 반영, docstring 갱신), `app/settings.py`(`LLM_PROVIDER = "anthropic"`), `.env.example`(`LLM_PROVIDER=anthropic`·`OPENAI_MODEL=`·`GEMINI_API_KEY=`·`GEMINI_MODEL=` 이름만 추가, 주석 갱신).
- 변경: `tests/test_er_judge.py`(신규 36건, 네트워크 없음) — (a) 요청 본문: Claude `tool_choice`/`strict`/`input_schema==JUDGEMENT_SCHEMA`, OpenAI `tool_choice`/`parameters==JUDGEMENT_SCHEMA`/`strict`, 두 공급자 모두 후보 id·이름·별칭·관계태그가 요청 문자열에 있고 `ensure_ascii=False` JSON 덤프에 `"ANTHROPIC"`/`"OPENAI_API_KEY"`/`"os.environ"`/`"api_key"` 문자열이 없음. (b) 정상 파싱 → `Judgement` 값·`tokens_in`/`tokens_out`·`model`·`provider`(둘 다). (c) `s_llm` 범위 밖 → `schema`(둘 다). (d)(배제 후보 id·존재하지 않는 id) → `out_of_range_id`(둘 다). (e) 도구 호출 블록 없음 → `schema`(둘 다). (f) 네 예외(`APITimeoutError`/`RateLimitError`/`APIStatusError`/`APIConnectionError`, `httpx2`/`httpx` 로 직접 생성) → `timeout`/`rate_limit`/`api_error`/`connection`(둘 다, 8건). (g) 기본 모델이 `ANTHROPIC_MODEL`/`OPENAI_MODEL` env 를 따르고 없으면 `claude-sonnet-5`/`gpt-4o-mini`(monkeypatch.setenv/delenv, 4건). (h) `FakeJudge` 결정성·`pick`·`fail`·빈/불일치 `table`(6건). (i) `judge_from_env` — anthropic 기본·openai(더미 키 필요)·gemini 예약(`InvalidValue`)·모르는 값 거부·`env` 생략 시 `os.environ` 사용(5건). 공유 함수 단언 1건, `build_prompt` 비밀 없음 1건.
- 변경: `tests/test_er_candidates.py`(신규 6건, 실 DB 5433 롤백 픽스처) — 전체 별칭 채움·hints 그대로 반영·hints 유도("부장님"→직장/상, "그사람"→빈 dict)·`embedding_skipped` 시 `s_emb=0.0`·DB 행 수(`persons`/`person_aliases`) 불변 2건·`top_k` 인자를 받아도 `search_person` 시그니처를 바꾸지 않음(예외 없이 통과).
- 이유(기획서·카드 연결): 01-plan 91행(U5 체크리스트) — `search_person` 재사용·hints 유도 주입·`s_emb` 정규화·`ClaudeJudge`(SDK·강제 스키마·타임아웃·1회 재시도·usage 토큰)·`FakeJudge`(표 기반 결정적)·"키·환경변수·전체 대화 이력 금지". 132~136행 결정3 a~d(도구 스키마 이유·모델 id 확인 요구·실패 분기·키 규약) — 다만 (a)"Anthropic SDK 단일"은 위 사용자 결정으로 공급자 중립으로 확장. 173행 결정9 FakeJudge·grouped_embedder(이 단위는 FakeJudge 만, grouped_embedder 는 U7 픽스처). 200행 리스크(SDK 방식 미확인 시 확인 절차) — `claude-api` 스킬로 확인 완료. F-5a97ef(632행) "범위는 통과 후보 id 집합 ∪ null 로 통일, 배제 후보 id 는 API 장애와 구분되는 error 유형" — `validate_judgement` 가 `out_of_range_id` 로 조기 차단(U4 `decide()` 레벨 방어는 그대로 유지, 이중 방어).
- 정합성 확인: 원칙1(통과 후보만 LLM 에 전달, 범위 밖 id 는 오병합 대신 `out_of_range_id`), 원칙3(`s_llm` 은 두 공급자 모두 구조화 출력 자기보고, 로그 확률 미사용 — 모듈 docstring 명시), 원칙4(LLM 호출은 4단계 중 3단계 하나일 뿐 — `resolve()` 오케스트레이션은 U6), 원칙8(LLM 자동 테스트 없음, `FakeJudge`/스텁만 — `er_smoke.py` 는 U8), 원칙9(trace `llm{}` 채우기는 U6 이 `Judgement.provider`/`model`/`tokens_in`/`tokens_out`/`reason` 을 그대로 옮겨 담는 것으로 인계). 보안 §1 — `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`GEMINI_API_KEY`/`sk-` grep 결과 `app/er/` 안에는 docstring 언급 3건뿐(값 접근 0건, evidence 파일). 툴 시그니처(`tools_check.py` 7/7)·스키마(DB 변경 없음) 무변경.
- 검증: `python -m pytest tests/test_er_judge.py -q` → 36 passed / `POSTGRES_PORT=5433 python -m pytest tests/test_er_candidates.py -q -rs` → 6 passed / `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → 354 passed, skip 0(기존 312 + 신규 36 + 6) / `python scripts/tools_check.py` → 7/7 ok / `grep -rn "ANTHROPIC_API_KEY\|OPENAI_API_KEY\|GEMINI_API_KEY\|sk-" app/er/` → docstring 3건, 값 접근 0건(evidence `20260906-1204-u5-*.txt`).
- 계획과 다르게 한 것: (1) 위 "사용자 결정" 절 그대로 — Anthropic 단일 구현을 공급자 중립(Judge 추상화 + `ClaudeJudge`/`OpenAIJudge` + `judge_from_env` + Gemini 예약)으로 확장. 01-plan 은 건드리지 않음(메인 세션이 개정 표기). (2) `ScoredCandidate.aliases` 채우기는 U3 인계("없으면 `person_aliases` 조회")를 방어적으로만 구현했다 — 현재 `search_person`(P2)의 `PersonOut.aliases` 는 이미 항상 채워지므로(`sorted(aliases_by_person[person_id])`) 별도 조회가 실행되는 실제 경로는 없지만, 인터페이스 계약(P2 가 나중에 `aliases` 를 비워 돌려주는 쪽으로 바뀌어도 깨지지 않도록)을 지키기 위해 helper(`_aliases_for`)를 남겼다.
- U6 인계: (1) `search_candidates()` 반환값은 `SearchCandidates(candidates: list[ScoredCandidate], hints: dict)` dataclass다 — U6 은 `.candidates` 를 `run_rule_stage(candidates, hints)` 에, `.hints` 를 2단계·trace `decision`/`llm` 양쪽에 **그대로 재사용**해야 한다(다시 유도하지 않는다). (2) `judge_from_env()` 로 실제 판정기를 고르되, 회귀 테스트·U6 자체 테스트는 `FakeJudge` 를 직접 생성해 주입한다(`resolve(..., judge=FakeJudge(...))`). (3) trace `llm{}` 스키마(01-plan 결정5)에 `provider` 를 추가로 넣을 것 — `Judgement.provider`/`model`/`s_llm`/`reason`/`tokens_in`/`tokens_out`/`self_reported=True`/`attempts`/`skipped`/`error` 를 그대로 옮긴다. (4) `er_smoke.py`(U8)는 `judge_from_env()` 를 써서 `LLM_PROVIDER` 로 공급자를 고를 것(하드코딩된 `ClaudeJudge()` 대신). (5) `JudgeUnavailable` 은 여전히 "plain exception"(메시지 문자열 하나, `.error` 속성 없음) — U6 이 `str(exc)` 를 `llm.error` 값으로 쓴다.
- Refs: P3-er R4 R9 D3 D4 D5 S3.3 F-5a97ef

## 2026-09-06 13:01 · feat(P3-er): U6 파이프라인 resolve()·trace(부수효과 0) · pending
- 변경: `app/er/pipeline.py`(신규) — `resolve(ctx, mention, utterance, hints=None, *, judge=None, config=None) -> Resolution`(S3.3/CLAUDE.md 시그니처 v2 그대로). 내부는 4단계를 순서대로 부르는 `_run_pipeline()`(순수 조립, DB·LLM 재구현 없음): 1단계 `search_candidates(ctx, mention, hints, top_k=config.top_k)`(U5) → `.hints` 를 2단계·trace 양쪽에 재사용(U5 인계 (1)); 2단계 `run_rule_stage(candidates, effective_hints)`(U3) → `(passed, all_scored, relaxed_retry)`; 3단계 — 통과 후보 0 이면 LLM 미호출(`llm.skipped=true`, 토큰 0, `attempts=0`), 아니면 `judge.judge(mention, utterance, passed)` 를 1회 호출(`attempts=1`)하고 `JudgeUnavailable` 을 잡아 `llm_failed=True`+`llm.error=str(exc)` 로 흡수(U5 인계 (5), 원칙1 "죽지 않고 묻는다"); 4단계 `decide(judgement=..., passed=..., llm_failed=..., config=config)`(U4) — 로직 재구현 없이 그대로 위임. `_build_ask_payload()` 가 `band` 에 따라 `merge`→`ask_payload=None`+`suggested_display_name`(귀속 후보의 `display_name`, 결정3-b)/`identity`→후보 표시 이름 최대 3(`-s_emb, person_id` 결정적 정렬)+거절 문구 `options`, `affirmative_options`, `context={mention,utterance,candidate_ids,confidence_breakdown,AFFIRMATIVE_KEY}`(U7 이 그대로 `ask_user(**...)` 에 splat 하도록 `app.tools.questions.ask_user` 의 `AFFIRMATIVE_KEY` 요구사항을 이미 채운 채로 만든다)/`new_person`→후보 나열 없이 mention 하나로 질문. `decision.decision` dict 는 `Decision.decision`(U4, 7키)에 `relaxed_retry`·`hierarchy_relaxed_retry`(같은 값)·`applied=False`·`pending_question_id=None`·`applied_at=None` 5키를 병합해 결정5 스키마 12키를 채운다(U4 03-log "U6/U7 인계 (c)" 그대로). `resolve()` 자체는 클로저로 감싼 `_traced_pipeline(ctx, mention, utterance, hints, config)`(judge 는 파라미터로 두지 않는다 — 아래 "계획과 다르게 한 것" 참고)를 `@traced(ER_TRACE_TOOL_NAME, step=ER_TRACE_STEP)` 로 감싸 부르고, 반환된 `Resolution`(`trace_id=None` 상태)을 `dataclasses.replace(result, trace_id=ctx.last_trace_id)` 로 완성한다. `app/er/__init__.py` — `resolve`·`Judge`·`FakeJudge`·`judge_from_env`·`Resolution`·`ERConfig`·`JudgeUnavailable`·trace 상수 재export(`apply_resolution` 은 아직 없음, U7 몫).
- 변경: `app/er/types.py` — `Resolution` 필드 재설계(위임 프롬프트 "필드 이름을 스키마에 맞춰 최소 조정 가능" 조항 적용): `breakdown`→`confidence_breakdown`, `decision`(dict, 12키)·`llm`(dict, 10키) 필드 신설, `candidates` 의 의미를 U6 시점부터 전체(배제 포함, `all_scored`)로 명확화. `to_dict()`(= trace `output`, `to_jsonable` 이 우선 사용)를 결정5 스키마 그대로 `{er_version, mention, relaxed_retry, candidates[], confidence_breakdown{}, decision{}, llm{}}` 로 다시 작성 — `trace_id`/`band`/`band_by_threshold`/`forced_reason`/`ask_payload`/`suggested_display_name` 은 dataclass 필드(편의 접근자)로는 남지만 `to_dict()` 출력에는 없다(이중 출처 방지 — `confidence_breakdown`/`decision` dict 가 단일 출처). `trace_tokens()` 메서드 신설(U1 `traced()` 규약 — `llm.tokens_in`/`tokens_out` 을 그대로 옮긴다). 신규 모듈 함수 `_candidate_trace_dict(candidate)` — `candidates[]` 항목을 결정5 스키마(`person_id`·`display_name`·`similarity`·`aliases_matched`·`rule_flags`·`passed_rules`·`excluded_by`·`rule_checked`·`rule_passed`·`relaxed_pass`·`s_emb`·`s_rule`)로 만든다. `ERConfig.to_dict()` 신설 — `{t_merge, t_new, weights{llm,emb,rule}, top_k}`(trace `input.config` 형태, `judge_timeout`/`judge_max_retries` 는 이 스키마에 없어 제외) — `to_jsonable` 이 `to_dict` 를 우선하므로 `ERConfig` 인스턴스를 `@traced` 대상 함수 인자로 그대로 넘기면 이 메서드가 자동 적용된다.
- 변경: `app/tools/context.py` — 최소 확장(위임 프롬프트가 명시적으로 허용한 부분): `ToolContext` 에 `last_trace_id: int | None = None` 필드 추가, `traced()` 성공 경로가 `AgentTrace` 객체를 변수(`trace_row`)로 받아 `flush()` 직후 `ctx.last_trace_id = trace_row.id` 를 채운다. 이유(모듈 docstring "trace 행 id 회수" 절 신설) — `Resolution.trace_id` 는 그 trace 행의 PK 여야 하는데(결정4), 행 PK 는 `@traced` 가 감싼 함수가 반환값을 이미 만든 뒤(flush 시점)에만 알 수 있다. `object.__setattr__` 로 frozen dataclass 를 몰래 고치는 대신, `resolve()` 가 트레이스 후 `ctx.last_trace_id` 를 읽어 `dataclasses.replace()` 로 새 `Resolution` 을 만드는 쪽을 택했다(Resolution 은 여전히 완전히 불변). 기존 6개 `@traced` 호출부(`search_person`·`create_person` 등)는 이 필드를 읽지 않으므로 동작 변화 없음(하위 호환, 회귀 스위트 367건이 증거).
- 변경: `tests/test_er_pipeline.py`(신규 10건, 실 DB 5433+롤백+`FakeJudge`+`fake_embedder`, `grouped_embedder` 불필요 — 정확한 승진/이모/동명이인 회귀 3종은 U7 이 `grouped_embedder` 로 채운다): `-k stages`(trace 행 1개, `step`/`tool_name`/`trace_id`==행 id, output 4개 하위 객체의 필수 키 존재), `-k no_side_effect`(persons/aliases/questions 행 수 불변, agent_traces 만 증가), `-k recompute`(정상 경로에서 `w·s` 재계산 절대오차 `<1e-9`, `band_for(confidence)`==`band`==`band_by_threshold`), `-k llm_failed`(`FakeJudge(fail="timeout")` → `forced_reason=llm_failed`·`band=identity`·`band_by_threshold=new_person`·`llm.error="timeout"`·`ask_payload.kind=identity`), `-k no_candidates`("이모" vs 직장 팀장 픽스처 → `llm.skipped=true`·토큰/attempts 0·`forced_reason=no_candidates`·`excluded_by=relation_tag_conflict`·`ask_payload.kind=new_person`), `-k null_path`(F-1d65ac — `FakeJudge(table={})` → 교집합 없어 `matched_person_id=None` 자동 생성, `band=identity`·`band_by_threshold=new_person`·`forced_reason=no_matched`·`confidence_breakdown` 세 신호 0, "통과 후보 0+null" 도달 불가를 docstring 에 명시하고 테스트하지 않음), `-k merge_payload`(exact-alias 매칭으로 `s_emb=1.0`+`s_llm=0.95`+`s_rule=1.0`→`confidence≈0.975≥0.8`→`band=merge`·`ask_payload=None`·`suggested_display_name`=후보 이름·`decision.applied/pending_question_id/applied_at` 초기값), `-k identity_payload`(동명이인 2명, `s_llm=0.5`→`confidence=0.75`→`band=identity`, `options`=후보 2명+거절 문구, `affirmative_options`·`context.candidate_ids`·`context[AFFIRMATIVE_KEY]` 매핑 확인), `-k provider_in_trace`(`llm.provider=="fake"`·`self_reported is True`), `-k applied_fields`(resolve 직후 `decision.applied is False`·`pending_question_id is None`·`applied_at is None`).
- 변경: `tests/test_er_types.py` — `Resolution` 스키마 변경(위) 때문에 깨지는 기존 단언 1건(`test_resolution_to_dict_serializes_candidates`)을 새 `to_dict()` 형태(`er_version`/`candidates[].similarity`/`aliases_matched`/`decision.band`)로 갱신하고, 신규 3건(`trace_tokens()` 정상/기본값, `ERConfig.to_dict()` 스키마) 추가.
- 이유(기획서·카드 연결): 01-plan 92행(U6 체크리스트) 그대로 — `resolve()` 4단계 오케스트레이션+trace 행 1개(`applied=false`·`pending_question_id=null`·`applied_at=null`)+`Resolution.trace_id` 반환. 25행("resolve 시그니처"·부수효과 없음)·29행(완화 trace 두 이름 `relaxed_retry`/`hierarchy_relaxed_retry` 같은 값)·31행(trace 기록 규약, `tool_name="er"` 예약값·`@traced` 확장 재사용)·33~34행(강등·후보 0 최적화, U4 `decide()` 에 위임)·128행(결정1 "P3/P5 경계" — `mention`/`utterance` 는 인자로만 받는다, 발화 추출·루프는 P5)·143~144행(결정4 — 판정/실행 분리, trace 행 갱신 주체는 U7)·145~166행(결정5 output 스키마 전체, 그대로 구현)·172행(결정8 — `config` 3층, `resolve(config=...)` 최우선)·200행(리스크 "완화 재검색"="재평가", `run_rule_stage` 가 이미 처리 — U6 은 그 값만 소비). F-1d65ac(665행, null 경로 U6 테스트 요구) 해소. `.claude/skills/entity-resolution`/`agent-observability` 두 스킬의 4단계·trace 체크리스트를 코드로 고정.
- 정합성 확인: 원칙1(오병합 방지 — `matched_person_id=None`/`llm_failed`/`no_candidates` 세 경로 모두 `decide()` 가 이미 강제한 `band!=merge` 를 그대로 통과시킬 뿐 U6 이 재계산·완화하지 않음, merge 경로도 `display_name` 을 바꾸지 않고 `suggested_display_name` 으로만 인계)·원칙4(4단계 각각을 U3~U5 가 만든 모듈 함수 호출로만 조립 — `rules`/`confidence`/`judge` 로직 재구현 0건, `grep -n "def decide\|def combine\|def band_for\|def apply_rules\|def run_rule_stage" app/er/pipeline.py` → 0건)·원칙8(재현 가능 — `FakeJudge`+`fake_embedder`+명시적 `config=ERConfig()` 로 환경변수 영향을 배제, 무작위 없음)·원칙9(trace 1행에 4단계 산출물 전부, 배제 후보도 `candidates[]` 에 `passed_rules=false` 로 남음)·결정4(부수효과 0 — `no_side_effect` 테스트가 persons/aliases/questions 행 수로 직접 확인, `apply_resolution` 미구현 — `ask_user`/`update_person`/`create_person` 호출 0건, `grep -n "ask_user(\|update_person(\|create_person(" app/er/pipeline.py` → 0건)·결정5(trace 스키마 그대로, `band`/`band_by_threshold`/`forced_reason` 분리 유지)·결정8(`config` 인자가 `er_config()` 호출보다 우선 — `config=ERConfig()` 를 넘긴 모든 테스트가 환경변수와 무관하게 결정적으로 동작하는 것이 증거). 툴 시그니처(`tools_check.py` 7/7)·스키마(`alembic check` 무변경) 무변경.
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/test_er_pipeline.py -q -rs` → 10 passed(evidence `20260906-1301-u6-pytest-pipeline.txt`) / 10개 `-k` 각각 단독 실행 전부 1 passed, 0 deselected 아닌 9 deselected(evidence `20260906-1301-u6-pytest-pipeline-kwise.txt`) / `POSTGRES_PORT=5433 python -m pytest tests/test_er_types.py -q -rs` → 15 passed(evidence `20260906-1301-u6-pytest-types.txt`) / `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → 367 passed, skip 0(evidence `20260906-1301-u6-pytest-all.txt`, 기존 354 + 신규 10(pipeline) + 3(types)) / `python scripts/tools_check.py` → 7/7 ok(evidence `20260906-1301-u6-tools_check.txt`) / `POSTGRES_PORT=5433 python -m alembic check` → `No new upgrade operations detected.`(evidence `20260906-1301-u6-alembic-check.txt`).
- 계획과 다르게 한 것: (1) `candidates[].similarity`/`aliases_matched` 는 `s_emb`/`aliases`(전체 별칭 목록)와 같은 값이다 — 결정5 스키마는 `similarity`(원시 유사도)와 `s_emb`(정규화값)를 별개 키로, `aliases_matched`(질의와 실제 일치한 부분집합)와 전체 별칭을 별개 개념으로 나열하지만, U5(`app/er/candidates.py._to_scored`)가 `ScoredCandidate` 로 변환하는 시점에 P2 `Candidate.similarity`(클램프 전 원시값)와 `Candidate.aliases_matched`(부분집합)를 보존하지 않고 각각 `s_emb`(클램프 후)와 `aliases`(전체 목록)로만 옮겨 담았다(U5 03-log 에 이 한계가 명시되지 않음). U6 은 U5 코드(`candidates.py`)를 재구현하지 않는 범위 제약(위임 프롬프트 "이미 있는 것을 import 해서 조립만 한다") 안에서, 트레이스 스키마의 두 키를 이미 보존된 값으로 채워 구조적으로는 결정5 스키마를 정확히 만족시키되 의미적으로는 근사임을 `_candidate_trace_dict()` docstring 과 여기 명시한다. P4 가 이 근사가 문제라고 판단하면 `candidates.py._to_scored()` 를 확장(P2 `Candidate` 의 두 필드를 그대로 보존)하는 FIX 로 닫을 수 있다. (2) `resolve()` 의 `judge` 인자를 `@traced` 대상 함수의 파라미터로 두지 않고 클로저로 감쌌다 — 위임 프롬프트가 예로 든 "trace_id 를 ctx 에 남기거나 반환 객체에 `_trace_id` 를 세팅" 중 전자(ctx 경유)를 택했을 뿐 아니라, `judge`(Protocol 구현 객체 — `FakeJudge`/`ClaudeJudge`/`OpenAIJudge` 클라이언트를 들고 있음)가 `@traced` 의 자동 `input` 캡처에 걸리면 결정5 가 정한 `input={mention, utterance, hints, config}` 4키를 넘어서는 값이 섞이고 `to_jsonable` 이 이를 의미 없는 `str()` 로 직렬화하게 된다 — 클로저로 빼는 쪽이 "정확히 그 4키" 를 만족시키는 최소 구현이었다. (3) `Resolution.candidates` 의 의미(전체 vs 통과만)를 U3 시점 docstring 이 명시하지 않았던 것을 U6 이 "전체(all_scored)"로 확정했다 — 원칙9 "배제도 근거다" 와 U6 테스트 `-k no_candidates` 가 배제된 후보를 `result.candidates` 에서 직접 조회하는 것으로 교차검증된다.
- 남은 것 · 다음 단위: U7(`apply_resolution()` + 회귀 3종 — `update_person(new_alias=...)`/`ask_user(...)` 호출, `resolution.trace_id` 행의 `decision.applied`/`pending_question_id`/`applied_at` 세 필드만 JSONB 부분 갱신, `tests/conftest.py` 에 `grouped_embedder` 팩토리 추가, 정확한 수치(결정9)의 승진/이모/동명이인 회귀 3종). U7 인계: (a) `Resolution.decision`/`confidence_breakdown`/`llm`/`ask_payload`/`suggested_display_name` 은 이미 완성된 dict 이므로 U7 은 이것을 읽기만 하면 된다(재계산 금지, 이중 출처 방지) — `ask_payload` 가 `None` 이 아니면 `ask_user(ctx, kind=ask_payload["kind"], question=ask_payload["question"], options=ask_payload["options"], context=ask_payload["context"])` 로 그대로 splat 가능(`context` 안에 `AFFIRMATIVE_KEY` 가 이미 채워져 있어 `app.tools.questions.ask_user` 의 강제 검사를 그대로 통과한다). (b) trace 행 부분 갱신은 `resolution.trace_id` 로 `agent_traces` 를 조회해 `output` JSONB 의 `decision.applied`/`decision.pending_question_id`/`decision.applied_at` 세 키만 바꿔야 한다(SQLAlchemy `AgentTrace.output` 전체 재대입 후 `flag_modified()` 필요 여부 확인 — JSONB 컬럼의 부분 갱신은 in-place mutation 이 감지 안 될 수 있다, U7 착수 시 `sqlalchemy.orm.attributes.flag_modified` 확인). (c) `band=="merge"` 인데 `passed` 안에 `matched_person_id` 후보가 없는 경우는 구조적으로 없다(`decide()` 불변식)지만 `_build_ask_payload` 의 `matched` 가 `None` 이면 `suggested_display_name=None` 으로 방어했으니 U7 은 이 값이 `None` 인 merge 결과를 받으면 방어적으로 거부(`InvalidValue`)하는 편이 안전하다(현재는 이론상 도달 불가).
- Refs: P3-er R4 R9 D3 D10 S3.3 원칙1 원칙4 원칙9 F-1d65ac
