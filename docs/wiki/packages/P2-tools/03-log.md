# P2-tools · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-05 17:30 · docs(P2-tools): 계획·계획검증 승인, 패키지 착수 · 7b793ba
- 변경: packages/P2-tools/01-plan.md(architect(opus) 초안 → 개정 1: 사용자 결정으로 U8 FastAPI 골격 포함·delete_person 제외, U1~U9), 02-plan-verify.md(verifier(fable) 점검표 8행 통과, 승인 줄), 05-remediation.md(verify-plan 필수 1 해소·권고 6(기존 registry 행) + review 권고 9: F-08b3db F-b97a06 F-4d8d96 F-3c8d4a F-fbaaae F-0010e6 F-2418ef F-4d2507 F-107a50), evidence/20260905-{1502,1511-2,1515-3,1521-final}-verify-plan*.txt·1520-verifier-plan-review.txt, CURRENT active: P2-tools, 03-log 생성
- 이유(기획서·카드 연결): resolution-plan §4 P2 "툴 7종 v2 구현 + 단위 테스트". S3.2 시그니처 v2·S3.4 ask_user 비동기(D2)·D1 신규 인물 확인형·D6 display_name 정책·R10 시그니처↔스키마. 선행 P1-schema 완료 5dc95bb. FastAPI 골격 포함은 P1 04-review §7 인계 존중(사용자 결정 16:40).
- 정합성 확인: 원칙1·2·4(search_person 은 신호만, 판단 P3-er)·6(패턴 감지 없음)·7(브리핑 문장화·제안 없음, A–B 없음)·9(@traced) / D1 D2 D4 D5 D6 D9 / S3.1 무변경(alias source 는 앱 상수) S3.2 S3.4 / 보안 §1 §5 — 위반 없음. WARN 6 은 기존 registry 행(비고만 갱신, 의도)
- 남은 것 · 다음 단위: U1 기반(requirements +fastapi·uvicorn[standard]·httpx 핀·pip freeze, ConnInfo.password repr=False F-8eeb9b + 테스트, app/db/session.py, app/settings.py APP_USER_ID, tests/conftest DB 롤백 픽스처·dbtest 마커·-rs). 권고 반영: ALIAS_SOURCES 상수는 U2 로 당김(F-0010e6), 판정 표 포트 변수(F-08b3db), "U8" 참조 2곳은 U9(F-3c8d4a). 사용자 승인 → `--stage backend-agent`.
- Refs: P2-tools R6 R7 R10 R18 D1 D2 D6 S3.2 S3.4

## 2026-09-05 15:45 · feat(P2-tools): U1 기반 — fastapi·uvicorn·httpx 핀, ConnInfo repr 비밀 제거, 세션·설정, DB 롤백 픽스처 · f217190
- 변경: requirements.txt(fastapi==0.135.1·uvicorn[standard]==0.41.0·httpx==0.28.1 3줄 추가, TestClient 용 주석), app/config.py(`ConnInfo.password`를 `field(repr=False)`로 — F-8eeb9b), tests/test_config.py(`test_conninfo_repr_and_str_never_contain_password_value` 1건 추가, 기존 5건 유지), app/settings.py 신규(`app_user_id()`, `DEFAULT_APP_USER_ID="local"`), .env.example(`APP_USER_ID=local` 이름·주석 1줄 추가), app/db/session.py 신규(`get_engine()` 지연 생성·모듈 캐시·`connect_args={"connect_timeout":5}`, `SessionLocal`(지연 평가 프록시), `session_scope()`), tests/conftest.py(`pytest_configure`의 `dbtest` 마커 등록, `db_engine`(세션 스코프, 접속 실패 시 skip)·`db_session`(함수 스코프, 바깥 트랜잭션+`join_transaction_mode="create_savepoint"`)·`fake_embedder`(해시 시드 `random.Random` → 1536차원 단위벡터) 픽스처 추가, 기존 sys.path 줄 유지), tests/test_settings.py 신규 2건, tests/test_db_session.py 신규 4건((a)(b)(c)(d))
- 이유(기획서·카드 연결): 01-plan U1. F-8eeb9b(P1-schema 05-remediation, security.md §1 — ConnInfo 기본 dataclass repr/str 에 password 노출)를 엔진·예외 메시지가 생기기 전에 처리. 결정8(SQLite 불가, 실 DB+롤백 픽스처) · 결정9(user_id 는 설정값) · 결정10(fastapi/uvicorn/httpx 3개, pydantic 은 전이 의존성이라 직접 적지 않음) · 리스크A(엔진 지연 생성 — `SessionLocal` 을 `__call__` 시점까지 미루는 프록시로 구현, `create_app()` 은 U8) · 리스크B(httpx↔TestClient 호환)
- 정합성 확인: 원칙9(트랜잭션 경계는 호출자, 툴은 flush 까지만 — session_scope/get_session 규약의 토대) / 보안 §1(.env 미접촉·비밀번호 repr 제거·접속 문자열 로그 미출력, connect_args 도 비밀 아님) / S3.2(세션·설정 산출물 경로 그대로) — 위반 없음
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` 56 passed(evidence/20260905-1535-pytest-u1.txt, F-08b3db 반영 — 포트·-rs 항상 포함), `POSTGRES_PORT=5599 python -m pytest tests/ -q -rs` 52 passed·4 skipped(dbtest 만, 사유 "local PostgreSQL not reachable" 그대로 노출, 비밀번호 문자열 0회, evidence/20260905-1535-pytest-u1-nodb.txt), `PYTHONIOENCODING=utf-8 POSTGRES_PORT=5433 python scripts/schema_check.py` 전항목 PASS·9테이블·exit 0(evidence/20260905-1535-schema-check-after-u1.txt — 스키마 무변경), `POSTGRES_PORT=5433 python -m alembic check` "No new upgrade operations detected."·exit 0(evidence/20260905-1535-alembic-check-u1.txt), TestClient 스모크(FastAPI 임시 앱, 저장소에 남기지 않음) GET 200 확인(evidence/20260905-1535-testclient-smoke.txt), `pip freeze` 전체·web 5줄 발췌(evidence/20260905-1535-pip-freeze{,-web}.txt) — 핀 5종: fastapi==0.135.1 / starlette==0.52.1 / uvicorn==0.41.0 / httpx==0.28.1 / pydantic==2.12.5. `repr(conn)`/`str(conn)` 에 고유 문자열(`UNIQUE_MARKER_9f3a21`) 미포함 확인(위 pytest-u1.txt 안에 포함되어 통과)
- 계획과 다르게 한 것: `get_engine()` 에 `connect_args={"connect_timeout": 5}` 를 추가(01-plan 산출물 절엔 명시 없었음). 이유: POSTGRES_PORT=5599(존재하지 않는 포트) 로 `db_engine` skip 경로를 검증할 때 psycopg 기본 접속 타임아웃이 OS 수준(수십 초, 두 주소 재시도)까지 늘어나 "DB 없이도 나머지 테스트가 빠르게 도는지" 검증이 사실상 멈췄다(schema_check.py 의 기존 `connect_timeout=5` 관례를 그대로 따름 — 새 값 도입 아님). 스키마·시그니처 변경 없음.
- 남은 것 · U2 에 넘기는 주의: `app/tools/types.py`·`context.py`(`ToolContext`·`@traced`) — `ToolContext.session` 은 이번 `SessionLocal()`/`session_scope()` 가 만드는 `Session` 객체를 그대로 받는다(별도 어댑터 불필요). U2 이후 픽스처에 `tool_ctx`(01-plan 결정13 대비 `db_session` 재사용)를 추가할 때 이번 `db_session` 픽스처를 그대로 의존하면 된다. `fake_embedder` 는 `EmbeddingProvider.embed(texts)` 형태(list[str]->list[list[float]])로 이미 맞춰 두었으니 U3 는 Protocol 만 정의하면 바로 주입 가능.
- Refs: P2-tools R6 R7 R10 R18 D1 D2 D6 S3.2 S3.4 F-8eeb9b

## 2026-09-05 16:00 · feat(P2-tools): U2 툴 공통 — ToolContext·@traced(agent_traces 원칙9)·types·ALIAS_SOURCES · 4eca3e9
- 변경: `app/db/models.py`(`ALIAS_SOURCES = ("user_said", "confirmed", "system")` 를 `QUESTION_KINDS` 다음 자리에 추가 — 컬럼·CHECK·테이블 변경 없음, F-0010e6 해소: U4 가 아니라 U2 에서 도입), `app/tools/__init__.py`(신규, docstring 만 — U3~U7 이 재export 할 자리 비움), `app/tools/types.py`(신규 — `PersonOut`·`Candidate`·`EventOut`·`ScheduleOut`·`PendingQuestionOut`·`BriefingOut` dataclass(frozen) + 각 `to_dict()`, 예외 계층 `ToolError`←`PersonNotFound`/`QuestionNotFound`/`QuestionNotAnswerable`/`ConfirmationRequired`/`InvalidValue`), `app/tools/context.py`(신규 — `ToolContext`(session·session_id·user_id·embedder·now·confirmed_question_id, `session` 만 `field(repr=False)`), `TRACE_MAX_STRING=2000`, `to_jsonable()`(dataclass→`to_dict()` 우선·datetime→isoformat·Decimal/숫자 그대로·list/dict 재귀·문자열 절단·그 밖 `str()`), `@traced(tool_name)`(성공 시 `step="tool_call"`, 예외 시 `step="tool_error"`+재발생, 둘 다 `tokens_in=tokens_out=0`, `wrapper.__signature__ = signature` 로 원함수 시그니처 보존)), `tests/test_tools_types.py`(신규 10건, DB 없이 — to_dict 직렬화 6종·예외 계층·메시지 형태·`ALIAS_SOURCES` 값 집합), `tests/test_tools_context.py`(신규 7건, `dbtest` — 성공 1행/예외 1행 재발생/3000자 절단/datetime isoformat/시그니처 보존/`repr(ctx)` 세션 미노출/`to_jsonable` 중첩 구조)
- 이유(기획서·카드 연결): 01-plan U2. 원칙9(모든 판정에 근거 — 툴 호출마다 `agent_traces` 1행), S3.2("모든 툴 호출은 agent_traces 에 input/output/tokens 기록"), 01-plan 24행(`ToolContext` 필드)·25행(agent_traces 기록 규칙 — session_id/step="tool_call"/tool_name/input=ctx 뺀 인자/output/tokens 0/절단)·13행(ctx 첫 인자 규약), 결정2(툴은 commit 하지 않고 flush 까지만 — `@traced` 가 `ctx.session.flush()` 만 호출).
- embedder 타입 결정: `ToolContext.embedder` 는 `app.embedding.EmbeddingProvider` Protocol 이 아니라 `Callable[[list[str]], list[list[float]]] | None`(`EmbedderCallable`)로 둔다 — 01-plan 산출물 목록이 `app/embedding.py`(Protocol)를 **U3** 몫으로 명시하므로, U2 에서 그 파일을 만들면 U3 산출물·registry 신규 행과 겹친다. `tests/conftest.py` 의 `fake_embedder` 가 이미 이 Callable 형태라 U3 는 Protocol 정의 후 타입 별칭만 좁히면 된다(코드 안에 TODO 주석).
- 정합성 확인: 원칙9(모든 툴 호출 trace, tokens_in/out 분리 — 이번엔 항상 0) / 결정2(commit 없음, flush 만) / D5 정합(별칭 임베딩 로직은 U3, 여기서는 손대지 않음) / S3.1 무변경(`alembic check` "No new upgrade operations detected.") / 보안 §1(ctx·session 은 `to_jsonable` 에 절대 넘기지 않음 — `@traced` 가 `ctx` 이름을 인자 바인딩에서 제외, docstring 에 명시) — 위반 없음
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → **73 passed**(기존 56 + 신규 17: types 10 + context 7), skip 0(evidence/20260905-1554-pytest-u2.txt). 변이 검증: `@traced` 성공 경로의 `tokens_in` 을 0→1 로 바꿔 `test_traced_success_writes_exactly_one_tool_call_row` 가 `assert 1 == 0` 으로 **FAILED**(1 failed, 6 passed) 하는 것을 확인한 뒤 원복(`git status --porcelain app/tools/context.py` → `??`(신규 파일이라 diff 없음), `grep -n "tokens_in=" app/tools/context.py` 로 두 자리 모두 `0` 확인, 재실행 73 passed)(evidence/20260905-1554-mutation-u2.txt). `POSTGRES_PORT=5433 python -m alembic check` → "No new upgrade operations detected.", exit 0(evidence/20260905-1554-alembic-check-u2.txt).
- 계획과 다르게 한 것: (1) `ALIAS_SOURCES` 를 계획의 U4 대신 U2 에서 추가(F-0010e6 반영, 계획 수정 없이 03-log 로 기록하는 것이 그 소견의 해결 단계). (2) F-4d8d96(tool_error 행이 호출자 rollback 으로 사라지는 한계)을 우회하지 않고 `app/tools/context.py` 모듈 docstring 에 그대로 명시 — 별도 커넥션 기록 여부는 P5-loop 01-plan 으로 인계(코드 변경 없음).
- 남은 것 · U3(`search_person`)에 넘기는 주의: `app/embedding.py` 에 `EmbeddingProvider` Protocol 을 정의하고 `ToolContext.embedder` 의 타입을 그 Protocol 로 좁힌다. `ALIAS_SOURCES` 는 이미 U2 에서 `app/db/models.py` 에 있으니 `person_aliases.source` 를 채울 때 이 상수를 import 해서 쓴다(리터럴 문자열 금지). `@traced("search_person")` 를 그대로 씌우면 되고, 반환값 `list[Candidate]` 는 `to_jsonable` 이 리스트 재귀 처리를 이미 지원한다.
- Refs: P2-tools R10 S3.2 원칙9 F-0010e6 F-4d8d96

## 2026-09-05 16:06 · feat(P2-tools): U3 search_person — 별칭 일치·cosine top-K 인물별 max·rule_flags(신호만, 판정은 P3-er) · a9cb254
- 변경: `app/embedding.py`(신규 — `EmbeddingProvider` Protocol(`embed(texts) -> list[list[float]]`), `EMBEDDING_DIM = 1536`(D4 확정값), `EmbedderCallable` 타입 별칭, `_CallableEmbeddingProvider` 어댑터, `as_provider(embedder)`: `None`/`EmbeddingProvider`/평범한 콜러블을 `EmbeddingProvider | None` 으로 정규화, 그 밖 타입은 `TypeError`), `app/tools/context.py`(`ToolContext.embedder` 타입을 `EmbeddingProvider | EmbedderCallable | None` 로 좁힘 — `app.embedding` 에서 import, 로컬 `EmbedderCallable` 정의·U2 TODO 제거, 모듈 docstring 갱신), `app/settings.py`(`SEARCH_TOP_K = 10` 추가, D5/S3.3), `app/tools/persons.py`(신규 — `search_person(ctx, query, hints=None) -> list[Candidate]`: 별칭 정확/부분 일치를 사용자 소유 `Person`+`PersonAlias` 조인 한 번으로 파이썬에서 판정, `as_provider(ctx.embedder)` 로 정규화한 뒤 이 사용자의 `embedding IS NOT NULL` 별칭이 1개 이상일 때만 `cosine_distance` top-K(K=10)→인물별 max, `hints`(`hierarchy`/`relation_tag`) 검증 후 **배제 없이** `rule_flags` 만 채움(`exact_alias`·`partial_alias`·`hierarchy_match`·`relation_tag_match`·`hierarchy_adjacent`·`embedding_skipped`), 결정적 정렬(exact_alias 우선→similarity 내림차순→person.id 오름차순)), `tests/test_tools_persons.py`(신규 10건 — (a)~(j) 전부: 정확/부분 일치, 임베딩 유사도·인물별 max 집계, embedder 없음, hints 배제 안 함(상↔동 인접 True·상↔하 False·동일 위계 False)+변이 대상, 잘못된 hints, 타 사용자 배제, 정렬 결정성, 빈 query, agent_traces 1행)
- 이유(기획서·카드 연결): 01-plan U3, 결정3(신호만 모은다 — similarity 인물별 max·aliases_matched·rule_flags 6종), 결정4(임베딩은 인터페이스만 P2, 쓰기 경로도 P2에 있음), D4(N=1536)·D5(별칭 단위 top-K→인물별 max), S3.3 1단계, `.claude/skills/entity-resolution` 1단계 절. 원칙1·2·4 — `search_person` 은 자동 병합·확신도 계산을 하지 않는다(함수 docstring 에 명시).
- 정합성 확인: 원칙1(hints 로 배제하지 않음 — 변이 테스트로 방어선 확인)·2(임계치·T_merge/T_new 없음)·4(ER 4단계 중 1단계만, LLM 호출 없음)·9(`@traced("search_person")` 로 모든 호출이 `agent_traces` 1행) / D4·D5 정합(별칭 단위 임베딩, `person_embeddings` 없음, N=1536 은 `app/embedding.py` 상수와 마이그레이션 `vector(1536)` 이 같은 값) / S3.1 무변경(`alembic check`) / 보안 §5(모든 조회에 `Person.user_id == ctx.user_id`) — 위반 없음
- **pgvector 0.5.0 `cosine_distance` 가용성(리스크 항목)**: `pgvector.sqlalchemy.Vector.comparator_factory` 에 `cosine_distance()` 가 실제로 존재하고 `self.op('<=>', return_type=Float)` 로 구현되어 있음을 확인(`python -c "from pgvector.sqlalchemy import Vector; ..."`). `text("<=>")` 후퇴는 **불필요** — 계획대로 `PersonAlias.embedding.cosine_distance(vec)` 를 그대로 썼다. SQL 컴파일 결과가 실제로 `<=>` 연산자를 내는 것을 `evidence/20260905-1606-search-sql.txt` 로 확인(값은 바인드 파라미터로만 나가고 컴파일 문자열에 벡터 값 자체는 없음).
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → **83 passed**(기존 73 + 신규 10), skip 0(evidence/20260905-1606-pytest-u3.txt). 변이 검증: 후보 집합을 `hierarchy_hint` 로 배제하도록 임시로 바꿔 `test_hints_do_not_exclude_candidates_only_set_rule_flags` 가 `assert 0 == 1` 로 **FAILED**(1 failed, 9 passed) 하는 것을 확인 후 즉시 원복(`app/tools/persons.py` 가 신규 미추적 파일이라 `git diff --stat` 대신 파일을 원래 텍스트로 되돌리고 `tests/test_tools_persons.py` 10건 재통과 + 전체 83 passed 로 원복 확인, evidence/20260905-1606-mutation-u3.txt). `POSTGRES_PORT=5433 python -m alembic check` → "No new upgrade operations detected.", exit 0(evidence/20260905-1606-alembic-check-u3.txt).
- 계획과 다르게 한 것: 없음(01-plan U3 산출물·결정3·리스크 항목 그대로 구현). `app/tools/__init__.py` 재export 는 이번 단위에서 하지 않음 — 7개 툴이 모두 갖춰진 뒤(U9, `scripts/tools_check.py` 필요 시점)에 한 번에 채운다(01-plan U2 로그의 관례 유지).
- 남은 것 · U4(`create_person`/`update_person`)에 넘기는 주의: 별칭을 새로 만들 때 `ALIAS_SOURCES`(이미 U2 에 존재)에서 값을 import 해 쓰고, `ctx.embedder` 가 있으면 `app.embedding.as_provider(ctx.embedder).embed([alias])[0]` 로 그 자리에서 `person_aliases.embedding` 을 채운다(D5 "확정되면 즉시 임베딩"). `create_person` 의 `display_name` 자동 별칭은 `source=ALIAS_SOURCES[2]`("system"). U3 가 embedder 없이 만든 `embedding IS NULL` 별칭의 백필은 U3 몫이 아니라 P3-er 몫(01-plan 결정4 "남는 일").
- Refs: P2-tools R10 D4 D5 S3.2 S3.3 원칙1 원칙2 원칙4

## 2026-09-05 16:20 · feat(P2-tools): U4 create_person·update_person — D1 확인 강제(긍정 답 규약)·D6 별칭 누적·facts upsert · f318d58
- 변경(소단계 순서: 예외/상수 → 별칭 보조 → create_person → update_person):
  - `app/tools/types.py`: `AFFIRMATIVE_KEY = "affirmative_options"` 신규 export(F-b97a06 긍정 답 규약의 단일 출처, `app/tools/questions.py`(U6)도 이 키를 그대로 쓴다). `ConfirmationRequired` 에 `__init__(self, reason: str)` 추가 — `self.reason` 속성 + `str(e) == reason`(짧은 코드성 메시지, 질문 원문·답 문자열 절대 미포함).
  - `app/settings.py`: `DEFAULT_FACT_CONFIDENCE = 1.0`(결정 6).
  - `app/tools/persons.py`: 모듈 docstring 에 "긍정 답 규약"·"별칭 격상 규칙"(표) 두 절 추가. `_ALIAS_RANK`(=`ALIAS_SOURCES` 튜플 인덱스 그대로, `user_said`(0) < `confirmed`(1) < `system`(2), 절대 격하 없음). `_require_confirmation(ctx, *, kinds)`(존재→`no_confirmation`, 없음→`not_found`, kind 불일치→`wrong_kind`, `answered_at IS NULL`→`not_answered`, `session_id` 불일치→`session_mismatch`, `context["affirmative_options"]` 에 `answer` 없음(키 없으면 거부)→`not_affirmative`). `_dedupe_aliases`(strip·빈 값 제외·중복 제거, 대소문자 구분 유지). `_add_alias(session, person, alias, *, source, embedder=None, confirmed_at=None)`(같은 인물·같은 문자열이면 새 행 없이 격상만, confirmed 로 격상 시에만 `confirmed_at` 채움, 새로 만들 때만 `as_provider(embedder)` 로 즉시 임베딩). `_person_out(session, person)`(별칭 재조회 → `PersonOut`). `create_person(ctx, display_name, aliases, relation_tag, hierarchy)`: `_require_confirmation(kinds=("new_person",))` 최우선 → `display_name`/`relation_tag`/`hierarchy` 값 검증(`InvalidValue`) → `Person` 생성 → `aliases` 각각 `confirmed`(`confirmed_at=ctx.now()`) upsert → `display_name` 을 `system` 별칭으로 upsert. `update_person(ctx, person_id, facts=None, new_alias=None, display_name=None)`: 셋 다 `None` 이면 `InvalidValue` → `Person` 조회(`user_id` 격리, 없으면 `PersonNotFound`) → `display_name` 이 현재 값과 다를 때만 `_require_confirmation(kinds=("identity","new_person"))` 뒤 갱신 + **새 이름을 `system` 별칭으로 upsert**(F-2418ef, 이전 이름 별칭은 삭제하지 않음) → `new_alias` 는 `user_said` 로 upsert(격상 규칙, 미결 7 — 다른 인물의 같은 별칭은 막지 않음) → `facts` 는 `(person_id, key)` 애플리케이션 upsert(`DEFAULT_FACT_CONFIDENCE`, `updated_at` 은 ORM onupdate 에 맡김).
  - `tests/test_tools_persons.py`: 22건 추가(dbtest) — `_make_pending_question` 헬퍼(U6 이전이므로 `pending_questions` 행을 직접 만들어 확인 상태 구성) 포함. D1 직접 호출 7건(confirmed_question_id 없음/kind=identity/answered_at NULL/다른 session_id/부정 답/affirmative_options 없음 각각 `ConfirmationRequired`, 모든 조건 충족 시 성공+별칭 3종 확인), embedder 유무에 따른 임베딩 NOT NULL(1536)/NULL 각 1건, 잘못된 relation_tag/hierarchy 2건, create_person trace 1건; update_person 11건(new_alias 누적, 중복 별칭 격하 없음, display_name 확인 없음/있음/같은 값, facts 같은 key 갱신/다른 key 2행, 다른 user_id → PersonNotFound, 인자 없음 → InvalidValue, trace 1건).
- 이유(기획서·카드 연결): 01-plan U4, D01(display_name…)"코드에서 지켜야 할 것: create_person 은 반드시 answered pending_question 을 거친다, 직접 호출 테스트는 실패해야 한다", D06("별칭은 절대 삭제하지 않는다", "확인을 거친 경우에만" display_name 갱신), D05("새 별칭이 확정되면 즉시 임베딩"), S3.2 create_person/update_person 시그니처·비고, S3.4 context 규약(재개에 필요한 것만 저장), 미결7(별칭 중복은 격상만, 동명이인 허용), 결정6(facts 애플리케이션 upsert), 원칙1(오병합은 미검출보다 훨씬 나쁘다 — 이 단위의 뿌리).
- **긍정 답 규약(F-b97a06) 확정**: `pending_questions.context.affirmative_options: list[str]` 에 답이 포함될 때만 확인 통과. 키가 없으면(구버전 질문·호출자 실수) 안전한 기본값으로 거부 — "긍정인지 모름"을 "긍정 아님"으로 취급(원칙1 과 같은 비대칭). U6(`ask_user`)이 이 키를 채워야 하는 쪽이다.
- **별칭 격상 규칙**: `_ALIAS_RANK` 는 `ALIAS_SOURCES = ("user_said","confirmed","system")` 튜플 인덱스를 그대로 랭크로 쓴다(0<1<2). 같은 인물·같은 문자열은 상위 랭크로만 이동하고 절대 격하하지 않는다. `update_person(new_alias=...)` 는 항상 `user_said`(최하위)로 upsert 하므로 이 경로에서는 기존 값을 절대 덮어쓰지 않는다(테스트 `test_update_person_duplicate_alias_does_not_add_row_and_never_downgrades` 로 확인). `create_person`/`update_person(display_name=...)` 의 표시 이름 자동 별칭은 둘 다 `system` 소스로 통일(대칭) — F-2418ef 해결 단계에서 D05 초안 제안(`confirmed`) 대신 `system` 을 선택한 이유는 03-log 아래 별도 문단 없이 05-remediation F-2418ef 해결 단계에 기록.
- 정합성 확인: 원칙1(D1 강제 + 긍정 답 규약 — 변이 2건으로 방어선 확인)·2(임계치·확신도·병합 없음, docstring 명시)·4(ER 판정 없음)·9(`@traced` 로 두 툴 모두 trace 1행) / D1 D5 D6 / S3.2 / S3.1 무변경(`alembic check`) / 보안 §5(모든 조회에 `user_id` 조건, `PersonNotFound` 로 다른 사용자 인물 은닉) — 위반 없음
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → **105 passed**(기존 83 + 신규 22), skip 0(evidence/20260905-1620-pytest-u4.txt). 변이 (a) `_require_confirmation` 의 `answered_at` 검사 제거 → `test_create_person_with_unanswered_question_raises` **FAILED**(1 failed) 확인 후 원복(재실행 1 passed). 변이 (b) 긍정 답 검사 제거 → `test_create_person_with_negative_answer_raises`·`test_create_person_without_affirmative_options_in_context_raises` **2 FAILED** 확인 후 원복(전체 재실행 105 passed)(evidence/20260905-1620-mutation-u4.txt). `POSTGRES_PORT=5433 python -m alembic check` → "No new upgrade operations detected.", exit 0(evidence/20260905-1620-alembic-check-u4.txt). 시그니처 `(ctx, display_name, aliases, relation_tag, hierarchy)`·`(ctx, person_id, facts=None, new_alias=None, display_name=None)` — CLAUDE.md 표와 이름·순서·옵션 동일(evidence/20260905-1620-signatures-u4.txt).
- 계획과 다르게 한 것: F-2418ef 의 "표시 이름 자동 별칭 source" 를 D05 초안 언급(`confirmed`)이 아니라 **`system`** 으로 결정(05-remediation F-2418ef 해결 단계에 근거 기록) — `create_person` 의 기존 규칙(display_name → system)과 대칭을 맞추고, `confirmed` 는 "사용자가 개별 확인한 호칭"(`aliases[]`/`new_alias`) 전용으로 의미를 좁게 유지하기 위해서다. 그 밖에는 01-plan U4 산출물 그대로.
- 남은 것 · U5(`add_event`/`add_schedule`)·U6(`ask_user`)에 넘기는 주의: **U6 은 반드시 `pending_questions.context` 에 `app.tools.types.AFFIRMATIVE_KEY`("affirmative_options") 키로 긍정 선택지 목록을 넣어야 한다** — 넣지 않으면 `create_person`/`update_person(display_name=...)` 이 항상 `ConfirmationRequired("not_affirmative")` 로 막힌다(안전한 기본값이지만 기능이 죽은 것처럼 보일 수 있다). `_require_confirmation` 은 `app/tools/persons.py` 에 있고 `app/tools/questions.py` 가 재사용할 수도 있다(P5-loop 가 identity/new_person 질문을 만들 때 이 규약을 그대로 따라야 한다). U5 는 이 단위와 독립적이며 `ALIAS_SOURCES`/`_add_alias` 재사용 대상이 아니다.
- Refs: P2-tools R6 R18 D1 D5 D6 S3.2 F-b97a06 F-2418ef

## 2026-09-05 16:33 · feat(P2-tools): U5 add_event·add_schedule — EVENT_TYPES 검사·raw_utterance 원문 보존·aware 시각만·패턴 감지 없음 · 9cb35b6
- 변경: `app/tools/persons.py`(`_owned_person(session, person_id, user_id) -> Person` 신규 helper — `update_person` 의 인라인 조회를 추출해 재사용, 없거나 다른 `user_id` 소유면 `PersonNotFound`; `update_person` 본문을 이 helper 호출로 교체, 동작 변화 없음), `app/tools/types.py`(`EventOut` 에 `created_at: datetime` 필드 추가 + `to_dict()` 에 `"created_at"` 키 추가 — 01-plan U5 산출물 목록의 "id·person_id·type·content·occurred_at·created_at"), `app/tools/records.py`(신규 — `_require_aware(value, name)`: `datetime` 이 아니거나 naive(`tzinfo is None`/`utcoffset() is None`)면 `InvalidValue`, 통과하면 UTC 로 정규화(`astimezone(timezone.utc)`)해 반환; `_require_text(value, name)`: strip 후 빈 문자열이면 `InvalidValue`; `add_event(ctx, person_id, type, content, occurred_at, raw_utterance) -> EventOut`: `_owned_person` → `type in EVENT_TYPES` 검사 → `content`/`raw_utterance` 비어있지 않음 → `occurred_at` aware+UTC 정규화 → `Event` 생성·flush → `EventOut`(패턴 감지·승격·`fact_sources` 없음, docstring 명시); `add_schedule(ctx, person_id, title, scheduled_at) -> ScheduleOut`: `_owned_person` → `title` 비어있지 않음 → `scheduled_at` aware+UTC 정규화 → `Schedule(briefed_at=None)` 생성·flush → `ScheduleOut`(과거 시각 허용, docstring 명시)), `tests/test_tools_types.py`(기존 2건의 `EventOut(...)` 생성에 `created_at` 인자 추가, `to_dict` 비교 딕셔너리에 `"created_at"` 키 추가 — 기존 검증 의도 유지), `tests/test_tools_records.py`(신규 26건, dbtest — add_event: 7개 type 전부 성공(parametrize)·잘못된 type·빈 raw_utterance(2)·빈 content(2)·naive occurred_at·비UTC aware 정규화(같은 instant 확인)·타 user_id·없는 person_id·raw_utterance 원문 DB 보존·행1+trace1·같은 type 3건에도 person_facts 0행(원칙6 경계); add_schedule: 성공(briefed_at None)·빈 title(2)·naive·과거 시각 허용·타 user_id·trace1)
- 이유(기획서·카드 연결): 01-plan U5, R10(시그니처↔스키마 일치), S3.1(events/schedules NOT NULL 계약)·S3.2(`add_event`/`add_schedule` 시그니처, "런타임이 raw_utterance 자동 주입"은 P5 몫 — P2 함수는 필수 인자로 그대로 저장), 결정2(`ctx` 첫 인자), 미결3("조용한 UTC 가정 금지" — naive 거절), 01-plan 49행/원칙6/D9("`add_event` 는 승격·패턴을 트리거하지 않는다" — P6-memory).
- **UTC 정규화 결정**: 컬럼이 `timestamptz` 라 aware 값은 어떤 오프셋이든 DB 에 정확히 저장되지만, 애플리케이션에서 UTC 로 정규화한 뒤 저장·반환한다. 이유: (1) `@traced` 의 `agent_traces.output` 에 남는 `isoformat()` 문자열이 호출자마다 다른 오프셋으로 섞이면 trace 가독성이 떨어진다(원칙9), (2) 이후 패키지(P6-memory "최근 90일" 윈도)가 항상 같은 기준에서 계산하게 한다. naive 는 여전히 조용히 UTC 로 가정하지 않고 `InvalidValue` 로 거절한다(미결3).
- `_owned_person` 도입: U5 지시("persons.py 에 같은 것이 있으면 그것을 import 해 재사용 — 중복 구현 금지")에 따라, 기존에 `update_person` 안에 인라인으로만 있던 소유 확인 조회를 `_owned_person` 함수로 추출해 `update_person`·`add_event`·`add_schedule` 세 곳이 공유한다(동작 동일, U4 기존 테스트 영향 없음).
- 정합성 확인: 원칙6(패턴 감지·승격 없음 — 회귀 테스트로 방어)·9(`@traced` 로 두 툴 모두 trace 1행) / R10(시그니처 evidence) / S3.1 무변경(`alembic check`) / 보안 §5(`_owned_person` 으로 모든 조회에 `user_id` 조건) — 위반 없음
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → **131 passed**(기존 105 + 신규 26), skip 0(evidence/20260905-1633-pytest-u5.txt). 변이: `_require_aware` 의 naive 검사를 `if False: raise ...` 로 무력화 → naive 관련 2건(`test_add_event_naive_occurred_at_raises_invalid_value`, `test_add_schedule_naive_scheduled_at_raises_invalid_value`) **FAILED**("DID NOT RAISE InvalidValue") 확인 후 원복, 전체 재실행 131 passed(evidence/20260905-1633-mutation-u5.txt). `POSTGRES_PORT=5433 python -m alembic check` → "No new upgrade operations detected.", exit 0(evidence/20260905-1633-alembic-check-u5.txt). 시그니처 `add_event(ctx, person_id, type, content, occurred_at, raw_utterance)`·`add_schedule(ctx, person_id, title, scheduled_at)` — CLAUDE.md 표와 이름·순서·옵션 동일(evidence/20260905-1633-signatures-u5.txt).
- 계획과 다르게 한 것: 없음(01-plan U5 산출물·미결3 그대로 구현). `EventOut`/`ScheduleOut` 확장은 계획이 명시한 최소 범위(`EventOut.created_at` 만 추가, `ScheduleOut` 은 이미 U2 시점에 필요한 필드를 다 갖추고 있어 변경 없음)로 한정했다.
- 남은 것 · U6(`ask_user`/`answer_question`)에 넘기는 주의: 이 단위는 독립적이며 `ask_user` 가 재사용할 대상이 없다(`_owned_person`/`_require_aware`/`_require_text` 는 person/event/schedule 전용). U6 은 U4 로그가 남긴 긍정 답 규약(`AFFIRMATIVE_KEY`)을 `pending_questions.context` 에 반드시 채워야 한다는 점을 다시 확인한다.
- Refs: P2-tools R10 S3.1 S3.2

## 2026-09-05 16:45 · feat(P2-tools): U6 ask_user·answer_question — pending_questions 저장·{question_id,status:pending}·24h 만료·긍정 답 규약 · 8162e09
- 변경: `app/tools/types.py`(`QuestionNotAnswerable.__init__(self, code: str)` 추가 — `self.code`(`"already_answered"`/`"expired"`), `ConfirmationRequired.reason` 과 같은 패턴, 메시지에는 코드만 담고 질문 원문·답 문자열 없음), `app/settings.py`(`QUESTION_TTL_HOURS = 24`·`QUESTION_TTL = timedelta(hours=24)` 추가 — S3.4 "미답변 24시간 후 만료"의 단일 출처, `datetime` 산술에 바로 쓴다), `app/tools/questions.py`(신규 — `QUESTION_STATUSES = ("pending","answered","expired")`, `question_status(q, now)`: 순수 함수, `now` naive → `InvalidValue`, `answered_at IS NOT NULL` → `"answered"`, `created_at + QUESTION_TTL <= now` → `"expired"`(F-081752 파생 규칙), 그 밖 `"pending"`; `ask_user(ctx, kind, question, options, context) -> PendingQuestionOut`: `kind ∈ QUESTION_KINDS` → `question` 비어있지 않음 → `options` 비어있지 않은 str 리스트·중복 없음 → `context` dict·최상위 키에 비밀로 보이는 단어(`password`/`api_key`/`token`/`secret`/`authorization`, 대소문자 무관) 없음 → `kind ∈ (identity, new_person)` 이면 `context[AFFIRMATIVE_KEY]` 가 존재하는 비어있지 않은 str 리스트이며 `options` 의 부분집합(이 검사가 U4 의 `_require_confirmation` 이 항상 막히지 않게 하는 전제) → `to_jsonable` 로 직렬화·절단 후 그대로 저장(미결 2) → `PendingQuestion` 추가·flush → `{question_id, status:"pending", kind, question, options}` 반환. 동기 대기·폴링·다른 툴 호출 없음(D2); `answer_question(ctx, question_id, answer) -> PendingQuestionOut`(툴 7종 밖, LLM 노출 안 함): 행 없음 → `QuestionNotFound`, `question_status` 가 `answered`/`expired` 면 각각 `QuestionNotAnswerable(code="already_answered"/"expired")`(만료 시 `answered_at` NULL 유지), `answer ∉ options` → `InvalidValue`(행 무변경), 정상 시 `answer`·`answered_at=ctx.now()` 기록·flush; `list_pending(ctx, session_id=None) -> list[PendingQuestionOut]`(칩 조회 보조, `@traced` 미부착 — 아래 이유), `tests/test_tools_questions.py`(신규 35건, dbtest)
- 이유(기획서·카드 연결): 01-plan U6, R7(ask_user 동기 반환 불가 → pending_questions 저장), D1(`kind="new_person"` 질문이 없으면 create_person 이 항상 막힌다 — U4 가 넘긴 주의), D2(동기 대기 금지·독립 툴 유지), S3.4(턴 N 저장 → question_id 응답 → 턴 종료, 미답변 24h 만료, `answered_at` null 유지), 미결 2(context 는 호출자 dict 그대로 + 직렬화·절단만).
- **`list_pending` 에 `@traced` 를 붙이지 않은 이유**(01-plan 지시 "필요 없다고 판단하면 만들지 말고 이유를 03-log 에" 의 반대 결정 — 만들되 trace 는 뺀다): 01-plan 25행 "agent_traces 기록은 7개 툴 + `answer_question` 에 붙는다"는 규약이 이 함수를 포함하지 않는다. `list_pending` 은 판정을 하지 않는 단순 조회(칩 렌더링용)이고, P5-loop 이 매 턴/폴링마다 이 함수를 호출하면 `agent_traces` 에 판정과 무관한 조회 행이 쌓여 원칙9 "판정 근거" 신호 대 잡음비가 나빠진다. 만든 이유는 S3.4 "프론트 확인 칩 = 미답변 pending_questions"를 위한 최소 조회가 있어야 U8/P5 가 이 규약을 재구현하지 않기 때문이다.
- **F-081752 닫음**: `question_status` 가 `created_at + QUESTION_TTL <= now` 를 `answered_at IS NULL` 과 함께 검사해 `pending`/`expired` 를 구분한다(P1 04-review §7 인계 규칙 그대로). `tests/test_tools_questions.py` 의 `test_question_status_pending_just_before_24h`/`test_question_status_expired_exactly_at_24h_and_after` 로 경계(23:59:59 pending, 정확히 24h·그 이후 expired)를 고정. `docs/wiki/packages/P2-tools/05-remediation.md` 에는 이 소견에 대한 별도 항목이 없음(P1-schema/05-remediation.md 소유, 이 패키지 01-plan·HANDOFF 에만 인계 문구로 존재) — 새 항목을 만들지 않고 이 03-log 항목으로 닫힘을 기록한다.
- **F-fbaaae 해소**: `app/tools/questions.py` 모듈 docstring "user_id 격리 부재" 절에 `pending_questions`(및 `agent_traces`)가 `user_id` 컬럼이 없어 "다른 사용자의 질문"을 걸러낼 수 없음을 명시하고, 그 격리는 P5-loop 의 세션→사용자 귀속 계층이 맡아야 함을 인계(코드 변경 없음, `05-remediation.md` 해결 단계 표 채움).
- 정합성 확인: 원칙4(`ask_user` 를 다른 툴에 합치지 않음 — 이 모듈이 독립 파일)·9(`ask_user`/`answer_question` 모두 `@traced` 로 trace 1행) / D1(긍정 답 규약 강제 검사 추가)·D2(동기 대기 없음, 변이+monkeypatch 로 확인)·D01("new_person 질문 → create_person 경로" 실제 ask_user→answer_question→create_person 왕복으로 검증) / S3.4(24h 경계, `answered_at` null 유지) / S3.1 무변경(`alembic check`) / 보안 §1(context 최상위 비밀 키 이름 거절 — 값 검사는 범위 밖으로 docstring 명시) — 위반 없음
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → **166 passed**(기존 131 + 신규 35), skip 0(evidence/20260905-1645-pytest-u6.txt, 최종 재실행 evidence/20260905-1645-pytest-u6-final.txt). 변이 (a) `question_status` 의 만료 비교를 `if False and ...` 로 무력화 → `test_question_status_expired_exactly_at_24h_and_after`·`test_answer_question_expired_raises_and_keeps_answered_at_null`·`test_list_pending_excludes_answered_and_expired` **3 FAILED** 확인 후 원복(재실행 35 passed). 변이 (b) `_validate_context` 의 `affirmative_options ⊆ options` 검사를 `if False and ...` 로 무력화 → `test_ask_user_affirmative_options_outside_options_raises` **1 FAILED** 확인 후 원복(재실행 35 passed, 전체 166 passed)(evidence/20260905-1645-mutation-u6.txt). `POSTGRES_PORT=5433 python -m alembic check` → "No new upgrade operations detected.", exit 0(evidence/20260905-1645-alembic-check-u6.txt). 시그니처 `ask_user(ctx, kind, question, options, context)`·`answer_question(ctx, question_id, answer)`·`question_status(q, now)`·`list_pending(ctx, session_id=None)`(evidence/20260905-1645-signatures-u6.txt). 수용 기준 직접 증거: `test_ask_user_stores_row_and_returns_pending_status` 가 같은 트랜잭션에서 `SELECT ... FROM pending_questions WHERE id=...` 로 행 존재·`kind`/`question`/`options`/`context`/`session_id`/`answered_at IS NULL` 확인. 별도로 `session_scope()` 로 ask_user 1회를 실제 커밋 경로에서 실행해 독립 조회(같은 스코프 안에서 `SELECT`) 후 의도적으로 예외를 던져 rollback 시키고 새 세션에서 그 id 가 사라졌음을 재확인(DB 에 영구 행을 남기지 않는 쪽을 선택, evidence/20260905-1645-ask-user-row.txt).
- 계획과 다르게 한 것: 없음(01-plan U6 산출물·결정 12 대로 구현). 다만 05-remediation 소견 F-081752 는 P2-tools 파일에 항목 자체가 없어(P1-schema 05-remediation.md 소유, P2-tools 는 01-plan/HANDOFF 인계 문구만 가짐) 새 항목을 만들지 않고 이 03-log 항목의 "F-081752 닫음" 문단으로 닫힘을 기록했다 — 위임 프롬프트의 "05-remediation F-081752 해결 단계 표 채움" 지시와 실제 파일 상태(항목 없음)가 어긋나 있음을 이 항목에 명시적으로 남긴다.
- 남은 것 · U7(`get_briefing`)/U8(FastAPI 골격)에 넘기는 주의: **U8 의 `POST /answers/{question_id}` 는 `answer_question(ctx, question_id, answer)` 을 그대로 호출**하고 예외를 `QuestionNotFound → 404`, `QuestionNotAnswerable → 409`(본문에 `code` 속성을 `already_answered`/`expired` 로 노출 가능, 결정 12), `InvalidValue → 422` 로 매핑하면 된다(추가 검증 로직 불필요 — 이 단위가 전부 끝냈다). `ctx.session_id` 는 U6 검증 그대로 **답할 `pending_questions` 행의 `session_id`** 를 써야 한다(결정 12, `app/api/deps.py` 조립 시 먼저 행을 조회해 그 값을 `ToolContext.session_id` 로 넘긴다 — `answer_question` 자체는 `ctx.session_id` 를 검사하지 않고 트레이스에만 쓰므로 이 조립 책임은 U8 몫). `user_id` 격리는 여전히 없다(F-fbaaae 해소는 "명문화"까지이며 실제 격리는 P5-loop). `list_pending` 은 P8-frontend 가 칩을 렌더링할 때 재사용할 수 있다(HTTP 엔드포인트는 아직 없음 — 필요하면 U8/P5 가 라우트를 얹는다).
- Refs: P2-tools R7 D1 D2 S3.4 F-081752 F-fbaaae

## 2026-09-05 18:37 · feat(P2-tools): U7 get_briefing — 자료 조회(facts·최근 events 5·다가오는 schedules 3)·schedule_id 시 briefed_at 기록, 문장화 없음 · 7c94aad
- 변경: `app/tools/types.py`(`BriefingOut` 확장 -- 결정 7 그대로 `generated_at: datetime`(필수)·`aliases: list[str]`(기본 `[]`)·`schedule: ScheduleOut | None = None` 필드 추가, `to_dict()` 를 결정 7 문장 순서(`person`/`aliases`/`facts`/`recent_events`/`upcoming_schedules`/`schedule`/`generated_at`)로 재작성; `ScheduleNotFound(ToolError)` 신규 -- `schedule_id` 가 없거나 다른 인물 소유일 때, "존재 여부를 흘리지 않는다" 패턴을 `PersonNotFound` 대신 별도 예외로 유지), `app/settings.py`(`BRIEFING_RECENT_EVENTS = 5`·`BRIEFING_UPCOMING_SCHEDULES = 3` 추가), `app/tools/briefing.py`(신규 -- `get_briefing(ctx, person_id, schedule_id=None) -> BriefingOut`: `_owned_person`(공유 helper, 소유 확인 재구현 없음) → `_person_out` 으로 `person`/`aliases` → `ctx.now()` 를 **한 번만** 호출해 `now`/`generated_at`/`upcoming_schedules` 필터/`briefed_at` 기록에 전부 재사용 → `person_facts` 전부 `updated_at DESC` `{key,value,confidence,updated_at}` → `events` `occurred_at DESC` 상위 5(`EventOut`, `raw_utterance` 없음 -- DTO 자체가 그 필드를 담지 않음) → `schedules` `scheduled_at >= now` `ASC` 상위 3(`ScheduleOut`) → `schedule_id` 있으면 `ctx.session.get(Schedule, schedule_id)` 로 조회해 없거나 `person_id` 불일치 시 `ScheduleNotFound`, 통과하면 `briefed_at = now` **덮어쓰기**·flush 후 `ScheduleOut` 채움; `schedule_id` 없으면 어떤 행도 쓰지 않는다(읽기 전용, docstring 명시)), `app/tools/__init__.py`(U7 시점에 7개 툴 함수 전부 갖춰졌으므로 재export 확정 -- `search_person`·`create_person`·`update_person`·`add_event`·`add_schedule`·`get_briefing`·`ask_user` + 보조 `answer_question`·`question_status`·`list_pending` 를 이 파일에서 import, `TOOL_NAMES`(툴 7종만, CLAUDE.md 표 순서) 정의 -- U9 의 `scripts/tools_check.py`/`tests/test_tool_signatures.py` 가 `app.tools` 하나에서 import), `tests/test_tools_briefing.py`(신규 13건, dbtest), `tests/test_tools_types.py`(`BriefingOut` 생성 호출에 `generated_at` 필수 인자 추가 -- U2 골격 시점 테스트가 U7 확장과 어긋나 실패했던 것을 수정, `test_briefing_out_to_dict_includes_schedule_when_set`·`test_schedule_not_found_inherits_tool_error` 신규 2건, `test_exception_hierarchy_all_inherit_tool_error` 에 `ScheduleNotFound` 추가)
- 이유(기획서·카드 연결): 01-plan U7·결정 7(`BriefingOut` 필드 집합·`schedule_id` 있을 때만 `briefed_at` 기록), CLAUDE.md 툴 표 `get_briefing(person_id, schedule_id?) -> Briefing`, 원칙7 경계 문장("브리핑의 '제안'은 기록된 사실에서 도출되는 한 줄 행동 제안으로 한정 -- 감정·고민 대화 없음"; P2 는 그 제안조차 만들지 않는다) -- 이 함수에 LLM 호출·문장화·요약이 전혀 없다. `specs/S3.2-tools-v2.md`(비고 "briefed_at 기록")·`S3.6-briefing-push.md`(P2 는 자료 조회까지, 문장화·주기작업·푸시는 P6/P7).
- **`ctx.now()` 단일 호출 결정**: 같은 브리핑 호출 안에서 "지금"이 여러 값으로 흩어지면 `generated_at` 과 `schedule.briefed_at` 이 미세하게 달라질 수 있어(실제 시계로는 마이크로초 단위 차이) 테스트에서 `==` 비교가 깨지거나, 사람이 trace 를 읽을 때 "언제 브리핑했다"는 것이 두 값으로 흩어져 보인다. 함수 시작부에서 한 번만 호출해 재사용한다.
- **`ScheduleNotFound` 를 `InvalidValue` 대신 신규 예외로 결정**: `PersonNotFound`/`QuestionNotFound` 와 같은 "이 id 는 이 소유자 것이 아니다(또는 없다)" 패턴이며, `InvalidValue`(값 집합·형식 위반)와 의미가 다르다. U8 의 예외 → HTTP 매핑(결정 11)에서 `PersonNotFound`/`QuestionNotFound` 와 같은 404 계열로 묶기 쉽게 한다.
- **`BriefingOut.to_dict()` 키 집합에 `summary`/`suggestion`/`advice` 없음을 테스트로 고정**(`test_get_briefing_to_dict_key_set_has_no_sentence_fields`) -- 원칙7 경계가 "이 패키지에서는 이 키들이 존재하지조차 않는다"는 것을 회귀로 방어한다.
- 정합성 확인: 원칙6(패턴 감지·승격 호출 없음 -- `person_facts`/`fact_sources` 를 읽기만 함)·7(문장화·제안·감정 언급 없음, LLM 호출 없음 -- 키 집합 테스트로 방어)·9(`@traced` 로 trace 1행) / R10(시그니처 evidence) / S3.1 무변경(`alembic check`) / S3.6(P2 는 자료 조회까지) / 보안 §5(`_owned_person` 으로 인물 조회 격리, `schedule.person_id` 대조로 일정도 간접 격리) -- 위반 없음
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → **181 passed**(기존 166 + 신규 13 + 기존 `test_tools_types.py` 확장 2), skip 0(evidence/20260905-1837-pytest-u7.txt, 최종 재실행 evidence/20260905-1837-pytest-u7-final.txt). 변이: `get_briefing` 이 `schedule_id` 유무와 무관하게 `upcoming_rows` 전체에 `briefed_at = now` 를 기록하도록 바꿈 → "읽기 전용" 테스트(`test_get_briefing_without_schedule_id_does_not_write_any_briefed_at`)와 "그 일정만" 테스트(`test_get_briefing_with_schedule_id_marks_only_that_schedule_briefed`) **2 FAILED** 확인 후 원복(재실행 13 passed, 전체 181 passed)(evidence/20260905-1837-mutation-u7.txt). `POSTGRES_PORT=5433 python -m alembic check` → "No new upgrade operations detected.", exit 0(evidence/20260905-1837-alembic-check-u7.txt). 시그니처 `get_briefing(ctx, person_id, schedule_id=None)` -- CLAUDE.md 표와 이름·순서·옵션 동일(evidence/20260905-1837-signatures-u7.txt).
- **툴 7종 함수 전부 구현 완료**: `search_person`(U3)·`create_person`/`update_person`(U4)·`add_event`/`add_schedule`(U5)·`ask_user`(U6)·`get_briefing`(U7) 전부 자리 잡았다. `app/tools/__init__.py` 재export 는 **U9 로 미루지 않고 이 단위에서 채웠다** -- 위임 프롬프트가 제시한 두 선택지 중 "여기서 채운다" 쪽을 택한 이유: 7함수가 막 다 갖춰진 시점이 재export 를 정의하기 가장 자연스러운 자리이고, U9(`scripts/tools_check.py`) 가 각 서브모듈 경로(`app.tools.persons`/`records`/`questions`/`briefing`) 를 개별로 알 필요 없이 `app.tools` 하나에서 `TOOL_NAMES` 와 함수를 가져오게 하면 그 스크립트의 책임이 "CLAUDE.md 표 파싱 → 대조"로 좁아진다.
- 계획과 다르게 한 것: 없음(01-plan U7 산출물·결정 7 그대로 구현). `app/tools/types.py`(`BriefingOut`)의 "필요 시 최소 확장" 지시대로 3 필드(`generated_at`·`aliases`·`schedule`)만 추가했고, 기존 U2 골격 테스트(`test_tools_types.py`)가 그 확장 때문에 깨진 것을 같은 단위에서 수정했다(계획에 명시된 대상은 아니지만 U7 변경의 직접 결과이므로 같은 커밋 후보로 묶는다).
- 남은 것 · U8(FastAPI 골격)에 넘기는 주의: `get_briefing` 은 `PersonNotFound`/`ScheduleNotFound` 두 예외를 던질 수 있다 -- U8 예외 매핑(결정 11)에 `ScheduleNotFound` 를 추가해야 한다(`PersonNotFound`/`QuestionNotFound` 와 같은 404 계열 권장, 근거는 위 "`ScheduleNotFound` 결정" 문단). `get_briefing` 은 아직 어떤 HTTP 라우트에도 연결되어 있지 않다(인물 카드·브리핑 조회 API 는 P6-briefing/P8-frontend 몫, 01-plan 45행) -- U8 은 `GET /health`·`POST /answers/{question_id}` 만 만들면 되고 `get_briefing` 을 라우트에 얹지 않는다.
- Refs: P2-tools R10 S3.2 S3.6 원칙7

## 2026-09-05 19:30 · feat(P2-tools): U8 FastAPI 골격 — GET /health·POST /answers/{id} 답 저장(200/404/409/422), 예외 매핑, 요청 단위 세션 · 4d5817e
- 변경: `app/api/__init__.py`(신규 — `router` 재export), `app/api/schemas.py`(신규 — pydantic v2 `AnswerIn{answer: str}`·`AnswerOut{question_id, status}`·`HealthOut{status: Literal["ok","degraded"], db: Literal["up","down"], alembic_revision: str|None}`, 접속 정보·비밀 필드 없음), `app/api/deps.py`(신규 — `get_session()`: 제너레이터 의존성, `SessionLocal()` 로 요청 단위 세션 → 정상 종료 시 commit·예외 시 rollback·finally close; `build_ctx(session, question_id) -> ToolContext`: **답할 `pending_questions` 행을 먼저 조회해 그 행의 `session_id` 로 `ToolContext` 를 만든다**(결정 12) — 행 없으면 `QuestionNotFound`, `user_id=app_user_id()`, `embedder=None`), `app/api/routes.py`(신규 — `GET /health`: `session.execute(text("SELECT 1"))` 실패 시 예외를 그대로 올려 `app/main.py` 의 전역 핸들러가 503 으로 매핑, `alembic_version` 조회 실패(테이블 없음)만 그 자리에서 `None` 으로 흡수(접속 실패와 다른 상황); `POST /answers/{question_id}`: `build_ctx` → `answer_question(ctx, question_id, body.answer)` → 200 `{question_id, status:"answered"}`, 재개·후속 툴 호출 없음(docstring 명시, P5-loop 확장 자리)), `app/main.py`(신규 — `create_app()` 팩토리: 라우터 등록 + 예외 핸들러 8개 등록(`PersonNotFound`/`QuestionNotFound`/`ScheduleNotFound`→404 `{"detail":{"code":"not_found"}}`, `QuestionNotAnswerable`→409 `{"detail":{"code":e.code,"question_id":id}}`(`request.path_params` 에서 추출), `InvalidValue`→422 `{"detail":{"code":"invalid_value"}}`, `ConfirmationRequired`→422 `{"detail":{"code":"confirmation_required","reason":e.reason}}`, 그 밖 `ToolError`→400, 캐치올 `Exception`→`/health` 경로면 503 degraded 본문 그 외 500 — 아래 "리스크 A 검증/Starlette 재발생" 문단), 모듈 레벨 `app = create_app()`), `tests/test_api.py`(신규 12건: `test_health_ok`·`test_health_degraded_on_db_down`(dbtest 아님, DB 불필요)·`test_answer_success_saves_row_same_transaction`(같은 트랜잭션 조회 + trace 1행 확인)·`test_answer_nonexistent_id_returns_404`·`test_answer_already_answered_returns_409`·`test_answer_expired_returns_409_and_answered_at_stays_null`·`test_answer_option_outside_stored_options_returns_422`·`test_answer_missing_body_field_returns_422_fastapi_default`·`test_answer_malformed_body_returns_422_fastapi_default`·`test_create_app_does_not_create_engine`(dbtest 아님, `get_engine` monkeypatch 로 호출 0회 확인) — `client` 픽스처가 `TestClient(create_app())` + `app.dependency_overrides[get_session] = lambda: db_session`(결정 13))
- 이유(기획서·카드 연결): 01-plan U8·결정 1(P2 = 앱 팩토리·`get_session`·`GET /health`·`POST /answers` 답 저장까지, 재개는 P5-loop)·결정 11(라우터 분리, 예외 매핑은 `main.py` 한 곳)·결정 12(409 vs 200 idempotent, `ctx.session_id` 는 답할 질문 행의 값)·결정 13(HTTP 테스트도 실 DB+롤백 픽스처 재사용). R7(ask_user 동기 반환 불가 → 답 저장은 되지만 재개는 아님을 라우트 docstring 으로 명시). S3.4 턴 N+1 앞 절반.
- **리스크 A 검증**: `test_create_app_does_not_create_engine` 이 `app.db.session.get_engine` 을 monkeypatch 해 `create_app()` 호출 중 실제 호출 0회를 확인한다(캐시 상태에 의존하지 않음 — 다른 테스트가 이미 엔진을 만들어 둔 경우에도 유효). 별도로 `python -c "from app.main import app; print(app.db.session._engine)"` 로 import 직후 캐시가 `None` 인 것도 수동 확인(evidence 로 남기지 않음, 회귀는 위 pytest 케이스가 담당).
- **Starlette `ServerErrorMiddleware` 재발생 이슈와 해결**: `GET /health` 를 `get_session` 을 예외 던지는 의존성으로 오버라이드해 시뮬레이션하면, 그 예외는 FastAPI 의 `solve_dependencies` 단계에서 발생해 `ExceptionMiddleware`(라우트별 핸들러 dict, `Exception`/500 은 여기서 제외됨)가 아니라 **`ServerErrorMiddleware`**(앱 최상단, `Exception`/500 전담)로 올라간다. 이 미들웨어는 등록된 핸들러로 응답을 만들어 전송한 **뒤에도 항상 예외를 다시 올린다**(Starlette 주석 "서버가 로그를 남기게 하기 위해" — 실제 운영에서는 무해, 클라이언트는 이미 전송된 응답을 받는다). 기본 `TestClient(raise_server_exceptions=True)` 는 이 재발생을 그대로 드러내 테스트가 응답 대신 예외로 실패한다. 해결: `test_health_degraded_on_db_down` 만 `TestClient(app, raise_server_exceptions=False)` 로 별도 인스턴스를 만들어 응답을 관찰한다(공용 `client` 픽스처는 기본값 유지 — 다른 테스트의 진짜 버그가 조용히 500 으로 삼켜지는 것을 막기 위해서다). 우회(예: 라우트 안에서 `Depends` 실패를 수동으로 잡는 것)는 하지 않았다 — `app/main.py` 의 "예외 매핑은 한 곳에서"(결정 11) 원칙을 지키기 위해 이 캐치올 하나로 두 시나리오(진짜 `SELECT 1` 실패·의존성 자체 실패)를 모두 받는다.
- **`test_health_degraded_on_db_down`/`test_create_app_does_not_create_engine` 은 `dbtest` 마커를 붙이지 않았다**: 실제로 `db_session`/`db_engine` 픽스처를 쓰지 않기 때문이다(conftest.py 지시 "마커만 붙고 픽스처를 안 쓰면 DB 없이도 돈다는 오해를 만든다"). `POSTGRES_PORT` 를 접속 불가한 값으로 두고 이 두 테스트만 실행되고 나머지 8건이 skip 되는 것을 확인했다(아래 검증 절).
- 정합성 확인: 원칙9(`answer_question` trace 는 U6 이 이미 담당, 이 단위는 추가하지 않음 — `/health` 는 툴이 아니므로 trace 없음, docstring 명시) / R7·D2(답 저장까지만, 재개·후속 툴 호출 없음 — 라우트 docstring + 계획 문구로 방어) / 보안 §1(모든 예외 핸들러가 `str(exc)` 를 응답에 넣지 않음 — 코드값만, `/health` degraded 본문·서버 로그 모두 비밀번호·접속 문자열·시뮬레이션 예외 메시지 미포함을 evidence 로 확인) / 결정 2(`app/api/deps.py` 의 `get_session` 이 트랜잭션 경계를 잡고, 오버라이드된 버전은 commit 하지 않음 — 툴은 여전히 flush 까지만) — 위반 없음
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → **191 passed**(기존 181 + 신규 10 — `test_api.py` 12건 중 `test_health_degraded_on_db_down`/`test_create_app_does_not_create_engine` 는 항상 실행, 나머지 `dbtest` 8건 포함), skip 0(evidence/20260905-1930-pytest-u8.txt). `POSTGRES_PORT=1`(접속 불가) 로 `tests/test_api.py` 단독 실행 시 `dbtest` 8건 skip·비-dbtest 2건 pass 확인(수동 확인, evidence 파일로 별도 저장하지 않음 — pytest-u8.txt 가 정상 접속 시 스킵 0 을 이미 증거로 담고 있어 중복 저장하지 않았다). 변이: `_question_not_answerable_handler` 의 상태 코드를 409 → 200 으로 바꿈 → `test_answer_already_answered_returns_409`·`test_answer_expired_returns_409_and_answered_at_stays_null` **2 FAILED** 확인 후 원복(재실행 191 passed)(evidence/20260905-1930-mutation-u8.txt, 원복 후 evidence/20260905-1930-pytest-u8-final.txt). `POSTGRES_PORT=5433 python -m uvicorn app.main:app --port 8765` 백그라운드 기동 → `curl -s -w "%{http_code}" http://localhost:8765/health` → `200` + `{"status":"ok","db":"up","alembic_revision":"0001"}`(evidence/20260905-1930-uvicorn-health.txt), 서버 로그(scratchpad)에 `password`/`postgresql://` 0건 확인 후 프로세스 종료(포트 리스닝 해제 확인). `POSTGRES_PORT=5433 python -m alembic check` → "No new upgrade operations detected.", exit 0(evidence/20260905-1930-alembic-check-u8.txt).
- 계획과 다르게 한 것: (1) `GET /health` 의 접속 실패 처리를 라우트 안의 `try/except` 가 아니라 `app/main.py` 의 전역 `Exception` 핸들러(경로가 `/health` 인지 확인 후 503 degraded)로 옮겼다 — 위 "Starlette `ServerErrorMiddleware` 재발생 이슈" 문단이 근거다. 라우트 안에서 `Depends(get_session)` 해석 자체의 실패(테스트가 요구하는 시나리오)는 라우트 코드가 실행되기 전에 일어나 route-level try/except 로 잡을 수 없기 때문에, 예외 매핑을 "한 곳"(결정 11)에 두는 원칙과 실제로 잡을 수 있는 지점을 일치시켰다. `alembic_version` 테이블 자체가 없는 드문 경우만 라우트 안에서 흡수한다(진짜 접속 실패와 다른 상황이므로). (2) 그 결과 `app/main.py` 에 `ToolError` 계열 6개 핸들러 + 캐치올 `Exception` 핸들러 1개, 총 8개를 등록했다(계획 문서의 "5개 매핑"보다 캐치올 1개가 늘었다 — 도메인 예외가 아닌 진짜 미처리 예외를 위한 안전망이자 `/health` degraded 경로).
- 남은 것 · U9(수용 기준 기계 검증 + 문서)에 넘기는 것: `scripts/tools_check.py`(CLAUDE.md 표 파싱 대조)·`tests/test_tool_signatures.py`, `docs/wiki/registry.md` 신규 행(`app/api/__init__.py`·`app/api/deps.py`·`app/api/routes.py`·`app/api/schemas.py`·`app/main.py`·`tests/test_api.py` — 이번 단위에서 만들었으나 registry 반영은 U9 몫으로 명시적으로 남겨둠) + 기존 행 비고 갱신, `README.md` 진행 표 P2 행과 로컬 API 실행 절(`uvicorn`·`curl /health` 예시 — 이번 단위의 uvicorn 기동 evidence 를 그대로 인용 가능). `get_briefing`/`search_person`/`create_person`/`update_person`/`add_event`/`add_schedule` 은 여전히 어떤 HTTP 라우트에도 연결되지 않았다(계획대로 — 인물·브리핑 조회 API 는 P6/P8).
- Refs: P2-tools R7 D2 S3.4

## 2026-09-05 19:35 · docs(P2-tools): U9 tools_check 시그니처 기계 검증·registry·README 실행 절·S3.1 alias source, verify-impl 사전 실행 · f2e9e05
- 변경: `scripts/tools_check.py`(신규 — CLAUDE.md "툴 7종" 표를 `## 툴 7종` 절부터 다음 `## ` 제목 전까지 정규식 파싱(`{name}`/`{시그니처}` 두 백틱 셀 행), 첫 `(...)` 안 매개변수를 top-level 콤마 분리(`{}`/`[]`/`()` 중첩 무시)로 이름·옵션(`?`)만 추출, `app.tools.TOOL_NAMES` 순서로 `inspect.signature` 대조 — 첫 매개변수가 `ctx` 인지, 그 뒤 이름·순서·옵션 일치 여부. 출력은 툴마다 한 줄 `[ok]/[FAIL] <툴>: CLAUDE.md (...) == actual (ctx, ...)`(F-107a50 — ctx 포함 원형을 그대로 찍는다), 마지막 `RESULT: n/7 ok`, rc 0/1. 7행 미검출 시 `[FAIL] CLAUDE.md 툴 표 7행을 찾지 못함` + rc 1. `--claude-md` 옵션(테스트용 가짜 표 주입)), `tests/test_tool_signatures.py`(신규 15건 — (a) `TOOL_NAMES` 7개 parametrize·`compare_signature` ok, (b) 실제 CLAUDE.md 파싱 결과가 정확히 7행·`TOOL_NAMES` 순서와 일치·`main()` 이 `RESULT: 7/7 ok`, (c) 부정 케이스 4종(매개변수 이름 변경/옵션 마커 제거/6행만/절 자체 없음 — 각각 rc 1), (d) 첫 인자가 `ctx` 아닌 가짜 함수 → `compare_signature` FAIL·사유에 "ctx" 포함), `docs/wiki/registry.md`(P2-tools 신규 26행 — 모듈 10·엔드포인트 5·스크립트 1·테스트 10; 기존 6행 비고만 갱신: `app/config.py`(38행)·`app/db/models.py`(40행)·`requirements.txt`(43행)·`tests/test_config.py`(44행)·`tests/conftest.py`(47행)·`README.md`(32행) — 새 행 없음, `grep -c "| <경로> |"` 각 1 확인; `scripts/embed_pilot.py`(26행) 비고도 결정4 대로 "P2/P3" → "P3-er" 로 좁힘), `README.md`(진행 표 P2 행 추가 — "구현 완료 · 검증 대기", P3 행은 "엔티티 해석 4단계 · 베이스라인"으로 분리; `### 백엔드 실행 (FastAPI)` 소절 신규 — 사전 조건(로컬 DB+`alembic upgrade head`), `POSTGRES_PORT=5433 python -m uvicorn app.main:app --reload`(bash/PowerShell), `curl http://localhost:8000/health` 기대 출력, `POST /answers/{question_id}` curl 예 + 상태 코드 표 200/404/409/422, "채팅·에이전트 루프는 P5 에서" 한 줄. 볼륨 삭제 옵션 문자열 없음), `docs/wiki/specs/S3.1-schema-v2.md`(`person_aliases.source ∈ {user_said, confirmed, system}` 한 줄 + "(P2-tools 결정 5, 앱 검증·DB CHECK 없음)" 주석 추가, `relation_tag`/`hierarchy` 불릿 옆), `docs/wiki/packages/P2-tools/01-plan.md`(U9 `[x]`), `docs/wiki/packages/P2-tools/05-remediation.md`(F-107a50·F-3c8d4a·F-08b3db 해결 단계 표 채움 + evidence 경로, registry 중복 WARN 6건(F-673d47 F-ca32f5 F-127d01 F-320519 F-0ffff5 F-2c37bd) 해결 단계 표를 "완료"로 채움 — **상태 줄(열림/해소)은 그대로 둔다**, 04-review 의 최종 판정은 verifier 몫), evidence/20260905-1908-{tools-check,pytest-signatures,pytest-questions,pytest-trace-d1,pytest-api,uvicorn-health,uvicorn-server.log,alembic-check-u9,pytest-all,mutation-u9}.txt
- 이유(기획서·카드 연결): 01-plan U9(개정 1). backlog P2 수용 기준 "시그니처가 CLAUDE.md와 일치, `ask_user`가 `pending_questions`에 저장" 을 기계로 재확인(R6 R7 R10 R18). F-3c8d4a·결정5(S3.1 카드에 alias source 값 집합 한 줄), F-08b3db(판정 표 두 행도 `POSTGRES_PORT=5433 … -rs` 로 실행해 skip 0 증거), F-107a50(tools_check 출력에 ctx 편차가 보이게).
- CLAUDE.md 파싱 규칙(위임 프롬프트 그대로 구현): 백틱 안 첫 괄호의 매개변수 목록만 본다, `name?` 는 옵션(기본값 있음), `name: type` 은 이름만 취함, `{...}`/`[...]` 중첩은 top-level 콤마 분리에서만 무시(값 자체는 비교하지 않는다 — 반환형 이름 차이(F-107a50 원인 분석)는 그래서 판정에 들어가지 않는다).
- 정합성 확인: 원칙9(tools_check·evidence 로 판정 근거를 남김) / R10(시그니처 CLAUDE.md 일치, 7/7 ok) / R6 R7 R18(pytest_questions 35 passed skip 0 — `ask_user` 저장·`answer_question`) / S3.1 무변경(`alembic check` "No new upgrade operations detected.") / 보안 §1(`.env` 미접근, tools_check·registry·README 어디에도 비밀·접속 문자열 없음) — 위반 없음. 툴·API·모델 코드는 이 단위에서 수정하지 않았다(결함 없음 — 아래 참고).
- 검증: `python scripts/tools_check.py` → `RESULT: 7/7 ok`, rc 0(evidence/20260905-1908-tools-check.txt) · `python -m pytest tests/test_tool_signatures.py -q -rs` → 15 passed(evidence/20260905-1908-pytest-signatures.txt) · `POSTGRES_PORT=5433 python -m pytest tests/test_tools_questions.py -q -rs` → 35 passed, skip 0(evidence/20260905-1908-pytest-questions.txt) · `POSTGRES_PORT=5433 python -m pytest tests/test_tools_context.py tests/test_tools_persons.py -q -rs` → 39 passed, skip 0(evidence/20260905-1908-pytest-trace-d1.txt, F-08b3db 재검증) · `POSTGRES_PORT=5433 python -m pytest tests/test_api.py -q -rs` → 10 passed(evidence/20260905-1908-pytest-api.txt) · `POSTGRES_PORT=5433 python -m uvicorn app.main:app --port 8765` 백그라운드 기동 → `curl -s -w "%{http_code}" http://localhost:8765/health` → `200` + `{"status":"ok","db":"up","alembic_revision":"0001"}`, 프로세스 종료 확인(evidence/20260905-1908-uvicorn-health.txt) · `POSTGRES_PORT=5433 python -m alembic check` → "No new upgrade operations detected.", rc 0(evidence/20260905-1908-alembic-check-u9.txt) · `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` → **206 passed**(기존 191 + 신규 15), skip 0(evidence/20260905-1908-pytest-all.txt) · 변이: `compare_signature` 의 옵션 비교 블록을 주석 처리 → `test_negative_optionality_marker_removed_fails` 1 FAILED 확인 후 원복, 재실행 15 passed(evidence/20260905-1908-mutation-u9.txt) · `bash .claude/scripts/verify-impl.sh P2-tools` 사전 실행(evidence/<verify-impl 타임스탬프>-{pytest,lint,commits,summary}.txt) — 결과는 아래 "verify-impl 사전 실행" 참고.
- 발견한 결함(툴 코드 수정 필요, F 로 올릴 사유): 없음. `app/tools/*`·`app/api/*`·`app/main.py` 는 이번 단위에서 손대지 않았고 U1~U8 의 pytest·evidence 가 그대로 재현됐다(206 passed).
- 계획과 다르게 한 것: (1) registry 신규 행을 산출물별 세분화(모듈 10·엔드포인트 5·스크립트 1·테스트 10, 총 26행)로 만들었다 — 01-plan 41행 "session·settings·embedding·tools 6모듈"이라는 대략적 표현보다 파일 단위로 촘촘하게 나눠(P1-schema 04-review 의 "모델 9개"를 한 행으로 묶은 것과 달리) 각 파일의 책임을 registry 에서 바로 확인할 수 있게 했다 — grep 시 더 명확하다는 점에서 계획 의도(중복 방지·소유 확인)에 반하지 않는다. (2) `scripts/embed_pilot.py`(registry 26행) 비고를 결정4 문구대로 "P3-er 에서 이동" 으로 좁혔다 — 01-plan 135행이 명시적으로 지시했으나 위임 프롬프트의 "할 것" 목록에는 없었던 항목이라 별도로 표기한다.
- 남은 것 · 다음 단위: **구현 단위 U1~U9 완료 → verifier 04-review.** verifier 권고 9건 처리 현황:

| F-ID | 단위 | 처리 |
|---|---|---|
| F-08b3db | U9 | 완료 — 판정 표 두 행(trace 기록·D1 강제)을 `POSTGRES_PORT=5433 … -q -rs` 로 재실행해 skip 0 확인(계획 문서 117·118행 자체는 승인 문서라 수정하지 않음), evidence/20260905-1908-pytest-trace-d1.txt |
| F-b97a06 | U4 | 완료 — `AFFIRMATIVE_KEY`(긍정 답 규약) 도입, `_require_confirmation`이 `answer ∈ affirmative_options` 강제(부정 답·키 없음 각각 `ConfirmationRequired`) |
| F-4d8d96 | U2 | 완료(문서화) — `app/tools/context.py` 모듈 docstring 에 tool_error 행이 롤백과 함께 사라지는 한계 명시, 별도 커넥션 기록 여부는 P5-loop 01-plan 인계 |
| F-3c8d4a | U9 | 완료 — `docs/wiki/specs/S3.1-schema-v2.md` 에 `person_aliases.source ∈ {user_said, confirmed, system}` 한 줄 + "(P2-tools 결정 5)" 주석 추가(CR 아님) |
| F-fbaaae | U6 | 해소(2026-09-05, 기존) — `app/tools/questions.py` docstring 에 `pending_questions`/`agent_traces` 의 `user_id` 격리 부재를 명문화, P5-loop 세션→사용자 귀속 계층으로 인계 |
| F-0010e6 | U2 | 완료 — `ALIAS_SOURCES` 를 계획의 U4 가 아니라 U2(`app/db/models.py`)로 앞당겨 U3 `search_person` 픽스처가 이중 출처 없이 이 상수를 바로 import |
| F-2418ef | U4 | 완료 — `update_person(display_name=X)` 확인 통과 시 X 를 `create_person`의 display_name 자동 별칭과 대칭으로 `system` 별칭으로 upsert(이전 이름 별칭은 삭제하지 않음, D6) |
| F-4d2507 | 인계(architect) | 열림 — `DELETE /persons/{id}` 를 받을 backlog 항목이 아직 없다. backend-agent 범위 밖(코드·문서를 어디에 걸지는 architect 가 backlog 를 고쳐야 정해진다) — P2-tools 계획 통과를 막지 않음 |
| F-107a50 | U9 | 완료 — `scripts/tools_check.py` 출력이 `actual (ctx, ...)` 형태로 `ctx` 를 포함한 원형 시그니처를 그대로 찍는다(반환형 이름 차이는 판정에 포함하지 않음, 원인 분석 결론 유지) |

  나머지: `ask_user`/`create_person`/`update_person`/`add_event`/`add_schedule`/`get_briefing`/`search_person` 은 여전히 HTTP 라우트에 연결되지 않았다(계획대로 — 채팅·툴 라우팅·에이전트 루프는 P5-loop, 인물·브리핑 조회 API 는 P6/P8). `OpenAIEmbeddingProvider` 이동·벡터 인덱스·ER 2~4단계는 P3-er.
- Refs: P2-tools R6 R7 R10 R18 D1 D2 D6 S3.2 S3.4
