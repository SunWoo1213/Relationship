# P2-tools · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-05 17:30 · docs(P2-tools): 계획·계획검증 승인, 패키지 착수 · pending
- 변경: packages/P2-tools/01-plan.md(architect(opus) 초안 → 개정 1: 사용자 결정으로 U8 FastAPI 골격 포함·delete_person 제외, U1~U9), 02-plan-verify.md(verifier(fable) 점검표 8행 통과, 승인 줄), 05-remediation.md(verify-plan 필수 1 해소·권고 6(기존 registry 행) + review 권고 9: F-08b3db F-b97a06 F-4d8d96 F-3c8d4a F-fbaaae F-0010e6 F-2418ef F-4d2507 F-107a50), evidence/20260905-{1502,1511-2,1515-3,1521-final}-verify-plan*.txt·1520-verifier-plan-review.txt, CURRENT active: P2-tools, 03-log 생성
- 이유(기획서·카드 연결): resolution-plan §4 P2 "툴 7종 v2 구현 + 단위 테스트". S3.2 시그니처 v2·S3.4 ask_user 비동기(D2)·D1 신규 인물 확인형·D6 display_name 정책·R10 시그니처↔스키마. 선행 P1-schema 완료 5dc95bb. FastAPI 골격 포함은 P1 04-review §7 인계 존중(사용자 결정 16:40).
- 정합성 확인: 원칙1·2·4(search_person 은 신호만, 판단 P3-er)·6(패턴 감지 없음)·7(브리핑 문장화·제안 없음, A–B 없음)·9(@traced) / D1 D2 D4 D5 D6 D9 / S3.1 무변경(alias source 는 앱 상수) S3.2 S3.4 / 보안 §1 §5 — 위반 없음. WARN 6 은 기존 registry 행(비고만 갱신, 의도)
- 남은 것 · 다음 단위: U1 기반(requirements +fastapi·uvicorn[standard]·httpx 핀·pip freeze, ConnInfo.password repr=False F-8eeb9b + 테스트, app/db/session.py, app/settings.py APP_USER_ID, tests/conftest DB 롤백 픽스처·dbtest 마커·-rs). 권고 반영: ALIAS_SOURCES 상수는 U2 로 당김(F-0010e6), 판정 표 포트 변수(F-08b3db), "U8" 참조 2곳은 U9(F-3c8d4a). 사용자 승인 → `--stage backend-agent`.
- Refs: P2-tools R6 R7 R10 R18 D1 D2 D6 S3.2 S3.4

## 2026-09-05 15:45 · feat(P2-tools): U1 기반 — fastapi·uvicorn·httpx 핀, ConnInfo repr 비밀 제거, 세션·설정, DB 롤백 픽스처 · pending
- 변경: requirements.txt(fastapi==0.135.1·uvicorn[standard]==0.41.0·httpx==0.28.1 3줄 추가, TestClient 용 주석), app/config.py(`ConnInfo.password`를 `field(repr=False)`로 — F-8eeb9b), tests/test_config.py(`test_conninfo_repr_and_str_never_contain_password_value` 1건 추가, 기존 5건 유지), app/settings.py 신규(`app_user_id()`, `DEFAULT_APP_USER_ID="local"`), .env.example(`APP_USER_ID=local` 이름·주석 1줄 추가), app/db/session.py 신규(`get_engine()` 지연 생성·모듈 캐시·`connect_args={"connect_timeout":5}`, `SessionLocal`(지연 평가 프록시), `session_scope()`), tests/conftest.py(`pytest_configure`의 `dbtest` 마커 등록, `db_engine`(세션 스코프, 접속 실패 시 skip)·`db_session`(함수 스코프, 바깥 트랜잭션+`join_transaction_mode="create_savepoint"`)·`fake_embedder`(해시 시드 `random.Random` → 1536차원 단위벡터) 픽스처 추가, 기존 sys.path 줄 유지), tests/test_settings.py 신규 2건, tests/test_db_session.py 신규 4건((a)(b)(c)(d))
- 이유(기획서·카드 연결): 01-plan U1. F-8eeb9b(P1-schema 05-remediation, security.md §1 — ConnInfo 기본 dataclass repr/str 에 password 노출)를 엔진·예외 메시지가 생기기 전에 처리. 결정8(SQLite 불가, 실 DB+롤백 픽스처) · 결정9(user_id 는 설정값) · 결정10(fastapi/uvicorn/httpx 3개, pydantic 은 전이 의존성이라 직접 적지 않음) · 리스크A(엔진 지연 생성 — `SessionLocal` 을 `__call__` 시점까지 미루는 프록시로 구현, `create_app()` 은 U8) · 리스크B(httpx↔TestClient 호환)
- 정합성 확인: 원칙9(트랜잭션 경계는 호출자, 툴은 flush 까지만 — session_scope/get_session 규약의 토대) / 보안 §1(.env 미접촉·비밀번호 repr 제거·접속 문자열 로그 미출력, connect_args 도 비밀 아님) / S3.2(세션·설정 산출물 경로 그대로) — 위반 없음
- 검증: `POSTGRES_PORT=5433 python -m pytest tests/ -q -rs` 56 passed(evidence/20260905-1535-pytest-u1.txt, F-08b3db 반영 — 포트·-rs 항상 포함), `POSTGRES_PORT=5599 python -m pytest tests/ -q -rs` 52 passed·4 skipped(dbtest 만, 사유 "local PostgreSQL not reachable" 그대로 노출, 비밀번호 문자열 0회, evidence/20260905-1535-pytest-u1-nodb.txt), `PYTHONIOENCODING=utf-8 POSTGRES_PORT=5433 python scripts/schema_check.py` 전항목 PASS·9테이블·exit 0(evidence/20260905-1535-schema-check-after-u1.txt — 스키마 무변경), `POSTGRES_PORT=5433 python -m alembic check` "No new upgrade operations detected."·exit 0(evidence/20260905-1535-alembic-check-u1.txt), TestClient 스모크(FastAPI 임시 앱, 저장소에 남기지 않음) GET 200 확인(evidence/20260905-1535-testclient-smoke.txt), `pip freeze` 전체·web 5줄 발췌(evidence/20260905-1535-pip-freeze{,-web}.txt) — 핀 5종: fastapi==0.135.1 / starlette==0.52.1 / uvicorn==0.41.0 / httpx==0.28.1 / pydantic==2.12.5. `repr(conn)`/`str(conn)` 에 고유 문자열(`UNIQUE_MARKER_9f3a21`) 미포함 확인(위 pytest-u1.txt 안에 포함되어 통과)
- 계획과 다르게 한 것: `get_engine()` 에 `connect_args={"connect_timeout": 5}` 를 추가(01-plan 산출물 절엔 명시 없었음). 이유: POSTGRES_PORT=5599(존재하지 않는 포트) 로 `db_engine` skip 경로를 검증할 때 psycopg 기본 접속 타임아웃이 OS 수준(수십 초, 두 주소 재시도)까지 늘어나 "DB 없이도 나머지 테스트가 빠르게 도는지" 검증이 사실상 멈췄다(schema_check.py 의 기존 `connect_timeout=5` 관례를 그대로 따름 — 새 값 도입 아님). 스키마·시그니처 변경 없음.
- 남은 것 · U2 에 넘기는 주의: `app/tools/types.py`·`context.py`(`ToolContext`·`@traced`) — `ToolContext.session` 은 이번 `SessionLocal()`/`session_scope()` 가 만드는 `Session` 객체를 그대로 받는다(별도 어댑터 불필요). U2 이후 픽스처에 `tool_ctx`(01-plan 결정13 대비 `db_session` 재사용)를 추가할 때 이번 `db_session` 픽스처를 그대로 의존하면 된다. `fake_embedder` 는 `EmbeddingProvider.embed(texts)` 형태(list[str]->list[list[float]])로 이미 맞춰 두었으니 U3 는 Protocol 만 정의하면 바로 주입 가능.
- Refs: P2-tools R6 R7 R10 R18 D1 D2 D6 S3.2 S3.4 F-8eeb9b
