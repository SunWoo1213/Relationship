SELECT person_aliases.person_id, person_aliases.embedding <=> %(embedding_1)s AS distance 
FROM person_aliases JOIN persons ON person_aliases.person_id = persons.id 
WHERE persons.user_id = %(user_id_1)s AND person_aliases.embedding IS NOT NULL ORDER BY distance 
 LIMIT %(param_1)s

OK: cosine_distance compiles to <=> (pgvector 0.5.0, no text() fallback needed)
exit=0
