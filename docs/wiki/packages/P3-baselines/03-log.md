# P3-baselines · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-10 10:45 · docs(P3-baselines): 계획·계획검증 승인, 패키지 착수 — 베이스라인 3종 + 공통 Resolver 인터페이스 · pending
- 변경: `01-plan.md`(architect 초안 + 결정 확정 A~H·I·J), `02-plan-verify.md`(verifier 통과, 권고 R-1~R-9, `승인: 사용자 (2026-09-10)`), `05-remediation.md`(F-cf1510 해소·F-0ffff5 의도된 WARN 열림), `evidence/20260910-1029-verify-plan.txt`·`20260910-1036-verify-plan-final.txt`, `CURRENT.md active: P3-baselines`, `docs/backlog.md` 41행 세분화 줄 U1~U8, 이 로그
- 이유(기획서·카드 연결): backlog 41행 "베이스라인 3종 … 제안 방식과 동일 인터페이스로 호출 가능". S3.7·eval-harness §3 "동일 데이터·동일 지표" 의 배관. P4 게이트의 입력을 만든다
- 정합성 확인: 원칙4(대비군은 `evaluation/` 격리, `app/` 변경은 결정 J 1건 예외) / 원칙8(베이스라인 약화 금지 장치: 결정 B·C·E) / 원칙1·2·D10(`identity` 어휘·`band_for`·`ERConfig` 재사용) / S3.1·S3.2 무변경 / security(키·프롬프트 미출력, 실호출 사용자 실행) — 위반 없음
- 남은 것 · 다음 단위: **U1 공통 인터페이스** eval-agent(L-004 승인 후). U5 전에 결정 I·J 반영. 권고 R-1·R-2(U7·U8), R-4(U5·U8), R-6·R-7(U6), R-8(U3) 을 각 단위 위임 프롬프트에 넣는다
- Refs: P3-baselines S3.7 S3.3 D3 D4 D5 D10 원칙4 원칙8 L-002 L-004

## 2026-09-10 13:58 · feat(P3-baselines): U1 공통 Resolver 인터페이스·방식 표 — evaluation/ 패키지 신설, 불변 규약 4개, 계약 테스트 39건 · 05d90f0
- 변경: `evaluation/__init__.py`(패키지·의존 방향 R-2 명문화), `evaluation/resolvers/base.py`(`DECISIONS`·`ResolverCandidate`·`MentionDecision`·`Resolver` Protocol, 불변 규약 4개 docstring), `evaluation/resolvers/registry.py`(`RESOLVERS`·`register`·`get_resolver`·`ALL_METHODS` 동적, 예약 이름 5개), `evaluation/resolvers/__init__.py`(공개 진입점, U2~U5 import 자리), `tests/test_baseline_base.py`(39건), `registry.md` 5행, evidence `20260910-1358-u1-pytest.txt`(39 passed)·`20260910-1358-u1-isolation.txt`(`app/` 변경 0줄·`grep evaluation app/` 0줄·U1 `ALL_METHODS` 빈 튜플), journal(PUSH·RELEASE 줄), HANDOFF, 이 로그
- 이유(기획서·카드 연결): 01-plan U1 "공통 인터페이스·방식 표" — S3.7·eval-harness §3 "동일 데이터·동일 지표" 는 **동일 호출**이 먼저 성립해야 한다. 결정 A(i) `evaluation/` 최상위, 결정 B(i) `supported_decisions`, D10 `band_for` 어휘·`ERConfig` 재사용
- 정합성 확인: 원칙1·2(`person_id` 는 `merge` 에서만 — `__post_init__` 강제) / 원칙4(`app/` diff 0줄, 역방향 import 0줄 = R-2 를 U1 에서 선반영) / 원칙8(빈 표에 자리표시자 없음, 예외 비대칭 금지 규약) / security(키·네트워크·DB 없음) — 위반 없음
- 남은 것 · 다음 단위: **U2 제안 방식 어댑터**(`evaluation/resolvers/proposed.py`, `app.er.resolve` 호출만, `app/er/` 무수정) eval-agent(L-004 승인 후). U1 시점 `ALL_METHODS` 는 빈 튜플이며 U2 가 첫 등록. R-1(U7·U8)·R-4(U5·U8)·R-6·R-7(U6)·R-8(U3) 유지
- Refs: P3-baselines S3.7 D10 원칙1 원칙2 원칙4 원칙8 R-2 L-002 L-004

## 2026-09-10 14:20 · feat(P3-baselines): U2 제안 방식 어댑터 — app.er.resolve → MentionDecision 변환, "proposed" 등록, 테스트 20건 · f212ad6
- 변경: `evaluation/resolvers/proposed.py`(`ProposedResolver` + 순수 변환 `to_mention_decision`, `register("proposed")`, `judge`·`config` 팩토리 인자), `evaluation/resolvers/__init__.py`(U2 import 자리 → 실제 import, `ALL_METHODS == ('proposed',)`), `tests/test_baseline_proposed.py`(20건: 세 밴드 변환·부수효과 0(`person_aliases`·`pending_questions` 행 수 불변)·`get_resolver` 경로·규약·도달 불가 조합 강등 4건), `registry.md` 2행, evidence `20260910-1411-u2-pytest.txt`(59 passed, skip 0)·`-u2-regression.txt`(er_pipeline·er_judge 54 passed)·`-u2-isolation.txt`(`app/` 0줄·역방향 import 0줄·`ALL_METHODS`)·`-u2-pytest-all.txt`(전체 526 passed), 이 로그(U1 항목 해시 05d90f0), journal(U1 COMMIT 줄), HANDOFF
- 이유(기획서·카드 연결): 01-plan 54행·101행 U2 "제안 방식 어댑터 … `app/er/` 는 한 줄도 고치지 않는다". S3.3 4단계 결과를 S3.7 "동일 인터페이스" 로 옮긴다(결정 G). D10 `ERConfig` 그대로 전달
- 정합성 확인: 원칙1·2(`person_id` 는 `merge` 에서만, `apply_resolution` 미호출 — 테스트가 행 수 단언) / 원칙4(`app/` diff 0줄·역방향 import 0줄) / 원칙9(`trace_id`·`confidence_breakdown`·`excluded_by` 를 `detail` 에 보존) / 원칙8(도달 불가 조합은 예외 대신 강등 + `adapter_forced_reason`, 규약 2) — 위반 없음
- eval-agent 판단 4건(verifier 04-review 확인 대상): (1) `trace_id` 를 전용 필드와 `detail` 양쪽에(출처 하나, 테스트가 동일성 단언) (2) 후보별 `score` 는 판정된 인물만 `confidence`, 나머지 0.0(제안 방식은 후보별 결합 확신도를 내지 않음, P3-er 결정3) (3) 문자열 `excluded_by` 는 `signals`(float) 대신 `detail`, bool `rule_flags` 6개는 1.0/0.0 (4) 계획에 없던 `detail` 키 추가 `band_by_threshold`·`confidence_breakdown`·`ask_kind`·`llm_skipped/attempts/error` — 판정을 바꾸지 않고 `Resolution` 값을 옮기기만 함
- 남은 것 · 다음 단위: **U3 완전일치 2변형**(`exact_raw`·`exact_norm`, 결정 C(iii), R-8) eval-agent(L-004). 이후 U4 임베딩 단독 → U5 LLM 단일(결정 I·J) → U6 적재기 → U7 parity → U8 evidence·README → verifier 04-review
- Refs: P3-baselines S3.3 S3.7 D3 D10 원칙1 원칙4 원칙8 원칙9 R-2 L-002 L-004

## 2026-09-10 14:35 · feat(P3-baselines): U3 문자열 완전일치 베이스라인 2변형 — exact_raw·exact_norm 등록, 순수 함수 분리, 테스트 61건 · bcb2fad
- 변경: `evaluation/resolvers/exact_match.py`(순수 `match_exact(mention, persons, *, normalizer)` + DB 조회 `load_known_persons(ctx)` 분리, `to_mention_decision`, `ExactRawResolver`(`exact_raw`, strip+casefold)·`ExactNormResolver`(`exact_norm`, `app.er.dictionary.normalize()`+casefold 를 mention·별칭·표시 이름 양쪽에, R-8), 1→`merge`(1.0)/2+→`identity`(`person_id` None)/0→`new_person`, 빈 문자열→`forced_reason="empty_after_normalize"`), `evaluation/resolvers/__init__.py`(U3 import, `ALL_METHODS == ('proposed','exact_raw','exact_norm')`), `tests/test_baseline_exact_match.py`(61건: 순수 함수 1/2/0건·공백 대소문자·"김팀장"↔"팀장" raw 불일치/norm 일치·대칭성·상위집합·DB 동명이인→identity·승진 호칭→new_person·부수효과 0·`ctx.embedder=None` 에서도 완전 판정), `registry.md` 2행, evidence `20260910-1423-u3-pytest.txt`(120 passed, skip 0)·`-u3-isolation.txt`(`app/` 0줄·역방향 import 0줄·`ALL_METHODS`), 이 로그(U2 항목 해시 f212ad6), journal(U2 COMMIT 줄), HANDOFF
- 이유(기획서·카드 연결): 01-plan 55행·102행 U3, 결정 C(iii) "두 변형 모두 등록 … P4 가 둘 다 보고" — 약한 베이스라인만 보고하지 않는다(원칙8). 동명이인은 임의 선택 없이 `identity`(원칙1). R-8 양쪽 정규화
- 정합성 확인: 원칙1(동명이인 2건 `identity`, `person_id` None — 테스트 단언) / 원칙4(`app/` 0줄·역방향 import 0줄) / 원칙8(강한 변형 `exact_norm` 함께 등록, 예외 비대칭 금지 공통 경로) / S3.7(임베딩·LLM·규칙·임계치 미사용, `uses_*` 전부 False) — 위반 없음
- eval-agent 판단 6건(verifier 04-review 확인 대상): (1) **R-8 의 예시 "님" 단독은 `normalize()` 가 빈 문자열을 내지 않는다**(`dictionary.py:154` 접미 제거 조건 `len > len(suffix)`) → 일치 0건 분기로 감. 빈 문자열은 `""`·공백뿐·None 뿐. 테스트로 사실을 고정, `app/` 미수정 (2) 빈 문자열 규칙을 `exact_raw` 에도 동일 적용(공통 경로, 분모 불변) (3) `normalize_dictionary` 에 `casefold()` 추가 → `exact_norm` ⊇ `exact_raw` 테스트로 단언 (4) `identity` 의 `score` 1.0 = "문자열은 맞았으나 특정 못 함", 확신도 아님 — docstring 에 P4 축 분리 명시 (5) `new_person` 의 `candidates` 빈 목록, 기록용 `detail` 키(`variant`·`normalized_mention`·`match_count`·`matched_person_ids`·`matched_names`·`ask_kind`·`uses_*`) (6) `load_known_persons` 는 `embedding IS NULL` 별칭도 포함(`search_person` 과 다름)
- 남은 것 · 다음 단위: **U4 임베딩 단독**(`embedding_only`, `search_candidates` + `band_for`, 결정 D(i) `s_emb` 에 `T_merge`/`T_new` 그대로) eval-agent(L-004). 이후 U5(결정 I·J, R-4) → U6 → U7 → U8 → verifier
- Refs: P3-baselines S3.7 원칙1 원칙4 원칙8 R-8 R-2 L-002 L-004

## 2026-09-10 14:50 · feat(P3-baselines): U4 임베딩 단독 베이스라인 — search_candidates·band_for 재사용, embedding_only 등록, 테스트 53건 · 8bfd880
- 변경: `evaluation/resolvers/embedding_only.py`(`register("embedding_only")`, `app.er.candidates.search_candidates()`·`app.er.confidence.band_for(s_emb, config)` 재사용, 순수 `decide_from_candidates(candidates, config, ...)` 와 DB 조회 분리, `top_k` 미전달·미스윕(F-bdd6c5), 동점은 `merge` 밴드에서만 `identity` 강등 + `tied_person_ids`, 공급자 없음은 `s_emb=0` 을 `band_for` 에 넣어 임계치가 결정(`T_new`=0.3 이면 `new_person`) + `embedding_skipped`, 빈 mention → `new_person` + `empty_mention`), `evaluation/resolvers/__init__.py`(U4 import, `ALL_METHODS` 4종), `tests/test_baseline_embedding_only.py`(53건: 경계값 `T_merge`·`T_new` `ge_with_tolerance` 일치·후보 0건·동점·공급자 없음·`grouped_embedder` 로 "팀장↔부장님 > 팀장↔이모" 밴드 반영·`T_merge` 변경 시 밴드 변화·부수효과 0·등록 순서), `tests/test_baseline_exact_match.py`(순서 단언을 `ALL_METHODS[:3]` 로 좁힘 — 뒤 단위 추가에 안 깨지게, 판정 로직 무변경), `registry.md` 2행, evidence `20260910-1437-u4-pytest.txt`(173 passed, skip 0)·`-u4-isolation.txt`(`app/` 0줄·역방향 import 0줄·`ALL_METHODS`)·`-u4-pytest-all.txt`(전체 640 passed), 이 로그(U3 항목 해시 bcb2fad), journal(U3 COMMIT 줄), HANDOFF
- 이유(기획서·카드 연결): 01-plan 56행·103행 U4, 결정 D(i) "`T_merge`/`T_new` 를 `s_emb` 에 그대로 적용, `band_for()` 재사용 — P4 곡선의 x축이 네 방식에 같은 의미". D5 별칭 top-K → 인물별 max 는 `search_candidates` 재사용으로 중복 구현 0. 결정 H(i) 가짜 임베딩만
- 정합성 확인: 원칙1(임의 선택이 실제로 생기는 `merge` 밴드 동점만 `identity` 강등) / 원칙2·D10(임계치 재정의 없음, `ERConfig` 그대로) / 원칙4(`app/` 0줄·역방향 import 0줄) / 원칙8(허용오차 밖 근접값은 동점 아님 — 베이스라인 인위적 약화 금지) / S3.7(규칙·LLM 미사용 `uses_*`) — 위반 없음
- eval-agent 판단 8건(verifier 04-review 확인 대상): (1) **동점 강등은 `band_by_threshold=="merge"` 일 때만**(위임 프롬프트의 "동점→identity" 를 밴드 무관하게 적용하면 `new_person` 밴드·`embedding_skipped` 전원 동점까지 `identity` 로 올라가 01-plan 103행 "→ `new_person`" 과 충돌) (2) `embedding_skipped` 를 `new_person` 으로 못 박지 않고 `s_emb=0` → `band_for`(규약 4, `T_new`>0 이면 동일 결과) (3) 동점 허용오차 = `ge_with_tolerance` 양방향(F-7fe239 단일 출처) (4) `signals` 에 `s_rule` 미기재(계산된 적 없는 값을 0.0 으로 쓰면 오독) (5) `s_emb=0` 별칭 일치 인물은 후보로 기록만, 판정엔 미사용(문자열 일치 안 씀을 DB 테스트가 단언) (6) 빈 mention 은 `search_person` 의 `InvalidValue` 전에 `new_person`(규약 2), DB 장애 예외는 삼키지 않음 (7) `score` = 최고 후보 `s_emb`(후보 0 → 0.0), 제안 방식 확신도와의 축 비교는 P4 몫 (8) 기록용 `detail` 키 `top_s_emb`·`candidate_count`·`tied_person_ids`·`embedding_skipped`·`hints`·`thresholds`
- 남은 것 · 다음 단위: **U5 LLM 단일 프롬프트**(`llm_single`, 결정 E(ii) 새 프롬프트·인물 목록 전체 제공, 결정 I `candidate_person_ids`, 결정 J `judge.py` 오류 매핑 공개 승격 1건 = `app/` 유일 예외, R-3·R-4 `OPENAI_MODEL`·R-5) eval-agent(L-004). 이후 U6(R-6·R-7) → U7(R-1) → U8 → verifier
- Refs: P3-baselines S3.7 D4 D5 D10 원칙1 원칙2 원칙4 원칙8 F-bdd6c5 R-2 L-002 L-004

## 2026-09-10 15:15 · feat(P3-baselines): U5 LLM 단일 프롬프트 베이스라인 — llm_single 등록, 결정 I·J 반영, 스모크 스크립트, 테스트 69건 · 0d98e47
- 변경: `evaluation/resolvers/llm_single.py`(`register("llm_single")`, `RESOLUTION_SCHEMA` 에 결정 I `candidate_person_ids`, `build_prompt` 사전 상태 인물 전체+발화+mention, 공급자 중립 `ClaudeSingleCaller`/`OpenAISingleCaller`/`caller_from_env`(`LLM_PROVIDER`·`ANTHROPIC_MODEL`·`OPENAI_MODEL` R-4, temperature 0, 타임아웃 20s·재시도 1, `call_with_error_mapping` 재사용으로 `llm.error` 어휘 동일), 순수 `validate_resolution`/`build_decision`/`resolve_from_state`, 강등 규칙(어휘 밖·id 사전 밖·merge 인데 id 없음·호출 실패 → `identity` + `forced_reason`, `identity` 후보 = 사전 안 `candidate_person_ids`, 밖은 `dropped_ids`, 비면 `identity_without_candidates`), `s_llm` 범위 밖 clamp+`score_clamped`, caller 지연 생성), **결정 J `app/er/judge.py`**(`_call_with_error_mapping`→`call_with_error_mapping`, 호출부 2곳·docstring 1줄, 옛 이름 호환 별칭 — `app/` 유일 수정, 동작 무변경), `evaluation/resolvers/__init__.py`(U5 import, `ALL_METHODS` 5종), `evaluation/resolvers/exact_match.py`(`KnownPerson` 에 `relation_tag`/`hierarchy` 기본 `None` additive 추가, SELECT 5열 — 사전 상태 조회 단일 출처 유지), `scripts/baseline_smoke.py`(키 없으면 rc=2, LLM 오류 rc=3, `resolve_from_state` 공용), `tests/test_baseline_llm_single.py`(69건: 요청 본문·파싱·세 결정·강등 5경로·`dropped_ids`·오류 어휘·키 미노출·부수효과 0·등록), `tests/test_baseline_exact_match.py`(`_FakeSession` 5열), `tests/test_baseline_embedding_only.py`(순서 단언 `[:4]`), `registry.md` 3행 + judge.py 비고, evidence `20260910-1458-u5-pytest.txt`(242 passed, skip 0)·`-u5-regression.txt`(er_judge·er_pipeline 54 passed 무수정)·`-u5-isolation.txt`(`app/` 정확히 judge.py 1줄, diff 이름 변경+별칭 9+/4-, 역방향 import 0줄, `ALL_METHODS` 5종)·`-u5-smoke-nokey.txt`(rc=2)·`20260910-1459-u5-pytest-all.txt`(709 passed)·`20260910-1457-u5-pytest-intermediate-fail.txt`(구현 중간 13 failed 기록, 삭제하지 않음 원칙8), 이 로그(U4 항목 해시 8bfd880), journal(U4 COMMIT 줄), HANDOFF
- 이유(기획서·카드 연결): 01-plan 57행·104행 U5, 결정 E(ii) "결정까지 LLM 이 내는 새 프롬프트, 인물 목록 전체" — 원칙4 가 금지한 방식을 가장 강한 합리적 형태로 대비군에 세운다(원칙8). 결정 I(R-3) `candidate_person_ids` 로 identity 품질 비교가 네 방식에 같은 의미. 결정 J(R-5) 오류 어휘 단일 출처. R-4 `OPENAI_MODEL`
- 정합성 확인: 원칙4(제품 경로 무변경 — `app/` diff 는 결정 J 이름 승격 1건뿐, 역방향 import 0줄) / 원칙1·2(`person_id` 는 `merge` 에서만, 임계치 미사용) / 원칙8(강등이지 예외 아님, 분모 불변; 실패 evidence 도 남김) / security §1(키·프롬프트 원문 미출력 — 테스트 표식 단언, 키 패턴 grep 0줄) / S3.7 — 위반 없음
- eval-agent 판단 10건(verifier 04-review 확인 대상): (1) 결정 J 는 밑줄 제거 + 옛 이름 별칭(`test_er_judge.py` 는 미참조) (2) `KnownPerson` additive 확장으로 사전 상태 조회 재사용(새 쿼리 금지) → U3 스텁 5열 (3) `s_llm` 범위 밖은 `schema` 가 아니라 clamp(결정을 버리면 분모가 달라짐), 자료형 위반만 `schema` (4) `forced_reason` 우선순위 호출 실패 > 어휘·id 위반 > `identity_without_candidates` (5) 빈 mention 은 LLM 미호출 `new_person`+`empty_mention`, 사전 상태가 비어도 LLM 은 부름 (6) `candidate_person_ids` 필터는 세 결정 공통, `merge` 는 판정 인물을 선두에 (7) caller 지연 생성 — `get_resolver` 는 키 없이 성공(U7 parity 대비) (8) `candidate_person_ids` 키 누락은 `candidate_ids_missing` 기록 (9) 스모크·평가가 같은 `resolve_from_state` (10) 기록용 `detail` 키(`raw_decision`·`raw_matched_person_id`·`reason`(모델 출력)·`dropped_ids`·`person_count`·`prompt_chars`·`llm_calls`·`llm_error` 등)
- 남은 것 · 다음 단위: **U6 시나리오 사전 상태 적재기**(R-6·R-7 `validate_scenarios` 적재 함수 재사용·`schema_version 2`, 결정 F·H `embedder` 인자 기본 None) eval-agent(L-004). 이후 U7 parity(R-1) → U8 evidence·README(R-1·R-2·R-4) → verifier 04-review. 사용자 몫: `scripts/baseline_smoke.py` 실키 실행은 사용자가(security §6) — `docs/user-setup/` 에 항목 추가는 U8 때
- Refs: P3-baselines S3.7 D3 원칙1 원칙2 원칙4 원칙8 원칙9 R-3 R-4 R-5 R-2 L-002 L-004

## 2026-09-10 15:35 · feat(P3-baselines): U6 시나리오 사전 상태 적재기 — seed_persons·aliases 그대로 적재, validate_scenarios 적재 함수 재사용, 테스트 104건 · e0812f7
- 변경: `evaluation/scenario_state.py`(`load_scenario_state(ctx, scenario, *, embedder=None) -> ScenarioState{scenario_id, person_id_map, created_person_ids, alias_count, embedded_alias_count}`, `PersonSpec`, `ScenarioStateError`, 순수 `seed_person_specs()`·`count_seed_persons_and_aliases()`, 얇은 래퍼 `load_scenarios()`·`check_schema_version()` = `validate_scenarios.load_dataset` 재사용(R-7, 새 파서 없음). 별칭 `source="confirmed"`·`confirmed_at=ctx.now()`(R-6), `embedder` 인자만 보고 `ctx.embedder` 는 읽지 않음(결정 H(i) 네트워크 0), 없으면 전부 NULL → docstring 에 "`embedding_only`·`proposed` 는 `embedding_skipped`" 명시(R-6). `events`·`schedules`·`pending_questions`·`person_facts`·`agent_traces` 미생성, `flush()` 까지만), `evaluation/__init__.py`(docstring 3줄), `tests/test_scenario_state.py`(104건: 순수 71 — 40건 전수 인물·별칭 수 = seed 56·별칭 112 상수, DB 33 — 실물 1건 적재 행 수 라벨 일치·다른 테이블 증가 0·`embedder=None` 이면 NOT NULL 0/`grouped_embedder` 면 별칭 수·`source`·`confirmed_at`·바이트 동일·`exact_raw` 로 `merge` 맞물림·반복 적재는 두 벌), `registry.md` 2행, evidence `20260910-1515-u6-pytest.txt`(204 passed, skip 0)·`-u6-data-unchanged.txt`(`validate_scenarios --strict --json` ok:true·total 40·schema_version 2·issue_count 0·rc=0, `git status data/` 0줄)·`-u6-isolation.txt`(`app/` 0줄·역방향 import 0줄)·`-u6-pytest-all.txt`(813 passed), 이 로그(U5 항목 해시 0d98e47), journal(U5 COMMIT 줄), HANDOFF
- 이유(기획서·카드 연결): 01-plan 58행·105행 U6, 결정 F(i) "이 패키지가 최소 적재기를 만들고 P4 러너가 재사용 — 테스트와 평가가 같은 적재", 결정 G(i) 실 PostgreSQL 공유, P1 04-review §7 인계 2(적재 = `seed_persons`+`aliases` 그대로)·9(라벨 재해석 금지)·10(`schema_version` 2)
- 정합성 확인: 원칙8(별칭 추가·추론·정규화 0, 데이터 무변경 증거 rc=0, 총계 상수로 데이터 변경 시 먼저 깨짐) / 원칙4(`app/` 0줄·역방향 import 0줄) / S3.1(`source` ∈ 허용값, `confirmed_at`) / D5(별칭 단위 임베딩, `embedder` 주면 배치 1회) / security(네트워크 0) — 위반 없음
- eval-agent 판단 10건(verifier 04-review 확인 대상): (1) `persons[]` 키는 `person_id`(위임 프롬프트 표기 `id` 는 오기, 실물 스키마 따름) (2) `schema_version` 은 manifest 에 있어 `check_schema_version(dataset)` 이 manifest 경로 확인, 2 아니면 예외 (3) `ctx.embedder` 미참조 — P4 는 `embedder=` 명시 필수 (4) 실물 성질: 인물 내 중복 별칭 0, `display_name` 이 `aliases` 에도 있는 인물 10명 → 그대로 별칭 행 생성(행 수 = `len(aliases)` 계약) (5) `validate_scenarios._dict_items`·`_scenario_id` private import 는 `dump_scenarios.py` 선례와 동일 (6) `seed_persons` id 가 `persons[]` 에 없으면 예외 (7) 교차 무결성 검사는 재실행 안 함(`--strict` evidence 가 담당) (8) 반복 적재는 두 벌 — 초기화는 P4 책임(P1 §7 인계 1), `commit()` 없음 (9) R-6 문구 양쪽 docstring (10) 삽입 순서 = (seed 순서, JSON 순서), 임베딩 배치 1회
- 남은 것 · 다음 단위: **U7 parity 테스트**(`tests/test_baseline_parity.py` — `ALL_METHODS` parametrize 에 `proposed` 포함, 픽스처는 이름→팩토리 kwargs 표만 허용, 테스트 본문 분기 0 = R-1 판정 명령 `grep -nE "if name ==|if name in|match name|name !=" tests/test_baseline_parity.py` 0줄, 실물 시나리오 적재기 사용, 부수효과 0 SQL) eval-agent(L-004). 이후 U8(evidence·registry·README·user-setup 스모크 항목, R-1·R-2·R-4) → verifier 04-review
- Refs: P3-baselines P1-pilot-dataset D5 S3.1 원칙4 원칙8 R-6 R-7 R-2 L-002 L-004

## 2026-09-10 15:55 · test(P3-baselines): U7 동일 인터페이스 parity 계약 테스트 — 5방식 같은 함수·같은 인자, 본문 분기 0(R-1), 46건 · pending
- 변경: `tests/test_baseline_parity.py`(46건: `registry.ALL_METHODS` 5종 parametrize — 방식 목록·표 정합 4, 키 없이 `get_resolver` 생성+Protocol 5, `resolve_mention` 5매개변수 동일 5, 계약 (i)~(iv)·(vi) 5×실물 시나리오 2(sc-025 normal "엄마"·sc-001 promotion "김팀장", U6 적재기 `embedder=` 명시) 10, 재현성 (v) 10, 방식 정의 카운터 10, 한 `ctx` 연속 호출 2. 픽스처 하나가 이름→kwargs 표(`proposed`: `FakeJudge`, `llm_single`: 스텁 client), 허용값은 `ALLOWED_*[name]` 집합 표 → `assert 관측값 in 표[name]`, 테스트 본문 분기 0. `hints` 는 전 방식 `None` 상수. `merge` 의 `person_id` 가 적재된 사전 상태 인물인지까지 단언), `registry.md` 1행, evidence `20260910-1537-u7-parity.txt`(parity 단독 46 passed, skip 0, `-v` id 에 5종 이름)·`-u7-no-branch.txt`(R-1 grep 0줄 rc=1)·`-u7-pytest-all.txt`(859 passed, skip 0)·`-u7-isolation.txt`(`app/` 0줄·역방향 import 0줄·`data/` 0줄), 이 로그(U6 항목 해시 e0812f7), journal(U6 COMMIT 줄), HANDOFF
- 이유(기획서·카드 연결): 01-plan 59행·106행 U7 — backlog 41행 수용 기준 "제안 방식과 동일 인터페이스로 호출 가능" 의 기계 증명(114~117행 4문장 해석: `ALL_METHODS` 3종+`proposed` 포함 / 방식 정의 카운터 / 실물 `data/scenarios/` / 같은 테스트 함수·본문 분기 없음). S3.7 "동일 데이터·동일 지표" 의 전제인 동일 호출
- 정합성 확인: 원칙1·2(인물·별칭·질문 3테이블 전 방식 증분 0, `merge` 에서만 `person_id`) / 원칙4(`app/` 0줄·역방향 import 0줄) / 원칙8(실물 시나리오·라벨 별칭만, 지어낸 문자열 0; 세 밴드 분포·정답 여부는 단언 안 함 = P4 몫) / R-1(grep 0줄 evidence) — 위반 없음
- **eval-agent 판단 7건(verifier 04-review 확인 대상)**: (1) **`agent_traces` 증분 실측이 01-plan 판정 표 131행 "제안 방식만 `agent_traces` +1" 과 다르다** — `proposed` **+2**(`er_resolve` 1 + 내부 `search_person` 툴 trace 1), `embedding_only` **+1**(`search_person`), 완전일치·`llm_single` 0. U2·U4 테스트가 이미 같은 성질을 기록. 구현 미수정, 표 `ALLOWED_TRACE_DELTA` 로 두고 3테이블 증분 0 은 별도 단언. **04-review 판정 표 대조 때 131행 기대값을 실측으로 읽어야 한다**(계획 문언 오차, 규약 1 "인물·별칭·질문 상태를 바꾸지 않는다" 는 충족) (2) 분기 금지를 허용값 집합 표로, LLM 호출 수는 `judge.calls + len(client.calls)` 단일 관측값 (3) R-1 정규식을 파일 docstring 에 인용하면 grep 이 자기 자신을 잡아 1줄 → 명령 문자열 제거 (4) `hints=None` 전 방식 동일(dict 를 주면 `derive_hints` 경로가 죽어 시나리오마다 의미가 달라짐) (5) 시나리오·mention 실물에서만 (6) `proposed` 의 LLM 1회는 정의가 아니라 측정값(주석 명시) (7) `merge` `person_id` ∈ 적재 인물
- 남은 것 · 다음 단위: **U8 수용 기준 기계 검증 + 문서**(01-plan 107행: 전체 pytest·parity 단독·부수효과 0 SQL 조회 출력·`tools_check.py` 7/7·`alembic check`·`validate_scenarios --strict --json` rc=0·R-1 grep·R-2 grep·`git diff --name-only 0e3447a..HEAD -- app/` = judge.py 1줄, `registry.md` 커밋 열 확정, `README.md` 베이스라인 실행법 절 + R-4 `OPENAI_MODEL`, `docs/user-setup/` 스모크 항목, `evaluation/resolvers/base.py` docstring R-3 한 줄 확인) eval-agent(L-004) → verifier 04-review(L-004)
- Refs: P3-baselines S3.7 S3.3 D10 원칙1 원칙2 원칙4 원칙8 R-1 R-2 L-002 L-004
