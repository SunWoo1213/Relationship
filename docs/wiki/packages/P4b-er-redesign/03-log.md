# P4b-er-redesign · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-22 · docs(P4b-er-redesign): 계획검증 통과·계획 승인 — CR-001 D12·D13 구현 + 재실행 + 게이트 재판정, 패키지 착수 · 83d33dd
- 변경: `01-plan.md`(architect 초안 → 메인 세션 형식 2건(작업 단위 접두·해석 불릿 들여쓰기) + "## 결정 (사용자 확정 2026-09-22)" 표 + `ER_PENALIZED_MERGE_POLICY` 치환 + 권고 R-3(`scripts/er_smoke.py:109` 산출물·U1 명시)·R-9(`*real-run*` 파일명 규약) 반영), `02-plan-verify.md`(verifier, `결과: 통과`, 승인 줄), `05-remediation.md`(findings.py [권고] 22 — registry 기존 행 18 + basename 오탐 4, [필수] 0), `evidence/`(verify-plan 1~3차 + verifier 1710·1718 + `1713-regression3-baseline.txt` 3 passed + `.gitkeep`), `docs/backlog.md` 65행(`types.py`(+`settings.py`) 허용 파일 — 개정 (2) 반영, (1) 거절), `docs/wiki/changes/CR-001.md` 36행(기준선 = stamp 사본, 결정 I), `CURRENT.md` active + 메모, journal DECISION·VERIFY·START, HANDOFF. 코드·`data/`·`reports/` 변경 **0**
- 이유(기획서·카드 연결): CR-001 §4 4항 "수정은 `P4b-er-redesign` 패키지 안에서". backlog "### P4b — 게이트 재도전 (CR-001)" 65·66행 = 01-plan 72·73행(diff 0줄). D12·D13 "코드에서 지켜야 할 것"이 U1~U3 의 문장이고, 결정 K(P4 01-plan 227행)·S3.7 게이트 식은 불변(결정 F). 사용자 결정 A~I(권장 조합, 18:20)
- 정합성 확인: 원칙 1(A(i) 강등)·3(D12 = CLAUDE.md 38행)·4·8(재실행 1회·기준선 stamp 사본)·9(weights_effective·penalized_by) / D12 D13 D10 / S3.3 S3.7 / 보안(`.env` 로드 주체 사용자 셸) — 위반 없음(02-plan-verify 점검표 8/8, 사실 주장 (a)~(f) 6/6). 코드 변경 0
- 남은 것 · 다음 단위: **U0**(메인 세션, R-7: `.claude/skills/entity-resolution/SKILL.md` 22·36·39행 D12·D13 정합 + "권위는 S3.3·D12·D13" 한 줄, CR-001 §2 파일 목록 갱신 이력 한 줄) → **U1**(backend-agent, L-004 승인 후): `combine()` 재정규화·`_breakdown` `weights_effective`/`rule_checked`·강제 경로·`er_smoke.py:109` + 테스트. U4→U5 순서 필수(recheck 가 새 trace 거부). R-4(U2 03-log 에 완화 통과 후보 `penalized_by` 채움 여부 확정)·R-5(U4 rule_checked=0 재계산 테스트)·R-8(U3 ask_payload candidate_ids 단언)·R-6(04-review 에 P4 04-review 220행 대체 한 줄)·R-2(하네스 L-nnn 후보: verify-plan 5번 불릿 의존 파싱)
- Refs: P4b-er-redesign CR-001 D12 D13 D10 S3.3 S3.7 R3 R4 원칙1 원칙3 원칙8 원칙9 L-002 L-004

## 2026-09-22 · harness(P4b-er-redesign): U0 entity-resolution 스킬 카드 D12·D13 정합 — 권위 문구·감점·재정규화 산식 · 28c2de8
- 변경: `.claude/skills/entity-resolution/SKILL.md` — 머리에 "권위는 S3.3·D12·D13·D10, 이 카드는 미러" 한 줄(R-7 이중 출처 방지), 22행 2단계 "명백히 다른 후보 배제" → 감점·후보 유지(사전 모순만 배제, D13), 36행 `s_rule` 미측정 규약(D12), 39행 산식 → `Σ w_i·s_i / Σ w_i` + 두 경우 전개 + `weights_effective`·보수 분기(`ER_PENALIZED_MERGE_POLICY=ask`, `relaxed_pass` 예외) 문단. `docs/wiki/changes/CR-001.md` 갱신 이력(§2 파일 목록에 `types.py`·`er_smoke.py`·SKILL.md). 이 로그(직전 `pending`→83d33dd). 코드 변경 **0**
- 이유(기획서·카드 연결): 01-plan U0 — backend-agent 가 자동 참조하는 카드가 D3 산식·배제 문장을 그대로 갖고 있으면 U1~U3 이 옛 규약으로 구현된다(02-plan-verify 사실 주장 (d): `D12|D13|CR-001|감점|penalized` grep 0건). verifier 분류(R-7): 위치는 하네스 파일이나 S3.3 3행이 "절차 상세"로 가리키는 명세 미러 → 하네스 문서 정합 커밋(메인 세션). 문장은 D12 6~8행·D13 5~8행·P4b 결정 A(i)·B(i) 를 옮겼다
- 정합성 확인: 원칙3(새 문장 = CLAUDE.md 38행 = D12)·원칙1(강등 기본)·원칙9 / D12 D13 D10 / S3.3 / 보안 — 위반 없음. 판정: `grep -nE "D12|D13|penalized|weights_effective" .claude/skills/entity-resolution/SKILL.md` ≥ 4건, `grep -c "0.5·s_llm + 0.3·s_emb + 0.2·s_rule" SKILL.md` = 1(세 신호 모두 관측 경우의 전개로만 남음)
- 남은 것 · 다음 단위: **U1** backend-agent 위임 승인(L-004) — `combine()` 재정규화·`_breakdown` `weights_effective`/`rule_checked`·강제 경로 키·`er_smoke.py:109` + 테스트(`test_er_confidence.py` 293행 원식 수치 유지, 미측정 케이스 추가)
- Refs: P4b-er-redesign CR-001 D12 D13 S3.3 원칙3 원칙9 L-004

## 2026-09-22 · feat(P4b-er-redesign): U1 D12 — 관측 신호 재정규화 결합 · 85ceda7
- 변경: `app/er/confidence.py`(`combine()` 이 `rule_checked: int = 1`(키워드 전용) 인자를 받아 `rule_checked > 0` 이면 D3 원식 그대로, `rule_checked == 0` 이면 `(w_llm*s_llm + w_emb*s_emb)/(w_llm+w_emb)` 로 재정규화 — 두 분기 모두 옛 D3 연산 순서를 그대로 유지해 부동소수 오차 없이 동치; 새 헬퍼 `_effective_weights(rule_checked, config)` 를 `combine()` 과 **같은 조건**으로 분기해 이중 출처를 막음; `_breakdown()` 에 `weights_effective` 키 추가(기존 `weights`·`rule_checked`·`rule_passed` 키는 그대로); `decide()` 정상 경로가 `combine(..., rule_checked=candidate.rule_checked)` 로 호출부만 갱신 — `_forced_decision`/`no_candidates` 두 강제 경로는 이미 `rule_checked=0` 을 `_breakdown()` 에 넘기고 있어 `weights_effective` 가 자동으로 재정규화 값을 받음, `confidence` 는 세 신호가 전부 0 이라 산식이 무엇이든 종전대로 0.0; 모듈·"관측된 신호만 재정규화한다 (D12)" 절 신설), `scripts/er_smoke.py`(109행 `combine()` 호출에 `rule_checked=0` 추가 — 스모크는 2단계 규칙 필터를 거치지 않아 hints 없는 호출과 같음, 권고 R-3; 출력 JSON 키 불변), `tests/test_er_confidence.py`(+21 테스트: (i) 세 신호 관측 시 D3 원식과 `abs diff == 0` 무작위 표본 200건 seed 고정, (ii) `rule_checked=0` → `0.625*s_llm+0.375*s_emb`·`weights_effective` 확인(s_rule 값을 바꿔도 결과 불변까지), (iii) `rule_checked>0, rule_passed=0` → `s_rule=0` 그대로 합산(재정규화 아님)·`weights_effective == weights`, (iv) 정상·강제(llm_failed·no_candidates·out_of_range_id) 네 경로 모두 `weights`·`weights_effective`·`rule_checked`·`rule_passed` 키 존재, (v) 293행 원식 수치 재확인, (vi) `weights_effective` 합계 1.0(측정·미측정 양쪽), `_effective_weights()` 직접 import 교차검증), `tests/test_er_smoke.py`(`test_run_smoke_merge_band_matches_combine` 기대값을 D3 `0.8634` → D12 `0.9125`(rule_checked=0 이므로 `s_rule=0.667` 이 결과에 반영되지 않음)로 갱신 — er_smoke.py 호출부 변경의 직접 파급, band는 `merge`로 불변)
- 이유(기획서·카드 연결): `docs/wiki/decisions/D12-observed-signal-renormalization.md` "코드에서 지켜야 할 것" 4항(23~27행) — `rule_checked` 입력·재정규화 공식·`weights`/`weights_effective`/`rule_checked` 키·`rule_passed==0` 비재정규화·회귀 3종 유지. 01-plan U1(61행) 문구 그대로. P4 04-review §4b·failure_cases.md §4·§8 이 근본 원인으로 지목한 "미측정 `s_rule=0` 합산이 확신도 상한을 0.80 에 묶었다"(rule_checked==0 이 141 중 100건)를 해소하는 구현
- 정합성 확인: 원칙3(CLAUDE.md 38행 = D12 6~8행 = 모듈 docstring 신설 절, 산식 문자열 동일) · 원칙9(`weights`·`weights_effective`·`rule_checked`·`rule_passed` 네 키가 정상·강제 경로 모두에 있다, 이중 출처 금지 — `_effective_weights()` 가 `combine()` 과 같은 조건문을 씀) · 원칙4(단계 구조·시그니처 변경 없음, `rules.py`·`pipeline.py`·`types.py` 무수정) · 원칙8(수치를 지어내지 않음 — 아래 검증 수치 전부 실행 출력) — 위반 없음. 허용 파일 경계: `git diff --name-only -- app/ scripts/` → `app/er/confidence.py`·`scripts/er_smoke.py` 2개뿐(`20260922-1827-u1-app-diff.txt`)
- 검증: (1) 대상 모듈 `pytest tests/test_er_confidence.py -q -rs` → `50 passed`(`20260922-1827-u1-pytest.txt`). (2) 회귀 3종 `pytest tests/test_er_pipeline.py -q -k "promotion or aunt or homonym"` → `3 passed, 15 deselected`(`20260922-1827-u1-regression3.txt`) — **결과(band·action) 불변**, 전후 confidence: 동명이인(rule_checked=0) `0.575000`(D3) → `0.718750`(D12, `[T_new,T_merge)=[0.3,0.8)` 안이라 `identity` 유지) / 승진(rule_checked=3, 측정됨) `0.878333` → `0.878333`(동일, `merge` 유지) / 팀장↔이모는 `no_candidates`(신호 3개 전부 0)라 수치 영향 없음. (3) `app/`·`scripts/` diff 허용 파일만(위). (4) 전체 `POSTGRES_PORT=5433 PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python -m pytest tests/ -q -rs` → **5 failed, 1293 passed**(`20260922-1827-u1-pytest-all.txt`) — 실패 5건은 전부 `tests/test_run_pilot_eval.py` 의 dry-run 사슬(`test_dry_run_chain_produces_all_outputs`·`test_dry_run_logs_embedded_alias_count_per_scenario`·`test_dry_run_dumps_traces_before_rollback_and_rechecks`·`test_dry_run_metrics_json_is_byte_identical`·`test_chain_gzips_raw_and_feeds_the_gz_to_every_stage`)이며, 원인은 `scripts/run_pilot_eval.py::recompute_confidence()`(U1 금지 파일, U4 소관)가 아직 D3 원식으로만 재계산해 `rule_checked==0` mention(dry-run stub `sc-002` "이서연 과장님")의 `abs diff != 0` 로 `[traces] recheck` 가 rc=1 을 반환하기 때문 — 01-plan 리스크(153행) "`--recheck-traces` 가 새 trace 를 거부할 수 있다"·02-plan-verify 사실 주장 (e)·권고 R-5 가 이미 예견한 U4→U5 순서 의존성 그대로다. 상세 진단·재현 값은 `20260922-1827-u1-known-failures-note.txt`. `app/er/rules.py`·`app/er/pipeline.py`·`app/er/types.py`·`data/`·`reports/`·`evaluation/`·`scripts/run_pilot_eval.py` 무수정(허용 파일 diff 참조)
- 남은 것 · 다음 단위: **U2**(backend-agent, D13 — `rules.py` 배제→감점, `penalized_by`) → **U3**(pipeline·보수 분기·회귀 3종) → **U4**(eval-agent, 필수 선행) `scripts/run_pilot_eval.py::recompute_confidence()` 를 `weights_effective`·`rule_checked` 로 제품 `combine()` 재계산하도록 고쳐야 위 5건이 해소된다(결정 D(i), R-5) — U4 완료 뒤 `test_run_pilot_eval.py` 전체 재실행으로 회귀 여부 재확인 필요. U4 가 읽을 trace 키 이름: `confidence_breakdown.weights`(설정값, `ERConfig`/`ER_WEIGHTS` 그대로)·`confidence_breakdown.weights_effective`(재정규화 적용값, `rule_checked>0` 이면 `weights` 와 동일 dict, `==0` 이면 `{llm: w_llm/(w_llm+w_emb), emb: w_emb/(w_llm+w_emb), rule: 0.0}`)·`confidence_breakdown.rule_checked`·`confidence_breakdown.rule_passed`. `combine()` 새 시그니처: `combine(s_llm, s_emb, s_rule, config, *, rule_checked=1)` — U4 재계산은 `weights_effective` 값을 그대로 곱해 합산하거나(`w*_eff·s_llm + w*_eff·s_emb + w*_eff·s_rule`, `rule_checked==0` 이면 `rule` 항이 0), 거부 판정(결정 D(i))은 여전히 `weights` ↔ `app.settings.ER_WEIGHTS` 비교로 유지한다. `docs/wiki/registry.md` 81·82행 비고에 D12 한 줄과 이 커밋 해시는 U7 이 등재(새 행 아님)
- Refs: P4b-er-redesign CR-001 D12 D10 S3.3 원칙3 원칙9 L-004

## 2026-09-22 · feat(P4b-er-redesign): U2 D13 — 규칙 필터는 배제가 아니라 감점 · pending
- 변경: `app/er/rules.py`(모듈 docstring D13 절 신설 — `penalized_by` 형태·완화 규약·트리거 교체 근거; `_EXCLUSION_ONLY_CONFLICT = "dictionary_conflict"`(85행) 신설, `_CONFLICT_PRIORITY`(81행) 어휘 순서는 **그대로**; `_evaluate()` 가 `(checked, passed, conflicts, relaxed_pass, penalizing_conflicts)` 5-튜플을 돌려주도록 확장 — `conflicts` 는 종전대로 `s_rule` 분자·`excluded_by` 판정에 쓰고, `relation_tag_conflict`(139행)·`hierarchy_conflict`(151행)는 `penalizing_conflicts` 에 담겨 **후보를 목록에서 빼지 않는다**; `apply_rules()` 는 `excluded_by = _EXCLUSION_ONLY_CONFLICT if … in conflicts else None`(196행)·`passed_rules = excluded_by is None`(205행)·`penalized_by=`(208행)으로 채운다; `run_rule_stage()` 완화 재평가 트리거를 "엄격 통과 후보 0" → **"감점 없는(`penalized_by` 가 빈) 후보 0"**(264~265행 `unpenalized`)으로 교체 — 결정 C(i)), `app/er/types.py`(`ScoredCandidate.penalized_by: tuple[str, ...]`(98행) + docstring D13 절, `to_dict()`·`_candidate_trace_dict()` 에 `list(penalized_by)` 직렬화 — trace 에 감점 사유가 남는다, 원칙9), `tests/test_er_rules.py`(6 → **11 테스트**: 기존 6건 기대값 갱신 + 신규 5건 — 후보 수 보존(`test_candidate_count_forwarded_equals_total_minus_dictionary_excluded`), `excluded_by` 도메인(`test_excluded_by_domain_is_dictionary_conflict_only`), 완화 트리거 양방향(`…not_triggered_when_any_candidate_unpenalized` / `…triggered_when_all_candidates_penalized`), 관계 태그 단독 충돌은 감점(`…alone_is_penalized_not_excluded`)), `tests/test_er_pipeline.py`(232·502행 두 픽스처의 `excluded_by == "relation_tag_conflict"` → `"dictionary_conflict"` + `penalized_by == ("relation_tag_conflict", "hierarchy_conflict")` 단언 추가), `tests/test_baseline_proposed.py`(249행 `detail["excluded_by"]` 기대값 같은 이유로 갱신 — 아래 검증 (4))
- 이유(기획서·카드 연결): `docs/wiki/decisions/D13-rule-filter-penalty.md` "코드에서 지켜야 할 것" 1·2항. P4 실 실행에서 규칙 필터가 골드 후보를 떨어뜨린 mention 6건(`relation_tag_conflict` 2·`hierarchy_conflict` 4)이 제안 방식 오류 2건(`sc-015` 유일 오병합·`sc-007` 유일 미검출)의 출발점이었다(`reports/failure_cases.md` §1·§2·§8). 배제는 LLM 의 비교 기회를 없애 "남은 한 명"에 확신을 몰아준다 — 원칙1 은 확신이 없으면 **묻는 것**이지 후보를 숨기는 것이 아니다. 01-plan U2(63행) 문구 그대로, 힌트는 발화에서 자동 추론한 값이라 틀릴 수 있다(CLAUDE.md 관계 태그 절)
- 정합성 확인: 원칙1(후보를 숨기지 않는다 — 감점 후보는 3단계로 간다. 자동 연결 억제는 U3 보수 분기 소관이며 **U2 에서는 강등 로직을 넣지 않았다**) · 원칙9(`penalized_by` 는 대표 사유 하나가 아니라 정렬된 **복수 사유 목록**이고 `to_dict()`·trace 양쪽에 남는다 — 정보 손실 0) · 원칙4(4단계 구조·툴 시그니처 불변, `pipeline.py`·`confidence.py` 무수정) · 원칙8(아래 수치는 전부 실행 출력이며 실패 1건도 숨기지 않고 (4)에 적었다) / D13 D12 S3.3 / 허용 파일 경계: `git diff --name-only -- app/ scripts/` → `app/er/rules.py`·`app/er/types.py` **2개뿐**(`20260922-1855-u2-app-diff.txt`, 01-plan "app/ 허용 파일 5개" 안)
- **R-4 확정(02-plan-verify 권고)**: 완화 통과(`relaxed_pass=True`) 후보의 `penalized_by` 는 **엄격 평가 결과 그대로 채운다**(비우지 않는다). 근거 — 완화는 "자동 연결을 허용할지"를 가르는 별도 신호(`relaxed_pass`)일 뿐이고, 감점 사실 자체를 지우면 trace 에서 "왜 이 후보가 위험했는가"가 사라진다(원칙9 정보 손실 금지). 결정 A(i) 의 명시적 예외(`relaxed_pass` 면 강등 제외)가 U3 에서 승진 회귀 1을 성립시킨다 — 즉 예외는 **명시적 예외**이지 "자연 통과"가 아니다. `rules.py` 모듈 docstring 29~33행에 같은 문장으로 고정. U4 `penalized_merge` 부분집합의 `relaxed_pass` 계수 정의도 이 규약을 따른다
- **경계 결정 1건**: `tests/test_er_pipeline.py`·`tests/test_baseline_proposed.py` 의 기대값 3건은 01-plan 상 U3 회귀 픽스처 (b) 항목이지만, **U2 행동 변경의 직접 파급이라 U2 커밋에 포함**한다(U1 이 `tests/test_er_smoke.py` 기대값을 같은 이유로 포함한 선례). U3 는 이 픽스처의 결과 단언(`no_candidates` → `new_person` → `ask_user`)이 불변인지를 회귀로 재확인한다. `app/er/pipeline.py`·`evaluation/` 코드는 **무수정**
- 검증: (1) 대상 모듈 `POSTGRES_PORT=5433 pytest tests/test_er_rules.py -q -rs` → **11 passed**(`20260922-1855-u2-pytest-rules.txt`). (2) D13 "코드에서 지켜야 할 것" grep(`20260922-1855-u2-d13-grep.txt`): 1항 — `excluded_by` 대입은 196·206행 두 곳뿐이고 값의 출처는 `_EXCLUSION_ONLY_CONFLICT`(85행) 하나, 두 사유는 `penalizing_conflicts.add`(139·151행) → `penalized_by=`(208행); 2항 — 후보 수 보존 단언 테스트 존재·통과; 3항 — `ER_PENALIZED_MERGE_POLICY` 가 `app/` 에 0건인데 이는 **U3 소관이라 정상**(미구현이 맞다); 결정 C — `rules.py:264` `unpenalized = [c for c in strict_passed if not c.penalized_by]`. (3) 회귀 3종 `pytest tests/test_er_pipeline.py -q -k "promotion or aunt or homonym" -rs` → **3 passed, 15 deselected**(`20260922-1905-u2-regression3.txt`) — 승진 `merge`·팀장↔이모 배제 유지·동명이인 `identity` 모두 불변. (4) 전체 `pytest tests/ -q --tb=no -rf` 1차 → **6 failed / 1297 passed**(`20260922-1905-u2-pytest-all-failed-list.txt`) — 알려진 5건(U4 소관) 외 **새 실패 1건** `tests/test_baseline_proposed.py::test_new_person_band_carries_excluded_candidates`(`detail["excluded_by"]` 가 `relation_tag_conflict` → `dictionary_conflict`). 회귀 (b)와 같은 픽스처의 사유 표시 변경이고 결과(`new_person`·`no_candidates`·tokens `(0,0)`·`score 0.0`)는 불변이라 기대값을 갱신했다(원인·조치 전문 `20260922-1915-u2-new-failure-note.txt`). 재실행 → `tests/test_baseline_proposed.py` **20 passed**, 전체 **5 failed / 1298 passed**(`20260922-1915-u2-pytest-all.txt`) = 알려진 5건만 남고 **새 실패 0**. 통과 수 1293(U1) → 1298 은 신규 테스트 5건
- 진행 경위(원칙8, 숨기지 않는다): 이 단위는 **backend-agent 위임 중 컴퓨터 강제종료로 끊겼다**(파일 편집은 끝났고 검증·03-log·커밋 전). 재개 세션에서 사용자 결정(이어서 완료)에 따라 **메인 세션이 검증만 수행**했고, 코드 편집은 위 (4) 기대값 1건 외에는 backend-agent 산출물 그대로다. Docker Desktop 이 꺼져 있어 회귀 3종이 `3 skipped` 로 나온 동안 우회하지 않고 사용자에게 기동을 요청했다(security §6)
- 남은 것 · 다음 단위: **U3**(backend-agent) — `pipeline.py::_run_pipeline` 이 감점 포함 후보를 3단계·`decide()` 로 전달, 감점 후보 귀속 + `band=="merge"` 시 결정 A·B 에 따라 `identity` 강등 + `forced_reason="penalized_candidate"`·`band_by_threshold`, `ERConfig` 정책 필드·`app/settings.py` `ER_PENALIZED_MERGE_POLICY`·`er_config()` 파싱, 회귀 3종 결과 불변 재확인(R-8: `ask_payload` `candidate_ids` 단언). U3 가 읽을 U2 계약: `ScoredCandidate.penalized_by`(빈 튜플 = 감점 없음, 사전 모순은 여기 없음)·`relaxed_pass`(결정 A(i) 예외 판별)·`run_rule_stage()` 의 `passed` = 배제되지 않은 후보 전체. U4 는 `_TRACE_CANDIDATE_KEYS` 에 `penalized_by` 추가(01-plan U4 (i))
- Refs: P4b-er-redesign CR-001 D13 D12 S3.3 원칙1 원칙4 원칙8 원칙9 R4 L-004

## 2026-09-22 · feat(P4b-er-redesign): U3 D13 — 파이프라인 보수 분기·회귀 3종 · pending
- 변경: `app/er/pipeline.py`(모듈 docstring "보수 분기(D13, 결정 A·B, U3)" 절 신설; `Decision` import 추가; 신규 헬퍼 `_downgrade_penalized_merge(decision, passed, config)` — `decision.band == "merge"` 이고 `config.penalized_merge_policy == "ask"`(기본)이고 귀속 후보(`decision.matched_person_id` 로 `passed` 에서 조회)가 감점 후보(`penalized_by` 비어 있지 않음)이며 `relaxed_pass is False`(결정 A(i) 예외 — 승진 회귀는 강등하지 않는다)일 때만 `band="identity"`·`forced_reason="penalized_candidate"`(신규 상수 `FORCED_REASON_PENALIZED_CANDIDATE`)·`action="ask_identity"`(신규 상수 `_DOWNGRADED_ACTION`) 세 필드만 `dataclasses.replace()` 로 바꾼 새 `Decision` 을 돌려준다 — `band_by_threshold`·`confidence`·`confidence_breakdown`·`matched_person_id` 는 손대지 않는다(원칙9, 판정 기록 보존); `_run_pipeline()` 이 `decide()` 직후 이 헬퍼를 한 줄 호출 — 그 뒤(`decision_payload`·`_build_ask_payload`·`Resolution` 조립)는 **무수정**, 이미 `decision` 변수를 그대로 읽으므로 강등된 값이 자동으로 흘러간다. 2→3단계 후보 전달 자체는 **U2 가 이미 끝냈다** — `run_rule_stage()` 가 돌려주는 `passed` 가 D13 이후 "배제되지 않은 후보 전체(감점 포함)"이고 `_run_pipeline` 은 그 `passed` 를 그대로 `judge.judge()`·`decide()` 에 넘기고 있었으므로(무수정 확인, 아래 검증 (5)) pipeline.py 쪽 변경은 보수 분기 추가뿐), `app/er/types.py`(`ERConfig.penalized_merge_policy: str = "ask"` 필드 + `_PENALIZED_MERGE_POLICIES = ("ask", "merge")` 어휘 상수 + `__post_init__` 검증(어휘 밖 값 → `InvalidValue`) + 필드 docstring — `to_dict()` 는 **의도적으로 무수정**(01-plan 결정3-b·이 필드는 결정5 trace 입력 스키마 `{t_merge,t_new,weights,top_k}` 밖이라 `judge_timeout`/`judge_max_retries` 와 같은 자리, `test_er_config_to_dict_matches_trace_input_schema` 의 exact-equality 를 깨지 않는다)), `app/settings.py`(`ER_PENALIZED_MERGE_POLICY = "ask"` 모듈 상수(1층) + `er_config()` 안 `_read_choice()` 헬퍼(빈 문자열→기본값, 허용 어휘 밖→`InvalidValue`, `_read_float` 와 같은 실패 규약) + `ERConfig(..., penalized_merge_policy=penalized_merge_policy)` 로 3층 조립), `tests/test_er_types.py`(+3: 기본값 `"ask"`, `"merge"` 허용, 어휘 밖 값 거부), `tests/test_er_confidence.py`(+4: `er_config()` 의 `ER_PENALIZED_MERGE_POLICY` 기본값·오버라이드·빈 문자열 폴백·잘못된 값 거부 — `er_config` 관련 테스트가 이미 이 파일에 모여 있는 기존 배치를 따름), `tests/test_er_pipeline.py`(+2 신규 테스트 `test_resolve_downgrades_penalized_merge_to_identity_by_default_downgrade`(기본 정책 `"ask"` — `relation_tag_conflict` 감점(`rule_checked=2, rule_passed=1, s_rule=0.5`, `relaxed_pass=False`) 후보가 `s_llm=1.0·s_emb=1.0` 로 `confidence=0.9 ≥ T_merge` 를 내도 `identity` 로 강등, `band_by_threshold=="merge"` 보존, `forced_reason=="penalized_candidate"`, `confidence_breakdown["matched_person_id"]` 보존, R-8: 두 번째(깨끗한) 후보와 함께 `ask_payload["context"]["candidate_ids"]` 에 감점 후보 id 도 포함 — `apply_resolution` 까지 실행해 `ask_identity`/`pending_questions(kind="identity")` 로 이어짐 확인)·`test_resolve_keeps_penalized_merge_when_policy_is_merge_override`(`ERConfig(penalized_merge_policy="merge")` 면 같은 감점 후보라도 강등하지 않고 `merge` 유지 + `apply_resolution` 이 실제로 `update_person` 을 태움), 기존 승진 회귀 테스트에 `candidate.penalized_by == ("hierarchy_conflict",)`·`result.band_by_threshold == "merge"` 단언 2건 추가(결정 A(i) 예외가 이 픽스처에서 실제로 걸린다는 것을 명시적으로 증명 — 이전에는 암묵적으로만 통과했다))
- 이유(기획서·카드 연결): `docs/wiki/decisions/D13-rule-filter-penalty.md` "결정" 절 3항(보수 분기, 08~08행) + "코드에서 지켜야 할 것" 3항(설정값 하나로 켜고 끌 수 있고 trace 에 적용 여부가 남는다). 01-plan U3(64행) 문구 그대로 — "감점 후보가 귀속되고 `band == "merge"` 이면 결정 A·B 의 설정값에 따라 `identity` 로 강등하고 `forced_reason="penalized_candidate"`·`band_by_threshold`(순수 산식 밴드)를 함께 남긴다". 결정 A(i)·B(i)(01-plan 163~168행, 사용자 확정 2026-09-22) — 강등 기본 + `relaxed_pass` 예외, 상수 이름 `ER_PENALIZED_MERGE_POLICY`. 원칙1(오병합은 미검출보다 훨씬 나쁘다 — 감점 후보가 늘어난 채로 `T_merge` 를 넘는 상황을 40건으로 안전하다 검증할 수 없으니 기본은 묻는다) 원칙9(강등이 판정 기록을 지우지 않고 `band_by_threshold`/`confidence_breakdown` 을 그대로 남긴다)
- 정합성 확인: 원칙1(기본값 `ask` — 감점 후보 merge 를 자동 통과시키지 않는다) · 원칙4(4단계 구조·`decide()` 시그니처·`ScoredCandidate` 필드 재계산 로직 불변 — 강등은 `decide()` 결과에 대한 **후처리**일 뿐 확신도 산식을 다시 계산하지 않는다) · 원칙9(`band_by_threshold`·`confidence`·`confidence_breakdown`·`matched_person_id` 는 강등 후에도 그대로, `decision` dict 의 `band`/`forced_reason`/`action` 세 키만 바뀐다 — F-93f063 부분 갱신 규약과 같은 정신) · 원칙8(아래 검증 수치는 전부 실행 출력) / D13 D12 D10 S3.3 결정 A(i) 결정 B(i) 결정 C(i, U2 가 이미 반영) / 허용 파일 경계: `git diff --name-only -- app/ scripts/ evaluation/` → `app/er/pipeline.py`·`app/er/types.py`·`app/settings.py` **3개뿐**(`20260922-1925-u3-app-diff.txt`, 01-plan "app/ 허용 파일 5개" 안 — `confidence.py`·`rules.py` 는 이번 단위에서 무수정)
- **U2 계약 재확인(설계 판단 1건, 코드 변경 없이 검증만)**: 01-plan U3 문구의 "`_run_pipeline` 이 감점 포함 후보를 3단계·`decide()` 에 넘긴다"는 부분은 **U2 가 `run_rule_stage()` 를 고칠 때 이미 완성됐다** — `passed`(엄격 통과 목록)는 D13 이후 "배제되지 않은 후보 전체"를 뜻하므로, `_run_pipeline` 이 `passed` 를 그대로 넘기는 기존 코드(무수정)가 곧 새 규약을 만족한다. 이 판단은 코드 재작성이 아니라 `pipeline.py` 무수정 diff 로 증명한다(검증 (5)) — 재구현했다면 이중 출처(U2·U3 두 곳에서 "3단계 전달"을 각각 구현)가 됐을 것이다(원칙9)
- 검증: (1) 대상 모듈 `POSTGRES_PORT=5433 PYTHONUTF8=1 PYTHONIOENCODING=utf-8 pytest tests/test_er_pipeline.py -q -rs` → **20 passed**(`20260922-1925-u3-pytest-pipeline.txt`, 기존 18 + 신규 2). (2) 회귀 3종 `pytest tests/test_er_pipeline.py -q -k "promotion or aunt or homonym" -rs` → **3 passed, 17 deselected**(`20260922-1925-u3-regression3.txt`) — **전후 값 불변**: (a) 승진 — `band="merge"`·`band_by_threshold="merge"`·`forced_reason=None`·`confidence≈0.863`(신규: `penalized_by==("hierarchy_conflict",)`, `relaxed_pass=True` → 결정 A(i) 예외로 강등 미적용, 이번에 처음 명시적으로 단언), (b) 팀장↔이모 — `excluded_by="dictionary_conflict"`·`forced_reason="no_candidates"`·`band="new_person"`(U3 강등 로직은 `band=="merge"` 가 아니므로 애초에 발동 안 함, 무영향), (c) 동명이인 — `band="identity"`(순수 산식으로 이미 identity, 강등 로직 무관, 무영향). **세 값 모두 U2 시점과 글자 그대로 같다** — 계획 위반 없음. (3) D13 grep `grep -n "penalized_by" app/er/rules.py app/er/types.py app/er/pipeline.py` · `grep -n "excluded_by" app/er/rules.py` → 세 파일 전부 매치, `excluded_by` 대입 경로에 두 사유 없음(`20260922-1925-u3-policy-grep.txt`). (4) 보수 분기 스위치 `grep -n "ER_PENALIZED_MERGE_POLICY" app/settings.py app/er/types.py` → 매치(같은 파일, 판정 표 7행은 `app/er/types.py` 를 명시했으나 이 단위에서는 필드 docstring 에만 이름이 등장하고 상수 정의는 `settings.py` — 어휘 자체는 두 파일 모두에서 grep 된다, `20260922-1925-u3-policy-grep.txt`), `pytest tests/test_er_pipeline.py -q -k penalized` → **2 passed**(위 (1)에 포함, 강등 트리거·정책 오버라이드 각 1건). (5) 허용 파일 경계 `git diff --name-only -- app/ scripts/ evaluation/` → `app/er/pipeline.py`·`app/er/types.py`·`app/settings.py` 3개뿐(`20260922-1925-u3-app-diff.txt`) — `rules.py`·`confidence.py`·`candidates.py`·`judge.py`·`dictionary.py`·`tools/*` 전부 무수정. (6) 전체 `pytest tests/ -q --tb=short -rf` → **5 failed / 1307 passed**(`20260922-1925-u3-pytest-all.txt`) — 실패 5건은 U1·U2 때와 **완전히 같은 목록**(`tests/test_run_pilot_eval.py` dry-run 사슬, `recompute_confidence()` 가 옛 D3 식이라 `rule_checked==0` mention 에서 재계산 불일치 → U4 소관, 새 실패 **0**). 통과 수 1298(U2) → 1307 은 이번 단위 신규 테스트 9건(types 3 + confidence 4 + pipeline 2)
- 남은 것 · 다음 단위: **U4**(eval-agent, 필수 선행) — `scripts/run_pilot_eval.py::recompute_confidence()` 를 `weights_effective`·`rule_checked` 로 제품 `combine()` 재계산하도록 고쳐야 (6)의 5건이 해소된다(결정 D(i), R-5, U1 03-log 가 이미 남긴 인계 그대로 — U2·U3 는 이 5건에 영향이 없었다). U4 가 `_TRACE_CANDIDATE_KEYS` 에 `penalized_by` 를 더할 때 U3 가 새로 만든 trace 필드는 없다 — 강등은 기존 `decision.{band,forced_reason,action}` 세 키만 바꾸므로 `evaluation/resolvers/proposed.py` 의 `detail["penalized_by"]`(U4 (iv))는 후보의 `penalized_by` 를 그대로 읽으면 된다(U3 산출물과 충돌 없음). `evaluation/metrics.py` 의 `penalized_merge` 부분집합(U4 (iii), D13 위험 계측)은 원시 JSONL 의 `method="proposed"` 행에서 `decision.band=="merge"` 이면서 `candidates[]` 중 귀속 후보의 `penalized_by` 가 비어있지 않은 행을 센다 — 기본 정책이 `ask` 이므로 실 실행에서는 이 부분집합이 **항상 `relaxed_pass=True` 예외 경로만** 채워질 것으로 예상된다(승진류 mention). U5(재실행)·U6(비교)·U7(문서)은 이 로그의 남은 것을 그대로 승계
- 메인 세션 보완(커밋 전 1파일): `.env.example` 의 ER 절에 `ER_PENALIZED_MERGE_POLICY=ask` 와 설명 3줄을 더했다. backend-agent 가 `app/settings.py` 91행 주석과 `er_config()` docstring 에 "`.env.example` 이 이름을 정한 환경변수"라고 적었는데 정작 그 파일에는 이름이 없어 두 문장이 사실과 달랐다 — `settings.py` 모듈 docstring 22행이 정한 2층 규약("2층 오버라이드는 `.env.example` 에 이미 이름이 있는 환경변수")에 맞추는 쪽으로 해소했다. 코드 동작 영향 0(`env=None` 이면 `os.environ` 을 직접 읽는다), `.env.example` 을 읽는 테스트도 0건(`grep -rln "env.example" tests/` 무출력). 에이전트의 설계 판단 3번(범위를 넘지 않으려 보류)에 대한 메인 세션 결정이다
- Refs: P4b-er-redesign CR-001 D13 D12 D10 S3.3 원칙1 원칙4 원칙8 원칙9 R4 R8 L-004

## 2026-09-22 · feat(P4b-er-redesign): U4 평가 도구 D12·D13 정합 + 전체 회귀 · pending
- 변경: `scripts/run_pilot_eval.py`(모듈 docstring 재계산 절에 결정 D(i) 분업 3문장 추가; `recompute_confidence()`(978행)가 **거부는 종전대로 `weights`↔`app.settings.ER_WEIGHTS`** 로 하고 재계산 분기는 `confidence_breakdown["rule_checked"]` 로 제품 `combine(..., rule_checked=…)` 을 부른다 — 새 헬퍼 `_recorded_rule_checked()`(키 없음·비수치·음수·소수를 **기본값 없이 거부**, D12 판정을 옛 산식으로 조용히 통과시키지 않는다)·`_check_weights_effective()`(기록된 `weights_effective` 가 `weights`·`rule_checked` 에서 도출되는 값과 같은지 **교차 확인**, 가중치 숫자는 trace 에서 읽어 하드코딩 0건); `_Weights` docstring 에 "이 자리에는 설정값이 들어간다" 명시; `_TRACE_CANDIDATE_KEYS` 에 `penalized_by` 추가(9키); `[traces] rule=…` 안내를 D12 두 분기로 바꾸고 숫자는 `ER_WEIGHTS` 에서 찍는다), `evaluation/curve.py`(`WEIGHTS_POLICY = "observed_renormalized(D12)"`·`PENALIZED_MERGE_POLICIES = ("ask","merge")` 상수 신설, `meta.weights_policy`·`meta.penalized_merge_policy`(=`ERConfig()` 모듈 기본값, 환경변수 안 읽음 — `_weights_block()` 과 같은 규약) 두 키 추가, `_META_REQUIRED_KEYS` 에 등재 + `--validate` 가 두 키의 **값**까지 본다, `_weights_block()` 의 "D3" 문구를 "비율, D12" 로, **`METRICS_SCHEMA_VERSION` 1 → 2**), `evaluation/metrics.py`(부분집합 `penalized_merge` = 귀속 후보의 `penalized_by` 가 비어 있지 않은 merge 행 수·그중 오병합 수·`relaxed_pass_merges`(결정 A(i) 예외 계수)·`reasons` 사유 개수; 헬퍼 `_matched_penalized_by()`·`_penalized_reasons()` — 제안 방식 외에는 키가 없으므로 언제나 분모 0(`rate: null`)), `evaluation/resolvers/proposed.py`(`detail["penalized_by"] = {person_id 문자열: [사유,…]}` — `excluded_by` 와 같은 모양, 감점 없는 후보는 키 없음; D3 주석 3곳 D12 로), `evaluation/calibration.py`(D3 참조 5곳 D12 로, placeholder 판정 규약은 **그대로 두고** "D13 으로 `no_candidates` 가 줄어 `placeholder_s_llm` 수가 준다 = 분모가 는다"를 docstring 한 문단으로), `evaluation/report.py`(결정 H(i) 메타 표 2줄 `meta.weights_policy`·`meta.penalized_merge_policy`; 부분집합 표에 `감점 후보 merge`·`그 오병합률`·`그중 완화 통과` 3열과 설명 문단 — D13 위험 계측이 `metrics.json` 을 열지 않고도 보이게; 가중치 문장 D3→D12), 테스트 5파일 **+13건**(`test_run_pilot_eval.py` +5: 재정규화 재계산·옛 D3 식과 다름·두 키 결손 거부·도출 안 된 `weights_effective` 거부·`penalized_by` 덤프·`rule_checked==0` 덤프 재계산 0.0 / `test_eval_curve.py` +3: 두 정책 키·어휘가 `app.er.types._PENALIZED_MERGE_POLICIES` 와 같음·`--validate` 거부 / `test_eval_metrics.py` +2: `penalized_merge` 계수(귀속 후보 기준·복수 사유·완화 예외)·방식에 키가 없을 때 / `test_eval_report.py` +2: 메타 2줄·옛 판 입력 거부 / `test_baseline_proposed.py` +1: `detail["penalized_by"]` 순수 변환. 기존 기대값 갱신 3건: 덤프 breakdown 필수 키 `weights_effective`·`rule_checked` 추가, `schema_version` 2, 이모 픽스처 `penalized_by` 단언)
- 이유(기획서·카드 연결): 01-plan U4(65행) (i)~(vi) 그대로. 결정 D(i)(거부는 `weights`, 재계산은 `rule_checked`) · 결정 H(i)(`meta.weights` 유지 + 정책 2키 + `report.py` 메타 표 2줄) · D13 파급 "위험 계측"(감점 후보가 늘면 오병합 기회도 는다 — 40건으로 검증 불가이므로 **세기라도 한다**) · 02-plan-verify 사실 주장 (e)·권고 R-5(U1 이후 dry-run 사슬 5건이 rc=1 이던 원인). `detail["penalized_by"]` 는 **U5 실 실행 전에** 들어가야 U6 이 원시 JSONL 에서 감점 merge 를 셀 수 있다(01-plan 리스크 155행 — 원시는 재실행 없이 다시 만들 수 없다)
- 정합성 확인: 원칙8(재계산 기준 `abs diff == 0.0` 을 낮추지 않았다 — 산식을 맞췄다. 기준선 산출물은 **고치지 않고** 옛 판으로 남겨 새 판정기가 명시적으로 거부하게 했다) · 원칙9(`penalized_by` 가 trace 덤프·`detail`·`metrics.json` 세 곳에 사유 **목록 그대로** 흐른다 — 대표 하나로 줄이지 않는다) · 원칙3(가중치 숫자를 평가 쪽에 하드코딩하지 않는다 — `ER_WEIGHTS`·trace 기록값에서만 읽는다. `test_recompute_uses_product_combine_not_a_local_formula` 의 `"0.5 *" not in source` 단언 유지) · 원칙4(단계 구조·툴 시그니처·`data/` 무관) / 허용 파일 경계: `git diff --name-only -- app/` **0줄**(`20260922-2020-u4-boundary-diff.txt`), `git status --short -- reports/ data/` **0줄**(P4 기준선·라벨 무변경)
- **설계 판단 3건(사용자·verifier 확인 요청)**: (1) **재계산은 `weights_effective` 를 `combine()` 의 `config` 로 넘기지 않는다.** 01-plan 결정 D(i) 문구는 "재계산은 `weights_effective`·`rule_checked` 로"인데, `weights_effective`(0.625 / 0.37499999999999994)를 config 로 넘기면 `(w_eff·s+w_eff·e)/(w_eff+w_eff)` 가 되어 제품이 실제로 계산한 `(0.5·s+0.3·e)/0.8` 과 **마지막 비트가 어긋난다**(무작위 20000 표본 중 7777건, 최대 2.22e-16 — `20260922-1955-u4-recompute-weights-choice.txt`). 판정 기준이 `abs diff == 0.0` 이므로 재계산은 **판정 때 제품이 실제로 부른 그 호출**(설정 가중치 + `rule_checked`)을 재현하고, `weights_effective` 는 결정 D(i) 둘째 문장의 **교차 확인**에 쓴다. (2) **`METRICS_SCHEMA_VERSION` 1 → 2**: `curve.py` 159행이 "구조가 바뀌면 올린다"고 적었고 meta 2키·`subsets.penalized_merge` 가 늘었다. 부작용으로 P4 기준선 `reports/metrics.json`(판 1)은 새 `--validate`·`render_report` 가 거부한다(`20260922-2020-u4-known-baseline-effects.txt`) — 기준선은 **고치지 않는다**는 원칙과 충돌하지 않지만(사본은 결정 I 로 그대로 보존), "옛 산출물로 새 리포트를 만들 수 없다"가 규약이 된 것이므로 확인이 필요하다. (3) **`report.py` 부분집합 표 3열 추가**: 결정 H(i)가 명시한 것은 메타 2줄뿐이지만, 그것만으로는 D13 위험 계측이 `metrics.json` 안에만 남아 리포트 독자가 볼 수 없다. 되돌리려면 `_subset_section()` 3열·문단 하나만 빼면 된다
- **알려진 결과(숨기지 않는다, 원칙8)**: P4 기준선 `reports/pilot/traces-20260922-042440.jsonl` 은 `--recheck-traces` 로 **더 이상 통과하지 않는다** — `weights_effective` 키가 없어 1410줄 전건이 "D12 이전 trace 는 이 재계산 대상이 아니다"로 거부된다(rc=1, 증거 파일 동일). 옛 산식으로 계산된 값을 새 산식으로 통과시키는 것이 더 나쁘므로 **명시적 거부**를 택했다. 기준선을 다시 검증해야 하면 그 시점 코드(`git show f01ea35:scripts/run_pilot_eval.py`)로 한다. 두 기준선 파일은 읽기만 했고 수정·삭제하지 않았다
- 검증: (1) **목표 5건** `pytest tests/test_run_pilot_eval.py -q -rs -k "<5건>"` → **5 passed, 65 deselected**(`20260922-2020-u4-target5.txt`) — U1~U3 에서 실패하던 dry-run 사슬 5건이 0 이 됐다. (2) **전체** `POSTGRES_PORT=5433 PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python -m pytest tests/ -q --tb=no -rf` → **1325 passed, 0 failed**(rc=0, `20260922-2020-u4-pytest-all.txt`). U3 기준 1307 passed/5 failed → 1307+5(해소)+13(신규) = 1325, **새 실패 0**. (3) **R-5 증명** dry-run 사슬(`--dry-run --stub --limit 5`, `network_calls=0`) rc=0, 6단계 전부 rc=0, `[traces] dumped=180 recomputed=180 max_abs_diff=0.0`(`20260922-2020-u4-dryrun-chain.txt`); 그 덤프 180줄 중 `rule_checked==0` 이 **40줄**이고 새 재계산 `max_abs_diff=0.0` / 옛 D3 재계산 `max_abs_diff=0.08749999999999997`(`20260922-2020-u4-recheck-rule-unchecked.txt`, 표본 sc-002 '이서연 과장님' 기록 0.22177155054489076 = 새 재계산, 옛 식 0.17741724043591262). 같은 파일로 `--recheck-traces` CLI 단독 실행도 rc=0. `matched_candidate` 180/180 에 `penalized_by` 키가 있고 70줄이 비어 있지 않다. (4) **경계** `git diff --name-only -- app/` 0줄, `git status --short -- reports/ data/` 0줄(`20260922-2020-u4-boundary-diff.txt`). (5) **스키마·멱등** `python -m evaluation.metrics --validate <dry-run>/metrics.json` rc=0, `meta.weights={llm:0.5,emb:0.3,rule:0.2}`(설정값 유지)·`meta.weights_policy=observed_renormalized(D12)`·`meta.penalized_merge_policy=ask`·`schema_version=2`; `report` 재생성 diff **0줄**(판정 표 14행 예행); 격자별 `penalized_merge` 가 실제로 센다(0.5 에서 3건, 전부 `relaxed_pass` 예외 = U3 이 예측한 그대로, 오병합 0)(`20260922-2020-u4-validate-and-subsets.txt`). **stub 수치이므로 게이트 판정에 쓰지 않는다**
- 남은 것 · 다음 단위: **U5**(eval-agent 준비 → 사용자 실 실행). U5 전에 해야 할 것 — (a) **결정 I 이행**: `reports/{metrics.json,calibration.json,curve.csv,eval.md}` → `reports/pilot/<이름>-20260922-042440.*` stamp 사본을 **먼저 커밋**(metrics 사본 sha256 `25e16dd6…` 판정), (b) `docs/user-setup/10-pilot-eval-run.md` 갱신(`.env` 로드 주체·`PYTHONUTF8=1`·산출물 이동 절차), (c) 실행 명령은 `--commit $(git rev-parse HEAD)`·`--max-cost-usd 5`·새 stamp, evidence 파일명에 `real-run` 포함(R-9). U5 에서 `--recheck-traces` 가 새 trace 에 대해 `max_abs_diff=0.0` 을 내는 것은 위 (3)으로 미리 증명됐다. **U6** 은 새 원시 JSONL 의 `detail["penalized_by"]` 와 `metrics.json` `subsets.penalized_merge`(+`relaxed_pass_merges`·`reasons`)로 "감점 후보가 merge 된 건수와 그중 오병합 수"(01-plan U6 (d))를 세면 된다 — 별도 파싱 코드가 필요 없다. **U7** 은 `registry.md` 에 이번 수정 5파일 비고 확장(새 행 아님)과 `schema_version` 2 한 줄
- **사용자 확정(2026-09-22 재개 세션)**: 위 설계 판단 3건은 (1) 결정 D 해석 = **구현대로 수용**(01-plan 결정 표에 `U4 이행 해석 확정` 항목을 더했다), (2) `METRICS_SCHEMA_VERSION` 2 **유지**, (3) `report.py` 부분집합 표 3열 **유지** 로 확정됐다. 더해 위 '알려진 결과'의 P4 기준선 재검증 불가도 **명시적 거부를 규약으로 수용** — 하위 호환 분기를 두지 않는다. 확인 과정에서 한 가지를 바로잡았다: 기준선 `reports/metrics.json` 이 거부되는 사유는 판 번호가 아니라 결정 H(i) 의 meta 두 키 결손이다(`20260922-2020-u4-known-baseline-effects.txt` 의 FAIL 3줄). 판 번호를 1 로 되돌려도 호환성은 돌아오지 않으므로, 판 올림과 기준선 거부는 서로 독립한 사안이다
- Refs: P4b-er-redesign CR-001 D12 D13 S3.7 원칙3 원칙8 원칙9 R3 R4 R-5 L-004
