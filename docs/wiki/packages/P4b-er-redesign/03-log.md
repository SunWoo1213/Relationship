# P4b-er-redesign · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-22 · docs(P4b-er-redesign): 계획검증 통과·계획 승인 — CR-001 D12·D13 구현 + 재실행 + 게이트 재판정, 패키지 착수 · 83d33dd
- 변경: `01-plan.md`(architect 초안 → 메인 세션 형식 2건(작업 단위 접두·해석 불릿 들여쓰기) + "## 결정 (사용자 확정 2026-09-22)" 표 + `ER_PENALIZED_MERGE_POLICY` 치환 + 권고 R-3(`scripts/er_smoke.py:109` 산출물·U1 명시)·R-9(`*real-run*` 파일명 규약) 반영), `02-plan-verify.md`(verifier, `결과: 통과`, 승인 줄), `05-remediation.md`(findings.py [권고] 22 — registry 기존 행 18 + basename 오탐 4, [필수] 0), `evidence/`(verify-plan 1~3차 + verifier 1710·1718 + `1713-regression3-baseline.txt` 3 passed + `.gitkeep`), `docs/backlog.md` 65행(`types.py`(+`settings.py`) 허용 파일 — 개정 (2) 반영, (1) 거절), `docs/wiki/changes/CR-001.md` 36행(기준선 = stamp 사본, 결정 I), `CURRENT.md` active + 메모, journal DECISION·VERIFY·START, HANDOFF. 코드·`data/`·`reports/` 변경 **0**
- 이유(기획서·카드 연결): CR-001 §4 4항 "수정은 `P4b-er-redesign` 패키지 안에서". backlog "### P4b — 게이트 재도전 (CR-001)" 65·66행 = 01-plan 72·73행(diff 0줄). D12·D13 "코드에서 지켜야 할 것"이 U1~U3 의 문장이고, 결정 K(P4 01-plan 227행)·S3.7 게이트 식은 불변(결정 F). 사용자 결정 A~I(권장 조합, 18:20)
- 정합성 확인: 원칙 1(A(i) 강등)·3(D12 = CLAUDE.md 38행)·4·8(재실행 1회·기준선 stamp 사본)·9(weights_effective·penalized_by) / D12 D13 D10 / S3.3 S3.7 / 보안(`.env` 로드 주체 사용자 셸) — 위반 없음(02-plan-verify 점검표 8/8, 사실 주장 (a)~(f) 6/6). 코드 변경 0
- 남은 것 · 다음 단위: **U0**(메인 세션, R-7: `.claude/skills/entity-resolution/SKILL.md` 22·36·39행 D12·D13 정합 + "권위는 S3.3·D12·D13" 한 줄, CR-001 §2 파일 목록 갱신 이력 한 줄) → **U1**(backend-agent, L-004 승인 후): `combine()` 재정규화·`_breakdown` `weights_effective`/`rule_checked`·강제 경로·`er_smoke.py:109` + 테스트. U4→U5 순서 필수(recheck 가 새 trace 거부). R-4(U2 03-log 에 완화 통과 후보 `penalized_by` 채움 여부 확정)·R-5(U4 rule_checked=0 재계산 테스트)·R-8(U3 ask_payload candidate_ids 단언)·R-6(04-review 에 P4 04-review 220행 대체 한 줄)·R-2(하네스 L-nnn 후보: verify-plan 5번 불릿 의존 파싱)
- Refs: P4b-er-redesign CR-001 D12 D13 D10 S3.3 S3.7 R3 R4 원칙1 원칙3 원칙8 원칙9 L-002 L-004

## 2026-09-22 · harness(P4b-er-redesign): U0 entity-resolution 스킬 카드 D12·D13 정합 — 권위 문구·감점·재정규화 산식 · 28c2de8
- 변경: `.claude/skills/entity-resolution/SKILL.md` — 머리에 "권위는 S3.3·D12·D13·D10, 이 카드는 미러" 한 줄(R-7 이중 출처 방지), 22행 2단계 "명백히 다른 후보 배제" → 감점·후보 유지(사전 모순만 배제, D13), 36행 `s_rule` 미측정 규약(D12), 39행 산식 → `Σ w_i·s_i / Σ w_i` + 두 경우 전개 + `weights_effective`·보수 분기(`ER_PENALIZED_MERGE_POLICY=ask`, `relaxed_pass` 예외) 문단. `docs/wiki/changes/CR-001.md` 갱신 이력(§2 파일 목록에 `types.py`·`er_smoke.py`·SKILL.md). 이 로그(직전 `pending`→83d33dd). 코드 변경 **0**
- 이유(기획서·카드 연결): 01-plan U0 — backend-agent 가 자동 참조하는 카드가 D3 산식·배제 문장을 그대로 갖고 있으면 U1~U3 이 옛 규약으로 구현된다(02-plan-verify 사실 주장 (d): `D12|D13|CR-001|감점|penalized` grep 0건). verifier 분류(R-7): 위치는 하네스 파일이나 S3.3 3행이 "절차 상세"로 가리키는 명세 미러 → 하네스 문서 정합 커밋(메인 세션). 문장은 D12 6~8행·D13 5~8행·P4b 결정 A(i)·B(i) 를 옮겼다
- 정합성 확인: 원칙3(새 문장 = CLAUDE.md 38행 = D12)·원칙1(강등 기본)·원칙9 / D12 D13 D10 / S3.3 / 보안 — 위반 없음. 판정: `grep -nE "D12|D13|penalized|weights_effective" .claude/skills/entity-resolution/SKILL.md` ≥ 4건, `grep -c "0.5·s_llm + 0.3·s_emb + 0.2·s_rule" SKILL.md` = 1(세 신호 모두 관측 경우의 전개로만 남음)
- 남은 것 · 다음 단위: **U1** backend-agent 위임 승인(L-004) — `combine()` 재정규화·`_breakdown` `weights_effective`/`rule_checked`·강제 경로 키·`er_smoke.py:109` + 테스트(`test_er_confidence.py` 293행 원식 수치 유지, 미측정 케이스 추가)
- Refs: P4b-er-redesign CR-001 D12 D13 S3.3 원칙3 원칙9 L-004

## 2026-09-22 · feat(P4b-er-redesign): U1 D12 — 관측 신호 재정규화 결합 · pending
- 변경: `app/er/confidence.py`(`combine()` 이 `rule_checked: int = 1`(키워드 전용) 인자를 받아 `rule_checked > 0` 이면 D3 원식 그대로, `rule_checked == 0` 이면 `(w_llm*s_llm + w_emb*s_emb)/(w_llm+w_emb)` 로 재정규화 — 두 분기 모두 옛 D3 연산 순서를 그대로 유지해 부동소수 오차 없이 동치; 새 헬퍼 `_effective_weights(rule_checked, config)` 를 `combine()` 과 **같은 조건**으로 분기해 이중 출처를 막음; `_breakdown()` 에 `weights_effective` 키 추가(기존 `weights`·`rule_checked`·`rule_passed` 키는 그대로); `decide()` 정상 경로가 `combine(..., rule_checked=candidate.rule_checked)` 로 호출부만 갱신 — `_forced_decision`/`no_candidates` 두 강제 경로는 이미 `rule_checked=0` 을 `_breakdown()` 에 넘기고 있어 `weights_effective` 가 자동으로 재정규화 값을 받음, `confidence` 는 세 신호가 전부 0 이라 산식이 무엇이든 종전대로 0.0; 모듈·"관측된 신호만 재정규화한다 (D12)" 절 신설), `scripts/er_smoke.py`(109행 `combine()` 호출에 `rule_checked=0` 추가 — 스모크는 2단계 규칙 필터를 거치지 않아 hints 없는 호출과 같음, 권고 R-3; 출력 JSON 키 불변), `tests/test_er_confidence.py`(+21 테스트: (i) 세 신호 관측 시 D3 원식과 `abs diff == 0` 무작위 표본 200건 seed 고정, (ii) `rule_checked=0` → `0.625*s_llm+0.375*s_emb`·`weights_effective` 확인(s_rule 값을 바꿔도 결과 불변까지), (iii) `rule_checked>0, rule_passed=0` → `s_rule=0` 그대로 합산(재정규화 아님)·`weights_effective == weights`, (iv) 정상·강제(llm_failed·no_candidates·out_of_range_id) 네 경로 모두 `weights`·`weights_effective`·`rule_checked`·`rule_passed` 키 존재, (v) 293행 원식 수치 재확인, (vi) `weights_effective` 합계 1.0(측정·미측정 양쪽), `_effective_weights()` 직접 import 교차검증), `tests/test_er_smoke.py`(`test_run_smoke_merge_band_matches_combine` 기대값을 D3 `0.8634` → D12 `0.9125`(rule_checked=0 이므로 `s_rule=0.667` 이 결과에 반영되지 않음)로 갱신 — er_smoke.py 호출부 변경의 직접 파급, band는 `merge`로 불변)
- 이유(기획서·카드 연결): `docs/wiki/decisions/D12-observed-signal-renormalization.md` "코드에서 지켜야 할 것" 4항(23~27행) — `rule_checked` 입력·재정규화 공식·`weights`/`weights_effective`/`rule_checked` 키·`rule_passed==0` 비재정규화·회귀 3종 유지. 01-plan U1(61행) 문구 그대로. P4 04-review §4b·failure_cases.md §4·§8 이 근본 원인으로 지목한 "미측정 `s_rule=0` 합산이 확신도 상한을 0.80 에 묶었다"(rule_checked==0 이 141 중 100건)를 해소하는 구현
- 정합성 확인: 원칙3(CLAUDE.md 38행 = D12 6~8행 = 모듈 docstring 신설 절, 산식 문자열 동일) · 원칙9(`weights`·`weights_effective`·`rule_checked`·`rule_passed` 네 키가 정상·강제 경로 모두에 있다, 이중 출처 금지 — `_effective_weights()` 가 `combine()` 과 같은 조건문을 씀) · 원칙4(단계 구조·시그니처 변경 없음, `rules.py`·`pipeline.py`·`types.py` 무수정) · 원칙8(수치를 지어내지 않음 — 아래 검증 수치 전부 실행 출력) — 위반 없음. 허용 파일 경계: `git diff --name-only -- app/ scripts/` → `app/er/confidence.py`·`scripts/er_smoke.py` 2개뿐(`20260922-1827-u1-app-diff.txt`)
- 검증: (1) 대상 모듈 `pytest tests/test_er_confidence.py -q -rs` → `50 passed`(`20260922-1827-u1-pytest.txt`). (2) 회귀 3종 `pytest tests/test_er_pipeline.py -q -k "promotion or aunt or homonym"` → `3 passed, 15 deselected`(`20260922-1827-u1-regression3.txt`) — **결과(band·action) 불변**, 전후 confidence: 동명이인(rule_checked=0) `0.575000`(D3) → `0.718750`(D12, `[T_new,T_merge)=[0.3,0.8)` 안이라 `identity` 유지) / 승진(rule_checked=3, 측정됨) `0.878333` → `0.878333`(동일, `merge` 유지) / 팀장↔이모는 `no_candidates`(신호 3개 전부 0)라 수치 영향 없음. (3) `app/`·`scripts/` diff 허용 파일만(위). (4) 전체 `POSTGRES_PORT=5433 PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python -m pytest tests/ -q -rs` → **5 failed, 1293 passed**(`20260922-1827-u1-pytest-all.txt`) — 실패 5건은 전부 `tests/test_run_pilot_eval.py` 의 dry-run 사슬(`test_dry_run_chain_produces_all_outputs`·`test_dry_run_logs_embedded_alias_count_per_scenario`·`test_dry_run_dumps_traces_before_rollback_and_rechecks`·`test_dry_run_metrics_json_is_byte_identical`·`test_chain_gzips_raw_and_feeds_the_gz_to_every_stage`)이며, 원인은 `scripts/run_pilot_eval.py::recompute_confidence()`(U1 금지 파일, U4 소관)가 아직 D3 원식으로만 재계산해 `rule_checked==0` mention(dry-run stub `sc-002` "이서연 과장님")의 `abs diff != 0` 로 `[traces] recheck` 가 rc=1 을 반환하기 때문 — 01-plan 리스크(153행) "`--recheck-traces` 가 새 trace 를 거부할 수 있다"·02-plan-verify 사실 주장 (e)·권고 R-5 가 이미 예견한 U4→U5 순서 의존성 그대로다. 상세 진단·재현 값은 `20260922-1827-u1-known-failures-note.txt`. `app/er/rules.py`·`app/er/pipeline.py`·`app/er/types.py`·`data/`·`reports/`·`evaluation/`·`scripts/run_pilot_eval.py` 무수정(허용 파일 diff 참조)
- 남은 것 · 다음 단위: **U2**(backend-agent, D13 — `rules.py` 배제→감점, `penalized_by`) → **U3**(pipeline·보수 분기·회귀 3종) → **U4**(eval-agent, 필수 선행) `scripts/run_pilot_eval.py::recompute_confidence()` 를 `weights_effective`·`rule_checked` 로 제품 `combine()` 재계산하도록 고쳐야 위 5건이 해소된다(결정 D(i), R-5) — U4 완료 뒤 `test_run_pilot_eval.py` 전체 재실행으로 회귀 여부 재확인 필요. U4 가 읽을 trace 키 이름: `confidence_breakdown.weights`(설정값, `ERConfig`/`ER_WEIGHTS` 그대로)·`confidence_breakdown.weights_effective`(재정규화 적용값, `rule_checked>0` 이면 `weights` 와 동일 dict, `==0` 이면 `{llm: w_llm/(w_llm+w_emb), emb: w_emb/(w_llm+w_emb), rule: 0.0}`)·`confidence_breakdown.rule_checked`·`confidence_breakdown.rule_passed`. `combine()` 새 시그니처: `combine(s_llm, s_emb, s_rule, config, *, rule_checked=1)` — U4 재계산은 `weights_effective` 값을 그대로 곱해 합산하거나(`w*_eff·s_llm + w*_eff·s_emb + w*_eff·s_rule`, `rule_checked==0` 이면 `rule` 항이 0), 거부 판정(결정 D(i))은 여전히 `weights` ↔ `app.settings.ER_WEIGHTS` 비교로 유지한다. `docs/wiki/registry.md` 81·82행 비고에 D12 한 줄과 이 커밋 해시는 U7 이 등재(새 행 아님)
- Refs: P4b-er-redesign CR-001 D12 D10 S3.3 원칙3 원칙9 L-004
