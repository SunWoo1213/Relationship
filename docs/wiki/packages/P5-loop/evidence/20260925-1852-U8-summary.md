# P5-loop U8 · 판정 방법 표 실행 요약

> 실행: 2026-09-25 18:52 (백엔드 실행 도구, DB `capstone2-postgres-1` 컨테이너 재사용 · `POSTGRES_PORT=5433`)
> 각 행의 명령·출력 원문은 같은 폴더의 `20260925-1852-U8-row<N>-*.txt` 파일이다.
> 실 LLM·임베딩 API 호출은 하지 않았다(계획이 지시한 대로 `FakeProposer`·`FakeJudge`·스텁 임베더만 사용, 셸에 `OPENAI_API_KEY`·`ANTHROPIC_API_KEY`·`GEMINI_API_KEY` 미설정 확인).

| # | 명령(요지) | 기대 출력(01-plan) | 실제 결과 | 일치 |
|---|---|---|---|---|
| 1 | 전체 `pytest tests/ -q -rs` | `1325 + 신규` 전부 통과, 실패 0·skip 0 | `1474 passed`, 실패 0·skip 0 | 일치 |
| 2 | `test_agent_propose/gate/loop/resume/api_chat` 네트워크 0 | 전부 통과, 실 LLM·임베딩 호출 0 | `127 passed`(파일명 보정 — `test_agent_resume.py` → 실제 `test_api_answers_resume.py`) | 보정 후 일치 |
| 3 | `test_api_chat.py -k "one_flow or end_to_end"` | 통과, `events` +1, `reply` 비어있지 않음 | `-k` 매치 0건(그런 이름의 테스트 없음). 가장 근접한 `test_chat_stores_event_and_replies_in_one_request` 1건 실행 → 통과 | **불일치**(이름), 기능은 통과 |
| 4 | 재개 서브셋 `-k "resume"` | 통과, `persons`/`events` 행 증가, 두 번째 재개 409 | `11 passed`(파일명 보정) | 보정 후 일치 |
| 5a | `grep -B1 "create_person\(" app/agent/` | 1줄, 앞줄 `confirmed_question_id` 대입 | 정확히 1줄, 앞줄이 `ctx.confirmed_question_id = question_id` | 일치 |
| 5b | `update_person(` 뒤 4줄 `display_name` / 호출 목록 | ① 0건 ② 허용 2곳 | ① 0건 ② `loop.py:771`(기록 단계)·`loop.py:1188`(identity 재개) 2곳 | 일치 |
| 6 | `grep "T_merge\|t_merge\|T_new\|t_new\|confidence" app/agent/` | 0건 | 0건 | 일치 |
| 7 | 7행 스크립트 (가)SEED=0 (나)SEED=1 | 줄1/2/3/4 상세(01-plan 127행) | (가)(나) 모두 행 목록·gate/record/pending_calls/question JSON·executed 대조·등식 `True` **전부 정확히 일치**(새 인물 옵션 6개, 시드 merge 시 `executed`에 `add_event` 1건) | 일치 |
| 8 | 수동 curl 왕복(uvicorn) | `question_id`·`options`·`session_id` 등 | **미실행** — 01-plan 이 이 행을 "수동 왕복(**사용자**)"로 명시했고, 셸에 실 LLM 키가 없어(`get_proposer()`가 `None`이면 운영 경로가 `proposer_from_env()`로 실제 공급자를 호출) 정상 진행 시 실제 OpenAI 호출을 시도하게 된다 — 과제 지시("실제 API 호출을 요구하면 실행하지 말고 보고")에 따라 보류 | **미실행(보고)** |
| 9 | `grep "time.sleep\|asyncio.sleep\|while True" app/agent/ app/api/` | 0건 | 0건 | 일치 |
| 10 | `git diff --name-only 1227026..HEAD -- app/` | 허용 파일 부분집합만 | `app/agent/*` 6개 + `app/api/{deps,routes,schemas}.py`·`app/settings.py` = 정확히 허용 목록, `app/er`·`app/tools`·`app/db`·`app/main.py` 없음 | 일치 |
| 11 | `alembic check` | `No new upgrade operations detected.` | 동일 | 일치 |
| 12 | `tools_check.py` | `7/7 ok` | `RESULT: 7/7 ok` | 일치 |
| 13 | ER 회귀 3종 | `3 passed` | `3 passed, 17 deselected` | 일치 |
| 14 | `git diff --name-only 1227026..HEAD -- data/ reports/` | 0줄 | 0줄(빈 출력) | 일치 |
| 15 | 비밀 미노출 grep | 이름+값 함께 찍는 코드 0건 | 매치는 전부 `ANTHROPIC_MODEL`/`OPENAI_MODEL`/`GEMINI_MODEL` **이름** 참조(`os.environ.get`, 주석, 안내 문구)뿐 — 값과 함께 찍는 코드 0건 | 일치(육안 판정 필요) |
| 16 | `grep "app/agent/" registry.md` | 새 모듈 행 전부 | 6개 모듈 행 전부 확인 | 일치 |
| 17 | P4b `04-review.md` 결과 | `결과: 완료` | `144:결과: 완료` | 일치 |
| 18 | `test_agent_loop.py -k "no_counseling or scope"` | 통과 | `1 passed` | 일치 |
| 19 | `test_agent_gate.py -k "person_id_from_llm"` | 통과 | `2 passed` | 일치 |
| 20 | `test_agent_gate.py -k "unknown_tool or not_callable"` | 통과 | `4 passed` | 일치 |
| 21 | `test_agent_gate.py -k "bad_args"` | 통과 | `8 passed` | 일치 |
| 22 | 7행 스크립트 SEED=1 REJECTS=1 | `rejected` 4종·`accepted` 1건·등식 `True` | 정확히 일치(`update_person`→`person_id_from_llm`, `search_person`→`not_callable_by_llm`, `delete_person`→`unknown_tool`, `add_event`(raw_utterance 포함)→`bad_args`) | 일치 |
| 23 | `grep "PendingQuestion\|flag_modified\|\.context *=" app/agent/` | 0건 | 문자열 그대로는 18건 매치되지만 **전부 `PendingQuestionOut`**(DTO)의 부분 문자열 — `app.db.models import` 정밀 확인 결과 ORM `PendingQuestion` 직접 import 0건 | 일치(패턴이 넓어 육안 판정 필요) |
| 24 | `test_agent_loop.py -k "resume_size"` | 통과 | `-k` 매치 0건(그런 이름 없음). 가장 근접한 `resume_byte_limit`/`resume_within_byte` 2건 실행 → 통과 | **불일치**(이름), 기능은 통과 |
| 25 | `test_api_chat.py -k "pending_survives"` | 통과 | `1 passed` | 일치 |
| 26 | `test_api_chat.py -k "db_error"` | 통과 | `1 passed` | 일치 |
| 27 | 재개 별칭 임베딩 `-k "alias_embedding"` | 통과 | `1 passed`(파일명 보정) | 보정 후 일치 |
| 28 | `-k "new_person_reject"` | 통과, `create_person` 0회 | `1 passed`(파일명 보정) | 보정 후 일치 |

## 불일치 정리 (원인과 제안)

1. **파일명 표기 차이**(행 2·4·27·28): 01-plan은 `tests/test_agent_resume.py`로 적었으나 U7 구현은 `tests/test_api_answers_resume.py`로 만들었다. 01-plan 196행이 이미 "`FakeProposer`/`run_turn`/`TurnResult` 의 이름이 U1·U2 에서 달라지는 경우"만 명시적으로 허용했는데, 실제로는 **파일명**도 U7에서 달라졌고 03-log U7 항목이 "01-plan 70행 표기와의 차이는 여기서 정리한다"고 이미 적어 두었다. 제품 코드·테스트는 옳게 동작한다 — 문서 표기 정정 사안이다. **제안**: 04-review 또는 다음 FIX에서 01-plan 62·94·123·147·148행의 `test_agent_resume.py` 표기를 `test_api_answers_resume.py`로 일괄 정정.
2. **`-k` 키워드 자체가 실제 테스트 이름에 없음**(행 3 `one_flow`/`end_to_end`, 행 24 `resume_size`): 실제 함수명은 각각 `test_chat_stores_event_and_replies_in_one_request`, `test_resume_byte_limit_drops_held_drafts_but_keeps_pending_calls`/`test_resume_within_byte_limit_keeps_held_drafts`다. 기능적으로는 요구된 동작을 검증하는 테스트가 존재하고 통과하지만, 01-plan이 예시한 `-k` 필터 문자열로는 **0건 선택**(exit 5)이 난다. 테스트를 고치거나 이름을 바꾸는 것은 이 작업 범위 밖(테스트 수정 금지)이므로 그대로 두고 보고한다. **제안**: 04-review 또는 FIX에서 01-plan 판정 표 3·24행의 `-k` 문자열을 실제 이름으로 정정(테스트 자체는 변경 불필요).
3. **grep 이 의도보다 넓게 걸림**(행 15·23): 패턴이 부분 문자열 일치라 `PendingQuestionOut`(DTO, 정상 재사용 대상)이 `PendingQuestion`에, 환경변수 **이름**(`ANTHROPIC_MODEL` 등)이 `ANTHROPIC_`에 걸린다. 두 경우 모두 "값과 함께 찍힘"·"ORM 직접 import"라는 실질 기준은 0건으로 확인했다(정밀 재확인 명령을 각 evidence 파일에 남겼다). 자동 판정 스크립트로 쓰기엔 오탐 소지가 있다는 점만 보고한다.

## 05-remediation [권고] 7건과의 관계

이번 행 실행과 별개로 05-remediation.md의 [권고] 7건(F-e93529·F-8e3e74·F-6ae8ad·F-d68447·F-fdb56f·F-7e6e84·F-0ffff5) 원인 분석을 채우고, 그중 6건(judge.py 제외)에 대해 `docs/wiki/registry.md` 비고를 실제로 확장했다. 상세는 `05-remediation.md` 참고.
