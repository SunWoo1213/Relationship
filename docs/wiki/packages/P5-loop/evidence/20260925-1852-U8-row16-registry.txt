$ grep -n "app/agent/" docs/wiki/registry.md
161:| 모듈 | 루프 계약 타입·trace 어휘(GateVerdict·PendingResume·LoopError 등) | app/agent/types.py | P5-loop | 3e4db92 | `ToolCallProposal`·`Proposal`·`GateVerdict`(accepted/rejected/limits)·`EventDraft`·`ScheduleDraft`·`TurnResult`·`PendingResume`·`ResumeInput`(frozen dataclass + `to_dict()`), step 어휘 6종(`loop_extract`·`loop_gate`·`loop_resolve_done`·`loop_record`·`loop_turn`·`loop_resume`)·`NEW_PERSON_TAG_OPTIONS`(M-1(d), `RELATION_TAGS` 1:1). U4(7a2ec5c)·U5(1835355): `SCHEDULE_UNKNOWN_OPTION` 등 추가. |
162:| 모듈 | 루프 패키지 재export 진입점 | app/agent/__init__.py | P5-loop | 3e4db92 | `run_turn`·`resume_turn`·타입·상수 재export. U2(069bdc2)·U3(8ecf75c)·U4(7a2ec5c)·U5(1835355)·U7(7df3ada)에서 단계별로 재export 추가. |
163:| 모듈 | 인식 단계 — 발화 → tool_calls 제안(LLM 1회) | app/agent/propose.py | P5-loop | 069bdc2 | `Proposer` Protocol·`PROPOSAL_SCHEMA`·`build_propose_prompt()`·`validate_proposal()`·공급자 3종(`app/er/judge.py` 의 `select_provider`/`call_with_error_mapping` 재사용, D11)·`PROPOSERS`·`proposer_from_env()`·`FakeProposer`(표 기반 결정적, 네트워크 0). |
164:| 모듈 | 게이트 — LLM 제안을 코드가 거른다(L(iii)) | app/agent/gate.py | P5-loop | 8ecf75c | `check(proposal, *, config) -> GateVerdict` — ①화이트리스트(`app.tools.TOOL_NAMES`) ②호출 가능 집합(`ask_user`·`search_person`·`get_briefing` 거부) ③`inspect.signature` 인자 대조 ④`person_id` 금지 ⑤상한(결정 A + `LOOP_MAX_PROPOSALS`), 적용 순서 ①②④③⑤. 거부 사유 6종(`unknown_tool`·`not_callable_by_llm`·`bad_args`·`person_id_from_llm`·`needs_confirmation`·`limit`), 버킷(`execute`·`hint_only`). 순수 함수(DB·LLM 미접근, `app.tools` 미호출). |
165:| 모듈 | 에이전트 루프 오케스트레이션(해석·기록·응답, run_turn/resume_turn) | app/agent/loop.py | P5-loop | 7a2ec5c | U4: 해석 구간(`resolve_mentions`, ER `resolve`/`apply_resolution` 호출, 되묻기 전 `context["resume"]` 확장 두 갈래 — identity/new_person). U5(1835355): `run_turn()`·기록 구간(`_record_impl`·`_execute_call`·`_ask_schedule`)·trace 래퍼. U7(7df3ada): `resume_turn()`(재개 — identity/new_person/schedule 답 처리, 재개 중 새 되묻기 시 그 자리에서 재종료, 부분 롤백은 `session.begin_nested()`). |
166:| 모듈 | 응답 단계 — 결정적 템플릿 응답 문장 | app/agent/respond.py | P5-loop | 1835355 | `build_reply()` — 저장 요약 + 되묻기 선택지만 조립(결정 B(i)), 감정·고민 문장 생성 없음(원칙7, 판정 표 18행). |
