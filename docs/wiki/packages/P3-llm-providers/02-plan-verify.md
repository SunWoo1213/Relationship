# P3-llm-providers · 계획 검증 (02-plan-verify)

대상: 01-plan.md | 검증자: verifier (fable) — 계획 작성자와 다른 모델·컨텍스트(L-002) | 날짜: 2026-09-11

선행 확인(`bash .claude/scripts/gitlog.sh P3-llm-providers D11 D3`, 14:16): dev = `701fb8d`(origin/dev ahead 1, main `0e3447a`, 승격 대기 11). 태그 `P3-llm-providers` 커밋 1건 = `701fb8d`(계획 초안 커밋 — 01-plan·P4 01-plan·backlog·INDEX·HANDOFF·journal 6파일, 코드 무수정) — 정상. 태그 `D11` 커밋 1건 = 같은 `701fb8d`. 태그 `D3` 커밋 14건 중 선행 패키지 닫는 커밋 `0527ab8`(P3-er)·`b1f2782`(P3-er U5 `judge_from_env`)·`0d98e47`(P3-baselines U5 결정 J 승격)이 모두 있다 — 01-plan 5행이 적은 해시와 일치. 미커밋 변경: `docs/wiki/HANDOFF.md`·`journal.md`(추적) + `decisions/D03-confidence-formula.md`(파급 1줄 + 갱신 이력 — 결정 6 의 상호참조, diff 로 확인: 결정 문장 무변경) + `decisions/D11-llm-provider-registry.md`·`packages/P3-llm-providers/05-remediation.md`·`evidence/`(미추적, 메인 세션 생성) — 제품 코드·데이터 변경 없음. `CURRENT.md` `active: none`. `google-genai` 는 현재 미설치(`python -m pip show google-genai` → `Package(s) not found`) — U2 착수 시 설치·실측이 필요하다는 01-plan 95·106행 전제와 일치.

## 1. 기계 검증 출력 (그대로 붙인다 — 요약 금지)

### 1a. 1차·2차 — 메인 세션 실행(02-plan-verify 작성 전)
명령: `bash .claude/scripts/verify-plan.sh P3-llm-providers | tee docs/wiki/packages/P3-llm-providers/evidence/20260911-1413-verify-plan.txt` → `== 결과: FAIL=2 WARN=11 ==`. FAIL 2 = `없음: …/02-plan-verify.md`(F-81e3e5, 이 문서 부재)·`카드 없음: D11 (decisions/D11-*.md)`(F-11fbee, 결정 6 (i) 카드 미신설). WARN 11 = registry 중복 9경로(`app/er/judge.py`·`app/settings.py`·`evaluation/resolvers/llm_single.py`·`requirements.txt`·`tests/test_er_judge.py`·`tests/test_baseline_llm_single.py`·`.env.examp`(정규식 절단)·`README.md` + 산출물 43행의 `llm_single.py` 재출현) — 뒤의 `judge.py`·`.env.examp` 2줄은 같은 경로 중복 출력이라 소견 ID 는 9개.

메인 세션이 D11 카드를 만든 뒤 재실행: `… | tee …/evidence/20260911-1414-verify-plan-2.txt` → `PASS  카드 존재: D11` 로 바뀌고 `== 결과: FAIL=1 WARN=11 ==`. 남은 FAIL 1 = 이 문서 부재(F-81e3e5)뿐. 두 파일 전문은 evidence 에 있다.

### 1b. 최종 — verifier 실행(이 문서 작성 후)
명령: `PYTHONIOENCODING=utf-8 bash .claude/scripts/verify-plan.sh P3-llm-providers | tee docs/wiki/packages/P3-llm-providers/evidence/20260911-1421-verify-plan-final.txt`
```
== verify-plan P3-llm-providers  (2026-09-11 14:25) ==
PASS  존재: docs/wiki/packages/P3-llm-providers/01-plan.md
PASS  존재: docs/wiki/packages/P3-llm-providers/02-plan-verify.md
PASS  카드 존재: D03
PASS  카드 존재: D11
PASS  카드 존재: D3
PASS  카드 존재: D4
PASS  패키지 id 등록됨: P3-baselines
PASS  패키지 id 등록됨: P3-er
PASS  패키지 id 등록됨: P3-llm-providers
PASS  패키지 id 등록됨: P4-pilot-eval
PASS  검증 항목 존재: R4
PASS  Refs 있음: - [ ] U1 **공급자 등록표·활성 스위치**: `app/er/
PASS  Refs 있음: - [ ] U2 **`GeminiJudge`**: `google-genai` 지연 import, �
PASS  Refs 있음: - [ ] U3 **`GeminiSingleCaller`**: `evaluation/resolvers/llm
PASS  Refs 있음: - [ ] U4 **문서·evidence**: `.env.example` 주석(이름�
PASS  backlog 일치: [backend-agent] LLM 판정기 공급자 확장 — Gemini �
PASS  의존 완료: P3-baselines
PASS  의존 완료: P3-er
PASS  검증자 = verifier (L-002)
PASS  점검표 8행 존재
PASS  점검표 모든 행에 판정(통과/보류) 있음
PASS  보류 0건
PASS  결과: 줄 존재
PASS  점검표 모든 행에 근거 있음
WARN  registry 에 다른 패키지로 이미 있음: app/er/judge.py → | 모듈 | LLM 판정(3단계, 공급자 중립) | app/er/judge.py | P3-er | b1f
WARN  registry 에 다른 패키지로 이미 있음: app/settings.py → | 모듈 | 런타임 설정값 | app/settings.py | P2-tools | f217190 | `app_use
WARN  registry 에 다른 패키지로 이미 있음: evaluation/resolvers/llm_single.py → | 모듈 | 베이스라인 3 LLM 단일 프롬프트(`llm_single`) | evaluation/
WARN  registry 에 다른 패키지로 이미 있음: requirements.txt → | 문서/설정 | 런타임·개발 의존성 선언(첫 도입, `==` 고정) | 
WARN  registry 에 다른 패키지로 이미 있음: tests/test_er_judge.py → | 테스트 | LLM 판정 요청/파싱/실패 분기(공급자 중립, 네트워
WARN  registry 에 다른 패키지로 이미 있음: tests/test_baseline_llm_single.py → | 테스트 | LLM 단일 프롬프트 요청/파싱/강등/오류/키 미노출(
WARN  registry 에 다른 패키지로 이미 있음: .env.examp → | 문서 | 환경변수 이름 목록(값 비움, 추적 유지) | .env.example 
| 모듈 | 런타임 설정값 | app/settings.py | P2-tools | f217190 | `app_use
WARN  registry 에 다른 패키지로 이미 있음: README.md → | 문서 | 프로젝트 README(전체 소개·스택·진행 상태·하네스·
PASS  registry 중복 없음: docs/user-setup/01-env-keys.md
PASS  registry 중복 없음: docs/user-setup/03-er-smoke.md
PASS  registry 중복 없음: 08-baseline-smoke.md
PASS  registry 중복 없음: docs/user-setup/09-gemini-smoke.md
PASS  registry 중복 없음: docs/wiki/decisions/D11-llm-provider-registry.md
PASS  registry 중복 없음: D03-confidence-formula.md
PASS  registry 중복 없음: docs/wiki/registry.md
WARN  registry 에 다른 패키지로 이미 있음: app/er/judge.py → | 모듈 | LLM 판정(3단계, 공급자 중립) | app/er/judge.py | P3-er | b1f
WARN  registry 에 다른 패키지로 이미 있음: llm_single.py → | 모듈 | 베이스라인 3 LLM 단일 프롬프트(`llm_single`) | evaluation/
| 테스트 | LLM 단일 프롬프트 요청/파싱/강등/오류/키 미노출(
WARN  registry 에 다른 패키지로 이미 있음: .env.examp → | 문서 | 환경변수 이름 목록(값 비움, 추적 유지) | .env.example 
| 모듈 | 런타임 설정값 | app/settings.py | P2-tools | f217190 | `app_use
== 결과: FAIL=0 WARN=11 ==
```
FAIL 0 / WARN 11. WARN 11줄 = 소견 9건(F-e93529·F-fdb56f·F-07a652·F-2c37bd·F-6ff7bc·F-c6bd9b·F-cfdfa4·F-0ffff5·F-22010b, 의도된 WARN — §3). `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python .claude/scripts/findings.py P3-llm-providers docs/wiki/packages/P3-llm-providers/evidence/20260911-1421-verify-plan-final.txt --source verify-plan` 실행(14:25, verifier) → 출력 `05-remediation.md 갱신: 새 소견 0, 해소 2, 열림 9 (필수 0)` / `✓ F-81e3e5  해소` / `✓ F-11fbee  해소`. 05-remediation.md 머리 줄 `열림: 9 (필수 0) | 해소: 2`.

## 2. 정합성 점검표 (기준: `.claude/skills/devlog/SKILL.md` "정합성 점검표")
근거 열에는 **카드 파일명 + 인용 문장**을 쓴다. "확인함" 같은 문구는 빈 것으로 간주한다.

| # | 항목 | 결과 | 근거(카드·절·인용) |
|---|------|------|--------------------|
| 1 | 범위 — 기획서 2장 제외 목록(상담·A–B·음성·네이티브·페르소나·태그 필터) 침범 없음 | 통과 | `docs/proposal.md` 2장 표 "제외 (의도적): 고민 상담 기능 / 인물 간(A–B) 관계 저장 / 관계 태그 필터링 / 상담 페르소나 / 톤 설정 / 음성 입력 / 네이티브 앱" · CLAUDE.md 원칙7 "브리핑의 '제안'은 기록된 사실에서 도출되는 한 줄 행동 제안으로 한정" ↔ 01-plan 산출물(30~44행)은 `app/er/judge.py`·`app/settings.py`·`evaluation/resolvers/llm_single.py`·`requirements.txt`·테스트 2·`.env.example`·README·user-setup·D11·registry·evidence 뿐이고 화면·테이블·툴·프롬프트 변경이 없다(25행 "`build_prompt()`·`JUDGEMENT_SCHEMA`·`validate_judgement()`·`app/er/confidence.py` 는 **읽기만** 한다"). 22~28행 "하지 않는 것" 에 "임베딩 공급자 확장 … `app/embedding.py` 무수정", "비동기·스트리밍·배치 API, 공급자별 캐시·요금 계산 — 범위 밖", "`app/` 의 그 밖 파일 수정 — `app/er/judge.py`·`app/settings.py` 둘뿐". 129행 "범위 이탈 유혹. '공급자를 늘린 김에 모델도 비교해 보자'는 P4·P10 이다". 제외 표의 여섯 항목 어느 것도 공급자 SDK 교체와 접점이 없다. |
| 2 | 불변 원칙 1~9 위반 없음 | 통과 | **원칙3** — CLAUDE.md "`s_llm`은 LLM이 구조화 출력으로 자기보고한 0~1 점수이며, LLM 로그 확률은 쓰지 않는다" ↔ 01-plan 9행 "D3(`s_llm` 은 **어떤 공급자의 API 도 제공하지 않는** 로그확률이 아니라 자기보고 점수)는 공급자에 의존하지 않는 문장", U2 49행 "`response_schema` = `JUDGEMENT_SCHEMA`"(실물 `app/er/judge.py:71~80` `"s_llm": {"type": "number", "minimum": 0, "maximum": 1}` — Gemini 도 같은 스키마로 자기보고), 125행 "보정표를 공급자 키로 분리 … P4 01-plan U3 이 이미 반영". **원칙4** — CLAUDE.md "엔티티 해석은 LLM 단일 호출로 하지 않는다. 반드시 4단계" ↔ 25행 "프롬프트·판정 로직·확신도 결합·임계치 변경 — … **읽기만**", 28행 "`app/` 의 그 밖 파일 수정 — `app/er/judge.py`·`app/settings.py` 둘뿐. `git diff --name-only -- app/` 로 증명", 판정 표 76행 "정확히 2줄", `docs/backlog.md` 52행 "수정 대상 `app/` 파일은 `app/er/judge.py`·`app/settings.py` 뿐(결정 J 이후 두 번째 예외)", D11 파급 "원칙4(제품 코드 수정은 `judge.py`·`settings.py` 2파일로 한정)". 평가 코드(`GeminiSingleCaller`)는 34행 `evaluation/resolvers/llm_single.py` 에만 있고, 의존 방향은 50행 "U1 등록표·활성 스위치를 **import 해 재사용**"(evaluation → app) — 현재 `grep -rn "evaluation" app/ --include=*.py` = 0줄이며 U1~U4 산출물에 `app/` 에서 `evaluation` 을 import 하는 항목이 없다. **원칙8** — 23행 "`reports/` 아래에 아무것도 만들지 않는다", 24행 "공급자·모델 성능 비교 실험 … 수치는 eval-agent 가 낸다", 95행 "U2 착수 시 반드시 실측한다: `python -c "import google.genai.errors as e; …"` 출력을 evidence 로 … 추측으로 적지 않는다", 결정 5 106행 "버전은 실제로 설치한 뒤 `pip show` 출력으로 적는다", 결정 3 (i) 116행 "기본값 없음"(모델명 추측 금지), 127행 "테스트를 고쳐서 통과시키는 일이 있으면 그 자체를 findings 로 올린다"(결정 2 로 바뀌는 테스트 목록은 R-1 로 미리 고정). **원칙9** — U2 49행 "토큰은 `usage_metadata` 에서, `provider="gemini"`", 125행 "`Judgement.provider`·`model` 을 반드시 채운다", D11 파급 "원칙9(`llm.provider`/`llm.model` trace 기록 유지)"; trace 를 쓰는 `app/er/pipeline.py` 는 무수정(28행). **원칙1·2** — 25행 "임계치 변경" 을 하지 않고 `app/er/confidence.py` 읽기만; 판정 분기(S3.3 10~12행)를 건드리는 산출물 없음. **원칙5·6** — 화면·패턴 감지 무관(산출물 없음). **원칙7** — 1행. |
| 3 | 인용한 D 카드의 "코드에서 지켜야 할 것"과 충돌 없음 | 통과 | `D11-llm-provider-registry.md` "코드에서 지켜야 할 것" 5문장 ↔ 01-plan: (a) "`JUDGES` 가 하나만 있고 `("anthropic", "openai", "gemini")` 세 키 … `FakeJudge` 는 그 표에 없다" ↔ 14행 "`JUDGES: dict[str, Callable[[], Judge]]`", U1 48행 "`FakeJudge` 는 **표에 넣지 않는다**", 판정 표 68행 `['anthropic', 'gemini', 'openai']` (`fake` 없음); (b) "`judge_from_env()`·`caller_from_env()` 는 `LLM_PROVIDER` 와 `LLM_PROVIDERS_ENABLED` 를 읽고, 미지·비활성 공급자에 `InvalidValue` … `caller_from_env()` 는 표·스위치를 `judge.py` 에서 import 한다(자체 표 금지)" ↔ 15행 "팩토리 호출 시점에 즉시 `InvalidValue(… 활성: …)`", 18행 "표를 두 벌 만들지 않는다", U3 50행 "공급자 이름 목록·거부 메시지 규칙을 복제하지 않는다" — 판정 표 71행의 `llm_single.CALLERS` 는 이름→caller 대응이지 이름 목록의 출처가 아니므로 충돌이 아니나 해석을 R-5 로 고정; (c) "`app/settings.py` `LLM_PROVIDER = "openai"`, `.env.example` `LLM_PROVIDER=openai`. `GEMINI_MODEL` 기본값 없음" ↔ 결정 확정 표 115행 "U1 `app/settings.py` 기본값 `"openai"`, `.env.example` 6행 `LLM_PROVIDER=openai`", 116행 "기본값 없음, `GEMINI_MODEL` 미설정 시 `InvalidValue`"; (d) "오류 매핑은 … 어휘 6종만" ↔ 17행 "기존 6종 … 그대로", 82행 "어휘는 기존 6종에서 **늘리지 않는다**", 결정 4 (i); (e) "키·프롬프트 원문은 로그·예외·evidence 에 남지 않는다" ↔ 16행·48행·49행(8행 근거 참조). D11 결정 문장 "기획서 전제(제품은 Claude API)와 다르며, 이는 **운영 기본값의 변경**이지 확신도 공식(D3)·판정 프롬프트·구조화 출력 스키마의 변경이 아니다" — 결정 2 의 기획서 전제와의 차이를 카드가 드러냈다. `D03-confidence-formula.md` 결정 `confidence = 0.5·s_llm + 0.3·s_emb + 0.2·s_rule` 무변경(01-plan 9행 "판정 로직·프롬프트·확신도 결합을 한 줄도 바꾸지 않는다"), 파급에 "공급자 선택·활성 스위치·기본 공급자는 **D11**(이 공식은 공급자와 무관하게 동일)" 1줄 추가(미커밋 diff 로 확인, 갱신 이력 "결정 문장 무변경") = 결정 6 (i) 그대로. `D04-embedding-provider.md` "임베딩 호출은 반드시 `EmbeddingProvider` 인터페이스 뒤에 둔다" ↔ 26행 "`app/embedding.py` 무수정"; D4 결정 "1차 공급자: OpenAI 임베딩 API" ↔ D11 이유 "임베딩(D4, OpenAI)과 키 하나로 돈다" — 기본 openai 와 정합. D4 파급의 "환경변수: `OPENAI_API_KEY`(임베딩), `ANTHROPIC_API_KEY`(LLM)" 은 서술이지 코드 제약이 아니며 LLM 몫은 D11 이 대체(R-9 메모, 이 패키지는 D4 를 고치지 않는다). 재사용 대상 실재: `app/er/judge.py:178 call_with_error_mapping(errors_module, fn)`(84행이 인용한 덕타이핑 4예외 이름 일치), `:444 _KNOWN_PROVIDERS`, `:449 judge_from_env(env=None)`, `app/settings.py:90 LLM_PROVIDER = "anthropic"`, `evaluation/resolvers/llm_single.py:468 caller_from_env(env=None, *, client, model, timeout, max_retries)`, `:502` gemini `InvalidValue` — 01-plan 9행 "judge.py 460행·llm_single.py 502행" 과 일치. |
| 4 | S 카드와 일치 (스키마·시그니처 v2, 임계치 2개, ask_user 비동기) | 통과 | `S3.2-tools-v2.md` 툴 7종 표 — 산출물(30~44행)에 `app/tools/` 없음, 판정 표 77행 "툴 시그니처 무변경 `python scripts/tools_check.py` → `7/7 ok`". `S3.3-er-pipeline.md` 8행 "3. LLM 판정 대화 맥락 + 후보 → {matched_person_id \| null, s_llm, reason} (구조화 출력)" = `app/er/judge.py:71` `JUDGEMENT_SCHEMA`(`matched_person_id` integer\|null · `s_llm` number 0~1 · `reason` string · `required` 3 · `additionalProperties: False`) ↔ 01-plan 16행 "`response_schema`(= `JUDGEMENT_SCHEMA` 와 **같은 스키마 딕셔너리**, SDK 가 요구하는 바깥 모양만 감싼다)", 124행 "스키마를 고치지 말고(세 공급자 공유 자산이다) Gemini 쪽에서 감싸는 변환 함수" — 프롬프트·스키마 무변경(R-6 이 변환 불가 시 멈춤 규칙을 고정). S3.3 16행 "초기값 T_merge=0.8, T_new=0.3 (설정값)"·10~12행 세 구간 분기 ↔ 25행 임계치·`confidence.py` 무수정, 판정 표 73행 P3-er 회귀(승진·이모 배제·동명이인)로 증명. S3.3 17행 "**LLM 단일 호출로 대체 금지**(원칙4)" — 2행. `S3.4` ask_user 비동기 — 이 패키지는 `ask_user`·`pending_questions` 를 부르지도 고치지도 않는다(산출물에 `app/tools`·`app/api`·`alembic/` 없음). `S3.1` 스키마 v2 — 테이블·컬럼 변경 없음(`alembic/` 산출물 없음). `S3.7-eval-spec.md`(U3 Refs) — 베이스라인 3 의 caller 추가일 뿐 지표·데이터 무관(23행 "P4 실행·지표·보정표 — P4-pilot-eval"). |
| 5 | 의존성 순서 — 선행 P 완료, P4 게이트 | 통과 | `docs/backlog.md` 51행 "의존: P3-er, P3-baselines". `packages/P3-er/04-review.md` 166~167행 "결과: 완료 / 승인: 사용자 승인 2026-09-06 19:20", 닫는 커밋 `0527ab8`(gitlog D3 목록). `packages/P3-baselines/04-review.md` 195~196행 "결과: 완료 — §1b `verify-impl.sh` FAIL 0·WARN 0 … / 승인: 사용자 (2026-09-11)", 닫는 커밋 `5a1bcbe`(gitlog 최근 커밋 2번째). 1a·1b "PASS 의존 완료: P3-baselines / P3-er". **P4 게이트** — `.claude/scripts/verify-plan.sh` 68~72행 `num -ge 5` 일 때만 P4 04-review 검사, `docs/wiki/INDEX.md` 69행 `P3-llm-providers` 행(닫는 R `—`, "R4 는 P4 가 닫는다"), `docs/backlog.md` 61행 P4 "의존: P3, P3-llm-providers" ↔ 01-plan 5행 "P4 게이트는 이 패키지에 해당하지 않는다 … 오히려 이 패키지가 P4 착수 **전에** 끝나야 한다", 133행 "P4 01-plan 5행 … 닫는 커밋 해시를 그 자리에 채운다". `packages/P4-pilot-eval/01-plan.md` 5행 "P3-llm-providers 완료(추가 의존, 사용자 결정 A 2026-09-11 … 04-review `결과: 완료` 해시를 여기에 적는다)", 201행 결정 A "(ii) OpenAI — P4 U7 은 `LLM_PROVIDER=openai` + `OPENAI_MODEL` 로만 돈다" ↔ 01-plan 결정 확정 표 115행 "기본 `openai` … P4 결정 A 와 정합", 135행 "P4 는 `LLM_PROVIDER=openai` 를 명시 설정으로 넘기고 `meta` 에 기록". 방향 일치. |
| 6 | 수용 기준이 backlog 와 글자 그대로 동일 | 통과 | `docs/backlog.md` 51행 `- [ ] [backend-agent] LLM 판정기 공급자 확장 — Gemini 구현 + 공급자 등록표·활성 스위치 / 의존: P3-er, P3-baselines / 수용기준: \`LLM_PROVIDER\` ∈ {anthropic, openai, gemini} 각각으로 \`judge_from_env()\`·\`caller_from_env()\` 가 해당 공급자의 판정기를 만들고, 비활성·미지 공급자는 \`InvalidValue\` 로 거부하며, 신규 테스트는 네트워크 0(스텁)이고 기존 pytest 전건이 통과한다` ↔ 01-plan 55행 같은 문장(체크박스 제외). verifier 대조: `a=$(sed -n 51p docs/backlog.md \| sed 's/^- \[ \] //'); b=$(sed -n 55p …/01-plan.md \| sed 's/^- //'); [ "$a" = "$b" ]` → `SAME`; 1a·1b "PASS backlog 일치". 해석 4문장(58~61행)마다 판정 표(65~78행)에 명령·기대 출력이 있다 — 세 공급자 parametrize(67행), 거부 2종(69~70행), 스텁·skip 0(60행), 전건(75행). 다만 해석 58행 "**키 없이도 객체 생성까지는 성공**해야 한다" 는 현 코드와 맞지 않는다 — `tests/test_er_judge.py:502~506` 주석 "OpenAI() 클라이언트는 생성 시점에 키 존재를 확인한다" + 더미 키 설정, `app/er/judge.py:308~314` `__post_init__` 에서 `openai.OpenAI(...)` 생성 → R-2(해석 문장 수정, backlog 문장은 그대로). 69행 명령이 스위치를 `os.environ` 에, 공급자를 `env` 인자에 나눠 넣어 현 `judge_from_env(env=…)` 규약(`env` 가 주어지면 그것만 읽는다, `judge.py:469~472`)과 어긋난다 → R-3. |
| 7 | 작업 단위마다 Refs 태그 | 통과 | 01-plan 48~51행 U1~U4 각 줄 끝 `/ Refs: P3-llm-providers …` — U1 `S3.3 D3 원칙8`, U2 `S3.3 D3 원칙4 원칙8 원칙9`, U3 `S3.7 D3 원칙8`, U4 `원칙8 원칙9`. 1a·1b "PASS Refs 있음" 4건. 태그가 가리키는 카드 실재: `decisions/D03-*.md`·`D11-*.md`(1b "PASS 카드 존재: D03/D11/D3/D4"), `specs/S3.3-er-pipeline.md`·`S3.7-eval-spec.md`(`ls docs/wiki/specs/`). U1·U2 는 D11 을 구현하는 단위인데 Refs 에 `D11` 이 없다(4행 태그 줄은 "신설 후보 D11" 표기) → R-9 에서 U1~U4 Refs 와 커밋 메시지에 `D11` 추가 요구(추적 `git log --grep D11`). 단위 크기: U1 = judge.py 팩토리 재작성 + settings 상수 + 테스트, U2 = 클래스 1 + 핀 1줄 + 테스트, U3 = caller 1 + 테스트, U4 = 문서·evidence — 각각 P3-er U5·P3-baselines U5 와 같은 규모로 커밋 하나 크기. |
| 8 | 보안 카드(`security.md`) — 비밀·외부 전송·삭제 규칙 위반 없음 | 통과 | `security.md` §1 "`.env` … 에이전트가 읽지도 쓰지도 않는다 … `.env.example`에 **이름만** 적는다" ↔ 01-plan 21행 "`.env.example` 주석(이름만)", 38행 "활성 스위치 이름 추가(값 비움)", 40행 user-setup 표 "`GEMINI_API_KEY`·`GEMINI_MODEL` 행"(이름); §1 "코드·문서·커밋에 키 문자열을 넣지 않는다 … `os.environ[…]` 로 읽는다" ↔ 16행 "키는 SDK 가 `GEMINI_API_KEY` 를 환경변수에서 읽게 두고 코드가 변수로 옮기지 않는다(security §1)", U2 49행 "클라이언트 생성(키 인자 미전달 — SDK 가 `GEMINI_API_KEY` 를 읽는다)"; §1 "로그·trace에 키·비밀을 남기지 않는다" ↔ 48행 "오류 메시지에 키 값·프롬프트를 넣지 않는다", 49행 테스트 "키 미노출(표식 문자열이 요청·예외·출력에 없음)", 59행 "키 값·프롬프트는 메시지에 없다", 78행 스모크 "키·프롬프트 원문 미출력"; §1 "환경변수 전체 출력 … 금지 / 존재 여부만 허용" ↔ 결정 1 (ii) 기각 사유 102행("키 **존재 여부**를 코드가 들여다보게 되고")으로 (i) 채택 — 코드가 키 존재조차 보지 않는다; §6 "우회하지 않고 사용자에게 명령을 그대로 보여주고 직접 실행을 요청" ↔ 80행 "키가 없으면 **우회하지 않고** 사용자에게 명령을 보여 주고 멈춘다(security.md §1·§6)", 78행 실 Gemini 호출 "**사용자 실행**", 80행 "실호출은 자동 테스트에 넣지 않는다". 외부 전송 = 사용자 스모크 2회뿐(128행). §3·§4 — 산출물·판정 표에 삭제·prune·DROP 명령 없음. `pip install google-genai`(결정 5)는 패키지 설치이지 데이터 전송이 아니다. |

## 3. 보류 소견과 조치 (있으면 05-remediation.md 의 F-id 를 적는다)

### 기계 검증 소견 (05-remediation.md 11건 — 원인·확인 명령·확인 결과·해결 단계는 그 파일)
- **F-81e3e5 [필수]** `02-plan-verify.md 없음` — 원인은 이 문서가 아직 없었던 것(검증 순서상 정상). 이 문서 작성 후 §1b 재실행으로 사라진다 → `findings.py` 재실행에서 **해소**.
- **F-11fbee [필수]** `카드 없음: D11` — 1a 1차(14:13) 시점에 결정 6 (i) 카드가 없었다. 메인 세션이 `decisions/D11-llm-provider-registry.md` 를 만든 뒤 2차(14:14)에서 `PASS 카드 존재: D11` → **해소**(미커밋 파일이므로 계획 승인 커밋에 D11·D03 diff 가 같이 들어가야 한다 — 메인 세션 몫).
- **F-e93529 · F-fdb56f · F-07a652 · F-2c37bd · F-6ff7bc · F-c6bd9b · F-cfdfa4 · F-0ffff5 · F-22010b [권고 9건]** `registry 에 다른 패키지로 이미 있음` — **의도된 WARN.** 아홉 경로(`app/er/judge.py` 82행 P3-er · `app/settings.py` 51행 P2-tools · `evaluation/resolvers/llm_single.py` 119행 P3-baselines · `requirements.txt` 45행 P1-schema · `tests/test_er_judge.py` 90행 · `tests/test_baseline_llm_single.py` 120행 · `.env.example` 36행 하네스 · `README.md` 33행 하네스; F-22010b 는 산출물 43행의 `llm_single.py` 재언급 = F-07a652 와 같은 경로)는 전부 **기존 파일의 확장**이고, 01-plan 43행 "`docs/wiki/registry.md` — `app/er/judge.py`(82행)·`llm_single.py`(119행)·`.env.example`(36행) 비고 갱신 + 신규 행 필요 시 추가" 가 비고 갱신 규칙을 적었다. `verify-plan.sh` 88~95행은 "확장"과 "재작성"을 구분하지 않는다. 같은 판정으로 닫힌 선례: `packages/P3-baselines/05-remediation.md` F-0ffff5 "조치 없음(계획 단계) — U8 에서 … 비고만 덧붙이고 새 행을 만들지 않는다 … `grep -c "\| README.md \|"` = `1` (행 수 불변)" → 04-review §5 에서 닫힘(2026-09-11, evidence `20260911-1305-close-f0ffff5.txt`). 01-plan 43행이 3경로만 이름을 적었으므로 나머지 5경로에도 같은 규칙을 적용한다는 것을 R-8 로 고정한다. 계획 승인을 막지 않는다 — 열림 유지, 04-review §5 에서 행 수 불변으로 닫는다.

### 보류 항목 (H-n)
- 없음.

### 권고 (R-n · 승인 조건 아님 — 실행 시 반영하고 04-review 에서 본다)

| # | 내용 | 근거 | 반영 단위 |
|---|------|------|-----------|
| R-1 | **결정 2(기본 `openai`)·결정 3(`GEMINI_MODEL` 기본 없음)으로 바뀌는 기존 테스트를 이 네 건으로 고정**하고, 그 밖의 기존 테스트 diff 는 0 이어야 한다(있으면 findings): (a) `tests/test_er_judge.py:495 test_judge_from_env_defaults_to_anthropic` — `env={}` 가 `ClaudeJudge` 를 단언하므로 실패한다 → `…_defaults_to_openai` 로 바꾸고 `OPENAI_API_KEY` 는 `_FAKE_KEY_MARKER` 규약(P3-er F-46f1eb)으로; (b) `tests/test_er_judge.py:510 test_judge_from_env_gemini_is_reserved_not_implemented` — U2 뒤에도 `GEMINI_MODEL` 미설정이라 `InvalidValue` 가 나와 **틀린 이유로 통과**하므로 삭제·대체(gemini 양성 케이스 + "`GEMINI_MODEL` 미설정 → `InvalidValue`" 케이스로 분리); (c) `tests/test_baseline_llm_single.py:334` parametrize `["gemini", "llama"]` — 같은 이유, `gemini` 를 양성 케이스로 옮기고 `llama` 만 남긴다; (d) `tests/test_er_smoke.py:86 test_main_missing_anthropic_key_returns_2` — `LLM_PROVIDER` 삭제 시 기본이 openai 가 되어 출력에 `ANTHROPIC_API_KEY` 가 없다 → `OPENAI_API_KEY` 기준으로 바꾼다. 04-review 증거: `git diff --stat <U1 직전 해시>..HEAD -- tests/` 와 위 네 함수 외 변경 0 을 `git diff <U1 직전>..HEAD -- tests/test_er_judge.py tests/test_baseline_llm_single.py tests/test_er_smoke.py \| grep "^-def\|^-    def"` 로 보인다(삭제된 함수가 위 네 개뿐). 01-plan 36·37·61·127행의 "무수정 통과" 는 "이 네 건을 제외한 무수정" 으로 읽는다(결정 확정 표 115행 "테스트가 기본값을 단언하면 새 기본으로" 가 이미 이를 허용). | `tests/test_er_judge.py:495~514`, `tests/test_baseline_llm_single.py:334~337`, `tests/test_er_smoke.py:86~94`(직접 열람); 01-plan 115행 vs 36·61·127행; `app/er/judge.py:308~314` `OpenAI()` 생성 시점 키 확인 | U1(a·d)·U2(b)·U3(c) |
| R-2 | 해석 58행 "키 없이도 객체 생성까지는 성공" 을 "**더미 키(`_FAKE_KEY_MARKER`)로 생성 성공, 네트워크는 `judge()` 시점에만**" 으로 읽는다. `OpenAI()` 는 생성 시 키를 요구하고(`tests/test_er_judge.py:503` 주석), `google.genai.Client()` 도 키 인자 없이 환경변수가 비면 생성 시 예외를 내는 것으로 알려져 있으나 **실측**한다: U2 evidence 에 `GEMINI_API_KEY` 미설정 상태에서 `python -c "from google import genai; genai.Client()"` 의 예외 클래스명(메시지에 키 없음)을 남긴다. 세 공급자 parametrize 테스트(판정 표 67행)는 공급자별 더미 키를 `monkeypatch.setenv` 로 넣고, 요청·예외·출력에 마커가 없음을 단언한다. backlog 문장은 바꾸지 않는다. | 01-plan 58행; `app/er/judge.py:227~233`·`:308~314`; P3-er F-46f1eb 더미 키 규약 | U1·U2·U3 |
| R-3 | **스위치·공급자는 같은 `env` 매핑에서 읽는다(단일 출처).** 판정 표 69행 명령은 `os.environ['LLM_PROVIDERS_ENABLED']` 와 `judge_from_env({'LLM_PROVIDER':'gemini'})` 를 섞어 놓아, 현 규약(`env` 가 주어지면 `os.environ` 을 보지 않는다 — `judge.py:469~472`)에서는 스위치가 무시되어 기대 출력이 나오지 않는다. 명령을 `judge_from_env({'LLM_PROVIDER':'gemini','LLM_PROVIDERS_ENABLED':'anthropic'})` 로 고치고, `enabled_providers(env)` 도 같은 `env` 를 받는다. 테스트: `env` 주입만으로 활성/비활성이 갈리고 `os.environ` 의 값이 새어 들어오지 않는다(`monkeypatch.setenv("LLM_PROVIDERS_ENABLED","gemini")` 상태에서 `judge_from_env({"LLM_PROVIDER":"gemini","LLM_PROVIDERS_ENABLED":"anthropic"})` → `InvalidValue`). | 01-plan 69·70행; `app/er/judge.py:449~472` docstring "`env` 를 생략하면 `os.environ` 을 읽는다" | U1 |
| R-4 | **`LLM_PROVIDERS_ENABLED` 파싱 규칙 고정**(01-plan 48행 "공백·대소문자 처리" 만 있고 규칙이 없다): 쉼표 분리 → 각 항목 `strip().lower()` → 빈 항목 제거. **미설정 또는 공백만 남으면 기본값(전체 켬)** — 01-plan 38행이 `.env.example` 에 "값 비움" 으로 넣기로 했으므로 빈 문자열이 "전부 끔" 이면 `.env.example` 을 복사한 사람이 모든 공급자를 잃는다. 목록 안의 **표에 없는 이름은 팩토리 호출 시점 `InvalidValue`**(오타를 조용히 무시하지 않는다, 원칙8 재현성). `LLM_PROVIDER` 값도 `strip().lower()` 뒤 표 조회(현재는 정확 일치). 테스트 6개: `" anthropic , OPENAI "` 정규화, `""`·미설정 = 기본, `"anthropic,,gemini"` 빈 항목 무시, `"anthropic,llama"` → `InvalidValue`, `"Gemini"` 대소문자, 기본 상수 `app.settings` 단일 출처(`grep -c "LLM_PROVIDERS_ENABLED" app/er/judge.py` 가 상수 정의를 포함하지 않는다). | 01-plan 38·48행·결정 1 (i); D11 "기본값 = 전체 켬" | U1 |
| R-5 | **등록표 단일성의 판정 명령**: `grep -c "^JUDGES" app/er/judge.py` = 1; `grep -c "_KNOWN_PROVIDERS" app/er/judge.py` = 0(또는 `JUDGES` 에서 파생된 한 줄만); `grep -n "^from app.er.judge import\|^from app.er import judge\|judge\.JUDGES\|enabled_providers" evaluation/resolvers/llm_single.py` 출력에 표·스위치 import 가 있다; `grep -c "InvalidValue(" evaluation/resolvers/llm_single.py` 의 `caller_from_env` 본문 분 = 0(거부는 `judge.py` 의 한 함수 — 예: `select_provider(env) -> str` 이 미지·비활성 검사 후 이름을 돌려주고 두 진입점이 그것을 부른다). 판정 표 71행의 `CALLERS`(이름→caller) 는 caller 클래스가 Judge 와 다르므로 허용하되, 테스트가 `set(CALLERS) == set(JUDGES)` 를 단언하고 `CALLERS` 의 키를 문자열 리터럴로 다시 쓰지 않는다(`JUDGES` 키를 순회해 만들거나 동일성 테스트로 묶는다). 이렇게 읽으면 D11 "자체 표 금지" 와 충돌하지 않는다. | D11 "코드에서 지켜야 할 것" (a)(b); 01-plan 14·18·50·71행 | U1·U3 |
| R-6 | **`google-genai` 실측 evidence 3건을 U2 첫 단계로**: `python -m pip show google-genai`(핀 버전), `python -c "import google.genai.errors as e; print([n for n in dir(e) if n[0].isupper()])"`(예외 이름 — 01-plan 95행), `python -c "from google.genai import types; print(sorted(types.Schema.model_fields))"`(`response_schema` 가 받는 필드). `JUDGEMENT_SCHEMA` 의 `"type": ["integer","null"]`·`additionalProperties: False` 가 그대로 안 들어가면 변환 함수는 `judge.py` Gemini 절 안에만 두고, 테스트가 (i) `JUDGEMENT_SCHEMA` 객체 불변, (ii) 변환 결과가 `required` 3필드·`s_llm` 0~1 number·`matched_person_id` nullable integer 를 요구함을 단언한다. **멈춤 규칙**: nullable integer 또는 `s_llm` 범위를 표현할 수 없으면 U2 를 멈추고 findings + 사용자 결정(01-plan 124행 "변환조차 불가능하면 멈추고"). 재시도는 SDK `HttpOptions` 에 재시도 설정이 있는지 같은 evidence 로 실측한 뒤 없을 때만 수동 1회 루프(`timeout`·`connection` 한정), 스텁으로 호출 횟수 = 2 를 단언. `RESOLUTION_SCHEMA`(`llm_single.py:116`, `candidate_person_ids` 배열 포함)도 같은 변환 함수를 거친다 — 두 번째 변환기를 만들지 않는다. | 01-plan 82~96·124행; `app/er/judge.py:71~80`; `evaluation/resolvers/llm_single.py:116~132`; 현재 SDK 미설치(`pip show` → not found) | U2·U3 |
| R-7 | **`scripts/er_smoke.py`·`scripts/baseline_smoke.py`·`tests/test_er_smoke.py` 를 산출물에 추가**(U1 또는 U4). 판정 표 78행 "`--provider gemini` … 키 없으면 종료 코드 2" 는 두 스크립트의 `_REQUIRED_KEY_BY_PROVIDER`(`er_smoke.py:42`, `baseline_smoke.py:49` — 현재 anthropic·openai 뿐, 주석 "여기 없는 공급자(예: `gemini`)는 키 사전 확인을 건너뛰고") 에 `"gemini": "GEMINI_API_KEY"` 행이 있어야 성립하고, help 문구 `(anthropic\|openai)`·docstring "기본 `anthropic`" 도 결정 2 로 낡는다. `scripts/` 는 `app/` 밖이라 원칙4·backlog 52행과 충돌하지 않지만 산출물 목록에 없으면 04-review 가 "계획 밖 수정" 으로 본다. 03-log 에 파일을 선언하고 04-review 는 `git diff --name-only <U1 직전>..HEAD -- scripts/ tests/` 를 선언 목록과 대조한다. | 01-plan 30~44행(scripts 없음) vs 78행; `scripts/er_smoke.py:38~46,84~88,130~141`; `tests/test_er_smoke.py:86~94` | U1·U4 |
| R-8 | **registry 는 여덟 경로 모두 기존 행 비고만**(새 행 금지). 04-review §5 닫는 명령: `for f in app/er/judge.py app/settings.py evaluation/resolvers/llm_single.py tests/test_er_judge.py tests/test_baseline_llm_single.py README.md; do grep -c "\| $f \|" docs/wiki/registry.md; done` 각 `1`, `grep -c "requirements.txt" docs/wiki/registry.md` = 1, `grep -c "환경변수 이름 목록" docs/wiki/registry.md` = 1, 그리고 각 비고에 `P3-llm-providers U<n>:` 문구. 새 행은 **새 파일**(결정 7 (ii) 를 택했을 때의 `09-gemini-smoke.md`)에만 — 결정 7 (i) 확정이므로 새 행 0 이 기대값. 소견 9건은 이 명령으로 04-review 에서 닫는다(F-0ffff5 선례). | 05-remediation 권고 9건; 01-plan 43행; `packages/P3-baselines/05-remediation.md` F-0ffff5 해결 단계 | U4 |
| R-9 | **추적·꼬리표**: (a) U1~U4 Refs 와 커밋 메시지에 `D11` 을 넣는다(현재 U1~U4 Refs 에 `D11` 없음, 4행은 "신설 후보"). (b) 04-review §6 에 "실호출로 검증된 공급자 0/3(F-87c597 열림, R4 꼬리표는 P4 가 닫는다)" 를 그대로 적는다(01-plan 126행). (c) `docs/proposal.md` 는 이 패키지에서 손대지 않는다 — 상단 안내문 갱신 여부는 D11 파급 "04-review 에서 판단" 대로 verifier 가 04-review 에 H 로 올리고 **사용자가** 정한다(`/devlog change` 여부 포함). (d) D4 파급의 "`ANTHROPIC_API_KEY`(LLM)" 문구는 D11 이 LLM 몫을 대체하므로 이 패키지는 D4 를 고치지 않는다(메인 세션 메모). (e) `docs/user-setup/01-env-keys.md` 7행 "(`LLM_PROVIDER=anthropic`)" 과 9행 "(`LLM_PROVIDER=openai` 일 때)" 의 기본/선택 관계가 뒤집히므로 U4 에서 두 행을 같이 고친다. | 01-plan 4·48~51·126행; D11 파급; `docs/user-setup/01-env-keys.md:7,9,12`; INDEX 69행 | U1~U4 |

## 4. 결정
결과: 통과 — 점검표 8행 모두 통과. 공급자 확장은 `app/er/judge.py`·`app/settings.py` 2파일로 한정되고 프롬프트·`JUDGEMENT_SCHEMA`·확신도 결합·임계치를 읽기만 하며(2·4행), D11 "코드에서 지켜야 할 것" 5문장이 U1~U4 와 1:1 로 대응하고 결정 2(기본 `openai`)는 D4 임베딩 OpenAI·P4 결정 A 와 정합하되 기획서 전제와의 차이를 D11 이 명시했다(3·5행). 수용 기준은 backlog 51행과 글자 그대로 같다(`SAME`, 6행). 권고 R-1~R-9 는 승인 조건이 아니나 R-1(결정 2 로 바뀌는 기존 테스트 4건 고정)·R-3(스위치·공급자 단일 `env` 출처)·R-7(`scripts/`·`tests/test_er_smoke.py` 산출물 추가)은 U1 착수 전에 반영해야 04-review 에서 "계획 밖 수정"·"틀린 이유로 통과하는 테스트" 소견이 생기지 않는다. §1b 최종 기계 검증 FAIL 0 이 이 판정의 기계 근거다(WARN 11 = 소견 9건 의도된 WARN, R-8 로 04-review 에서 닫는다).
승인: 사용자 (2026-09-14) — 결정 1~7·권고 R-1~R-9 실행 시 반영, U1 backend-agent 위임 승인(L-004)
