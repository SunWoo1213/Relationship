# P3-llm-providers · 구현 로그 (03-log)

> /commit 이 커밋마다 항목 하나를 **아래에** 붙인다(LLM 작성). 이어서 작업하는 에이전트는 마지막 두 항목만 읽으면 된다.
> 형식은 고정. 지우거나 고쳐 쓰지 않는다.

## 2026-09-14 16:01 · docs(P3-llm-providers): 계획검증 통과·계획 승인·D11 카드 신설 — 공급자 등록표·활성 스위치·기본 openai, 패키지 착수 · 025a7c4
- 변경: `02-plan-verify.md`(verifier fable 통과 — §1 verify-plan 1413(FAIL 2)→1414(FAIL 1)→1421 final(FAIL 0/WARN 11), 점검표 8/8 카드 인용, 보류 0, 권고 R-1~R-9, `승인: 사용자 (2026-09-14)`), `05-remediation.md`(소견 13: 필수 2 해소 F-81e3e5·F-11fbee, 권고 11 = registry 기존 행 의도된 WARN 열림 → 04-review §5 에서 닫음), `evidence/20260911-1413-verify-plan.txt`·`1414-verify-plan-2.txt`·`1421-verify-plan-final.txt`, `decisions/D11-llm-provider-registry.md` 신규(등록표 `JUDGES` anthropic·openai·gemini, `LLM_PROVIDERS_ENABLED` 기본 전체 켬, 기본 `LLM_PROVIDER=openai`(결정 2), `FakeJudge` 표 밖, `GEMINI_MODEL` 기본 없음, 오류 어휘 6종 유지, "코드에서 지켜야 할 것" 5문장), `decisions/D03-confidence-formula.md`(파급 D11 상호참조 1줄 + 갱신 이력, 결정 문장 무변경), `CURRENT.md active: P3-llm-providers`, journal START, HANDOFF, 이 로그
- 이유(기획서·카드 연결): backlog 51행 "LLM 판정기 공급자 확장 — Gemini 구현 + 공급자 등록표·활성 스위치". P4 결정 A(실행 OpenAI 1벌)의 선행 조건 — D4 임베딩 OpenAI 와 키 하나로 돈다. D11 은 사용자 결정(2026-09-11)의 기록이며 기획서 전제(Claude)와의 차이를 카드가 명시한다(운영 기본값 변경이지 D3 공식·프롬프트·스키마 변경이 아님)
- 정합성 확인: 원칙3(`s_llm` 자기보고 — 공급자 무관) / 원칙4(`app/` 수정은 `judge.py`·`settings.py` 2파일, 프롬프트·`JUDGEMENT_SCHEMA`·`confidence.py`·임계치 읽기만) / 원칙8(모델명·버전 추측 금지 — U2 첫 단계 실측 evidence R-6) / 원칙9(`provider`/`model` trace 유지) / D3·D4·D11 / S3.2·S3.3·S3.7 무변경 / security(키 이름만, SDK 가 환경변수 읽음, 실호출 사용자 실행) — 위반 없음
- 남은 것 · 다음 단위: **U1 공급자 등록표·활성 스위치** backend-agent(사용자 승인 2026-09-14, L-004 마커 후 1회). 위임 프롬프트에 R-1(바뀌는 기존 테스트 4건 고정, 그 밖 diff 0)·R-3(단일 `env` 출처)·R-4(파싱 규칙: 쉼표·strip·lower·빈 항목 제거·미설정/공백=전체 켬)·R-5(등록표 단일성 판정 명령)·R-9(Refs·커밋에 D11) 를 넣는다. R-2(더미 키 생성 규약)·R-6(google-genai 실측 3건)은 U2, R-7(스모크 스크립트·테스트 산출물)은 U1 또는 U4, R-8(registry 비고만)은 매 단위
- Refs: P3-llm-providers D11 D3 D4 S3.3 S3.2 S3.7 원칙3 원칙4 원칙8 원칙9 R4 L-002 L-004

## 2026-09-14 16:45 · feat(P3-llm-providers): U1 공급자 등록표·활성 스위치 — JUDGES 단일 표·select_provider 거부 판정·LLM_PROVIDERS_ENABLED 파싱·기본 openai, 테스트 11건 · c01381d
- 변경: `app/er/judge.py`(`JUDGES` 등록표 anthropic·openai·gemini(U1 자리표시 `_gemini_reserved`, U2 에서 `GeminiJudge` 로 교체)·`enabled_providers(env)` R-4 파싱·`select_provider(env)` 유일한 거부 판정 자리(미지 이름→표 키 목록, 비활성→활성 목록)·`judge_from_env` 2단계 재작성, `_KNOWN_PROVIDERS` 제거, 판정 프롬프트·`JUDGEMENT_SCHEMA`·`call_with_error_mapping`·Judge 클래스 본문 무변경), `app/settings.py`(`LLM_PROVIDER = "openai"` 결정 2, `LLM_PROVIDERS_ENABLED_DEFAULT = "anthropic,openai,gemini"` 결정 1 단일 출처), `.env.example`(`LLM_PROVIDER=openai`, `LLM_PROVIDERS_ENABLED=` 이름만), `tests/test_er_judge.py`(R-1(a) `test_judge_from_env_defaults_to_openai` 개명 + 신규 11건: 표 3키·fake 없음·공급자별 생성·거부 메시지 2종·R-3 단일 출처·R-4 파싱·`select_provider`·더미 키 미노출; `…gemini_is_reserved_not_implemented` 무수정 통과), `tests/test_er_smoke.py`(R-1(d) `test_main_missing_openai_key_returns_2` 개명 + **계획 밖 2건** 기존 테스트의 `monkeypatch.setenv` 키 이름 `ANTHROPIC_API_KEY`→`OPENAI_API_KEY` — 아래 소견), `scripts/er_smoke.py`·`scripts/baseline_smoke.py`(docstring 기본값 문구만), evidence `20260914-1620-u1-*.txt` 11개 + `20260914-1655-u1-pytest-full-db.txt`, 05-remediation 소견 1(출처 review)·F-11fbee 단계 2 완료, 이 로그(이전 항목 해시 025a7c4), HANDOFF
- 이유(기획서·카드 연결): backlog 51행 U1. D11 결정 1(활성 스위치 기본 전체 켬)·결정 2(기본 openai)·"코드에서 지켜야 할 것" (a)(b)(c)(e). R-3(스위치·공급자 같은 `env`)·R-4(쉼표·strip·lower·빈 항목 제거·미설정/공백=기본)·R-5(`^JUDGES` 1, `_KNOWN_PROVIDERS` 0)·R-9(a) D11 태그
- 정합성 확인: 원칙3(s_llm 자기보고 무관) / 원칙4(`git diff --name-only -- app/` = `judge.py`·`settings.py` 2줄, 판정 로직·확신도·임계치 무변경) / 원칙8(오타 이름 조용히 무시 안 함 — `InvalidValue`; 기존 테스트 변경은 R-1 2건 + 계획 밖 2건을 소견으로 기록) / 원칙9(trace 무관) / D11 / S3.3 무변경 / security(키 이름만, 메시지에 키 값·프롬프트 없음, 더미 키 마커 미노출 테스트) — 위반 없음. **예외**: `tests/test_er_smoke.py` 기존 2건(`test_main_success_path_prints_expected_json`·`test_main_judge_unavailable_returns_3`)의 키 이름 변경은 R-1 목록 밖 — 기본값 변경의 같은 원인(R-1(d)와 동일), 검증 로직 무변경, 소견 F-(아래) 로 기록
- 검증(evidence): `u1-judges-keys` `['anthropic','gemini','openai']` · `u1-invalidvalue-messages` 거부 2종 · `u1-import-both-modules` ok · `u1-r5-grep`·`u1-r4-grep` · `u1-tools-check` 7/7 · `u1-git-diff-app` 2줄 · `u1-git-diff-tests-removed-defs` 개명 2개 · pytest er 회귀 43 passed/17 skip(DB) · parity 14 passed/32 skip(DB) · **전체 641 passed / 0 failed / 232 skipped — DB(Docker Desktop) 꺼짐, 우회하지 않음(security §6). → 사용자가 Docker Desktop 기동 후 재실행 `20260914-1655-u1-pytest-full-db.txt` **873 passed / 0 failed / 0 skipped**(기존 859 + 신규 14)**
- 남은 것 · 다음 단위: **U2 `GeminiJudge`**(R-6 실측 evidence 3건 먼저: `pip show google-genai`·`google.genai.errors` 예외 이름·`types.Schema.model_fields`, R-2 `Client()` 키 미설정 예외 클래스, 멈춤 규칙; `_gemini_reserved` 교체, `requirements.txt` 핀, R-1(b) 테스트 대체, 스텁 테스트) backend-agent(L-004) → U3 → U4(R-7 gemini 키 행·R-8 registry 비고·R-9(e) user-setup 01)
- Refs: P3-llm-providers D11 D3 S3.3 원칙4 원칙8 R-1 R-3 R-4 R-5 R-9 L-002 L-004

## 2026-09-14 17:45 · feat(P3-llm-providers): U2 GeminiJudge — google-genai 2.23.0 실측 기반 오류 매핑·스키마 변환·1회 재시도, 테스트 29건 · cf01e9f
- 변경: `app/er/judge.py`(모듈 docstring "Gemini 전용" 절, `_to_gemini_schema()` JSON Schema→Gemini dict 변환 — `JUDGEMENT_SCHEMA` 불변·nullable integer·min/max·array items 처리, `call_with_gemini_error_mapping()` 어휘 6종만(`ClientError` 429→`rate_limit`, 그 외 `ClientError`/`ServerError`/`APIError`→`api_error`, `httpx` `TimeoutException`/`ConnectError`(+표준 예외 방어)→`timeout`/`connection` **1회 재시도 후** 확정), `GeminiJudge` dataclass(`GEMINI_MODEL` 생성 시점 읽기·미설정 `InvalidValue` 결정 3, 키 인자 미전달 — SDK 가 환경변수 읽음, `google.genai` 지연 import, `client` 스텁 주입, `HttpOptions(timeout)`, `temperature=0`·`response_mime_type=application/json`·`response_schema`, 파싱 실패·후보 없음·본문 없음→`JudgeUnavailable("schema")`, `validate_judgement` 공유, `usage_metadata` 토큰, `provider="gemini"`), `_gemini_reserved` 제거·`JUDGES["gemini"]=GeminiJudge`. `call_with_error_mapping`·`ClaudeJudge`·`OpenAIJudge` 무변경), `requirements.txt`(`google-genai==2.23.0` + Refs 주석, 결정 5), `tests/test_er_judge.py`(R-1(b) `test_judge_from_env_gemini_is_reserved_not_implemented` 삭제→대체 2건: gemini 양성 생성·`GEMINI_MODEL` 미설정 `InvalidValue`; parametrize gemini 추가; `GeminiJudge` 스텁 26건: 요청 본문·파싱·`out_of_range_id`·schema 3변형·오류 4종+재시도 횟수(timeout/connection 호출 2, api_error 1)·키 미노출·SDK 없이 import; `_to_gemini_schema` 3건), evidence `20260914-1700-u2-*.txt` 실측 6(pip show·errors 클래스·Schema 필드·HttpOptions·Client() 키 없음 예외·재시도 근거)·`1720/1730/1735-u2-*.txt` 10, 이 로그(U1 항목 해시 c01381d), HANDOFF
- 이유(기획서·카드 연결): backlog 51행 U2 "`GeminiJudge` (`google-genai`, `response_schema` 로 `JUDGEMENT_SCHEMA` 강제, 오류 어휘 동일) + 의존성 핀 + 스텁 테스트". D11 결정 3·4·5, "코드에서 지켜야 할 것" (c)(d)(e). R-6 실측 선행(멈춤 규칙: `types.Schema` 에 `nullable`·`minimum`·`maximum`·`items` 존재 → 표현 가능, 멈추지 않음), R-2(`genai.Client()` 키 없음 → `ValueError`, 키 값 미노출)
- 정합성 확인: 원칙3(s_llm 자기보고, 같은 스키마) / 원칙4(`git diff --name-only -- app/` = `judge.py` 1줄, 기존 함수·클래스 본문 무변경) / 원칙8(SDK 버전·예외 이름·Schema 필드·재시도 설정 전부 실측 evidence, 추측 0; 1차 전체 pytest FAIL 1(docstring "evaluation" 문자열) 을 evidence 로 보존 후 문구 수정) / 원칙9(`provider`·`model`·tokens) / D11 (d) `grep "safety\|blocked"` 0 / D3·S3.3 무변경 / security(키 인자 미전달·마커 미노출 테스트) — 위반 없음
- backend-agent 판단 2건(verifier 04-review 확인 대상): (1) `HttpOptions.retry_options` 는 존재하나 429/5xx 도 함께 재시도해 "timeout·connection 에만 1회" 정책을 표현할 수 없어 미채택 → 수동 1회 루프(01-plan 96행 "동등한 설정이 없으면" 해당, evidence `20260914-1700-u2-retry-and-error-mapping-detail.txt`·docstring) (2) `response_schema` 에 변환 dict 를 그대로 전달(pydantic 이 검증, `types.Schema` 명시 구성 없음)
- 검증(evidence): `u2-judges-keys` `['anthropic','gemini','openai'] GeminiJudge` · `u2-grep-vocab-judges` safety|blocked 0·`^JUDGES` 1 · `u2-pytest-er-regression` 100 passed · `u2-pytest-k-provider` 15 passed · **전체 `20260914-1730-u2-pytest-full.txt` 896 passed / 0 failed / 0 skipped**(873 + 신규 23) · `u2-tools-check` 7/7 · `u2-git-diff-app` 1줄 · `u2-git-diff-tests-removed-defs` 삭제 함수 1개
- 남은 것 · 다음 단위: **U3 `GeminiSingleCaller`**(`evaluation/resolvers/llm_single.py`: `select_provider`·`JUDGES` 를 `app.er.judge` 에서 import, `caller_from_env` 자체 표·거부 제거, `CALLERS` 키 = `JUDGES` 키 단언, `_to_gemini_schema`(`RESOLUTION_SCHEMA` 배열 포함)·`call_with_gemini_error_mapping` 재사용, R-1(c) parametrize `gemini` 양성 이동·`llama` 만 음성, 스텁 테스트) backend-agent(L-004) → U4. 실 Gemini 스모크는 사용자 몫(R-9(b), F-87c597 열림)
- Refs: P3-llm-providers D11 D3 S3.3 원칙3 원칙4 원칙8 원칙9 R-1 R-2 R-6 L-002 L-004

## 2026-09-15 12:10 · feat(P3-llm-providers): U3 GeminiSingleCaller — D11 등록표·select_provider 재사용, Gemini 스키마 변환·오류 매핑 공유, R-1(c) 테스트 이동, 테스트 22건 · 7b94a69
- 변경: `evaluation/resolvers/llm_single.py`(`GeminiSingleCaller` dataclass — 지연 import·키 인자 미전달·`GEMINI_MODEL` 생성 시점 읽기 미설정 `InvalidValue`(결정 3)·`response_schema=_to_gemini_schema(RESOLUTION_SCHEMA)` 재사용(R-6, 배열 `items` 유지)·`temperature=0`·`call_with_gemini_error_mapping` 재사용(결정 J, 재시도는 헬퍼 안 1회)·강등 경로는 `OpenAISingleCaller` 와 동일·`provider="gemini"`; `CALLERS` 표는 `JUDGES` 키를 순회해 생성(리터럴 재기입 없음, R-5); `caller_from_env()` 는 `select_provider(env)` 로만 공급자 결정 — 자체 이름 목록·거부·`InvalidValue(` 0(D11 (b)); docstring Refs 에 D11), `tests/test_baseline_llm_single.py`(R-1(c) parametrize `["gemini","llama"]` → `llama` 만 음성 + gemini 양성·`GEMINI_MODEL` 미설정·비활성 거부·`CALLERS`==`JUDGES` 키·헬퍼 동일 객체(`is`)·배열 `items` 변환; 4b절 `GeminiSingleCaller` 스텁 — 요청 본문·정상 파싱·강등 5경로·스키마 위반 2·오류 매핑 4(429 무재시도·4xx/5xx·timeout 1회 재시도·connection 1회 재시도)·비대칭(실패→merge 없음)·키 미노출), evidence 9개. **`app/` 무수정**
- 이유(기획서·카드 연결): backlog 51행 U3 "`GeminiSingleCaller` + `caller_from_env` 등록표 재사용". D11 "코드에서 지켜야 할 것" (b) `caller_from_env()` 는 표·스위치를 `judge.py` 에서 import(자체 표 금지). R-1(c)·R-5(거부 단일 자리)·R-6(변환기 단일)·결정 J(오류 어휘 5방식 동일). 01-plan 판정 표 "두 진입점이 같은 표"
- 정합성 확인: 원칙3(자기보고 `s_llm` — RESOLUTION_SCHEMA 무변경) / 원칙4(`git diff --name-only -- app/` 0줄, 판정·확신도·임계치 무관 — 베이스라인 전용) / 원칙8(기존 테스트 변경은 R-1(c) 1곳뿐 — 삭제 함수 grep 으로 증명; 실 Gemini 호출은 사용자 스모크 몫 R-9(b)) / 원칙9(`provider`·`model`·tokens 기록) / D11 (a)(b)(c)(d) / D3 / S3.7(베이스라인 단일 프롬프트 방식 유지) / security(키 인자 미전달·마커 미노출 테스트·메시지에 키 값 없음) / 의존 방향 evaluation→app 만 — 위반 없음
- backend-agent 판단 3건(verifier 04-review 확인 대상): (1) `CALLERS = {name: _CALLER_FACTORY_BY_NAME[name] for name in JUDGES}` — 키 집합은 `JUDGES` 에서 파생, 팩토리 3개는 내부 매핑(`JUDGES` 에 팩토리 없는 이름이 생기면 import 시 `KeyError` 로 즉시 실패) (2) `GeminiSingleCaller` 는 `max_retries` 필드 없음(`GeminiJudge` 와 동일) — 팩토리 래퍼가 `max_retries` 를 버림 (3) Gemini 테스트는 기존 Claude/OpenAI parametrize 에 섞지 않고 4b 절로 분리(기존 테스트 무수정 유지)
- 검증(evidence, `evidence/20260915-1200-u3-*`): `git-diff-app` 0줄 · `r5-grep` import `select_provider`/`JUDGES`, `caller_from_env` 본문 `InvalidValue(` 0(유일한 호출은 `GeminiSingleCaller.__post_init__` 527행), 변환기·매핑 재정의 0 · `callers-keys` `['anthropic','gemini','openai']` == `JUDGES` True · `git-diff-tests-removed-defs` 삭제 1곳(R-1(c)) · `pytest-llm-single` 90 passed · `pytest-parity` 46 passed · `pytest-er-regression` 91 passed · **`pytest-full` 917 passed / 0 failed / 0 skipped**(896 + 신규 21 수집 기준; `def test_` 47→69) · `tools-check` 7/7. Docker(capstone2-postgres-1) 켜져 있어 skip 0
- 남은 것 · 다음 단위: **U4 문서·evidence**(`.env.example` 주석 이름만, README 두 절, `docs/user-setup/01-env-keys.md` `GEMINI_*` 행·활성 스위치(R-9(e)), 스모크 카드 03·08(또는 09) `--provider gemini`(결정 7), R-7 gemini 키 행, R-8 registry 기존 행 비고만, 전건 evidence: 전체 pytest·P3-er 회귀·parity·tools_check·`git diff --name-only 701fb8d..HEAD -- app/` 2줄) backend-agent(L-004) → `/devlog done`(verifier 04-review, R-9(b) "실호출 검증 공급자 0/3" 명시)
- Refs: P3-llm-providers D11 D3 S3.7 원칙4 원칙8 원칙9 R-1 R-5 R-6 L-002 L-004

## 2026-09-15 15:00 · feat(P3-llm-providers): U4 문서·evidence — .env.example 확인·README 두 절·user-setup 01/03/08·D11 갱신 이력·registry 비고 8곳·er_smoke gemini rc=2 테스트, `app/` 무수정 · pending
- 변경: `.env.example`(U1 에서 이미 `LLM_PROVIDER=openai`·`LLM_PROVIDERS_ENABLED=`(이름만) 반영 확인 — **변경 없음**), `README.md`(217행 절: `--provider anthropic`/`--provider gemini` 두 줄 추가·`LLM_PROVIDERS_ENABLED`/등록표 문장, 293행 절: `--provider gemini` 줄·`GEMINI_*` 환경변수 이름 추가, 진행 표 P3 행 "판정기 공급자 중립(Claude·OpenAI·Gemini, `LLM_PROVIDER`·`JUDGES`·`LLM_PROVIDERS_ENABLED`, D11)"·pytest 859→917), `docs/user-setup/01-env-keys.md`(R-9(e): 7행을 `OPENAI_API_KEY`/`OPENAI_MODEL`(기본, D11) 로, 9행을 `ANTHROPIC_API_KEY`/`ANTHROPIC_MODEL`(선택, 비교용) 로 뒤집음, 12행 `GEMINI_*` "예약값. 미구현" → 사용 가능 행(스모크 `--provider gemini`, `GEMINI_MODEL` 미설정 시 `InvalidValue`) + `LLM_PROVIDERS_ENABLED` 행 신설), `docs/user-setup/03-er-smoke.md`·`08-baseline-smoke.md`(각 절차에 "(선택) Anthropic 비교"·"(선택) Gemini 비교" 두 줄, 출력 파일명 `...-real-gemini.txt`, 이후 번호 재정렬), `docs/wiki/decisions/D11-llm-provider-registry.md`(갱신 이력에 U1~U3 구현 완료 해시 c01381d·cf01e9f·7b94a69, U4 문서 반영 1줄 — 결정 문장 무변경), `docs/wiki/registry.md`(**새 행 0** — 기존 8곳 비고에 `P3-llm-providers U<n>(<해시>):` 문구만 추가: `app/er/judge.py`·`app/settings.py`·`evaluation/resolvers/llm_single.py`·`tests/test_er_judge.py`(73건 수집/`def test_` 68)·`tests/test_baseline_llm_single.py`(90건 수집/`def test_` 69)·`requirements.txt`(google-genai 핀)·`.env.example`·`README.md`, `tests/test_er_smoke.py` 비고에도 U4 gemini 행 추가), `scripts/er_smoke.py`·`scripts/baseline_smoke.py`(R-7: `_REQUIRED_KEY_BY_PROVIDER`에 `"gemini": "GEMINI_API_KEY"` 행, 주석 "예: `gemini`" 문구 제거, `--provider` help `(anthropic\|openai)`→`(anthropic\|openai\|gemini)`, docstring 은 이미 "기본 openai" — U1 에서 갱신됨, 무변경), `tests/test_er_smoke.py`(R-7: `test_main_missing_gemini_key_returns_2_with_provider_flag` 1건 추가, 기존 10건 무수정 — 이 단위의 유일한 `app/` 밖 코드 수정)
- 이유(기획서·카드 연결): 01-plan U4 "문서·evidence". D11 갱신 이력·"코드에서 지켜야 할 것". R-9(e)(`docs/user-setup/01-env-keys.md` 7·9행 기본/선택 관계 반전). R-7(스모크 스크립트 `_REQUIRED_KEY_BY_PROVIDER`·help 문구·`tests/test_er_smoke.py` gemini 행). R-8(registry 새 행 금지, 기존 행 비고만 — 닫는 명령 결과 아래)
- 정합성 확인: 원칙4(`git diff --name-only 025a7c4..HEAD -- app/` = `app/er/judge.py`·`app/settings.py` 2줄, 이 단위는 `app/` 무수정) / 원칙8(문서 수치는 실측 evidence 그대로 — pytest 918/tools_check 7/7/parity 46/er-regression 91) / 원칙9(trace 무관, 문서 단위) / D11 결정 문장 무변경(갱신 이력만 추가) / security(gemini 스모크 exit2 evidence 에 키 값·전체 환경변수 미출력, `unset`으로 우회 — `env`/`printenv` 명령 직접 사용은 훅이 차단해 `unset GEMINI_API_KEY` 서브셸로 대체) — 위반 없음
- backend-agent 판단 1건(verifier 04-review 확인 대상): `.env.example` 은 U1 에서 이미 `LLM_PROVIDER=openai`·`LLM_PROVIDERS_ENABLED=`(이름만, 값 비움)을 반영해 두어 U4 에서 **추가 변경이 없다**(위임 프롬프트 1번 "변경 없으면 03-log 에 변경 없음으로 적는다" 그대로) — 03-log 에 명시하고 registry `.env.example` 행에는 U1 커밋(c01381d)에 이미 실려 있으므로 U4 비고 추가 대상에서 제외
- 검증(evidence, `evidence/20260915-1500-u4-*`): `pytest-all` **918 passed / 0 failed / 0 skipped**(Docker Desktop 이 꺼져 있어 사용자 개입 없이 재기동 후 재실행, 917 + 신규 gemini rc=2 1건) · `pytest-er-regression` 91 passed · `pytest-parity` 46 passed · `tools-check` 7/7 · `app-diff` `git diff --name-only 025a7c4..HEAD -- app/` 2줄(`judge.py`·`settings.py`) + `scripts/tests/requirements.txt` diff 6개 파일(R-7 대상과 일치) + 이 세션 `git status --short`(문서 10개 M + evidence 신규) · `providers-3-and-switch-reject` anthropic/openai/gemini 3종 생성 확인 + `LLM_PROVIDERS_ENABLED=openai`+`LLM_PROVIDER=gemini` → `InvalidValue` 거부 메시지(키·프롬프트 미노출) · `registry-r8` 8개 판정 명령 전부 `1`(기대와 일치) · `smoke-gemini-exit2` exit code 2(환경변수 전체 미출력)
- 남은 것 · 다음 단위: 패키지 완료 — `/devlog done`(verifier 04-review, R-9(b) "실호출로 검증된 공급자 0/3(F-87c597 열림)" 명시 유지). 사용자 몫: 03·08 스모크 카드의 실 Gemini 호출(`GEMINI_API_KEY`·`GEMINI_MODEL` 필요)은 아직 미실행
- Refs: P3-llm-providers D11 원칙4 원칙8 원칙9 R-7 R-8 R-9 L-002 L-004
