# <패키지 id> · 검증 결과 조치 계획 (05-remediation)

> 이 파일은 `python .claude/scripts/findings.py <id> <검증출력> --source <출처>` 가 만들고 갱신한다. 손으로 새로 만들지 않는다.
> 소견(F-xxxxxx) 하나의 형식은 아래와 같다. 에이전트는 빈 항목을 채우고, 해결 단계의 "완료 판정 명령"을 실제로 실행해 그 출력을 evidence/ 에 남긴다.
> 평가(eval) 미달·사용자 리뷰처럼 스크립트가 못 잡는 문제는 같은 형식으로 블록을 직접 추가한다(출처: eval / review).

## F-xxxxxx · [필수|권고] <검증 메시지>
상태: 열림 | 발견: YYYY-MM-DD (출처) | 해소: -

### 증상 (검증 출력 인용)
```
FAIL  …
```

### 원인 분석
- 가설: (한 문장. 여러 개면 번호)
- 확인 방법(명령): (가설을 확인하는 명령 — grep, 테스트 하나, SQL)
- 확인 결과: (출력 인용)

### 해결 단계 (단계 하나 = 확인 가능한 변경 하나)
| # | 변경 (파일 · 방법) | 완료 판정 명령 | 기대 출력 | 상태 |
|---|--------------------|----------------|-----------|------|
| 1 | app/x.py 에 … 추가 | `python -m pytest tests/test_x.py -q` | `1 passed` | 대기/완료 |

### 재검증
- 명령: `bash .claude/scripts/verify-impl.sh <id>` (같은 명령을 다시 돌려 이 소견의 줄이 사라져야 한다)
- 결과 파일(evidence/):

### 영향 확인
- 관련 카드(D/S/원칙)와 충돌: 없음 | 있음 → 어느 카드
- FIX/CR 로 올려야 하는가: 아니오 | 예 (FIX-nnn / CR-nnn) — 결정·명세를 바꿔야만 풀리는 소견은 여기서 멈추고 사용자에게 보고
