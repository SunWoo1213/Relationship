# CR-nnn · <기획서 변경 제목>

상태: 열림 | 승인 | 거절 | 이행완료 | 제기: YYYY-MM-DD, 사용자

## 1. 무엇이 바뀌는가
기획서 § · 이전 문장 → 새 문장. 범위(2장 표)가 바뀌면 포함/제외 어느 칸이 움직이는지.

## 2. 영향 분석 (태그로 추적)
- 검증 항목 R:
- 결정 D (유지 / 대체 → 새 D / 폐기):
- 명세 S:
- 불변 원칙 (CLAUDE.md):
- 패키지 P — 완료된 것 / 진행 중 / 예정:
- 영향 커밋: `git log --oneline --grep '<태그>'` 결과를 붙인다
- 수정할 파일 목록:

## 3. 선택지
| 안 | 내용 | 비용 | 위험 |
|----|------|------|------|
| A 수용 | | | |
| B 부분 수용 | | | |
| C 거절(현행 유지, 사유 기록) | | | |

## 4. 이행 계획 (승인 후, 이 순서로)
1. `CURRENT.md` → `frozen: CR-nnn` (제품 코드 쓰기 차단)
2. 결정 카드: 새 D 작성, 옛 D 를 대체됨으로 · S 카드 갱신 · `CLAUDE.md` · `docs/backlog.md` · `review-index.md`
3. `docs/proposal.md` 상단 "원본 이후 확정된 사항"에 한 줄 (본문은 여전히 원본 유지)
4. 영향 코드: 되돌릴 커밋(`git revert`) 목록 또는 FIX-nnn 으로 수정. 재계획이 필요한 패키지는 01-plan 부터 다시
5. `frozen: none` · journal 에 CR 줄 · 이 문서 상태를 이행완료로

## 5. 되돌리기 (이행 도중 실패하면)
이 CR 이 만든 커밋 목록을 역순으로 revert. 문서는 CR 직전 커밋으로.

승인:
